# 无业务依赖的技术工具

这里只容纳多个模块共同使用的纯技术算法，不导入业务模块、数据库、HTTP、队列或第三方服务。模块业务规则、权限、repository 和状态不得迁入此目录以绕过依赖检查。

canonical-json 固定现有 V4 序列化与 SHA-256 算法，不宣称符合 RFC 8785。对象键排序、数组顺序、数字和 Unicode 编码必须保持兼容；不能在普通重构中改变已有持久摘要。技术错误由消费者映射为自身业务错误；execution 保留 execution_action_invalid/422。
