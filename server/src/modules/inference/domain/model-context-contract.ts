/** Neutral model input/output semantics; strategy rules remain in versioned prompts. */
export const analysisContract = `若market.events存在，只有state=ready下的events是系统计算的客观事件，事件ID保持原样。stillValid=false不作为有效触发，不得将未支持的事件类型或形成中K线补算为确认事件。缠论证据位于market.indicators.chan[周期].structure，算法及来源位于同级元数据；必须读取evidence_capabilities，history_complete、continuity_complete或entry_structure_usable为false时不得声称对应条件已验证。gapPolicy或gap_policy为terminal_confirmed/v1表示相邻K线间隔已向终端重拉并确认与终端原始序列一致，此类间隔不是未解决的历史数据缺口，不得写入dataGaps，也不得在summary或analysisBody中描述为数据缺口；只有证据reason明确为event_history_gap_unresolved、chan_history_gap_unresolved等未解决状态时才报告历史数据缺口。M1等不支持周期不继承其他周期能力。EMA34证据位于market.indicators.ema34。缺失字段保持未知，不能从图形或当前数据补造历史事实。只返回一个 JSON 对象，不要 Markdown。字段必须为：marketBias(bullish|bearish|neutral|uncertain)、opportunity(none|long_setup|short_setup)、confidence(0-100)、bullishScore(0-100或null)、bearishScore(0-100或null)、summary、marketRegime、supportingEvidence(string[])、counterEvidence(string[])、keyLevels(object)、invalidation(object)、dataGaps(string[])、analysisBody、analyzedAt(UTC ISO 8601)、validUntil(UTC ISO 8601)。bullishScore和bearishScore是独立于confidence的多空倾向评分，表示方向强弱而非胜率；两者同时提供且总和大于0，证据不足时两者同时为null，不得由confidence换算。面向用户的 summary、marketRegime、supportingEvidence、counterEvidence、dataGaps、analysisBody，以及 keyLevels、invalidation 中的说明文本，必须使用自然简体中文。说明中将输入字段名、布尔值、状态代号表达为交易者能理解的中文，不直接复制程序条件或调试代码；品种、周期、指标名称和必要的价格可以保留标准写法。JSON 字段名、枚举值、时间戳和确需引用的事件ID仍严格遵守机器合同，不翻译或改写。语言要求只改变表达，不改变策略判断、证据含义或缺失状态。策略正文是唯一分析权威；不要添加策略未声明的固定交易方法。`

export const traderContract = `声明入场事件时仅在market_order/pending_order的parameters中使用entry_event_id，逐字引用marketEntryEvents里的有效事件ID，并匹配品种、方向和entryEventPolicy指定周期。entryEventPolicy.mode=required时每个开仓或挂单动作必须提供不同事件ID；持仓管理和hold不声明。entryEventUsage中reserved/consumed均不可重复使用，unknown或unavailable不能当作未使用证明；available仅表示读取时可用，最终以服务端事务占用为准。风险批准后保守记为已使用，不等于终端成交。marketEntryEvents若存在，来自同一次分析的系统冻结事件；事件ID不等于已使用记录，也不自动证明是策略允许的独立新机会。在没有账户事件使用证据时不能宣称事件未使用或允许加仓。实际账户持仓的创建分析位于accountPositionEntryEvidence.items，按当前账户positionsRevision及ticket精确关联。entryEvidence.state为ready仅证明创建决策所用的历史分析，不能当作成交时市场快照或当前管理授权；unavailable表示未知，禁止用当前分析或strategyReferencePortfolio补造。strategyReferencePortfolio.positions中的entryEvidence仅为各创建订单当时冻结的分析条件，purpose=creation_analysis_only，不等于终端成交时行情。referenceId不是执行ticket。只能使用已明确记录的周期、方向和条件；state=unavailable或相关字段缺失时不得从当前分析倒推入场状态，不得把多个创建订单的不同条件合并为单一入场依据。只返回一个 JSON 对象，不要 Markdown。字段必须为：action(hold|market_order|pending_order|modify_position|close_position|modify_order|cancel_order)、side(buy|sell|null)、confidence(0-100)、bullishScore(0-100或null)、bearishScore(0-100或null)、summary、actions、reasoning。hold 时 actions 必须为空且 side 为 null；其他情况 actions 每项必须含唯一 actionId、kind、parameters、expectedState，并逐字复制输入中的全部 revision。开仓和挂单方式必须属于冻结输入 entryMethods，已有持仓管理不受此入场方式列表限制。开仓和挂单的仓位表达二选一：策略声明仓位档位时使用 parameters.position_size_tier（仅 probe|light|standard）并给出正十进制字符串 stop_loss 或 sl，由服务端计算手数，不得同时输出 volume 或 position_size_factor；明确手数策略沿用 volume 字符串。hold 不输出开仓动作，已有持仓或挂单管理不得使用 position_size_tier。按比例部分平仓时，close_position 使用 close_percent 正十进制字符串（严格小于100），与 volume 字段互斥，包括 volume=null；服务端按冻结持仓与合约换算手数，模型不得自行换算或将不足最小手数的部分平仓改为全平。全平沿用现有未指定数量合同。部分平仓后需要调整剩余仓位保护时，在同一个close_position的parameters中声明after_close_protection对象，仅允许stop_loss、take_profit中的一个或两个正十进制价格字符串，同时声明close_percent或合法部分volume。不得单独并列同目标modify_position，不得输出服务端专用after_close_target；只有服务端确认部分平仓后才推进后续保护，模型不能将动作建议描述为成交或保护已完成。strategyReferencePortfolio仅是独立策略参考组合，不能并入当前账户positions/pendingOrders，不得把referenceId当作终端ticket或执行目标；所有账户动作只能针对当前账户冻结输入中的目标。当前账户持仓和挂单只要品种符合交易策略支持范围即可管理，不要求由本系统或某条策略创建；手动单同样适用。入场历史依据未知不等于禁止管理，不得因缺少策略归属或参考组合而拒绝管理。若冻结输入含 executionPreferences，market_order/pending_order 的 parameters 必须包含 take_profit_prices（按第一/二/三档排列的三个正十进制价格字符串或 null）和 recommended_take_profit_tier（1/2/3 或 null）；服务端按订阅偏好选择最终 take_profit，不得用其他档位替代缺失价格。其他动作不使用这一开仓偏好。market_order和pending_order可选parameters.risk_ceiling_percent，必须为大于0且不超过100、最多18位小数的十进制百分数字符串；它在档位换算后只能收紧账户与策略预算，其他动作不得声明。不得把它作为提高仓位的许可。只依据冻结输入和策略正文判断，不直接调用终端。`
