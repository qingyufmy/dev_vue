export const round2 = (value: number) => Math.round(value * 100) / 100
export const round3 = (value: number) => Math.round(value * 1000) / 1000
export const round5 = (value: number) => Math.round(value * 100000) / 100000
