# 风控审核任务收尾修复（本地开发）

## 故障证据

开发库 `dev_vue` 中发现 32 条超过五分钟的 `proposed` 决策；对应 outbox 已投递，BullMQ 任务却以 `{status: stale}` 返回成功。原因包括 `risk_review_market_context_incomplete` 和 `risk_review_context_revision_conflict`。任务成功返回并不代表风控审核成功，原实现没有重试或持久化终态。

品种读取保持现有账户、归属、终端绑定、连接 epoch 和 300 秒新鲜度校验。模型调用前可用的规格，不保证模型结束后仍可用；本次没有放宽这些校验。

## 修复

- 风控读取不到可信品种规格时，通过 trading 公开端口请求重新采集。
- `stale` 审核结果进入队列已有的五次指数退避重试，每次重新读取并审核。
- 最后一次仍无法审核时，由 inference 域事务更新尚未审核的提议为 `stale`，保存原因、释放未使用入场预留并写入通知 outbox。
- 更新要求仍为 `proposed` 且没有风控决策，不覆盖已经获批或拒绝的并发结果。
- 沿用既有状态和通知合同，无数据库结构变更，无需新增结构迁移。

## 验证与边界

服务端构建及模块边界检查通过。确定性风控 63 项、规格端口 1 项、队列收尾 3 项、事务收尾 3 项测试通过。

本地风控进程已重启。33 条历史记录（含重启前最后一轮）仅在队列证实任务已经完成且结果为上述失败原因、动作是 hold 时收尾；不重放订单。回执保存在 `D:/dev_codex/.local-runtime/dev-vue/risk-stale-settlement-20260916.json`。

本记录不代表公网部署或真实成交验收。工作区存在大量继承修改，本次未将这些混合改动提交推送。

重启后自然调度证据：2026-09-16 00:15:35 UTC 决策 `9ad42ddd-367b-4e4d-a25b-be459a01dba2` 已变为 accepted，对应风控 `450545a1-6374-481f-a805-f2c1b8133b2e` approved，队列 completed。此轮没有验证补拉重试分支；该分支目前以定向回归测试覆盖。
