# 重启后的任务交接 — 2026-09-06

## 1. 接手定位

这是截至 2026-09-06 的交接快照，不替代项目规则和路线图，也不新增执行授权。接手先核对当前分支、改动和最新提交，再读取当前规则；不要把长会话中已废弃的旧版 UI、Rust Bridge 或旧分支安排重新当成任务。

- 开发仓库：`D:/dev_codex/dev_vue`；分支：`dev_vue`；上游：`origin/dev_vue`。
- 旧版参考：`D:/dev_codex/wall-street-skill-local`，仅只读参考，不在该目录实施重构。
- 本交接写入前最新提交：`a6d063f7`（规则收口），此前业务提交：`4f542938`（配对身份和账户缓存隔离）。创建交接前工作区干净。
- 首先读 `AGENTS.md`，再读 `docs/refactor-master-roadmap.md` 顶部状态、§2 和 §3.1。其它合同和专项设计按实际任务选读，不全仓扫描所有历史报告。

## 2. 总体进度与当前重点

路线图记录：阶段 0–7 的盘点与设计、阶段 9 工程骨架已完成；阶段 10 为实现与离线验证完成。阶段 8 Bridge、阶段 11 账户实时链路和阶段 12 AI 实验室仍有集成缺口；主站/后台完整业务迁移、全量回填、最终非功能验收和切流尚未完成。不要据此宣称 V4 可正式交付。

当前重点是 M1 / B2 中的普通用户首次连接、重连与换号。近期源码已实现：

| 提交 | 内容 | 证据边界 |
| --- | --- | --- |
| `f35b976e` | 一次性配对签发/领取，服务端事务仓储和 .NET 领取组件 | 源码、合同与离线验证；024 迁移未执行 |
| `5e587afe` | trade 配对页面、客户端配对窗口与 DPAPI 重试恢复 | 未验证真实浏览器/WinForms 完整使用流程 |
| `5aee1bcb` | 新增/编辑档案发现已运行终端、识别并填入账号 | 假进程/假响应与离线测试；未探测当前真实终端 |
| `4f542938` | 换号保留配对 profile ID，SQLite 按精确账户路线隔离，档案活动锁 | 离线验证；不代表首次账户认领或真实重连已通过 |

路线图前部“首次连接阶段的源码核查与待办”包含实施前快照，例如手填 refresh token、编辑随机换 profile ID；这些已有后续提交修正。以紧随其后的最新实施结果和当前源码复核，不重复实现已完成事项。终端发现也已有源码，未完成的是实机验收，不是完全未实现。

最近业务验证记录（来自路线图，本次交接未重跑）：服务端 58 files / 446 tests、类型检查和 V4 构建通过；客户端 x86 编译及 67 组离线烟雾入口通过。此前页面接入批次前端 32 files / 143 tests、类型检查、构建和边界检查通过。不同批次的数字不能合并成当前全系统验收证明。

## 3. 下一步按这个顺序接续

1. **补首次账户认领。** 核对 hello/终端事实合同，明确币种等事实来源，不假设 USD。配对仅证明系统用户授权；账户认领仍需精确 platform/server/login、档案身份、既有 owner 冲突与并发唯一性处理，不能凭客户端字符串接管他人账户。
2. **补首次连接/重连/换号失败恢复。** 优先检查关闭异常、Worker 长任务停止期间的连接和活动锁释放，旧路线失效与迟到响应隔离。之前被生成错误随机 ID 的档案不能猜测映射恢复；已有记录要求重新配对。复用现有机制，不另造通用补偿平台。
3. **准备有限真实只读联调。** 先明确目标环境、依赖版本和必要迁移，再取得对应授权。终端只读不代表服务端不写数据库。Win7、跨进程、真实关闭/重连和窗口可用性仍未验收。
4. **再接 P5B。** 观摩主动进入/退出与 Pro 无本人桥接的分析订阅是独立能力；先明确合法平台行情来源，再接用户分析订阅与账户执行关联。Pro 没有本人 Bridge 时不可使用本人终端数据和交易能力，不能要求先进入观摩才允许订阅。
5. 其余 B2/B3、前端业务迁移按路线图继续。此时不因旧 UI 争论重做全站，也不增加新框架。

同一已授权用户流程内持续完成合同、实现、修复和必要验证；提交只是保存进度。真正的新阶段或新授权边界才介绍并请求确认。本次“写交接文件”本身不等于授权立即启动上述联调。

### 直接相关代码入口

- `server/src/modules/bridge/application/bridge-pairing-service.ts`
- `server/src/modules/bridge/infrastructure/mysql-bridge-pairing-repository.ts`
- `server/src/modules/bridge/application/bridge-gateway-session.ts`
- `server/src/modules/bridge/domain/bridge-gateway.ts`
- `contracts/`（按 Bridge 对应合同定位）
- `bridge/prototypes/net48-win7/src/LiangjianBridge/ProfileEditorForm.cs`
- `bridge/prototypes/net48-win7/src/LiangjianBridge.Core/Runtime/BridgeProfileConnectionManager.cs`
- `bridge/prototypes/net48-win7/src/LiangjianBridge.Core/Storage/ProfileAccountDataLocation.cs`
- `bridge/prototypes/net48-win7/src/LiangjianBridge.Core/Terminal/WindowsTerminalDiscovery.cs`

以上均为仓库相对路径，避免读错参考仓库中的同名实现。

## 4. 必须保留的边界

- **当前 MT5 是用户实际使用账户，禁止交易指令测试。** 下单、挂单、改单、撤单、平仓都不允许；需要测试时先请用户切换并明确确认测试账户。旧会话中早期“模拟账户随意操作”的授权已被后续限制取代。
- 本轮规则修改与交接没有启动服务、连接数据库/Redis/Bridge/终端、执行迁移、部署、生成安装器或发布更新。依据当前路线图，018–024 新增迁移及回填未执行；接手不能把这当作实时数据库查询结果。
- 数据保留、已发布迁移历史、未知结果 `uncertain` 精确对账规则不变；不得重放可能已送达的交易命令。
- Bridge 保持精简：发现已运行终端、提供数据、缓存可重建历史、执行服务端指令并回传结果；业务额度、会员、策略和风控在服务端。不自动启动、登录或改变终端账户。
- 前端统一 Vue/Nuxt + shadcn-vue，按项目规定模块隔离；后台任务异步隔离；AI 判断与确定性执行/风控职责不变。详细规范以 `AGENTS.md` 为准。
- 用户长期要求修改后及时提交推送当前 `dev_vue`；只提交本任务内容，不推送无关文件，不强推、合并、部署或改其它分支。

## 5. 刚完成的指令与 Skill 清理

项目内 `a6d063f7` 已推送：同步阶段持续推进规则，限定两轮复审用于重大方案，验证按影响范围选择，标明路线图历史记录不重新激活旧确认门。

本机文件调整（不在项目 Git 内）：

- 新增 `C:/Users/Administrator/.codex/skills/shadcn-vue/SKILL.md`；原 `shadcn` Skill 限定 React，避免 React CLI/API 误用于 Vue。
- `ui-ux-pro-max` 只在需要新视觉方向时生成设计系统；小修复使用对应指南，Python 缺失可静态回退，不因此停工。
- Impeccable 保留维护源：`C:/Users/Administrator/.agents/skills/impeccable`。调整“固定 QA 次数”及卡片/标题绝对禁令，保持可访问性和事实约束。
- `C:/Users/Administrator/.codex/config.toml` 已添加两条精确路径停用配置：本机 `.codex/skills/impeccable/SKILL.md` 和插件缓存 `impeccable/4.1.1/skills/impeccable/SKILL.md`。未删除重复文件、未修改插件缓存；插件升级后需重新核对版本路径。
- 全局 `AGENTS.md`、历史记忆未修改。备份与恢复映射：`C:/Users/Administrator/.codex/backups/instruction-cleanup-20260906-134209/RESTORE.md`。备份含私人配置，不能提交或公开。

配置 TOML、四份 Skill 的 YAML 已解析；新 Vue Skill 和 ui-ux-pro-max 通用校验通过。既有 Impeccable `version`、React Skill `user-invocable` 字段不被通用校验器识别，因此保留并单独审阅，不能表述成四份 Skill 通用校验全通过。没有为纯文档配置变更运行业务全量测试。

**重启后还需确认加载结果。** 上次会话工具目录仍显示三份 Impeccable，说明仅凭写入配置不能宣布去重已经运行生效。新任务查看实际可用 Skill 列表；若仍重复，针对配置加载做诊断，不擅自删除插件。新 shadcn-vue 已在后续会话目录出现。

## 6. 新任务可直接使用的接续消息

> 在 D:/dev_codex/dev_vue 的 dev_vue 分支继续。先读 AGENTS.md、docs/handoff-2026-09-06.md 和 docs/refactor-master-roadmap.md 的当前状态及 §3.1，核对 Git 状态。继续当前已授权的普通用户首次连接、重连与换号源码闭环，优先处理首次账户认领与关闭失败恢复；已实现的配对、发现和账户缓存隔离不要重做。同一流程持续推进，定向验证并及时提交推送。不要启动真实依赖、执行迁移、部署或发送任何 MT4/MT5 交易指令；需要这些权限时先明确目标与范围。先简要说明接手后的具体工作点，再实施。
