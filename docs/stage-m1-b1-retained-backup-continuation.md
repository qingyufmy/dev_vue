# M1 B1：冻结备份的生成列适配与隔离恢复续接

日期：2026-09-05。用户已确认上一轮提出的“修正检查器，再使用现有备份继续隔离恢复”。承接 [加密备份现场记录](./stage-m1-b1-encrypted-restore-rehearsal.md)。本文件初始状态为实施中，实际结果在验收后追加。

## 1. 精确范围

- 只使用运行 `20260905-01` 已保留的加密包、口令、冻结观测；不再次导出 `dev_vue`，不查询当前源业务数据来替换冻结基线。
- 实例 UUID 固定 `ac423207-6ef3-11f1-b302-000c29fda104`；唯一可首次创建/导入的库是 `dev_vue_m1_source_20260905_01`。已存在即停止，不复用、清空或重试。
- 新工具和证据只在 `/www/backup/aurum-v4/m1/20260905-01/continuation-01`；目录 0700、文件 0600，独占创建。原运行目录的工具、备份和失败记录不覆盖。
- 原库、A/B、环境、授权、全局设置、应用、Bridge、MT4/MT5 均不改。不部署、不启动交易链路。
- 原备份与原 SQL 大小/hash、元数据文件 hash 全部钉住；历史失败必须明确未尝试建库，才允许本次续接。

## 2. 实施与验收步骤

1. 将 DEFAULT/生成表达式的合法语法与函数调用区分。生成列只能从冻结 CREATE DDL 识别，不能仅靠列名、INVISIBLE 或默认表达式推断。
2. 普通列仍要求完整、按顺序出现；生成列省略符合 dump 语义。未知/跨库函数、活动对象、客户端命令、表达式 INSERT 继续拒绝。
3. 在新目录完整重新解密和解压已保留加密包，原始 SQL hash 必须匹配；对全部 SQL 审查，不仅检查 DDL 样本。
4. SQL 通过后再次确认实例、版本、目标不存在和可用空间。只创建精确新库，随后以不可重放的客户端导入。
5. 恢复库 SHOW CREATE 完整指纹与冻结观测一致；再导出恢复库，以全部有序普通列 INSERT、逐表行数和真实 COUNT 对比冻结 dump。生成列由同一 DDL 表达式重算，不宣称 dump 保存了原生成列的独立物化字节。
6. 复核原证据未改；记录源码提交、工具 hash、阶段时间、对账结果和未验证边界。任何失败保留现场，没有删除/自动续跑。

MySQL 将生成列与普通不可见列区分；表达式默认值也不等于生成列。参考 [生成列](https://dev.mysql.com/doc/refman/8.4/en/create-table-generated-columns.html)、[不可见列](https://dev.mysql.com/doc/refman/8.4/en/invisible-columns.html)、[默认值](https://dev.mysql.com/doc/refman/8.4/en/data-type-defaults.html)。

## 3. 执行前两轮复审

第一轮（需求/职责）：仅修复备份恢复工具，不做 V4 字段映射或业务回填；沿用现有 GPG/MySQL/gzip，无新服务、新加密体系或通用数据库重试框架。生成列适配由独立编码子任务完成，主代理审查差异后才准入实际 SQL。

第二轮（安全/异常）：续接入口不是原执行器的“跳过检查”。冻结文件钉 hash、新目录独占、完整解密审查先于 CREATE，最终仅允许恢复目标。凭据仍为匿名 memfd；原 source 不再次 dump。CREATE 超时或导入中断后目标视为可能存在，保留现场并停止，不自动重放。禁止扩大函数白名单来强行通过，未支持语法宁可提前失败。

剩余风险：同机备份非异机容灾；hash 不能防御已控制 root 的攻击者；SQL 白名单只支持本次实际需要的 MySQL dump 子集。源冻结观测前后结构一致是观察证据，不等价于证明期间从未发生瞬时 DDL。恢复一致也不等价于 V4 全量业务迁移完成。

## 4. 验证与实际结果

本地备份/迁移回归 **16 files / 129 tests 通过**，其中 SQL 审查器 30 项、续接约束 6 项。MJS 语法检查、Python AST 检查和 `git diff --check` 通过。主代理复核子任务差异，收紧了标点识别、索引函数边界与生成列显式赋值；不以子任务自报通过代替最终验证。

实施期间一次全量只读预审：`682966225 B` / 165 表 / 271007 行 / 272014 条语句，约 80 秒，原 SQL SHA 一致；无数据库连接或文件写入。该次 scanner SHA 为 `ad083aede1de7af8c1fade58681a126222991cddbea08a10aef2b51bed8446b2`，随后进一步收紧语法；最终 scanner SHA 为 `fdeff43a990150c3052ea632d671fd268c035ab026d55d935b67ca3f18540388`，实际执行将重新进行完整 SQL 审查，不能直接复用初步结果放行。

恢复前独立只读 SELECT：实例 UUID/8.4.8 一致；源 165 表、A/B 各 102 表；新恢复库不存在。宝塔现有 Python 3.7 依赖给出弃用警告，本阶段未安装/升级其依赖。

真实恢复结果待执行后追加，当前未预先宣称恢复成功。
