# 行情自动选源实施进度（2026-09-14）

状态：选源核心及分析读取接线已实现；整体公共行情流程未完成，当前前端尚未切换。不把这批源码和离线测试作为运行验收。

## 已完成

- 撤下未采用的账户品种缓存表草稿及持久化接线；保留尚在使用的终端读取接口，待共享接口替换后收口。
- market 域新增公共/私有池、品种作用域、共享选源状态和 MySQL CAS。首次按数字账户 ID 排序，选定来源保持；当前连接消失、重连 epoch 改变或持续探测失败后切换代次。成功探测五秒合并，临时失败至少三次且跨十五秒后换源，选择循环有二十秒截止检查。
- auth 提供活动管理员候选；trading 通过当前账户所有权和有效网关路由筛选候选；固定只读 instrument 查询由桥接解析真实品种，繁忙与不支持分开处理。
- strategies 提供按用户、策略、活动版本授权的数据源模式读取。analysis Worker 已在源码组装统一选源；分析所有周期读取同一来源，读取后重查策略授权及来源代次，不再自动查找其它账户补缺失缓存。
- MT5 去掉独立的十六字符后缀上限，保留现有传输字符及总长度约束；MT4 只读报价、合约、K 线入口加入完整匹配优先的后缀解析。MT4 多候选是稳定选择，MT5 仍拒绝歧义，两端异常多候选政策后续需统一；正常单候选不受影响。
- 内部 bridge-market-read 请求新增机器合同及正反例；错误码不回显任意终端错误内容。
- 新增已认证的 `GET /api/v4/market/public-snapshot`，按基础品种及周期读取公共池已选来源的现有行情缓存；复用缓存，不新增公共 K 线副本表。读取前后复核管理员资格、账户归属、来源代次及真实品种；换源或越界行拒绝返回。响应仅含行情及不透明来源标识，不包含管理员账户信息。API 主入口、HTTP 源合同、生成类型/运行合同和前端 API client 已接入；尚未接入首页。
- 公共基础品种目录复用 `system_settings` 的 `market_data/symbols`，现有管理员设置接口可维护；新增 `GET /api/v4/market/public-symbols` 及客户端读取。目录只接受不重复的基础品种，删除品种同时阻止后续公共快照读取。目录不可读时明确失败，不偷偷恢复默认值。
- 新增 `worker-public-market` 独立入口及 PM2 角色，健康端口默认 3029。复用已有五秒异步循环、选源器及 Bridge stream demand；每个品种一份报价及七周期订阅，续租不创建新订阅，换源、权限失效、目录删除及退出均释放旧租约。它只维持报价/当前 K 线采集，不承担历史补窗或 AI/交易执行。

## 已执行的数据库变更

用户单独确认后，在 192.168.1.254:3306/dev_vue 新增 market_source_selections 表，并登记 inplace_081_01_market_source_selections。

- 源文件：server/db/migrations/20260914_029_market_source_selections.sql；已经执行，禁止再修改正文。
- 文件 SHA256：dd175e7c7d4ec3419657e93214b8ac386aab62f86a4efec1f74809f95b7a39e3。
- 步骤 checksum：e7e5ba695eba1502926f6457ab280b05fbda90d9aa71a09040edb02559f4199e。
- 执行器：scripts/apply-market-source-schema-local.mjs；固定主机、库、server UUID，复核已有 272 步、同名迁移锁、表字段/主键/引擎/排序规则/触发器。只执行单表 DDL 与迁移日志，不改原账户、订单和行情表。
- 回执：D:\dev_codex\.local-runtime\market-source-build-20260914\migration-receipt.json。只读复查返回 already_completed。
- 暂停使用新功能即可回退运行，不删除表、改历史迁移或重写旧数据。

用户再次单独确认后，执行 `scripts/sql/initialize-public-market-symbols.sql`，只新增一条 `market_data/symbols=["XAUUSD"]` 配置，回读 revision=1、affectedRows=1。没有改表结构或覆盖已有配置。SQL SHA256：34253243890d09a4598fded0aa756351686704bb77e15dd6e7dc248ada33a223。该文件属于配置初始化，不能放入仅接收结构变更的迁移计划。

## 验证

- 七组服务端定向测试共 44 项通过，涵盖共享选源、权限、CAS、分析来源一致、内部协议及原有分析基线。
- MT5 Worker 76 项单元测试通过。
- 服务端构建、现有模块边界与生成合同检查通过；.NET 核心 x86 checked/warnings-as-errors 编译通过。
- 本轮相关源文件在兼容仓库 CRLF 的 whitespace 检查下通过。全工作区既有大量 CRLF/未提交改动，未清理无关文件。
- .NET 构建结果在 D:\dev_codex\.local-runtime\market-source-build-20260914\LiangjianBridge.Core.dll，未替换运行版本。
- 公共快照新增 8 项服务端用例通过（含 HTTP 认证、普通用户读取、来源切换、权限撤销、错误账户行）；API client 34 项测试通过，其中新增公共快照消费者 3 项。API client 类型检查、服务端构建、模块边界及 HTTP 生成一致性通过。
- 本轮采集、目录/快照、设置、需求协议 36 项测试及合约品种读取 3 项回归通过；公共行情客户端 4 项测试、客户端类型检查、服务端构建和生成合同校验通过。

## 仍需完成，不能宣称已上线

用户已确认历史补齐范围：仅页面或策略实际使用的品种触发历史补齐，不遍历终端全部品种，也不因品种仅存在于管理员目录就全量预补。公共数据仍受管理员品种目录限制，私有策略从用户桥接端取数。被使用品种的 M1/M5/M15/M30/H1/H4/D1 各补最近 2000 根已收盘 K 线；首页初始展示最近 200 根。后续仅增量和断线补缺，不合成休市蜡烛。按需补窗已实现，当前真实验收范围见下节；数量达到目标不代表所有市场休市日历和历史正确性已经全面验收。

1. 管理员基础品种目录后端维护/读取、黄金初始化及首页接入公共目录已完成；管理端专用编辑界面仍待完成。
2. 公共/私有按需历史补窗已接线；继续做私有策略真实账户验收和不足历史的专用页面提示。历史仍不足时分析不得用模拟数据补齐。
3. 公共快照 API、公共 realtime 分发及首页切换已完成本地真实链路验证。首页公共行情不再订阅个人账户行情，账户指标和持仓仍按账户隔离；多管理员来源切换、撤权及跨用户完整运行验收仍需继续。
4. 私有交易策略阶段的新行情读取及混合公共分析/私有交易证据标签；本批覆盖分析阶段。
5. 选源离线后旧历史展示、完整窗口 ready 状态、复用缓存及闭市/新鲜度验证；instrument 探测本身不证明历史完整或新报价可用。
6. MT4 EA 编译、重新加载及 MT4/MT5 真实只读多账号验收；未运行真实交易，未启动新的自动分析 Worker，未部署公网。

工作区约 2444 个未提交路径，部分接线文件包含跨批改动，当前不能安全视为单一职责可发布提交。

## 首页公共行情联调与空数据展示（2026-09-14）

- 首页品种改用管理员公共目录，默认 XAUUSD。公共行情不带用户账户 ID，已提交的缓存增量经 Worker 校验当前来源和代次后通过现有 Redis/realtime 推送；浏览器遇到来源变化重新读取 HTTP 快照，不混合两个来源。
- 删除图表下方“图表由 TradingView 提供”文案。K 线与报价无数据时显示加载动画，不填充模拟蜡烛或默认价格；HTTP 错误保留错误提示和重试。报价超过 30 秒未更新时隐藏旧价格和点差，历史 K 线保留并标为历史快照。这是展示新鲜度规则，不是交易或闭市判断。
- 报价只裁掉十进制定点字符串末尾无意义的零，至少保留两位小数；不通过浮点数改写价格精度。公共图表使用 UTC，不继承查看者交易账户的经纪商时区。
- 联调发现 Bridge 网关旧排队上限为 8 条，多周期行情突发正常超过此数量，触发 backpressure 关闭；后续排队操作产生 session_fenced，掩盖原始原因。新增真实 WebSocket 回归先复现失败，再调整为最多 64 条且总量最多 4 MiB，保留单帧 512 KiB 限制，关闭后停止处理排队帧并记录原始原因。
- 本地网关更新后，7 秒只读监听收到 4 条报价、28 条 K 线及 32 条公共行情事件。浏览器已显示 08:15:16 UTC 报价 4312.68/4312.84，切换 M1 后显示 08:17:17 UTC 报价 4311.45/4311.61，点差 0.16；无页脚文案。说明本次真实增量恢复，不代表历史窗口完整。
- 验证：网关及公共转发 13 项、首页定向 38 项测试通过；贸易端类型检查及构建、前端边界、服务端构建与 HTTP 生成一致性通过。贸易端全量运行发现三项旧 Mock/源码断言未同步，已修正并定向复测通过，其余 228 项首次通过。
- 本轮仅重启本地 API、浏览器实时、Bridge Gateway、公共采集 Worker；未重启终端、未发交易命令、未启动自动分析 Worker、未部署公网。临时诊断构建已由标准构建覆盖。

## 本轮采集入口审查与运行边界

### 按需历史补齐后续验收

- 第一轮复核：复用现有 Bridge 窗口查询、market_candles 表和公共采集 Worker。页面或获授权策略登记使用的品种；不遍历终端品种，也不对管理员目录中未使用的品种预补。Redis 仅保存可重建的需求及进度，价格事实仍在 MySQL，无新表或新服务。
- 第二轮复核：一次一页、最多 500 条，只计已收盘记录。向前查询最多 24 页，不用时间间隔推造休市蜡烛；不足保存 insufficient，六小时后再核实；失败保留游标并退避 30 秒。同一账户重连复用进度，换账户重新验证。完成后每小时只重查上次核实时间之后的窗口。入库用最多 100 行短事务，再次锁定账户、所有权、凭据、绑定和会话证明；版本比较避免覆盖较新的实时蜡烛。
- 公共历史入库发送小型 market.public.history.updated 通知，浏览器仅为当前品种/周期回读 HTTP 快照，不推送历史正文或来源账号。私有策略需求也已接线，但未启动真实自动分析任务验收。
- 本地联调更新了较旧的 Bridge Core；此前请求返回 bridge_projection_refreshing，新核心使用已有直接历史窗口读取。后台复用选源时已确认的实际品种，不为每页重复查询完整终端目录。
- 2026-09-14 08:51 UTC：仅 XAUUSD 登记需求。数据库已收盘记录为 M1=2025、M5=2214、M15=2333、M30=2036、H1=2030、H4=2114；均达到 2000 根窗口。D1=1699，查询 24 页后标记 insufficient，没有补造记录。按整页缓存可能略超目标，首页仍只读最近 200 根。
- 本地进程重启后，已完成周期各只核实一页近期窗口并再次完成，没有重新全量回溯。首页已自动显示连续历史且报价持续更新。
- 验证：40 项补齐/选源/快照/查询/实时合同测试及 3 项写入权限测试通过；贸易端定向 12 项、类型检查和构建通过；服务端标准构建、模块边界及 HTTP 生成校验通过。
- 剩余边界：insufficient 尚无专用用户状态页面；多管理员与私有策略真实联调仍待执行。Redis 进度丢失时会重查窗口，不承诺跨缓存清空完全不重复读取。未发交易命令、未新增迁移、未启动自动分析 Worker、未部署公网。

第一轮：复用现有设置表、行情缓存、租约和异步循环。只增加独立采集 Worker，避免 HTTP/浏览器实时网关承担后台定时任务；不新增公共行情副本表。32 个基础品种是单轮并发上限，七周期始终复用同一个已选来源。

第二轮：补齐目录删除期间的快照拒绝、退出期间的异步选择回调、重入、租约替换顺序和配置读取失败清理。冻结的 59 项旧设置导入规则保持原样，新增运行写策略单独验证。

只读运行检查确认有 2 个活动管理员、1 个符合条件的在线管理员来源。联调发现旧 Gateway 没有订阅新增 market-read 通道；更新网关并通过健康检查后，进一步定位到 instrument 合同返回 `name`，候选探测错误读取 `symbol`，已修复并补 3 项回归。

复测采集返回 active=1、failed=[]，实际解析 `XAUUSD.s`；2026-09-14T07:48:45.195Z 的报价从数据库回读成功，M5 缓存返回 10 根 K 线。随后启动本地 `worker-public-market`，3029 健康检查返回 ok、dependencies_ready=true，已有成功工作时间且无错误码。未启动 AI 分析/交易 Worker。新 API 入口尚未在现有 3010 进程重载；浏览器公共 realtime 与首页切换、完整历史补窗仍未完成，10 根 K 线不能作为完整历史窗口验收。


### 图表历史浏览与带宽控制验收

- 首次读取 200 根；距离已加载历史左边缘 40 根时，防抖 200ms 后按最早 `open_time` 游标读取前一页，每页 200 根。单请求串行，请求起点至少相隔 750ms，失败冷却 3 秒；空页停止，后续权威快照更新可重新发现补齐的数据。
- 历史只从管理员选定来源的数据库缓存读取，不因浏览向 Bridge 发起独立历史采集。相同来源、品种和周期按时间去重；实时快照保留已加载历史，来源切换清空；追加历史按逻辑索引偏移恢复视口。
- 禁用拖动惯性，避免一次快速手势滑过多页触发持续下载。成交量使用独立底部 pane，共享时间轴，标明 Tick；不会把报价次数宣称为实际成交手数。
- 验证：公共快照 11 项测试、前端公共行情与实时隔离 13 项测试通过；前端类型检查及构建、服务端构建与合同生成检查通过。实际浏览器已验证分区和向左预加载；限速版本一次拖动仅新增一个带 before 的 200 根请求，无静止轮询历史。API 已在本地重启，未发布公网。
- 当前工作区仍含大量前期未提交改动，本次未混合提交或推送。


### 图表时间与界面精简

- 移除成交量说明和“加载更早历史”整行，保留独立成交量区及左侧自动预加载。
- 图表坐标和底部显示时区统一采用后端账户快照确认的偏移；未获得偏移时使用前端已有 UTC+3 展示默认值。默认值只用于显示，不写入终端校准或交易证据。
- 底部改为浏览器本地时钟按显示时区换算，每秒本地刷新，无时钟网络请求，不依赖 Bridge/报价推送走时。行情自身的 UTC 采样与开盘时间保留，用于新鲜度判断和 K 线定位。
- 本地页面已确认移除额外栏、坐标及底部显示 UTC+03:00；前端类型检查和构建通过。
