# 策略 API 数据切换补齐

真实恢复库 API 验收确认：strategy_write_receipts_v4 的048 SQL未进入240步升级链，写操作在读取幂等回执时返回ER_NO_SUCH_TABLE；订阅同策略ID恢复执行还缺少active状态和版本校验。

实施：保留048原SQL和既有240步checksum，追加第241步，引用048原文，仅创建策略写回执表。用294表完整结构参考库验证DDL、用户外键；复用既有持久升级协调器，在恢复库注入DDL执行成功但回执丢失，再恢复并验证重复执行无DDL，最后应用当前dev_vue。迁移前冻结完整数据摘要，以已验证备份和最新数据迁移归档为恢复依据，不删改任何原业务行。API启动schema检查纳入241步和回执表，缺表启动直接失败。

第一轮复审：该表是现有策略写入模块的必要依赖，不新增业务模型或修改HTTP请求结构。执行链仍使用原模块端口；API消费者保持响应兼容，启用无效策略明确返回409。

第二轮复审：只增加表和一条升级记录，全部旧业务行及原240步记录保持；恢复验证先行，DDL未知结果通过已存在结构摘要恢复，禁止再次盲建。恢复库API使用真实合同/服务/SQL，在外层事务内以保存点模拟请求提交，最终整体回滚。该验证不声称真实SSO、真实COMMIT或真实终端执行通过。

## 完成证据

- strategy-receipt-full-schema-v1-20260911.json：完整294表结构参考，048仅增加回执表，真实用户外键拒绝孤立引用；参考库已删除。
- strategy-receipt-restored-v1-20260911.json：DDL回执丢失恢复、重复执行无DDL、旧数据和checksum保持。
- strategy-receipt-current-v1-20260911.json：当前dev_vue已升级241步/295表，新增回执表为空，没有业务行改写。
- strategy-api-transition-v8-20260911.json：36次请求通过，包含草稿拒绝启用和正常新建/发布/订阅流程，最终整体回滚且全表数据摘要不变。v1-v7保留失败历史，其中v3/v4暴露真实缺表问题，其余为探针对已删除/历史权限或HTTP格式预期不符，未以放宽实际权限处理。
- strategy-current-readonly-v1-20260911.json：使用现有.env应用账号验证当前库新schema及策略/订阅读取；3当前订阅保持暂停，2历史订阅不可见。
- 23项相关回归、服务端构建、98项运行合同、依赖边界和schema生成校验通过。

启用响应新增稳定code：strategy_subscription_strategy_unavailable（409）。状态active且启用某角色时，该角色必须仍可访问、为active、kind匹配，且当前active版本等于订阅绑定版本；不自动改绑旧版本。现有问题响应合同已支持409及字符串code。暂停/结束不要求原策略仍可执行，保持退出能力。

整体剩余：原策略风险/记忆/模型参数实际运行承接，身份→账户→推理→风控→执行→复盘整条服务端流程，随后前端及Bridge完整流程。当前测试不授予真实账户交易或发布权限。
