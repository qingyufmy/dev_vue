# 道诚策略首次分析准备

范围：管理员查看、校验并发布已有平台策略版本；交易端选择可用默认模型；随后以道诚分析策略执行一次手动分析。不启用自动交易，不修改策略提示词，不新增数据库表。

第一轮复审：平台发布使用独立管理员会话入口，复用现有策略列表、详情、合同校验和写入回执。普通用户仍只能发布自己的策略。模型页面只做默认模型选择，未验证模型明确不可选；不引入新的供应商或密钥配置流程。

第二轮复审：发布事务重新检查管理员身份、平台归属、版本归属、配置与修订号；重试复用幂等键，记录现有回执。初次发布不打开任何订阅开关。模型选择重新验证所有权和共享政策，服务端推理仍保留原有校验。页面失败保持可重试，切换目标丢弃旧响应。验证包括普通用户拒绝、并发修订、配置无效、回执重放、类型及生成合同一致性。

剩余风险：平台策略发布对所有可见用户生效；既有订阅版本保持原绑定，本阶段不批量升级订阅。模型已验证代表配置可用，不保证供应商此刻在线；真实调用后才能确认分析完成。

实施：新增管理员策略列表、详情与发布接口及页面；复用原有写入回执和事务，不改普通用户发布权限。新增模型列表与默认选择接口及页面，过滤他人模型、隐藏凭据、验证个人配置/平台共享政策，并以原默认值比较避免覆盖并发修改。模型选择为幂等 PUT，相同目标重复提交不重复写入。

验证：服务端构建及边界检查、两端类型检查、前端边界检查和合同生成一致性通过。新增发布、模型选择及 HTTP 定向测试3项通过；原有策略版本写入测试3项通过。全量 server/tsconfig.json 检查仍报告既有 Bridge/行情测试的 Ajv 类型及导入扩展名问题，不能宣称全量类型检查通过。

本地实际状态：API 3010 已重启；交易端成功读出个人未验证模型与共享已验证模型，并将当前账号默认模型设为共享 deepseek-v4-pro（配置5）。管理员经正常身份中心登录成功。实际页面检查补修了管理员路由被错误注册到交易域名校验组，以及管理员适配器未按开发环境 Cookie 名称读取会话的问题；新增 Cookie 测试验证生产配置仍拒绝开发 Cookie 和交易会话。

道诚分析策略1、版本1（业务版本v44）已通过管理员接口发布，页面确认成功。仅发布分析策略，交易执行策略与订阅开关保持原状态。启动本地分析 Worker（3023）与 outbox 服务（3020）前检查待分发事件，只有本次 analysis.requested。启动脚本限定这两个角色及 dev_vue 数据库。

真实分析任务 b0a6331b-9b57-42b0-8a88-e0688a0a7032、模型任务 de08f457-34cd-482d-b1d1-9b368565d218 已完成，一次 DeepSeek 调用成功。结果4325e20f-6d40-49c4-8a5c-bfdf78f35fa8自动出现在分析记录，首页也显示道诚最新结果。结果为观望：多周期K线覆盖完整，但模型指出缺少道诚所需缠论结构、能力对象、入场事件、结构保护与目标对象。它证明发布、选模型、排队、推理、落库及前端结果读取可用，不代表策略证据输入已完善。

未启用自动分析、自动交易或发送终端指令。后续优先补道诚需要的通用结构/事件数据，并改善过长英文结论的呈现；不改写本次冻结结果，不放宽策略条件。
