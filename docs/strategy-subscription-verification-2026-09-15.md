# 策略订阅定向验收

本轮继续 PC 订阅流程，复用现有 shadcn-vue 表单和接口，不调整调度规则。运行间隔来自策略，订阅选择接收时间。

修复：保存或结束旧账户订阅后，仅在账户加载范围未变化时刷新列表；切换账户关闭旧表单；保存期间忽略重复提交；保留已绑定但不可用的策略选项并标注；错误改为中文说明，缺失策略名称不展示内部编号。

验证：Mock 验证旧账户保存延迟返回不会覆盖新账户、保存字段正确、内部错误不泄露；现有订阅新增和更新幂等测试通过。浏览器确认 ATR 订阅已暂停、编辑回填及不可用策略名称显示。未改变真实订阅启停或发送交易权限。

未验证：本轮未发起真实配置写入，实际启停调度需后续在指定测试订阅验证。工作区继承改动未安全拆分，不整体提交推送。

下一步：完善 AI 分析师的历史筛选、详情与运行状态，核对策略订阅到分析结果的展示。
