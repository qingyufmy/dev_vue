# 顶栏运行状态调整

左侧保持连接、自动分析、自动交易、市场状态；右侧增加公共时区时钟，默认 UTC+3，浏览器本地每秒计时，不增加时间推送。

复审一：沿用既有组件与订阅设置。自动交易只存发送许可，不自动启用分析、交易策略或改变暂停状态。没有订阅时提示先添加订阅。去除开关状态文字。

复审二：会话和账户变化清理运行状态；下次调度按 nextDueAt 倒计时，过期显示等待调度。分析中仅依据真实 running 事件显示非百分比背景动画，断线清空，超过 15 分钟不继续展示未确认的运行状态。减少动态效果偏好时停止动画。

验证：交易端类型检查、服务端构建通过，运行控制 5 项测试与策略服务/执行偏好 17 项测试通过。本地 API 已重启，订阅读取返回 200。浏览器检查时间和可用的关闭状态开关，未开启自动交易。

限制：刷新页面时已经开始、且没有新状态事件的任务暂不显示分析中；仍显示真实调度倒计时或等待调度。没有伪造任务完成百分比。
