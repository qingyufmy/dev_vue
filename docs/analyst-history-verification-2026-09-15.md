# 分析师历史与状态验收

本轮范围：最近 50 条分析历史的品种/策略名称搜索、历史策略名称、状态顺序保护和结构化字段中文展示。搜索在已加载列表本地完成，不新增网络请求。

调整：保留已停用策略用于历史名称显示，手动运行仍仅使用已发布策略；忽略过期任务状态消息；读取失败不直出接口正文；已识别结构化字段拆成中文键值，不序列化内部对象和编号。

验证：类型检查通过，8 项定向测试通过。浏览器验证 11 条真实历史的搜索、无匹配提示、中文价格字段；没有发起模型调用、交易或数据库写入。

剩余限制：搜索范围为最近 50 条，不是全历史。旧模型正文与市场环境仍有英文及策略代号，未改写历史推理；下一步应处理新分析结果的中文输出规则及已有正文的阅读体验。当前只识别已登记字段，其余结构不展示，不推断数据含义。

本轮文件混合在既有大量改动中，未整体提交推送。
