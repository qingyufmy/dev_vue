# 模型管理与用途分配

范围：添加模型、软删除模型、按行情分析/交易决策/复盘总结分配模型。三个选择为空时使用用户默认模型，再使用平台默认模型。已开始任务保持冻结模型。只支持已确认的三类，不增加分层路由、策略级覆盖或多模型自动切换。

添加默认个人模型，管理员可创建共享模型。新模型先未验证，手动验证成功后才可选择。提供商随新增填写，密钥沿用加密存储；禁止回显。

删除检查默认模型及用途引用，仍被引用时要求先改配置；不删除历史任务、结果或密钥证据。使用软删除，并按模型指纹与幂等键记回执。

新增用途表一人一行、三个可空模型 ID、一份修订号。保存采用用户行锁、模型行锁和修订号校验，使用现有模型配置回执记录请求摘要和结果；用途变更以 model_profile_id=0 标记，绝不保存密钥。运行时显式用途未验证或失效时报告不可用，不自动换模型或凭据。

第一轮复审：三类覆盖现有分析、交易与复盘，减少旧版七类；不做版本绑定、自动降级、自动模型调用。新表避免覆盖旧版用途记录。
第二轮复审：所有写操作认证、权限、CSRF、幂等及并发校验；软删除保留历史，选择与删除共用模型锁；运行时重新检查共享权限。迁移仅新增表，须用户单独授权执行。未授权前完成源码、合同和离线验证，暂不部署依赖新表的代码。

## 本地接入验收（2026-09-15）

用户明确授权后，在 192.168.1.254/dev_vue 执行 031 迁移，已核对六个字段。仅新增用途表，未新增、删除模型或更改现有分配。

本地 API 于 04:22:56 UTC 更新，自动分析进程经用户重新授权于 04:23:34 UTC 重启；两者 ready=true、dependencies_ready=true。浏览器确认三类用途选择与保存按钮可用，原先接口不可用提示消失。

27 项相关离线测试通过；本轮额外复验用途保存及选模 11 项通过。前端类型检查通过，添加抽屉与列表布局已检查。未发起实际模型调用，未用真实模型增删或配置改写进行验收；交易决策与复盘实际任务调用仍需后续流程验证。

当前工作区含 2606 项已存在及本次变更，无法安全拆出单一职责提交，本次不执行批量提交或推送。下一步由用户配置并验证模型连接，再检查自然分析任务的选模及结果。
