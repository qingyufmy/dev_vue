# 后端核心包 1、2：实现与剩余范围

## 2026-09-13 更新：场景风险额度确定性执行已接入

新增通用 risk_budget v2：策略声明总上限、默认额度和 market_regime_limits，原 v1 固定预算保持兼容。inference 从已验证的原交易员快照中读取分析场景，核对分析 ID、内嵌分析内容哈希及整个快照哈希；risk 通过公开证据端口选择额度，交易员动作不能覆盖场景。未知/缺失场景使用策略显式默认值；分支值不得超过总上限，百分比比较使用定点整数。

H1/H4 最新候选为 `architecture/h1-h4-role-prompt-candidate-v5-20260913.json`，对应 `migration/h1-h4-role-section-review-v5-20260913.json`。默认0.5%，h1_reversal_watch/h4_fallback为0.5%，只有精确h1_continuation为1%。档位换算与显式手数审核共用这一额度；省略或提高动作 risk_ceiling_percent 不会放宽策略上限。风险提交事务再次读取原场景和配置，变化时在写入前拒绝。

本项关闭“缺少分支额度强制执行”的源码缺口。分析师是否正确识别原策略场景仍属语义验收，不能把标签匹配当作客观缠论条件证明；新候选仍 executable=false、semanticAcceptance=pending。保留10节原文，开发库只读源哈希核验/配置编译通过，databaseWrites=0。本轮不涉及数据库迁移、服务启动、真实模型或终端调用，未更改既有策略和订阅。

验证：5个服务端定向文件73项测试、候选/转换13项测试通过；完整服务端类型检查、构建、零新增模块越界、109项API运行合同、267步/73表readiness通过。覆盖精确小数越界、未知场景、冻结分析串号/哈希不一致、档位及显式手数、动作放大额度无效、提交前场景变化回滚。证据级别为源码与模拟端口测试，未声称真实账户或MySQL分支风险实测。两轮复核见 `backend-strategy-regime-risk-20260913.md`。继承工作区尚无法安全拆分，未提交或推送。

下一步：独立M15机会与M5触发、结构保护/目标及允许事件类型的映射，随后核对ATR加速和可信休市区间；场景分类一并纳入原策略语义验收。

## 2026-09-13 更新：角色候选配置绑定与范围纠正

H1/H4 v4 候选已显式追加分析价格事件开关、交易员 required M5 事件策略和 1% 单笔风险总上限。生成器只允许白名单字段追加，拒绝覆盖原声明，调用实际策略编译器，并校验事件来源已启用及周期存在；记录基础、追加内容和结果配置哈希。普通旧配置转换器未改，不按策略 ID 自动注入规则。

重新只读核对 dev_vue 中 source 1/version 44 和 source 3/version 11 的完整源哈希，生成 `architecture/h1-h4-role-prompt-candidate-v4-20260913.json` 与 `architecture/atr-role-prompt-candidate-v3-20260913.json`。10/13 节原文完整保留，角色配置编译通过；两份均 executable=false、semanticAcceptance=pending、databaseWrites=0。ATR 不套用 H1/H4 的事件与风险规则。当前账户创建分析入口已实现，已从两份最新候选的缺失项中移除，真实账户验证仍留待功能验收。

**范围纠正：** source 1/version 44 原文是“同方向独立新事件建议冷却 15 分钟”，并非强制间隔。因此不新增平台 15 分钟硬拦截，不再把它列为核心实现阻断，原文建议照常保留。下方历史记录中的“同向间隔待实现”以本更新为准。

1% 总上限已经有确定性风险实现，本轮完成候选声明；H1 reversal_watch/H4 降级的 0.5% 分支约束仍未完成，不能用共同上限替代。事件账本也不证明独立 M15 机会，当前突破/收回两种事件不能冒充所有旧允许类型。结构保护/目标、独立机会及事件类型覆盖、分支风险、ATR 入场加速语义和可信休市区间仍是剩余语义范围。

验证：候选绑定/原文保留/配置转换 13 项测试和确定性风险/事务重验 43 项测试通过；实际开发库只读生成成功。本轮未修改运行服务端、API 或数据库结构，不重复全仓构建。两轮复核记录在上述候选对应的 migration/section-review 计划中：第一轮核对原文与职责，第二轮核对兼容、哈希、字段约束及不激活边界。继承工作区改动尚未能安全拆分，本轮不提交或推送。

下一步直接处理旧策略分支风险及独立机会/结构证据的映射，不新增建议性冷却硬门禁或扩展归档页面。

## 2026-09-13 更新：事件使用账本和事务去重已完成

完成 inference 的事件声明校验、账户级事务占用、风险批准/拒绝时的状态结算，以及交易员冻结使用记录。唯一键保证并发请求不会重复消耗同一用户/账户/策略/事件。风险拒绝释放占用并保留历史记录；风险批准后保持 consumed，未知终端结果不自动释放。旧策略未声明 entry_event_policy 时不强制改变原有行为；显式声明或动作主动引用 entry_event_id 后执行确定性校验。

注册时再次读取已持久化交易员快照和哈希，不接受自造事件 ID、串号、无效/未来事件、错误品种/方向/周期或一决策重复引用。迁移完成时间以前的空账本记录为 unknown，注册时不能视为未使用。提交回执未知明确报告 inference_commit_unknown，保留已可能提交的账本，不盲目重放。

开发库 192.168.1.254/dev_vue 已执行追加迁移 079：267 步、315 张表。`architecture/entry-event-current-v1-20260913.json` 证明 1 次 DDL、原业务数据行数/哈希未变、旧升级校验值未变、重跑无 DDL。服务端 readiness 已同步 267 步/73 个所需表。

`architecture/entry-event-reference-v1-20260913.json` 验证真实 MySQL 的并发唯一获胜、风险拒绝释放、批准后保留、跨作用域隔离、事务回滚和提交回执丢失，以及 DDL 回执丢失恢复。`architecture/entry-event-worker-v3-20260913.json` 验证实际 AnalysisWorker/TraderWorker/Risk Worker、MySQL 和 Redis/BullMQ 的冻结事件→占用→周末风险拒绝→释放链路，未创建执行意图。两类测试均只使用临时隔离库，已清理库、队列、路由和租约；无真实模型或终端调用。早期 worker-v1 仅是测试克隆表清单遗漏导致失败，v3 为当前完整证据。

源码定向测试、完整服务端类型、构建、模块边界和 109 项 API 运行合同通过。设计及两轮复核见 `backend-entry-event-contract-20260913.md`。本更新关闭基础事件使用账本与事务去重项，不把它等同于旧策略完整等价。

下一项核心工作：收口旧策略的显式配置和准入规则（同向间隔、独立新机会、结构保护/目标与风险额度），随后处理可信休市区间。当前不扩展前端页面或其它事件类型，不自动启用策略。

## 2026-09-13 更新：客观事件身份与冻结链路已完成

market 新增两根已收盘突破及后续收回确认，沿用旧版 20 根参考窗口和最近 8 个确认候选。事件使用稳定 `event:<sha256>` 标识，身份由源账户、品种、周期、方向、原触发/确认时间及收回父事件组成；不使用分析 ID、窗口下标或当前价格。同一事件追加行情、滑动窗口后保持身份，失效保留原身份并标记 stillValid=false。完整输入最多 30 根，随事件及其哈希进入原分析快照，支持离线重放。

分析策略显式配置 `price_action_evidence: { version: 1, enabled: true }` 后，客观证据位于 `market.events[周期]`。未配置或关闭时不新增读取或事件输出；当前旧策略和订阅未被自动修改。时钟不可信、未来/未闭合数据、历史缺口、历史不足或源过期均输出不可用。仅支持明确列出的突破和收回；K 线形态及其它事件不能由模型补造。

交易员通过原分析的 source reader 获取 `marketEntryEvents`：校验分析/run/snapshot 关联、来源账户和快照哈希，再从冻结输入重放验证事件，剔除计算原始 K 线后投影。它不从当前行情重算，也不从模型 keyLevels 接受自造事件 ID。源快照与交易员原分析不匹配则拒绝。

验证：`architecture/price-action-event-parity-v1-20260913.json` 记录 500 组离线旧算法对照，包含 251 个突破、81 个收回，事件方向、确认时间、有效性、失效时间和收回条件一致；30 根有界窗口与完整窗口结果一致。5 个定向测试文件 102 项通过，另有缠论/行情集成 5 项通过；服务端类型、构建、模块边界和 109 项 API 运行合同通过。本轮未连接数据库、模型或终端，没有 DDL/DML。

两轮设计复核及明确事务边界见 `backend-entry-event-contract-20260913.md`。本次关闭“缺少客观事件身份及原分析冻结事件输入”子项；账户使用账本、并发占用/释放、同向 15 分钟间隔及独立新机会的策略准入仍未完成，因此不能宣称重复入场已被阻止，也不解除旧角色候选的事件阻断。下一步直接实现使用账本和最终事务去重，不扩展事件种类或前端页面。


## 2026-09-13 更新：实际账户持仓创建分析已接入

新增独立的 `accountPositionEntryEvidence`，由正式 `worker-trader` 组装，按当前账户、持仓版本和 ticket 进入交易员冻结输入。它不依赖观察员发布权限，也不从策略参考组合选择账户。复用 trading 的持仓来源/所有权/终端凭据校验、trade-history 的完整生命周期及采集覆盖、execution 的精确开仓订单归因和 inference 的创建决策冻结分析，无新增表或迁移。

净额仓位逐笔保留所有开仓贡献；任何一笔来源不明、策略混合、分析缺失、历史覆盖不完整、持仓版本/数量/稳定标识变化时均标记不可用，不返回残缺的单一入场依据。已有旧策略版本的创建证据可以保留，不要求等于当前交易员策略版本。投影仅提供冻结条件、原版本、时间及哈希，不发送完整提示词或私有决策正文。单仓最多 32 笔/32 KiB，总量最多 256 KiB。

`entryEvidence.state=ready` 仅证明创建决策使用的历史分析，不证明成交时行情或授予管理权限。MT4、手工仓以及缺少 V4 精确归因的旧仓仍可出现在原持仓列表，创建分析明确不可用；禁止用当前分析补写历史。

验证：5 个定向测试文件共 81 项通过（其中新账户证据 14 项、交易员输入新增 1 项）；服务端全部 TypeScript 类型检查、构建、模块边界和 109 项运行 API 合同通过。本轮仅本地源码及模拟端口验证，没有连接数据库、执行迁移、模型调用或终端交易，实际有仓账户联调仍待前端/Bridge 功能验收。

复核一：账户持仓和参考组合保留独立入口，仅共享精确历史解析算法；跨域工厂由 bootstrap 组装，不在业务模块中直接跨域取 repository。复核二：一致事务、当前所有权与连接守卫、读取前后 Redis 路由一致、完整持仓集合/版本、每笔开仓决策与哈希、不可用语义和聚合容量已逐项检查。

本更新完成下文“实际账户持仓创建分析入口”的源码接入。旧角色候选报告是生成当时的快照，不能仅删除其中阻断就自动启用。核心后续按顺序继续：事件身份及已使用记录、结构保护/目标与原策略风险约束、可信休市区间；ATR 入场加速等旧语义仍需逐项核对，完整旧策略等价验收尚未完成。


本页记录 2026-09-12 本地源码及开发数据库实测。当前数据库仍是 192.168.1.254/dev_vue，本轮业务库写入为 0。临时整链测试库、队列、路由和租约均已清理。

## 2026-09-13 更新：旧角色的实际字段映射与阻断复核

将通用模型合同从 HTTP 供应商适配器移至 inference/domain/model-context-contract.ts。分析合同明确 market.indicators.chan[周期].structure、能力字段及 EMA34 入口；交易员合同补齐已实现但之前遗漏说明的 after_close_protection：与部分平仓同一动作声明，仅使用 stop_loss/take_profit，禁止模型指定 after_close_target 或并发修改同一目标，后续保护不能被描述为已成交。未增加策略专属交易规则。

已生成保留全部旧文本的角色候选：H1/H4 使用 `architecture/h1-h4-role-prompt-candidate-v3-20260913.json`（对应 migration 同名 section-review-v3 计划），ATR 使用 `architecture/atr-role-prompt-candidate-v2-20260913.json`。两者源哈希核对及配置编译通过，提示词完整内容保存在既有受控本地目录；文件名包含计划哈希，后续生成不会覆盖旧候选。候选仍 executable=false、semanticAcceptance=pending，未写入现有策略版本或启用订阅。

复核结果：参考组合、参考持仓的创建订单分析证据、部分平仓后保护执行链、可选 risk_ceiling_percent 均已有实现，不能继续当作完全未开发。与此同时，创建订单时分析不是终端成交时行情，参考组合证据不是实际账户持仓证据；可选风险百分比字段也不证明旧策略的强制额度已声明。H1/H4 v3 特别保留结构保护/目标市场证据的映射阻断，不能用执行保护链已存在来替代它。

91 项模型/部分平仓/风险合同定向测试和 2 项候选保留原文测试通过，服务端类型、构建、模块边界与 109 项合同检查通过。第一轮核对已存在能力的实际字段；第二轮核对语义不等价处，不解除无法证明的阻断。

**下一项核心实现：实际账户持仓的创建分析证据。** 随后核对触发事件使用记录、结构保护/目标与原策略风险额度；可信休市区间证据仍保留。旧策略“完整运行等价”尚不能关闭，归档页面扩展不作为这一步的替代。

## 2026-09-13 更新：旧执行的成交明细 HTTP 已接通

新增 `GET /api/v4/history/executions/{legacy_id}/deals`，前端 API client 提供 `listArchivedExecutionDeals`。trade-history 应用服务经 execution 公开读取端口确认原用户和旧账户，再由本域 repository 按订单→交易结果→成交的显式关联读取；不按票号、品种或邻近时间猜关联，不用当前账户 ID 替换旧账户 ID。

结果包含原成交/结果 ID、ticket、方向类型、手数、价格、盈亏、佣金、隔夜费、其它费用及 UTC 时间。金额全部由 MySQL DECIMAL 转为字符串，未知价格/时间仍为 null。列表采用原成交 ID 倒序游标，最多 100 条；空列表仅表示没有匹配的归档事实，不证明终端未成交。系统 user_id=0 不能作为普通会话读取身份。

`architecture/history-archive-deals-http-current-v1-20260913.json` 是实际 dev_vue 仓库+HTTP 组装的只读证据。除原 455 条信号、154 条执行记录分页对账外，旧订单 406、404、398 的 6 条成交以每页 1 条读出，票号、手数和费用/盈亏逐值与原表一致，跨用户请求 404。认证由测试注入，未冒称浏览器登录或终端联调，数据库写入为 0。

服务端归档/入口定向测试 10 项、前端归档客户端 2 项通过；服务端、contracts 和 api-client 类型检查及构建通过，合同操作增至 109。第一轮复核跨域边界与身份命名空间；第二轮复核十进制字符串、未知字段、游标、错误响应及权限拒绝。未新增迁移或修改旧表。

当前个人历史执行→成交的基础追踪接口已具备。系统级原始推理/提示词和完整风控审计内容仍属受控归档；核心后续聚焦旧策略语义映射与可信休市区间证据，不把本次历史接口视为整个包 1、2 完工。

## 2026-09-13 更新：完整计算输入归档已完成

新增 market 的 `captureChanCalculation` / `replayChanCalculation` 公开入口。归档保存计算所用完整 OHLC、时间、收盘状态、周期、源账户、参考时间和时钟样本，并记录输入与输出的规范化 SHA-256。单周期最多 2000 根；重放使用冻结参考时间，不读取当前数据库、终端或系统时钟。未知版本、超限输入、输入变化或输出不一致均拒绝。

AnalysisMarketSource 将归档置于 `market.calculation_archive`，沿现有 beginAnalysis 事务与 inference_snapshot_payloads 持久化并纳入原快照哈希，无需新增表或迁移。AnalysisWorker 在模型调用前生成独立副本并移除归档，HTTP 模型适配器再次执行同样投影；原模型窗口及缠论可见结果保持不变。关闭缠论不生成归档。

验证：`architecture/chan-durable-archive-worker-v1-20260913.json` 使用实际 MySQL/Redis/BullMQ 和 AnalysisWorker，持久快照中 1800 根行情读回后重放结果与原冻结投影一致；改变 K 线会被拒绝，模型端仍只含 300 根且没有 calculation_archive。周日风控正常拒绝，测试临时库、4 个队列、路由和租约全部清理，原业务库写入 0。行情、模型与终端端口仍为参考数据，不当作实盘证明。

27 项定向测试通过，服务端类型、边界、108 项 API 合同与构建检查通过。顺带更新了模型解析器测试中已落后于个人默认模型查找顺序的模拟数据；未更改模型授权逻辑。

第一轮复核：归档与重放属于 market，模型上下文裁剪属于 inference，保存沿既有推理事务；不创建运行时跨域 SQL。第二轮复核：序列化后的输入/输出哈希、原快照不可变性、模型请求不泄露内部历史及真实持久化重放均有证据。过去快照缺失的内部历史不从今天行情补造。

本更新关闭下文“完整内部历史归档未实现”项。当前核心剩余重点是旧策略角色/动作/风险语义，以及可信休市区间证据；系统级旧推理快照与完整成交追踪的浏览器入口仍未开放。

## 后续更新：个人历史归档 HTTP 已接通

新增并在生产 API 入口组装 4 个只读接口：`GET /api/v4/history/signals`、`GET /api/v4/history/signals/{legacy_id}`、`GET /api/v4/history/executions`、`GET /api/v4/history/executions/{legacy_id}`。列表采用原始 ID 倒序游标，最多 100 条；详情返回旧信号分析正文或旧执行状态/回执标识。接口保留 UTC、原始字符串 ID、`identity_namespace=retained-legacy`、`executable=false`。旧执行的账户/品种允许为空并返回 null，不映射为当前账户。

查询主体只能来自当前会话，所有 SQL 都限定原记录 user_id。普通浏览器请求不能用 user_id=0 读取系统归档，不能用参数冒充其他用户。服务端请求/响应同源校验、前端生成类型、独立运行时响应解析以及 API client 方法均已实现；当前 108 个合同操作与实际注册路由一致。

真实 dev_vue 只读验证 `architecture/history-archive-http-current-v1-20260912.json`：用户 1 的 455 条信号、154 条执行记录全部分页读取，数量一致且无重复；详情合同通过，其他用户 404、系统用户 0 返回 401。使用实际仓库和 HTTP 注册，认证用户由本地测试注入，不冒称完成浏览器登录联调。无数据库写入。

服务端定向 HTTP/入口测试 10 项通过，新增客户端用例通过；contracts 包 55 项通过，服务端/客户端/contracts 类型检查和服务端构建通过。生成校验过程中还修正了认证解析器的已有 remember 不一致：按现有服务器 `remember === true` 语义，未传入时默认 false，并分别检查 wire 输入与解析后输出类型。

第一轮复核：inference 与 execution 分别拥有归档读取端口、SQL 实现、HTTP 适配，入口仅组装；未创建跨域万能归档查询器。第二轮复核：非法游标、未知查询参数、重复分页参数、跨用户、系统用户 0、异常 SQL 和无效响应均有定向验证；旧票号不转为浮点数。

本更新替代下方“历史信号/执行归档浏览器入口全部未实现”的状态。**仍未开放系统级推理原始快照/提示词，以及跨域完整成交、风控明细的浏览器追踪。** 这些内容继续保留在本地受控归档；不能把个人信号与执行基础接口说成全部历史页面已完成。包 1 的角色语义、休市区间证据与完整计算输入归档仍待收口。

## 包 1：完整缠论已接入，旧策略运行等价尚未完成

market/domain/chan-v8 已完成完整 v8 引擎、固定历史窗口、多窗口一致性、证据映射与选择、时间验证和能力降级。没有引入旧 HTTP 路由、Bridge 运行代码或 v9 实验算法。通过 market 公开入口进入 TradingAnalysisMarketSource、AnalysisContextBuilder 和生产 AnalysisWorker 组装。

M5/H1 内部历史 1800 根，M15 2000 根，H4 1000 根；模型可见 K 线仍按策略原配置截取。`chan_evidence: {version: 1, enabled: false}` 不读取额外历史或注入缠论。M1 可与 H1 等共存供 EMA/原始行情使用，但不会虚构 M1 缠论窗口。

验证：完整引擎 327 项冻结合成输入新旧对比一致，覆盖非空线段、中枢、跨窗口确认及可信锚点，见 `architecture/chan-v8-engine-parity-v1-20260912.json`。16 项分析源/覆盖/调度定向测试、完整服务端类型检查及构建通过。真实 MySQL、Redis/BullMQ、AnalysisWorker 和 TraderWorker 的开发参考链通过，见 `architecture/chan-analysis-worker-weekend-v1-20260912.json`；周六风控按预期拒绝，无执行意图。本次未覆盖允许交易分支，不能把它写成终端执行成功。

旧配置转换器已映射缠论开关，保留不支持周期、私有账户组合和未支持结构化策略的明确阻断。8 项转换测试通过。当前 3 份源策略均可生成 `configStatus=converted` 的配置候选，见 `architecture/strategy-canonical-inventory-v6-20260912.json`；这不是已写入现有策略版本，也不是提示词语义已验收或已自动激活。

**仍需完成的真实能力差距：**

- 历史连续性目前仅接受可证明的连续 K 线。跨休市缺口不能靠猜测放行；需要 Bridge/行情采集提供可信闭市或完整区间证据。该缺口可能影响真实长窗口，不能列作纯展示细节。
- 旧角色提示词还有触发事件身份/使用记录、结构保护与目标证据、旧动作及百分比风险语义的映射审查。ATR 策略还需核对入场周期与持仓绑定。现有候选保留原文和限制说明，尚未宣称运行等价。
- 模型快照冻结缠论投影及原模型窗口，不包含内部全部 1800/2000 根原始行情；独立重放计算时仍需完整历史输入归档。

## 包 2：数据关联已核实，历史读取目前是本地归档入口

生产 worker-trader 使用 `createStrategyReferencePortfolioReader`。复核既有非空组合 SQL 参考证据后，相关应用端口、组合入口、读取实现和验证脚本的 5 个哈希均未变化，见 `architecture/strategy-reference-evidence-reuse-20260912.json`。既有非空待成交订单会得到不可执行的 referenceId，未知归因拒绝，跨策略过滤；该证据使用注入路由，不能当作实时终端证明。

`architecture/core-lineage-current-v4-20260912.json` 是当前只读核对结果：

| 历史事实 | 当前保留数量 | 读取去向 |
|---|---:|---|
| ai_signals | 1764 | 原始信号归档 |
| inference_snapshots_legacy_v3 | 789 | 原始推理快照归档 |
| ai_model_tasks_legacy_v3 | 9317 | 原始模型任务归档 |
| auto_signal_deliveries | 1442 | 原始分发归档 |
| order_intents | 412 | 原始订单意图归档 |
| risk_decisions | 395 | 原始风控归档 |
| signal_outcomes | 334 | 原始交易结果归档 |
| signal_outcome_deals | 427 | 原始成交明细归档 |

核对了 11 张新运行核心表的 56 条实际外键，包括组合键，孤儿数均为 0。旧快照→信号、信号→任务、分发→订单、交易结果→订单、成交→交易结果这 5 组精确关联的缺失/归属不一致数量均为 0。记录了全行摘要、逐用户数量、逐用户/账户 DECIMAL 金额汇总及已有账户 ID 映射；摘要是当前基线，不伪称与独立备份逐字节比较已通过。

新增 `scripts/lib/legacy-trading-archive-reader.mjs`：本地管理工具可按原始用户+信号 ID 读取信号/推理/任务，按原始用户+订单 ID 读取风控/结果/成交；已用真实记录验证拒绝其他用户。仅使用显式外键字段，不解析 source_id 猜关联、不按 ticket/时间相近猜关联、不把旧账户 ID 当作 V4 ID。789 份旧推理快照均属于原系统 user_id=0，该值仅是历史归档命名空间，不能冒充当前登录用户。

复现核对：`node scripts/verify-core-lineage-current-readonly-local.mjs <新的绝对路径报告.json>`。探针使用只读一致性事务，不输出提示词、模型请求正文或凭据。归档函数需由持有本地数据库权限的管理工具调用，不是浏览器授权接口。

**尚未完成：历史信号/执行归档的浏览器 HTTP 列表及详情入口。** 当前新运行 API 不自动读取旧记录，不能说旧历史已经全部在新页面可见；后续历史页面必须接入明确的归档 API 和权限模型。旧记录保留原 ID 与 UTC 时间，不转换成可再次执行的新任务。

## 下一步与范围复核

下一步继续处理包 1 的语义与真实历史证据差距，以及包 2 的归档 API；登录、账户切换等前端可并行启动，不依赖这些历史页面能力。不能据本页宣布包 1、2 全部完成，也不需要再重复已通过的复盘大链或重新设计数据库底座。

第一轮复核：计算属于 market、策略开关属于 strategies、上下文属于 inference，运行入口仅组装；跨模块均经公开入口，测试未使用真实模型或发送终端指令。

第二轮复核：区分配置可转换、提示词可执行、开发参考链和实机结果；区分实际外键通过、历史归档可读、浏览器历史接口未实现。未删除旧表、修改历史迁移、启用策略或部署公网。
