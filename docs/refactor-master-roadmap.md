# 全量重构主路线图

> 状态：2026-09-06 当前按用户确认推进终端时间链路：原始采样/查询适配已完成，可信校准及服务端/前端接续仍待完成。公网无损升级成果保留，见 §3.1 和 U1–U6 方案：U1已完成165表覆盖及身份11表148字段复核；U2事务框架、预检和读取器已有实现，A/B至025各115表，A库合成事务演练通过。身份11表352行两次只读摘要一致，历史归属墙钟顺序已核对；仍缺全源内容证明、时间/账户的完整转换与独立业务对账。完整界面、实机联调和P5B仍未完成；未部署、未业务回填，不代表已可公网覆盖升级。
>
> 建立日期：2026-09-02
>
> 适用范围：www 主站、trade AI 交易实验室、admin 管理后台、backend、database、量见智桥

## 1. 文档定位

本文是本轮从零重构的唯一执行顺序。它负责回答“下一步做什么、完成到什么程度才能进入下一步”，不替代各阶段的详细设计。

阅读历史记录时，以顶部当前状态、§3.1 当前顺序及最新获准范围判断当前任务；已完成阶段内的“下一阶段须确认”等表述是当时的交接记录，不重新激活已完成的确认门。当前执行与复审统一遵循根 `AGENTS.md` §10 和下述 §2；历史验收证据保留，不作为重复执行旧流程的命令。

2026-09-05 补充：[观摩主动切换与 Pro 无桥接分析订阅](./observer-and-pro-analysis-subscription-requirements.md)。[P4B](./stage-m1-b2-p4b-observer-management-report.md) 已补主动退出与晚返回防串号回归，未接入或改变个人订阅；后续策略/订阅 B2 必须补用户级分析订阅、独立行情来源和账户执行关联，支持无本人账户的 Pro，仅禁用本人 Bridge 数据与交易能力。Pro 无桥接订阅仍未实现完成。

以下文档继续作为强制输入：

- [全量重构需求基线](./refactor-requirements-baseline.md)
- [Vue 前端重构需求规范](./vue-frontend-requirements-standard.md)
- [单点登录与统一认证架构方案](./single-sign-on-authentication-architecture.md)
- [AI 交易实验室前端功能盘点](./ai-trading-lab-frontend-function-inventory.md)
- [管理后台现有功能盘点与目标能力建议](./admin-function-inventory.md)
- [全项目功能迁移总矩阵](./full-project-function-migration-matrix.md)
- [API V4、浏览器实时协议 V4 与 Bridge 设备协议 V4](./api-v4-and-realtime-protocol.md)
- [后端目标架构与异步任务隔离方案](./backend-target-architecture.md)
- [数据库规范化与全量数据迁移方案](./database-normalization-and-data-migration-plan.md)
- [数据库逐表迁移矩阵](./database-table-migration-matrix.md)

旧 UI 稿、旧前端实现方案和未标记为本轮权威基线的历史文档只可作为功能证据，不得覆盖本路线图。

## 2. 逐阶段确认规则

每个阶段开始前，主 Agent 必须先向用户介绍：

1. 本阶段为什么现在做。
2. 范围与明确不做的内容。
3. 将检查、设计或修改哪些模块。
4. 交付物和验收条件。
5. 可能影响的数据、接口、用户或客户端。

介绍完成后必须停下并等待用户明确确认，未确认不得开始该阶段。上一个阶段的确认不能自动延续到下一个阶段。

阶段按一个可验收的用户流程组织。同一已确认范围内的合同细化、实现、缺陷修复和必要验证连续完成，不为每个内部工作包重复增加确认门；新增业务范围仍先说明并确认。

提交是保存进度，不是停工条件；同一已授权流程持续推进到目标完成、实际阻塞或需要新增授权。两轮正式复审用于架构、迁移、身份权限、交易执行或重大跨模块方案；已批准方案内的普通修复做定向复核，不因提交或内部工作包切分而重走两轮流程。

即使用户已确认某个阶段，下列动作仍需在执行前再次取得针对该动作的明确授权：

- 数据库 DDL、DML、回填、修数或删除。
- 删除旧代码、旧表、旧接口、旧安装包或用户数据。
- 真实 MT4/MT5 下单、挂单、改单、撤单或平仓。
- 部署、重启、发布 Bridge 更新或切换生产流量。

提交、推送遵循用户后续长期授权及根 `AGENTS.md` §10：本次改动验证通过后，按单一职责提交并推送当前 `dev_vue`；不重复询问，不包含部署、强推、合并或无关文件。

用户在任意阶段补充新要求时，先更新需求基线、迁移矩阵和本路线图，再判断是否影响当前阶段或后续顺序。

## 3. 冻结执行顺序

| 阶段 | 名称 | 核心交付物 | 进入下一阶段的条件 | 当前状态 |
| --- | --- | --- | --- | --- |
| 0 | 重构基线与数据库只读审计 | 总需求、Vue 规范、SSO 方案、AI 实验室盘点、数据库总方案 | 文档存在且两轮复审完成 | 已完成 |
| 1 | 主站现有功能盘点 | 页面、入口、API、权限、数据源、支付、内容、社区、会员、异常状态清单 | 功能迁移项无未解释入口 | 已完成 |
| 2 | 管理后台现有功能盘点 | 用户、会员、策略、模型、内容、订单、通知、Bridge、审计、TDK、域名和运营配置清单 | 管理能力和权限入口完整 | 已完成 |
| 3 | 全项目功能迁移总矩阵 | www、trade、admin、backend、database、Bridge 的功能去向、权限、合同和验收用例 | 每项现有能力都有保留、合并、重塑或候选删除结论 | 已完成 |
| 4 | API V4 与实时协议 V4 | OpenAPI、实时消息合同、统一错误、分页、幂等、版本、订阅、恢复和带宽规则 | 前端、后端和 Bridge 可共同生成或推导类型 | 已完成 |
| 5 | 后端目标架构 | 业务域、route/service/repository 边界、RBAC、后台任务、缓存、日志、指标和错误治理 | 禁止跨域直连和路由散落 SQL 的规则可测试 | 已完成 |
| 6 | 交易执行统一状态机 | 信号、风控、订单意图、分发、Bridge、MT 回执、成交、撤单、平仓、对账和审计状态图 | 每个状态转换有幂等键、权限、失败恢复和审计证据 | 已完成 |
| 7 | 数据库逐表迁移设计 | 165 张表逐表映射、时间语义规则、索引计划、迁移工具、对账和回滚设计 | 165/165 覆盖、两轮复审和静态验证通过；DDL 阻塞项明确 | 已完成 |
| 8 | Bridge V4 原型与技术冻结 | Win7 双原型结果、MT4/MT5 适配、精确查询、命令账本、时区校准、配对和更新方案 | Win7/10/11 原型及升级回滚实测通过 | 进行中 |
| 9 | 清理旧前端并建立三前端工程与设计系统骨架 | 删除 `dev_vue` 旧手写前端；建立 Nuxt www、Vite trade/admin、唯一 shadcn-vue 源、合同包、令牌、图标、图表库和 CI 边界 | 旧前端源码归零且三应用独立测试、构建，跨应用依赖检查通过；旧版只从参考仓库读取 | 已完成 |
| 10 | SSO 端到端竖切 | auth 中心、三应用独立会话、PKCE、CSRF、WebSocket 单次票据和退出范围 | trade 登录、HTTP、WebSocket 和注销真实链路通过 | 已完成（实现与离线验证） |
| 11 | 账户、终端与实时行情竖切 | 账户归属、按用户 WebSocket 额度多档案连接、观摩模式、Bridge 状态、报价、K线、持仓和挂单 | 真实 MT4 与 MT5 数据、断线和切换账户验收通过 | 源码与离线批次完成；首次连接/换号及实机门待补，不等于可交付 |
| 12 | AI 交易实验室全模块迁移 | 首页、分析师、交易员、风控师、策略师、复盘师、交易记录、审计、市场行情和设置 | AI 实验室迁移矩阵全部关闭，多终端验收通过 | 进行中（已至 12U 离线实现；宏观 M1 与迁移缺口补齐中，完整联调未完成） |
| 13 | 主站全模块迁移 | 企业展示、课程、内容、社区、会员、支付、账户入口和公开页面 | 主站迁移矩阵全部关闭，SEO、性能和支付验收通过 | 未开始 |
| 14 | 管理后台全模块迁移 | 用户、会员、内容、策略、模型、商业、通知、Bridge、审计和系统配置 | 后台迁移矩阵全部关闭，权限矩阵验收通过 | 未开始 |
| 15 | 全量数据迁移演练 | `dev_vue` 到旁路目标库的重复迁移、逐表/逐用户对账、恢复和回滚演练 | 零未解释差异，备份恢复和回滚可重复 | 前置工作已提前开展：结构演练与 B1 恢复通过，B2 进行中；全量业务回填尚未开始 |
| 16 | 全系统非功能验收 | 安全、并发、死锁恢复、查询性能、带宽、长时运行、浏览器、Win7、MT4/MT5 和故障演练 | 所有质量门通过且剩余风险被用户接受 | 未开始 |
| 17 | 切换与剩余旧实现清理 | 发布顺序、灰度、监控、回滚窗口、旧 API/表/客户端候选清单，以及阶段 9 后遗留前端资产复核 | 用户逐项确认后才执行切换或删除；已发布迁移文件永久保留 | 未开始 |

### 3.1 当前优先顺序与最小交付范围（2026-09-06 修订）

本节覆盖历史进度记录中的“下一步 P5B”安排，不重排已执行迁移或否定既有离线成果。后续只在本路线图更新总体状态，专项文档保留具体合同和证据，不为每个内部步骤新建一层计划。

| 顺序 | 用户流程 | 本阶段完成判据 |
| --- | --- | --- |
| 1 | 普通用户首次连接、重连与换号 | 不手工粘贴 refresh token；从已登录用户配对到终端只读数据形成完整生产者/消费者调用链；覆盖新增、删除、换号与失败恢复 |
| 2 | 真实依赖的有限只读联调 | 获准后，在指定测试环境验证实际 MySQL/Redis、Bridge 只读会话与数据；不发送交易命令；记录实际依赖及迁移版本 |
| 3 | P5B 观摩发布和 Pro 无桥接订阅 | 先明确合法平台行情来源，再贯通各自权限与数据路径；观摩切换、用户分析订阅、账户执行关联互不代替 |
| 4 | 其余 B2、B3 与前端业务迁移 | 按既有阶段补齐；全量回填、最终非功能验收、切流与清理保留各自边界 |

**首次连接阶段的源码核查与待办：**

- `BridgeCredentialService` 当前只提供旧凭据兑换和会话票据；`.NET ProfileEditorForm` 新档案仍要求输入 refresh token。复用 auth 身份和 Bridge 凭据仓储，补一次性配对签发/领取，不新建第二套登录体系，不把长期凭据放 URL 或日志。
- 配对证明系统用户授权，不单独证明账户归属。补首次账户认领合同：精确 platform/server/login、已认证档案与终端事实、既有 owner 冲突处理、并发唯一性与历史区间；不得凭任意客户端账号字符串自动接管他人账户。合同须在接通自动登记前明确。
- 当前客户端变更路线生成新 profile；明确本地档案、服务端身份及凭据的一致生命周期，旧 `default` 使用稳定唯一映射，不合并不同用户。SQLite 与 uncertain 命令不得因换号混用或直接复制到新账户。
- 新连接采用准备、额度领取、激活和失败补偿的明确步骤；不能将 MySQL 与 Redis 伪装成一个事务。额度读取、领取、激活和 welcome 失败都纳入清理路径；同路线重连失败不应无故销毁可用旧连接。终端实际换号时旧路由必须失效，保留配置/历史不等于继续在线，也不允许客户端自动登录回旧账户。
- 删除档案撤销相应凭据、释放当前连接，保留服务端账户历史和交易证据；额度只计算当前有效 WebSocket，不计算保存的档案或历史账户数量。
- 开始新增事务前统一配对、账户归属、档案、凭据和网关激活的实际锁序；高频消息鉴权记录 SQL 次数与耗时，验证后再决定批量触达或缓存，不盲目减少授权校验。

**验收与状态：**

- 离线覆盖首次配对/超时/重复领取、跨用户拒绝、身份映射、并发认领、额度失败、同路线重连、实际换号、档案删除和晚返回；服务端合同与 .NET 消费者共同验证。前端只接入必要配对流程，使用已有 shadcn-vue 组件，不在本阶段重做全站样式。
- 获准的真实联调前移，但不自动授权连接或写入真实依赖。即使终端只读，服务端登记仍会写测试数据库，因此启动、迁移、数据范围须先明确。当前真实使用的 MT5 禁止交易测试；MT4 账户/环境未就绪就明确保留未验项。
- 后续统一区分“源码/离线完成”“真实集成通过”“可交付”；未满足最后一项不写成完整完成。阶段 16 仍做全系统验收，不能替代每个关键流程的早期集成。

**第一轮复审——需求与复杂度：**先完成普通用户入口，P5B 后移；无桥接订阅不依赖进入观摩；不增加万能 RPC、插件平台、数据库或无独立故障边界的新进程。退役技术记录默认保留备份、定位与迁移对账，不强制建设逐行在线解密查询平台；活动业务历史不能因此只留备份。宏观首版仍只交付客观因子与日历，预测模型不是上线前置条件。

**第二轮复审——异常与数据：**补新连接失败、真实换号和旧配置保留的区别；配对与账户所有权分开；未知执行结果继续精确对账，不重放。旧迁移、冻结清单和用户数据不改；退役记录缩减须在后续新版映射中提供处置证据，不能凭文档解除阻断。真实锁序、性能、Win7 更新与 MT4/MT5 验收仍待相应环境验证。

**首次连接阶段实施记录：**2026-09-06 开始。首个基础修复是网关失败清理，不代表首次配对完成。第一轮复审确认只复用现有会话、租约和关闭端口，不增表、队列或通用补偿服务。第二轮确认额度查询应进入 pending 失败清理范围；Redis claim 可能已经生效但响应丢失，须按精确 connection ID 尝试释放；Redis/SQL 清理互不阻断，错误保留可诊断，关闭失败可再次清理且不得再次接收业务消息。该修复不恢复旧账户在线状态，不调整 owner/epoch/换号授权或重放交易。

本批结果：网关连接失败/关闭清理源码完成，新增 7 项回归（额度、claim 响应丢失、激活、welcome 失败、清理双失败、新连接保护、关闭重试）。服务端 57 files / 426 tests、类型检查和 V4 构建通过；测试仅使用 fake/Mock 及本机测试用回环 WebSocket，不连接实际 MySQL/Redis/Bridge/MT。无迁移文件、客户端或页面修改，无运行服务启动/部署。后续仍需配对签发与领取、首次账户认领、档案身份映射和完整换号链路；不能把本批结果称为普通用户首次连接已经可用。

**配对签发/领取实现前复审：**复用 trade 会话与 CSRF。网页本地生成 32 字节随机配对码，只提交 SHA-256；用户将短期码粘贴到客户端，客户端生成 48 字节随机 refresh token 并保留供响应丢失重试。服务端领取事务只存 token 哈希，返回分配的唯一 profile ID、generation 和固定路径；不把凭据放 URL，不新建 SSO、不由配对推导账户 owner。第一轮确认以一次性码替代手填长期凭据，不增加匿名申请/轮询服务；用户级十分钟最多十次签发，不限制正常账户切换。第二轮确认 SQL UTC 十分钟过期、用户锁后配对行锁、同一幂等键参数冲突拒绝、同码仅同 installation/token 重取、消费与凭据写入同事务、重取必须检查凭据仍未撤销和用户仍有权益。新建 024 配对表只承接 V4 新请求，不改旧验证码、001–023 或冻结清单，不执行迁移。配对码及客户端随机凭据只在各自内存/受保护存储中保留，不在服务端明文存储；客户端重启恢复和界面接入必须明确验收后才称完整配对可用。

本批结果：新增配对签发/领取 HTTP 合同、服务及 MySQL 事务仓储，并提供 .NET 客户端领取组件；撤销全部设备同步撤销未消费配对码，避免旧码在撤销后继续签发凭据。服务端与迁移 runner 联合回归 59 files / 478 tests 通过；最终参数边界调整后配对/SSO 定向 2 files / 26 tests、服务端类型检查及 V4 构建通过。迁移相关回归 25 files / 187 tests 通过，计划为 25 文件/159 语句；27 个既有 SQL/冻结产物哈希未变。客户端编译及离线烟雾通过，包含重复领取、响应字段校验和失败恢复；未连接真实数据库、Redis 或交易终端，未生成安装包、部署或执行迁移。后续接网页生成码、软件配对窗口与受保护的重试凭据保存，再补首次账户认领及旧档案身份映射。部署本批服务端前必须应用 024，不能在缺表状态下启用新版设备撤销路径；真实并发锁行为及普通用户完整连接仍待验证。

**配对入口接入前复审（2026-09-06）：**第一轮：只替换 trade `/bridge` 占位页并接入生成/复制配对码；软件新增档案改用配对码，沿用 WinForms 与 DPAPI，不新加登录体系、轮询、二维码协议或通用向导。配对结果不能标记为终端连接成功。第二轮：浏览器仅内存保存短期码，HTTP 只发哈希，失败复用幂等键，卸载/身份改变丢弃迟到响应；软件领取前原子加密保存待配对请求，响应与档案保存失败后复用相同安装 ID/凭据，保存档案成功后才清理待配对记录。网络工作不阻塞 UI；保留既有档案，不自动连接或发送交易。首次账户认领、终端发现和换号仍在后续闭环，不隐含本批已完成。

本批入口结果：trade `/bridge` 使用现有 shadcn-vue Card/Field/Input/Button/Alert 组合生成、复制及到期清除配对码，只向 HTTP 提交哈希；失败重试保留幂等键，退出页面/切换身份丢弃迟到响应。软件新增档案移除手填长期凭据，后台线程领取前保存完整 DPAPI 加密草稿，重启后复用同一请求；成功回执也先持久化，正式档案保存后才精确清理草稿。前端全量 32 files / 143 tests、全前端类型检查/构建与边界检查通过；bridge 懒加载本体 gzip 3.26 kB（不含共享依赖）。.NET x86 编译及离线烟雾通过，包含新持久化恢复场景；没有连接真实依赖/终端或启动服务，浏览器宽度与 WinForms 实机可用性尚未验收。没有新增/执行迁移、安装包或部署。首次 owner 认领、终端路径自动发现、既有编辑换号重新生成 profile ID 的凭据映射仍须后续处理；不得将本批配对成功等同于可正常取得终端数据。

**终端识别接入前复审（2026-09-06）：**第一轮：复用 MT5 Worker 已有 `--probe --terminal` 只读入口和 MT4 EA 已建立的会话快照；软件先列出已运行终端，由用户选择后识别账号，不扫描磁盘、不启动/登录终端、不自动保存或连接。保持 WinForms 原生控件，异步状态、失败保留输入、多个终端明确选择，不增加发现守护进程或新协议。第二轮：MT5 仅读取 `terminal64` 运行进程的实际路径，探测前后校验仍在运行、返回路径一致，超时只结束本次 Python 探测进程；限制输出，错误不回显原始内容。MT4 只接受仍在线的精确实例快照。识别不等于服务器 owner 授权；当前 hello 缺账户币种，后续需明确终端事实合同后再接首次认领，禁止假设 USD 或给已有/撤销账户重建归属。换号的 profile 凭据与账户隔离缓存另行贯通，本批不以发现功能掩盖该未完成项。测试只使用假进程目录/探测响应与离线会话，不对当前真实终端运行探测。

本批识别结果：新增/编辑档案已接“刷新列表 → 选择终端 → 识别并填入”，选择与识别不直接保存配置。MT5 路径去重、稳定实例 ID、返回路径/版本/字段校验、前后在线校验以及 15 秒子进程上限已接通；MT4 仅读在线 EA 快照，过时选择拒绝。用 `ui-ux-pro-max` 复核了明确选择、异步反馈、失败保留输入和原生控件语义；未应用搜索建议中的页面换肤。x86 编译与 66 组离线烟雾入口全部通过，新增发现测试覆盖多路径、每次读取当前账号、路径不符、异常/过大响应、MT4 换号、进程超时/退出；子进程测试启动的是测试程序自身，不是 Python/MT5。`git diff --check` 通过。没有运行真实终端探测、GUI、服务或迁移，没有生成安装器；Windows/Win7 进程枚举和窗口可用性仍待实机验收。Python/Worker 路径仍依赖现有配置，发行包内默认路径、首次 owner 认领、编辑换号后的凭据/缓存映射均未在本批解决；现有编辑换号仍不得视为已可用。

**换号映射实施前复审（2026-09-06）：**第一轮：档案代表已配对设备槽，换号保留 profile ID 与凭据；账户缓存按 terminal instance/platform/server/login 的长度编码哈希分目录，不复制旧账本，不新增身份服务。服务端仅在目标账户已有当前用户有效归属、凭据和安装身份均有效时允许同档案换平台，既有账户首次认领不在本批自动放行。第二轮：旧 `profiles/<id>/bridge.db` 仅读取身份，精确匹配时原地复用；不匹配的新账户进入独立目录。连接前从该档案保存的数据集读取最大 epoch，切回旧账户不得倒退；数据集及账本不删除，缓存内容清理不删除身份/epoch 元数据。损坏、重复身份或跨档案数据拒绝，不能猜测恢复。当前单进程每档案仅一个活动连接，编辑保存后停止旧连接，不自动连新账户；真实多进程/服务端并发及账号自动变化仍需后续联调。回滚只回退源码，不移动或删除缓存。

补充复核：源码没有全局单实例约束，因此连接创建前增加同档案 `active.lock` 独占文件句柄，持有至连接关闭；文件本身不是持久“已连接”标记，进程退出后由系统释放。不同档案仍可并行，不把本地互斥当作服务器连接额度。关闭异常及 Worker 长任务期间的完整停止行为仍需单独故障注入/实机验收，不能由本次文件互斥测试推导已完成。

本批换号结果：编辑账户不再随机更换配对 profile ID；连接管理使用按精确路线隔离的 SQLite 目录，旧单库只在身份匹配时复用，无数据库复制/删除。切回旧账号时读取同档案所有数据集的最大 epoch；测试确认旧 `uncertain` 记录保留且不会出现在新账号账本。服务端在既有有效 owner、credential、installation 验证后允许档案更换平台；失败事务回滚平台/绑定变更，旧路线不能继续通过授权。服务端 58 files / 446 tests、类型检查和 V4 构建通过；客户端 x86 编译和 67 组离线烟雾入口通过（含账户往返、路线隔离、凭据持久化、损坏缓存拒绝及档案互斥）。没有真实依赖、终端交易、迁移、安装器或部署。首次新账户认领及其币种等事实合同尚未完成；已在上批生成错误随机 ID 的档案也不会被猜测修复，需后续重新配对恢复。实机关闭/重连、跨进程及 Win7 验收仍未通过。

**首次账户认领与停止恢复实施前复审（2026-09-06）：**

第一轮（需求与职责）：复用已认证 Bridge hello、终端 `account.snapshot` 和现有账户/归属区间表。每次连接由客户端直接读取当前终端账户快照，校验精确 login/server 后发送 `account_facts`（currency、login、broker_server、observed_at_utc_msc），不从档案填写币种、不默认 USD。只为数据库中从未登记的账户创建首次 owner；既有账户必须通过原 owner 校验，禁止对无主、撤销、软删除或他人账户补 owner。配对和终端事实属于受认证客户端声明，不是券商签名或独立产权证明，不允许据此转移已登记归属。继续使用现有表，不引入通用认领服务。

第二轮（兼容与异常）：`account_facts` 为 V4 hello 可选扩展，旧客户端只能连接已有归属账户；新客户端每次 hello 重新读取事实，缺失、过期、串号或格式错误时失败关闭。服务端按接收时间检查 60 秒事实窗口及最多 5 秒未来偏差；首次账户、owner 区间、凭据校验、profile/binding 与 pending session 同事务提交或回滚。保持 account → owner → credential/user → profile → binding/session 锁序，新账户先插入受唯一键保护的账户行，再验证凭据/profile 后写 owner 区间；重复键、死锁明确失败，普通重连重新核对，不覆写竞争赢家。现有账户唯一键对 broker server 的大小写/重音折叠冲突保守拒绝，不能映射为另一账户。历史起点使用服务端当前时间，不把终端历史回填成旧 owner 权限。

停止恢复在独立客户端工作流修复：停止先阻止新连接和新消息，迟到连接关闭；线程未退出不得释放运行时和档案活动锁。关闭异常保留可重试的资源引用，独立资源清理互不阻断；未知命令保持原账本且不重放。验证使用假终端/假连接、事务替身和离线编译；真实并发锁、Win7、窗口和终端长稳仍需获准实机验证。本批不启动真实依赖、不执行迁移或交易、不发布安装器。

**本批认领与停止恢复结果：**首次新账户登记已接入 MySQL 路由事务与 .NET hello 生产者；旧客户端仍仅连接已有归属账户，新客户端在建立服务器通道前读取当前终端事实，避免 15 秒查询与服务端 10 秒 hello 等待冲突。币种冲突拒绝且不覆写已有数据。账户、归属区间/当前投影、档案绑定及 pending session 的失败回滚已由事务替身验证，重复键和死锁保持明确失败，不覆盖竞争赢家。

客户端停止状态与长任务锁分离，停止超时、关闭抛错、创建取消和部分初始化失败均保留可重试对象及档案锁；迟到 hello/查询响应被隔离，暂停发生在连接期间仍可恢复。换号/移除档案先停止再保存；窗口退出失败保留重试窗口，成功清理后才退出。新增回归检查长查询期间锁仍被持有、关闭失败不阻断独立清理、停止重试、未注册对象及损坏缓存初始化失败的资源保留。`uncertain` 仍留在原账户账本，不新增交易重放。

验证：服务端定向回归后运行 `pnpm exec vitest run server/tests`，58 files / 455 tests 通过；`pnpm run typecheck:server` 和 `pnpm run build:server:v4` 通过。客户端最新 x86 编译及 72 组离线烟雾入口通过，含 MT4/MT5 事实字段、重连刷新、串号/离线/格式/过期拒绝以及停止恢复。`git diff --check` 通过。没有新增或执行迁移，没有启动真实 MySQL/Redis/Bridge/终端或 WinForms 窗口，没有生成安装器、部署或发送终端交易指令。

证据与后续缺口：并发回归注入重复键/死锁，不证明真实 MySQL 锁竞争；窗口仅编译，Win7/跨进程/实际终端换号和长稳仍待实机验收。当前本地移除档案尚未接入服务端精确凭据撤销，不能把“本地已移除”视为“服务器设备已撤销”；普通用户完整连接流程仍未标记交付。后续继续核对这条源码缺口及真实换号时旧路线失效，再准备独立获准的真实依赖联调。

**精确撤销与实际换号失效实施复审（2026-09-06）：**

第一轮（需求与职责）：删除只撤销指定 installation/profile 的当前 V4 refresh credential，使用原 token 哈希作为精确凭据与幂等身份，不调用 revoke-all，不删交易账户、owner 区间、绑定历史或 uncertain 账本。新增设备 HTTP `POST /api/v4/bridge/credential-revocations`，请求与 session-tokens 相同三字段；返回 `credential_type=bridge_revocation`、`installation_id`、`profile_id`、`generation`、`revoked=true`。仅持有匹配 token 的设备可撤销自己；会员到期或凭据已撤销不阻止同凭据完成幂等确认。网关已有 credential/generation 授权检查负责阻断旧票据与路线，物理断线和 lease 清理由关闭/心跳/TTL 完成，不把跨进程通知假装成 SQL 事务。

第二轮（兼容与异常）：事务只锁精确 credential 行并将 revoked_at 从 NULL 改为服务端当前时间，重复请求保持原时间/代次，不撤销同档案后来轮换的新 token。已消费配对码不能复活已撤销凭据。客户端在网络前原子保存 RemovalPending=true、AutoConnect=false，停止当前连接后发送请求；仅验证严格且匹配的成功回执后保存最终目录移除。失败或响应丢失保留待移除档案及 DPAPI 凭据，重启不自动连接，可手动重试；不暴露 token 或服务器原始错误。撤销与本地保存没有跨网络事务，本地保存失败重用原凭据确认撤销。旧目录缺少可选 RemovalPending 字段视为 false；旧服务端不支持接口时保留待移除状态，不能伪装成功。

本批并行核对实际终端换号：复用 MT4/MT5 当前身份事实和现有连接生命周期，旧身份不符后关闭旧服务器路线，不自动登录或修改档案。验证包括精确范围、代次隔离、幂等、失败回滚、旧票据拒绝、待移除重启/重试、晚返回及实际身份变化的假终端场景。真实 MySQL/Redis、真实终端、窗口验收及迁移/部署仍未授权。

**本批结果：**精确撤销 HTTP、事务仓储与 OpenAPI、客户端严格回执校验及待移除持久化均已接通。先停止连接，再以原凭据撤销；失败与重启后保留重试入口，确认撤销后才从目录移除。网络动作后台执行，移除期间暂停目录编辑与自动更新触发，避免覆盖目录或中途退出；不删除账户、归属、历史或 uncertain 账本。普通档案省略新标志保持旧读取形状，待移除目录旧程序失败关闭，不能回滚绕过该状态。

实际换号缺口已补：终端执行前原有身份检查保留，新增每 10 秒账户专属事实复核，独立线程不占用心跳控制锁；源事实超过 25 秒或复核失败停止旧路线。迟到结果不延长旧事实有效期，失效后 accepted 响应不得再触发未执行命令，重连清空待触发内存命令并保留原 uncertain 账本。25 秒为过期判断阈值，I/O/调度可能延迟实际关闭；远端 Redis lease 仍按最后有效心跳后 45 秒 TTL 清理，不表述为瞬时失效或已验证硬实时上限。

验证：服务端定向 6 files / 67 tests 后，完整 `server/tests` 为 59 files / 462 tests 通过，服务端类型检查和 V4 构建通过；最新客户端 x86 编译及 77 组离线烟雾入口通过。覆盖精确撤销/重复确认/代次隔离/事务回滚/旧票据和旧路线拒绝/配对不可复活、丢回执与本地保存失败重试、其他档案和账本保留、空闲换号/阻塞探测与 Handle/时钟回绕/迟到 accepted 零执行。没有连接真实依赖、启动终端/WinForms、执行迁移、部署或发布安装器。新增机制的生产者与消费者已有源码及离线证据；后续进入指定环境的有限真实只读联调准备，真实数据库并发、网络时延、窗口、Win7 和 MT4/MT5 仍待相应授权验收。

有限真实联调准备已收口至 [Bridge V4 只读联调手册](runbooks/bridge-v4-readonly-integration.md)：包含目标记录、实际依赖隔离、必要写入、迁移核对、流程验收及两轮复审。源码核对确认 Gateway 自带交易命令 consumer，不能仅停掉 execution Worker 就宣称只读。纯文件解析确认迁移计划为 25 文件/159 语句，另有 1 个 correction；未连接数据库或执行迁移。用户已指定本机；本地有运行中的 MT4/MT5，但在所查 PATH、服务与常见安装目录未找到 MySQL/Redis/Docker，默认依赖端口和 V4 端口未监听。独立依赖来源、测试身份与具体初始化/运行范围待确认，所有真实用例保持未执行。

用户随后指定使用 `server/.env` 连接。只读实测：MySQL 8.4.8 的 `dev_vue` 库有 165 张表，迁移记录格式不兼容 V4 且缺少 V4 核心表；Redis 8.0.5 的配置 DB 3 可达、当次为空，但缺少 `QUEUE_REDIS_HOST` 和完整 V4 配置。尚未执行 DDL/DML 或启动应用。建议同主机新建独立 `dev_vue_v4` 目标库，保留现有源库，具体初始化待用户授权，详见联调手册。

2026-09-06 用户明确公网覆盖升级必须保留现有全部数据，以 `dev_xin` 作为公网结构基线。初次凭据访问受限后，经用户授权 SSH 已确认两库有效结构一致：165 表、2,598 字段、570 索引、214 条旧迁移 ID；4 表建表 SQL 仅字符集显式声明写法不同。详见 [兼容核查与升级验收要求](migration/public-upgrade-compatibility-review-20260906.md)。新建空库仅为可选联调手段，不作为公网交付方案；全量旧数据转换、对账和覆盖/回滚演练是发布前必需项。

公网覆盖升级的接续实施方案见 [U1–U6 工作包](migration/public-upgrade-execution-plan-20260906.md)。建议下一批先完成 U1 全源表覆盖登记与既有身份/账户11表148列的新版本字段合同及离线校验，再进入回填工具和获准演练；保留既有B1/A/B成果，不立即新建库或执行回填。该条是本次规划建议，未增加数据库写入或切换授权。

U1首批实施已完成：[结果报告](migration/public-upgrade-u1-report-20260906.md) 与机器清单登记165表/2,598源字段，复核11表148列、更新52项映射、定位81项目标SQL声明。新增离线源目标/快照/不可执行校验，3文件22项测试通过。既有冻结清单、迁移SQL、运行配置未改，没有数据库连接或写入。其余154表逐字段合同、67列历史/待定去向、时间/关系及安装证据仍为后续门；U2可继续源码实现，U3真实回填尚不具备条件。

U2事务框架首批完成：[合同与结果](migration/public-upgrade-u2-transaction-contract-20260906.md)。新增025仅定义5张迁移审计表；业务writer、ID map、逐行receipt、batch及checkpoint同目标事务，提交响应丢失先按回执恢复，原子回滚和冲突拒绝有离线证据。29文件223项相关测试通过；SQL计划为26文件164语句，另有既有correction。未连接数据库或执行迁移；实际结构预检、源分页、领域转换、独立业务对账和真实回填继续在后续完成。

U2只读配套源码已完成：[预检与读取合同](migration/public-upgrade-u2-reader-contract-20260906.md)。实际结构指纹与迁移记录分别核对，源读取采用InnoDB只读一致快照及数据库主键排序，保留逻辑值hex并绑定来源游标。30文件236项相关测试通过；本批没有新增/执行SQL迁移，没有连接真实数据库。源内容冻结证明、权限完整性、真实MySQL验证、领域转换和独立目标对账仍待完成，所有预检保持`readyForBackfill=false`。

U2身份标量转换与逐值核对源码完成：[结果及边界](migration/public-upgrade-u2-identity-values-20260906.md)。完整登记首批11表148列；25列规则可进入整数/文本/DECIMAL候选校验，其余123列专用规则保持待处理，原有业务阻断不解除。31文件244项相关测试通过；未接业务writer或真实目标回读，字段相等不代表整行/全库对账通过。下一步继续时间/关系/状态等专用规则证据与领域转换，真实回填仍不可执行。

U2专用规则核验与源信封接入已完成：[源码、测试及现场证据](migration/public-upgrade-u2-specialized-rules-20260906.md)。增加日历/epoch、用户注销状态和精确归属图诊断，33文件257项测试通过。已授权SSH只读重查确认镜像165表、25用户/4账户/274归属区间，选定生命周期/缺父项/多open owner检查为0；274条身份字节差异均为服务器名大小写，1个归一重复账户组涉及不同用户，仍须明确映射和平台证据。A/B各102表、bootstrap+001–017已登记，018–025均未安装；没有DDL/DML或部署。真实时间来源、合并映射、历史处置和全量对账继续阻断回填。

U2 A/B结构准备已获准完成：[安装报告](migration/public-upgrade-u2-ab-install-report-20260906.md)。在用户对明确安装范围回复“继续”后，仅对dev_vue_m1_a/b先后执行018–025，每库21条计划语句和迁移日志；独立重连确认各115表、26条完成记录、无pending，实际结构hash一致。旧表计数保持不变，新增表仅022控制行1条，其余为空；本轮没有源库写入、业务回填或服务部署。上段102表/未安装记录保留为安装前历史。后续025真实事务测试数据写入需具体范围，身份语义、源内容和全量业务对账仍待完成。

U2账户候选映射已接入源审计，见[候选合同](migration/public-upgrade-u2-account-candidates-20260906.md)：平台按用户终端隔离，多源账户保留各ID和用户设置，ASCII大小写分组仅为待复核候选。35文件268项测试通过；没有真实ID合并或业务回填。[A库事务演练范围及脚本](migration/public-upgrade-u2-mysql-rehearsal-scope-20260906.md)已准备，限定025五表、最多18条合成记录及成功后的精确清理；测试DML尚未获准或执行，真实网络故障不在该模拟提交边界演练结论中。

U2 A库真实MySQL合成演练已获准完成：[结果报告](migration/public-upgrade-u2-mysql-rehearsal-report-20260906.md)。同batch并发只调用writer一次；writer错误整批回滚；commit后注入错误恢复为committed且不重放，rollback后注入错误恢复为not_committed后显式重试。最多18条合成元数据行全部精确清理；独立新进程复核A库115表结构、journal及计数恢复到演练前，B库计数与安装结果相同。没有业务表/源库写入或部署；真实网络断线、领域writer及全量数据对账未验收。上段待批准记录保留为本次演练前历史，下一步继续完整冻结源与身份映射只读核验。

U2身份11表真实完整读取已完成：[结果报告](migration/public-upgrade-u2-identity-read-report-20260906.md)。冻结镜像352行两次独立连接遍历摘要一致，每表读取到底且匹配同事务COUNT；现场修复mysql2预处理LIMIT绑定类型错误，相关35文件268项通过。4源账户形成3候选组，其中1组涉及2个不同用户；274历史归属与3绑定差异全部是ASCII服务器名大小写。1353个非NULL墙钟值仍需历史时间依据，另74个NULL保留空值；没有真实合并、业务回填或部署。只证明11表本次读取稳定，不是全源备份内容证明。下一步核实旧时间写入路径及重复账户的实体/设置/历史owner映射，然后接身份writer与独立对账，并推进登录、账户、Bridge只读页面联调。

U2历史时间与账户承接已进一步收敛：[39字段依据及承接合同](migration/public-upgrade-u2-time-ownership-contract-20260906.md)。旧写入代码和Git历史支持UTC+8候选，来源证明仍未逐行封口；冻结镜像同事务聚合确认274条历史含3条open、271条account_transferred，源墙钟倒置/零长度/重叠均0。形成3账户实体候选、4份用户设置、274条完整历史及3份当前owner候选的承接草案，保留旧ID/用户/区间，未合并或授予权限。下一步补003/019公共属性冲突检查和完整目标字段转换/独立对账；用户时间、历史来源、内容封印和真实业务写入门仍保留。本批只读DB，无运行源码、SQL、服务或部署修改。

2026-09-06用户确认接续[终端时间链路阶段](stage-terminal-clock-chain.md)：AI实验室按账户/终端校准偏移显示，服务端UTC存储、MySQL保存可信结果、Redis按需缓存，仅在状态/偏移变化时推送，历史保留当时偏移。第一批已实现Python原始tick/UTC采样和.NET `terminal.clock`查询消费，70项Python测试、x86构建和烟雾通过；真实本机只读样本未推进，未推定时区。可信校准、服务端持久化/缓存变化和前端统一显示仍待完成，阶段未验收；此前U2迁移成果保留，无业务回填或部署。

### 当前主线调整：数据库全面规范化（2026-09-06）

用户明确当前应推进数据库结构优化和全面规范化。正式[接续实施方案](database-standardization-execution-plan-20260906.md)已完成两轮复审，沿用U1–U6、既有A/B与迁移框架，优先进入U1-A实际结构/源码差异核实与U1-B全量字段和目标反向覆盖合同。下一批先产出可检查的表级问题、字段去向、关系和查询/索引清单，再分域追加结构修正及回填演练。当前仅形成方案，不新增数据库写入、部署或切换授权；MT5开市验证保留为独立事项，不阻断数据库规范化规划。

## 4. 阶段通用完成门

每个阶段至少满足以下条件才能标记完成：

- 交付物写入仓库并链接到本路线图。
- 需求、权限、时间语义、数据范围和错误状态没有未说明的歧义。
- 涉及 §2 所列重大正式方案的阶段完成两轮不同侧重点复审；普通修复记录与影响相称的定向复核。
- 静态检查、定向测试、构建、真实依赖验证按阶段风险执行并记录结果。
- 未验证的真实数据库、Redis、浏览器、MT4/MT5、Bridge、支付或生产事实明确标记为未验证。
- 发现的新缺口进入迁移矩阵或路线图，不以“以后再说”静默遗漏。
- 开始下一阶段前重新向用户介绍并请求确认。

## 5. 第一轮路线图复审

复审范围：需求覆盖、业务边界、现有能力复用、最小实现和是否设计过度。

结论与调整：

- 原顺序只有前端、认证和 Bridge 摘要，缺少主站/后台盘点、API、实时协议、后端模块、交易状态机和逐表数据库设计，已补为独立阶段。
- 先盘点再冻结合同，避免按旧路由直接生成 API V4 后遗漏业务能力。
- 没有先搭建大量空前端模块；工程骨架放在合同、后端边界、数据库设计和 Bridge 原型之后，减少返工。
- Bridge 原型先于真实行情竖切，确保前端不会依赖一个尚未验证 Win7 和 MT4/MT5 的临时协议。
- 不引入微服务、事件总线或新数据库作为默认目标；后端仍可保持模块化单体。

第一轮结论：顺序覆盖当前已知产品与工程边界，且没有为了“规范化”提前引入无明确收益的复杂架构。

## 6. 第二轮路线图复审

复审范围：兼容性、数据与迁移、并发与幂等、异常恢复、时间语义、安全、测试、回滚和连带 Bug。

结论与调整：

- 数据库总方案不能代替逐表设计，已把 165 张表映射和旧时间语义冻结放在首个 DDL 之前。
- 交易业务不能只按页面迁移，新增统一执行状态机阶段，避免手动单、自动单、分发和平仓在不同模块产生重复副作用。
- SSO 与 Bridge 会话继续隔离；账户、终端、观摩源和实时数据在同一竖切中验收，防止跨账户缓存和消息串号。
- 数据迁移采用旁路目标库反复演练，结构实施、切换和删除分开授权，避免确认一个阶段后隐含授权破坏性操作。
- 自动更新必须在 Bridge 技术冻结阶段验证从旧版到新版的升级、失败回滚和配置保留，不能拖到最终发布才发现不兼容。
- 非功能验收单独成阶段，覆盖长连接、锁竞争、带宽、Win7 和故障恢复；单元测试通过不能代替这些证据。

第二轮结论：调整后的路线图可执行。路线图复审完成时，阶段 5 已完成，下一阶段为“交易执行统一状态机”；阶段 6 后续已按单独确认启动并完成，见阶段进度记录。

## 7. 阶段进度记录

### 2026-09-02：阶段 1 主站现有功能盘点

- 用户已确认开始，交付物为[主站现有功能盘点与迁移边界](./main-site-function-inventory.md)。
- 已覆盖路由、入口、账号、会员、TRC20 目标、课程、内容、社区、通知、工具、公开交易案例、API 业务域、数据表、后台任务、外部依赖、权限与异常状态。
- 冻结“共享身份和合同、各应用独立账户页面”的用户中心边界，取消通用账户前端和 iframe 嵌入方案。
- 当前证据来自源码、路由、测试清单和 `dev_vue` 数据库只读结构/估算行数；真实支付、短信、外部存储、视频播放和生产静态资源留待对应实施阶段验证。
- 阶段 2“管理后台现有功能盘点”其后已获得用户确认并完成。

### 2026-09-02：阶段 2 管理后台现有功能盘点

- 用户已确认开始，并新增“按页面配置 TDK”和“配置主域名及各应用子域名”两项目标能力。
- 本阶段先盘点现有管理能力，再对照成熟后台形成建议新增清单；建议项必须经用户确认后才能进入迁移矩阵或实施。
- 域名配置的应用数据与 DNS、证书、反向代理等基础设施变更分层设计，避免普通后台保存直接破坏登录、Cookie、回调或线上可用性。
- 已形成[管理后台现有功能盘点与目标能力建议](./admin-function-inventory.md)，覆盖当前导航、用户、商业、内容、AI、风控、通知、审计、配置、API、数据和后台任务。
- TDK 与域名中心已列为必做能力；用户确认 A1–A5、B2–B7 加入，B1 站点运营扩展暂缓。
- 阶段 2 已完成。阶段 3“全项目功能迁移总矩阵”仍未开始，必须先单独介绍范围、交付物和风险并等待用户确认。

### 2026-09-02：阶段 3 全项目功能迁移总矩阵

- 用户已确认开始，并补充“前端实时更新数据走 WebSocket，其它数据走 HTTP API”的传输要求。
- 本阶段将为每项功能记录所属应用、用户范围、权限、HTTP 快照或命令、WebSocket 增量、数据权威源、时间语义和验收条件。
- 本阶段不实现 API、不修改数据库、不开发前端、不改变 Bridge 协议，也不删除旧功能。
- 已形成[全项目功能迁移总矩阵](./full-project-function-migration-matrix.md)，覆盖 SSO、www、trade、admin、后端业务域、数据库数据域、Bridge、后台任务和旧实现候选。
- 矩阵采用“HTTP 一致快照/查询/写命令 + WebSocket 实时增量/结果事件”，不沿用旧版浏览器 WebSocket RPC。
- 阶段 3 已完成。全量旧源码测试为 244/245 个文件、3814/3816 项通过；2 项 Bridge 发布工具测试因子进程无法识别 `Get-FileHash` 失败，已在矩阵验证记录中保留，未在文档阶段修改发布脚本。
- 阶段 3 完成时尚未开始阶段 4；其后已按逐阶段确认规则取得用户确认并完成阶段 4。

### 2026-09-02：阶段 4 API V4 与实时协议 V4

- 用户已确认开始。本阶段只冻结 HTTP API、浏览器实时通道和 Bridge 设备协议，不实现路由、不修改数据库、不改 Bridge 客户端，也不执行交易。
- 现状审查确认：旧浏览器 WebSocket 同时承载查询、设置和交易写命令；V4 将其收敛为服务端到浏览器的实时增量与异步结果，所有浏览器写操作改用 HTTP 幂等命令。
- Bridge V3 的终端身份、账户引用、连接代次、命令截止时间、执行结果确认和 revision 连续性作为 V4 的保留约束；旧动作名称和消息包络不作为兼容目标。
- 已形成 [API V4、浏览器实时协议 V4 与 Bridge 设备协议 V4](./api-v4-and-realtime-protocol.md)，冻结 `/api/v4`、`/realtime/v4`、`/bridge/v4/ws`、统一响应/错误、cursor、ETag、幂等、异步 operation、订阅恢复、流控、精准查询和确定性命令。
- 已新增 `contracts/openapi-v4.json`、`contracts/realtime-v4.schema.json` 和 `contracts/bridge-v4.schema.json`；当前 OpenAPI 是公共包络与首个 trade 竖切骨架，后续领域只能扩展同一主合同。
- 两轮复审已写入正式方案。阶段 4 不兼容旧浏览器 RPC 或 Bridge V3，但未授权删除、切换或实现。
- OpenAPI 通过 Redocly recommended lint；两份 JSON Schema 通过 Draft 2020-12 元 Schema 检查，内部引用、负例边界、文档链接和 diff 检查均通过。
- 阶段 5“后端目标架构”其后已获得用户确认并完成；阶段 6 必须重新介绍并等待用户确认。

### 2026-09-02：阶段 5 后端目标架构

- 用户已确认开始，并补充“所有后台任务必须异步，不能影响主进程或其它任务”。
- 现状审查确认旧 `server/index.js` 在单一 Node 进程同时启动 HTTP、Bridge WebSocket、调度、AI、交易协调、支付、通知、复盘和维护任务；68 个 route 文件直接导入数据库能力，当前无 BullMQ。
- 已形成[后端目标架构与异步任务隔离方案](./backend-target-architecture.md)，冻结 Node.js 24 LTS + TypeScript + Fastify 的模块化单体，以及 HTTP API、浏览器实时网关、Bridge 网关、scheduler、六类 Worker 的独立进程边界。
- 关键交易、支付和会员任务采用 MySQL 业务事务 + outbox + 确定性 job ID + 幂等消费者；BullMQ 只负责可靠投递和唤醒。queue Redis 与 cache Redis 默认隔离。
- 已将旧后台任务逐项映射到 execution、analysis、review、payment、notification、maintenance 进程组，并把禁止 route SQL、网关启动任务、Worker 调用路由等规则写入项目标准。
- 两轮复审已写入正式方案；本地文档链接、必要章节和 `git diff --check` 通过。本阶段没有安装依赖、运行新后端、修改数据库、执行任务、提交、推送或部署。
- 用户确认继续使用宝塔 Node 项目界面统一管理；部署映射已补充为单个 `aurum_ai` PM2 项目加载 `ecosystem.config.cjs`，面板统一启停、PM2 按角色隔离和监控。
- 阶段 5 已完成。下一阶段为阶段 6“交易执行统一状态机”，必须先介绍状态范围、幂等、未知结果、恢复和审计边界并等待用户确认。

### 2026-09-02：阶段 6 交易执行统一状态机

- 用户已确认开始。本阶段只冻结状态、转换、幂等、权限、恢复和审计规则，没有实现后端、修改数据库、改 Bridge 或执行交易。
- 现状审查确认订单意图、Bridge 命令账本、自动信号分发、AI 持仓管理、管理员策略分发/平仓/撤单均已有局部状态机，但 `failed`、`skipped`、`uncertain` 和 `partial` 语义不一致。
- 目标统一为公共 operation、账户级 execution intent、Bridge command、终端资源投影、来源/批量分发五类关联状态，避免一个巨型枚举。
- 公共操作新增 `partially_succeeded` 供批量父操作表达部分成功；删除目标公共语义中的模糊 `skipped`，按事实映射为成功、拒绝、取消、过期或未知。
- 一旦 Bridge 命令已标记投递并尝试写出，禁止普通重发；超时、断线、回执落库失败和资源未确认统一进入 `uncertain` 对账。
- 策略分发冻结目标并为每个账户创建独立子 operation；分发平仓只按原 distribution/outcome/ticket 精确归因。
- 已完成两轮复审，正式方案见 [交易执行统一状态机](./trade-execution-state-machine.md)。
- 阶段 6 已完成。下一阶段为阶段 7“数据库逐表迁移设计”，开始前必须介绍表域、旧新映射、数据核对、双读切换、回滚和旧表删除门，并等待用户确认。
- 阶段 7 新增输入：每个用户默认 1 条 MT4/MT5 共用账户 WebSocket 并发连接额度，可购买额外额度；额度只统计当前在线连接。数据库需设计权益 grant、并发上限投影、连接会话审计、隔离终端档案和账户级策略订阅；实时 Gateway 使用带 TTL 的原子连接 lease，并保留所有既有账户与订阅数据。详见 [Bridge 并发连接额度与账户级策略订阅模型](./bridge-connection-quota-and-account-subscription-model.md)。

### 2026-09-02：阶段 7 数据库逐表迁移设计

- 用户已确认开始。本阶段只读取 `dev_vue` 元数据和聚合统计并设计迁移，没有执行 DDL、DML、回填、修数、建库或删除。
- 已形成[数据库逐表迁移矩阵](./database-table-migration-matrix.md)，逐一覆盖源库 165 张 InnoDB 表、271,007 行和约 682.50 MiB 数据；机器覆盖检查为 165/165、无遗漏、无重复、无未知表。
- 矩阵为每张表标记保留、重塑、拆分、合并、归档或候选删除，冻结目标实体、字段类型、UTC 分类、唯一键、外键、索引、分块回填、逐用户/全局对账、切换和回滚规则。
- 已纳入 Bridge 当前账户 WebSocket 并发额度、可自由新增/删除/更换 MT4/MT5 账户、账户级策略订阅和实时 TTL lease；没有建立持久账户槽位。
- 只读审计确认 1,015 条平台信号使用 `user_id=0`、4 条通知引用缺失用户，以及一个由两个旧账户 ID 表达的交易账户接管历史；最终方案均保留证据并给出显式映射，禁止直接删除。
- 两轮独立复审完成。进入任何 DDL 前仍需生成逐列时间语义 manifest、可恢复基线备份并取得用户对目标旁路库及结构实施的单独授权。
- 阶段 7 已完成。下一阶段为阶段 8“Bridge V4 原型与技术冻结”，必须先介绍 Win7 双原型、MT4/MT5 多档案、精确请求、时区、命令账本和自动更新验证范围并等待用户确认。

### 2026-09-02：阶段 8 Bridge V4 架构审查与原型准备

- 用户已确认开始，并明确要求重新评估 Rust 前旧版的 Win7 安装体验、打包方式、兼容性和杀毒误报；本阶段不发布、不安装、不连接真实交易。
- 已核查 Gitee 提交和标签：传统安装体验来自 Inno Setup；早期 Python/PyInstaller/Nuitka 可见版本绑定 Python 3.11/3.12/3.13，Rust 前 C# 版目标为 `net10.0-windows`，两者都不能作为受支持 Win7 运行时证明；当前 Rust 普通 MSVC 目标官方为 Windows 10+。
- 已形成 [Bridge V4 Windows 7 架构、安装与更新技术冻结方案](./bridge-v4-win7-architecture-and-packaging-plan.md)。初版双 EA/Service 在 MT5 实机前被纠偏：首选原型改为 `.NET Framework 4.8 + WinForms + MT4 精简 EA + MT5 官方 Python Worker + 命名管道 + Inno Setup`，Rust Win7 Tier 3 仅作对照；不恢复 Python GUI 或自解包打包器。
- 设备职责收敛为基础数据、确定性交易执行、诊断和更新；复杂快照、图表、统计、策略和风控组合由服务器完成。已有基础字段可由服务端重新组合，新增终端原始能力仍需版本化扩展。
- 第二轮复审发现 .NET Framework 公共 `ClientWebSocket` 在 Windows 7 不可用，已将独立 RFC 6455/TLS 传输、真实经纪商终端 build、无 Authenticode 时的 SmartScreen 边界和多档案故障隔离加入硬门。
- `contracts/bridge-v4.schema.json` 已删除通用 `select/filter/sort`，收敛为 14 个基础资源的专属 `params`、7 类窄实时资源和 6 个确定性命令；当前只作为阶段 8 原型合同，具体响应字段仍需结合 MT4/MT5 实机结果后最终冻结。
- 用户已确认首选技术方向，并补充 `.NET Framework 4.8` 依赖交付：前端下载页提供在线安装包与离线完整安装包，Bootstrapper 自动检测 `Release >= 528040`；普通用户无需预装或手动判断依赖，缺少 Win7 SP1/前置更新或需要重启时必须给出明确续装流程。
- 已建立 `bridge/prototypes/net48-win7/` 隔离原型：x86/x64 当前各 55 项离线冒烟通过，真实 TLS 1.2 WSS Echo 通过，MT4/MT5 共用窄命名管道合同与 SQLite 幂等账本通过；在线/离线 Inno 安装包均可编译，本机安装、Launcher 启动和卸载闭环通过。signed manifest V2/P-256、包大小与 SHA-256、安全解压、版本原子暂存、激活中断恢复、健康检查失败回滚和版本外数据保留已完成双架构离线演练。
- 当前 Win11 开发机的 MT5 官方 Python 路径已完成真实模拟账户探测、行情/K线成交量/账户/持仓挂单/历史只读验收，以及最小手数挂单、改单、撤单、开仓、保护修改、部分和全部平仓矩阵；清理后无测试挂单或持仓残留。
- `.NET Framework 4.8` 多 MT5 Worker 主机原型已接通现有 Python IPC v2：每个终端档案独立进程、管道、nonce、route 和 epoch；连接超时、空闲进程退出及“握手后立即退出”均会 fail closed，x86/x64 共 20 项冒烟及真实 MT5 只读探针通过，退出后无 Worker 残留。
- CPython 3.8.10 x64 embeddable、MetaTrader5 5.0.5735 cp38 x64 和 NumPy 1.24.4 cp38 x64 候选已完成固定文件/哈希解析，并在当前 Win11 由 x86 Host 驱动 x64 Worker 通过真实 MT5 只读探针；这仍不替代 Win7 SP1、Universal CRT 与 MT5 Build 5320 的实机兼容验证。
- MT4 V4 EA 已完成六类固定交易命令、严格预期状态和不确定结果处理，MQL4 编译与 x86/x64 离线合同测试通过；当前 `8950701 / DPrimeVU-Demo 5` 没有 EA 交易权限，只完成真实账户、时钟、品种、报价、K 线及成交量、持仓挂单、历史和诊断的只读探针。真实 MT4 交易矩阵按用户要求延期，旧版 EA 只作为源码与行为对照。
- 用户进一步冻结 Bridge 的最小职责：全部策略、风控、会员、额度、品种、手数、止盈止损和交易时段限制均在服务端；Bridge 只提供基础数据、调用终端并回传成功/失败/不确定结果。K 线和交易历史等大体量事实按终端档案缓存到独立 SQLite，以覆盖范围和稳定游标支持随时精确读取。
- SQLite 缓存边界已经补充并完成 Core 运行时接线：V1/V2 追加迁移、每档案 epoch 隔离、已收盘 K 线与历史事实、账户/持仓/挂单恢复投影、stream 状态、合约规格短缓存、同步任务租约、Outbox ACK、维护状态和分级小批清理均已落地；每档案 Runtime 现统一拥有 Store 与命令账本，覆盖命中直接稳定分页，缺口仅登记后台同步，终端读取不持有 SQLite 写锁。分批同步保存有界 cursor，旧 epoch 的同步任务和 Outbox 不进入当前队列；服务端 `persisted/duplicate` ACK 必须严格匹配 route/resource/scope/range/revision 才能推进清理证据。实时报价和当前 K 线仍以内存为主。
- V4 查询会话离线闭环已接入：严格解析服务端 `query.request`，核对 terminal/account/epoch，实时小查询走统一终端接口，范围查询走 SQLite 覆盖与快照游标；缺口只返回可重试刷新状态并由异步同步队列补齐。已增加 RFC 6455 消息通道适配和单步会话泵。查询响应不进入需 ACK 的 Outbox，避免协议无查询 ACK 时永久重发；分页中的部分覆盖不会提前冒充完整覆盖。
- 每档案独立会话 Worker 已加入短时单次 session 凭据 WSS 工厂、严格 `session.hello/welcome`、连接代次切换、心跳 ACK 半开检测、1/2/4/8/16/30 秒有界重连及跨 epoch 持久 Outbox 刷新。更新协议新增服务端 `restart_not_before_utc_msc`，包暂存后还必须等待本机命令、未知结果和关键写入空闲，才调用版本激活/重启边界；不在 Bridge 复制自动分析调度。MT4 管道与 MT5 `live/archive` Worker 的 K 线和历史原始响应已统一映射为投影批次：当前柱不落盘，历史中间页不发布完整覆盖，出金保留负号，票号按文本保存；MT4 EA 的范围 K 线已改为按请求窗口定位，不再只扫描最近 500 根。WinForms 已接入可新增、编辑、连接、断开和移除的多终端档案；配置原子写入，长期 refresh 凭据仅保存为当前用户 DPAPI 密文，建连前换取短时 session 凭据。每个档案独立 WSS、SQLite、epoch 与 MT5 `live/archive` Worker，移除配置不删除本地安全账本。离线冒烟计数以后续迁移批次的最新脚本输出为准，MT5 Worker 通过 67 项单元测试，MT4 MQL4 编译为 0 错误、0 警告。
- 确定性命令离线闭环已完成：`command.request` 先持久写入 SQLite 再回 `command.accepted`，响应成功发出后才调用终端；重复 command/idempotency 不会重放，过期命令不执行，已进入终端边界但结果未知时落 `uncertain`，只能经 `command.reconcile` 精确查询。最终或不确定结果在 `command.result_ack` 前跨重连保留；MT5 支持原生 Stop Limit 和显式移除止损、止盈、到期时间，MT4 对不支持的 Stop Limit 保持明确拒绝。当前仅完成模拟终端和离线适配验证，没有连接公网或执行新的实机交易。
- 自动更新本地闭环已完成：系统代理下载、最多 3 次逐跳安全重定向、严格 UTF-8/大小限制、固定 P-256 签名与 SHA-256 校验、分包原子暂存已串联；全部活动档案先检查命令账本，再暂停网络处理并等待关键操作归零，达到服务端 `restart_not_before_utc_msc` 后由固定 Launcher 等待旧 PID 退出、健康切换或回滚。健康、回滚和启动失败会写入版本外状态并作为 `release.status` 在 WSS 恢复后回传。x86/x64 已通过本地 HTTP 模拟、Worker 暂停恢复、状态回传和真实双版本 Launcher 进程演练；没有上传、发布、连接公网或证明 Win7 实机兼容。
- V3→V4 迁移第一批已完成：按当前实装目录和实际 `control_url/realtime_url` 合同实现只读幂等快照，只保留偏好与终端绑定，不复制旧 DPAPI 刷新凭据、不迁移缓存/日志/旧命令账本/游标；真实 V3 数据演练识别 2 个档案并核对 7 个源文件哈希未变化。V4 主程序已兼容 V3 Launcher 的健康、真实就绪和最小化参数，x86/x64 均通过 62 项离线烟雾测试，主程序与 Launcher 版本为 4.0.0.0。本轮没有安装、发布、改写旧目录或执行交易。
- V3→V4 迁移第二批已完成本地源码闭环：Fastify 5 + TypeScript 独立模块、OpenAPI V4 合同和显式 SQL 迁移文件实现 V3 refresh 到 V4 设备 refresh 的一次性交换；一个 V3 source session 唯一绑定一个 V4 安装/档案，同绑定重试只轮换 generation，V3 源会话保持不变。客户端在当前用户上下文解密旧凭据，全部档案交换成功后才原子写入 V4 catalog，并在每次 WSS 建连前换取最长 60 秒的单次 session 凭据。当前没有挂载到正式目标进程、执行数据库迁移、连接公网或投放更新。
- V3→V4 迁移第三批完成原生过渡入口和稳定系统入口的本地闭环：当前实装 V3 的桌面、开始菜单、HKCU 自启动和卸载项都固定指向根目录 `AURUMBridge.Launcher.exe`。第一跳由 V3 更新器启动并验证仍保留 V3 Launcher 的过渡候选；候选核心健康后，才把同目录无 .NET 依赖的 Win32 x86 入口原子提升为根稳定启动器。第二跳由原生入口安装/续装 4.8 并把 V4 激活交还旧 Launcher 验证。V4 首次健康启动后原子生成 `current.txt/previous.txt`，此后改由版本目录托管 Launcher 接管；系统入口路径无需重写。x86/x64 烟雾、V3 参数转发、真实 V4 核心交接、当前实装 V3 Rust Launcher 的失败回滚，以及在线 Inno 临时安装/启动/卸载均通过。实装目录、注册表、快捷方式和线上清单均未修改。
- V3→V4 迁移第四批完成隔离安装副本的临时签名两跳演练：三个版本包均按 signed manifest V2、大小、SHA-256 和 P-256 签名进入正式解析/验签/安全暂存路径，随后验证过渡候选健康、原生稳定入口提升、V4 健康接管、失败回滚及 `rolled_back` 恢复。当前实装 V3 的三个关键文件前后哈希一致。演练私钥不落盘，没有安装 Runtime、投放清单、连接公网或执行新增交易。
- 用户确认新版 Vue 工程不保留任何旧前端兼容代码；阶段 9 开始时从 `dev_vue` 清除旧手写前端，需要时只读参考 `D:\dev_codex\wall-street-skill-local`。数据库迁移不在清理范围：公网旧结构到 V4 的迁移文件、版本识别、legacy ID map、回填 checkpoint 和对账逻辑必须作为发布链长期保留并持续演练。
- 阶段 8 核心研发阶段性完成并冻结。Core 层 SQLite/同步/Outbox、查询响应、每档案 WSS 会话生命周期、MT4/MT5 原始只读数据到统一投影批次、确定性命令、WinForms 本地多档案/DPAPI 凭据/启停编排、旧 V2 清单验签、安全暂存、Launcher 健康回滚、V3 配置只读迁移、V3 refresh 到 V4 设备 refresh 交换，以及隔离副本 signed 两跳升级均有本地证据。后续 Vue 与服务端若需要新的基础数据、诊断或确定性操作，按版本化窄合同小步补充，不扩展 Bridge 业务职责。
- 阶段 8 的发布与实机验收门延期到最终打包前：目标 Fastify 进程和公网 Bridge 网关正式接线、真实终端多页与中断续传、公网端到端鉴权、正式发布密钥签名、缺少 .NET 4.8 的 Win7 SP1 安装/重启/RunOnce、断电/磁盘满/杀进程、Win7/10/11、固定 Python/MT5、代理/断网/Autobahn、长稳和杀毒矩阵。MT4 经纪商历史可见性及真实交易矩阵也继续延期；未经后续授权不执行。完成这些发布门前仍不得宣称 V4 已正式支持 Win7，或向 V3 客户端投放 V4 清单。
- 阶段 9 已完成：旧 `public/` 手写页面和仅绑定旧 DOM 的测试已退出版本树；主站、AI 交易实验室和管理后台分别建立 Nuxt/Vite 独立工程，共享唯一 shadcn-vue 组件源、设计令牌、V4 合同与传输客户端，并通过应用构建、类型、测试和跨应用边界检查。后端合同测试从混合旧 UI 测试中保留，数据库迁移和 Bridge 文件未作为前端清理对象。
- 阶段 10 已完成实现与离线验证，正式验收记录见 [阶段 10：SSO 端到端竖切验收记录](./stage10-sso-vertical-slice-report.md)。已建立 auth 身份中心、三个 Host-only 应用会话、Authorization Code + PKCE、CSRF、一次性 WebSocket 票据和三种退出范围；根测试 3397 项、前端测试/类型/构建与边界检查均通过。未执行数据库迁移，也未连接真实 MySQL、Redis、HTTPS 子域或 WebSocket 网关，相关证据明确延期到旁路迁移和联调阶段。
- 阶段 11 已完成实现与离线验证，正式记录见 [阶段 11：账户、终端与实时行情竖切验收记录](./stage11-account-terminal-realtime-vertical-slice-report.md)。已建立账户归属、当前在线连接额度、终端档案、观摩上下文、账户快照、实时报价、K 线与成交量、持仓、挂单和浏览器精确恢复链路；投影数据与 revision 原子提交，浏览器按单账户、单资源隔离订阅。迁移未执行，真实 MySQL/Redis/Bridge/MT4/MT5/浏览器验收明确延期到阶段 15–16，不据此宣称实机门已通过。
- 阶段 12A 已完成实现与离线验证，正式记录见 [阶段 12A：分析师与账户级交易员核心验收记录](./stage12a-analyst-trader-core-report.md)。V4 已把系统用户归属的不可变 `market_analysis`、交易账户级 `trade_decision` 与后续 `execution_intent` 分开，手动分析不再携带自动执行开关；策略固定为 `analysis` 和 `trader` 两类，交易员按账户订阅 revision 独立排队，完整正文走 HTTP，小型状态变化走 WebSocket。迁移未执行，模型 Worker、确定性风控接线、真实 MySQL/Redis、多账户和 MT 验收仍属后续子阶段。
- 阶段 12B 已完成实现与离线验证，正式记录见 [阶段 12B：分析调度、Worker 与条件扇出验收记录](./stage12b-analysis-scheduler-worker-report.md)。同用户、同分析策略版本、同品种、同五分钟槽只生成一份分析；行情、宏观和提示词被显式冻结；模型 task/attempt 有界重试并用 fencing 拒绝迟到结果；仅“有市场机会”或“已有持仓/挂单”的账户创建 `entry/manage/both` 交易员任务。迁移未执行，真实 provider、Trader Worker、确定性风控接线和实机验收仍属后续子阶段。
- 阶段 12C 已完成实现与离线验证，正式记录见 [阶段 12C：账户级 Trader Worker 验收记录](./stage12c-account-trader-worker-report.md)。单账户指标、持仓、挂单、目标报价、合约和风险摘要均显式冻结；同账户任务串行领取，模型 attempt 有界重试，迟到结果保存为 `stale`，且不接 Bridge、不创建可执行订单。
- 阶段 12D 已完成实现与离线验证，正式记录见 [阶段 12D：确定性风险策略与决定评审验收记录](./stage12d-deterministic-risk-review-report.md)。平台不可关闭边界、账户版本/CAS、账户风险摘要、减险例外和 `trade_decision` 的确定性批准/拒绝已独立建模；落库前复核全部 revision，完整结果走 HTTP，小型失效通知走 WebSocket。本阶段不创建执行意图、不接 Bridge，迁移未执行。
- 阶段 12D.1 已完成实现与离线验证，正式记录见 [阶段 12D.1：账户风控手动解锁验收记录](./stage12d1-manual-risk-release-report.md)。账户所有者可以对当前亏损、回撤、次数、连续亏损或冷却触发进行一次有限、审计化解锁；解锁绑定风险基线、业务日期和策略身份，指标恶化或策略变化后自动失效，且不能绕过平台绝对上限、全局停机、权限、数据完整性和单笔指令校验。迁移未执行。
- 阶段 12E 已完成本地实现与离线验证，正式记录见 [阶段 12E：执行意图与风险预留验收记录](./stage12e-execution-intent-reservation-report.md)。已批准风险决定会幂等转换为账户级 operation/intent；新增敞口动作建立 30 秒短期风险预留，同账户事务聚合活动预留以阻止并发超额，零动作保持 `noop`，过期准备记录释放预留。V4 表与旧 `order_intents`/`risk_reservations` 并存，迁移未执行，Bridge/MT 投递仍属于后续阶段。
- 阶段 12F 已完成本地实现与离线验证，正式记录见 [阶段 12F：Bridge V4 持久命令与结果对账验收记录](./stage12f-bridge-command-reconciliation-report.md)。每个 prepared execution intent 通过稳定的 intent+sequence 命令身份进入旁路 V4 命令账本；服务端先提交 `dispatched` 再尝试 WebSocket 写入，写入结果不明只进入 `uncertain`，不自动重放。Bridge 回执先持久化并推进 intent/风险预留，随后才生成 `command.result_ack`；冲突证据追加保存并转入人工复核。成功结果的 committed 预留继续占用容量，直到可信投影明确标记 absorbed。迁移未执行，正式 Gateway、MySQL/Redis、投影吸收、MT4/MT5 与公网端到端联调仍属于后续阶段。
- 阶段 12G 已完成服务端模块边界与离线验证，正式记录见 [阶段 12G：Execution Worker、Bridge Gateway 与可信投影交接验收记录](./stage12g-execution-worker-bridge-gateway-report.md)。V4 会话按单次票据、安装/档案、账户归属、终端实例和数值 epoch 授权；额度按 WebSocket/档案计数，当前 route 用 Redis fencing，执行用账户短租约及数据库活动命令门串行。重连每次只精确对账一条 uncertain 命令，不重发 command.request；持仓/挂单完整快照同时更新浏览器投影与 exact-state，并只在 revision、时间和 ticket/状态证据完整时吸收 committed 预留。阶段复用 010 迁移，不新增或执行迁移；正式 WSS upgrade 接线、真实 MySQL/Redis、Bridge/MT4/MT5 和公网联调仍待后续阶段。
- 阶段 12H 已完成本地运行时接线与离线验证，正式记录见 [阶段 12H：Bridge Gateway、Execution Worker 与 Outbox 运行时接线验收记录](./stage12h-v4-runtime-wiring-report.md)。`/bridge/v4/ws` 严格使用 Authorization bearer、首帧 hello、512 KiB 帧和有界串行背压；prepared intent 与 queued command 均在各自 MySQL 事务写 outbox，由独立 dispatcher 以确定性 job ID 唤醒 execution worker 和 Bridge gateway。重复队列投递只有仍为 queued 的 command 可以进入“先标 dispatched、后写 socket”，已跨边界的命令不重发。新增三个独立 PM2 角色、健康/就绪和优雅停机骨架，仍由宝塔一个 ecosystem 项目管理；显式 V4 总开关默认关闭，未启动进程、未执行迁移、未连接真实依赖或终端。
- 阶段 12I 已完成 API V4 与浏览器实时网关的本地运行时接线，正式记录见 [阶段 12I：API V4 与浏览器实时网关运行时接线验收记录](./stage12i-v4-api-browser-realtime-wiring-report.md)。API 统一挂载 SSO、Bridge 凭据和当前已实现的 trade V4 模块，并按精确 trade Host 隔离；浏览器实时网关只消费经活动会话复核的一次性 HttpOnly 票据及 Redis 已提交事件，保留账户授权、revision 缺口重拉、上/下行帧、慢消费者和心跳边界。trade 前端已改为断线前先重拉 HTTP 快照，再按 1/2/5/10/20/30 秒抖动退避携新 revision 重连。宝塔 ecosystem 增至五个独立角色；仍未启动真实进程、执行迁移或连接真实基础设施与终端。
- 阶段 12J 已完成 AI 异步运行链路的本地接线，正式记录见 [阶段 12J：AI 调度、模型 Worker、风控与执行运行时接线验收记录](./stage12j-ai-runtime-orchestration-report.md)。Analysis Scheduler、Analysis/Trader/Risk Worker 分离为独立 BullMQ/PM2 角色，任务型 Outbox 从分析请求一直唤醒到执行意图和 Bridge 命令；模型按用户与策略逐任务解析，平台共享请求在发网前原子检查每日额度并保留用量证据，崩溃遗留任务按绝对 deadline fail closed 回收且不重放 provider/终端。宝塔 ecosystem 增至九个角色；非任务领域事件实时投影、旁路数据库迁移、真实 provider/MySQL/Redis/Bridge/MT 和长稳压测仍待后续阶段。
- 阶段 12K 已完成 AI、风控与异步操作的小事件实时投影，正式记录见 [阶段 12K：浏览器领域实时投影与模型用量恢复验收记录](./stage12k-browser-realtime-projection-report.md)。分析记录保持系统用户级，交易员、风控和 operation 保持账户级；一个用户会话可订阅多个自有账户，观摩会话不能读取所有者专属领域事件。业务事务仍只写 Outbox，Dispatcher 在稳定 BullMQ 任务与 Redis 事件均成功后才完成该行；断线先 HTTP 重拉，完整推理、规则和快照不进入 WebSocket。遗留用量 reservation 只结算为 usage unknown，不重放模型请求。迁移、真实依赖、浏览器页面接线与端到端验收仍待后续阶段。
- 阶段 12L 已完成交易实验室首页最新分析与 AI 分析师真实前端竖切，正式记录见 [阶段 12L：交易实验室 AI 分析前端竖切验收记录](./stage12l-trade-analysis-frontend-report.md)。分析历史按系统账号归档，完整推理按需走 HTTP，手动分析固定使用已发布分析策略、独立幂等键和服务端 3 分钟冷却；首页复用单一实时连接接收用户级最新分析事件，断线先同步账户与分析 HTTP 快照。页面统一组合 shadcn-vue 与语义令牌，并保留后续 Astra 视觉换肤边界。真实服务、浏览器多宽度、MySQL/Redis/provider/Bridge/MT 仍待阶段 15～16 联调。
- 阶段 12M 已完成 AI 交易员真实只读账户工作区，正式记录见 [阶段 12M：AI 交易员账户工作区验收记录](./stage12m-trade-trader-read-workspace-report.md)。当前账户、持仓、挂单、账户级决定与完整推理已接入 HTTP 权威读取和账户级 WebSocket 小事件；指标事件不重复打 HTTP，账户快速切换采用串行 latest-wins，交易价格保留实际精度，交易时间按终端校准时区显示。手动下单、改单、平仓、撤单、策略分发和分发平仓因缺少统一 execution 用户命令入口而未伪造前端能力，须在下一服务端子阶段完成权限、幂等、风控、父子 operation、精确归因和只追加迁移后再接 UI。
- 阶段 12N 已完成统一用户执行命令与策略分发服务端闭环，正式记录见 [阶段 12N：用户执行命令与策略分发验收记录](./stage12n-user-execution-command-distribution-report.md)。手动市价单、挂单、持仓/挂单修改、平仓和撤单统一经过账户归属、观摩只读、精确 revision、确定性风控、风险预留、operation/intent、Outbox 与 Bridge V4；管理员分发在确认时按交易账户去重冻结目标，以独立异步子 operation 执行，并只按原 distribution/outcome/ticket 创建精确平仓。迁移仍未执行，真实 MySQL/Redis/Bridge/MT 与交易前端写操作联调属于后续阶段。
- 阶段 12O 已完成 AI 交易员写操作前端竖切与只读辅助端点，正式记录见 [阶段 12O：AI 交易员写操作工作区验收记录](./stage12o-trader-write-workspace-report.md)。页面使用 shadcn-vue Sheet、Field、Select、Table 与 AlertDialog 接入手动市价单/挂单、持仓和挂单修改、平仓、撤单、管理员策略分发及精确分发平仓；提交前读取服务端六类权威 revision 与目标资源 revision，危险操作二次确认，HTTP 受理后只展示 operation 状态，不推断终端成交。用户级分发父 operation 通过空账户 scope 的受控 WebSocket 目标更新；迁移仍未执行，真实 MySQL/Redis/Bridge/MT、浏览器多宽度和实盘行为仍待阶段 15～16 联调。
- 阶段 12P 已完成 AI 风控师真实前端竖切，正式记录见 [阶段 12P：AI 风控师前端工作区验收记录](./stage12p-risk-frontend-workspace-report.md)。当前账户风险状态、六项实时额度、账户规则编辑、服务端手动解除可用性、风控决定列表与逐规则详情已接入 V4 HTTP；账户级风险 WebSocket 只发送失效事件并回读权威资源。手动解除不能绕过平台停机、账户暂停、交易发送关闭、数据/时钟完整性和平台硬上限，且仍以摘要 revision、策略 revision、CSRF 与幂等键重新校验。观摩模式保持只读，迁移和真实依赖仍未启动。
- 阶段 12Q 已完成 AI 策略师与账户订阅真实竖切，正式记录见 [阶段 12Q：AI 策略师与账户订阅工作区验收记录](./stage12q-strategy-management-workspace-report.md)。平台策略对普通用户只读，个人分析/交易执行策略使用不可变版本、确定性编译、CAS 发布与退役；发布时原子推进尚未结束的账户订阅版本，普通开关不发生隐式版本漂移。不同自有账户可独立绑定分析和交易执行策略，同账户同品种只保留一个活动执行槽位。前端统一组合 shadcn-vue；迁移、真实依赖和模型/终端运行仍未启动。
- 阶段 12R 已完成 AI 复盘师与策略记忆规范化核心，正式记录见 [阶段 12R：AI 复盘师与策略记忆工作区验收记录](./stage12r-reviewer-memory-workspace-report.md)。日/月/手动复盘统一为用户级 case、冻结账户/策略/订阅/终端时区证据，不可变版本分别评价分析、交易员、确定性风控和终端执行；记忆只在人工确认后进入候选，长期候选需三个独立已确认 case 再额外确认，合并后可通过新 revision 撤销。复盘使用独立低优先级 Worker，浏览器实时只接收状态失效事件。周期证据生产、旧数据迁移、真实依赖和实机验收仍未执行。
- 阶段 12S 已完成权威交易记录结构、HTTP 读模型、实时失效合同和交易实验室工作区，正式记录见 [阶段 12S：权威交易记录与终端证据链验收记录](./stage12s-authoritative-trade-history-report.md)。只展示终端历史事实已经封口的已平仓交易，上游分析、风控、命令状态不冒充成交；迁移、Bridge 历史采集和旧数据回填当时尚未接线。
- 阶段 12T 已完成 Bridge 历史采集的本地运行时接线与离线验证，正式记录见 [阶段 12T：Bridge 历史采集、权威投影与迁移演练验收记录](./stage12t-bridge-history-collector-report.md)。独立 scheduler 只登记在线账户到期任务，MySQL outbox/BullMQ 只携账户 ID；Gateway 使用当前 route/epoch 发出 `history.orders/trades/deals` 精确只读查询，分页事实短事务入库，完成后递增 history revision 并发布轻量失效事件。迁移、真实 MySQL/Redis、Bridge/MT 和旧数据回填仍未执行。
- 阶段 12U 已完成系统审计与执行链路离线竖切，正式记录见 [阶段 12U：系统审计与执行链路工作区验收记录](./stage12u-system-audit-workspace-report.md)。审计页通过 HTTP 冻结游标读取当前用户的权威领域记录，WebSocket 只发送 `audit.changed` 失效通知；详情使用精确外键串联分析、交易员、确定性风控、operation/intent、Bridge 与终端结果，不下发原始模型、终端请求或内部堆栈。迁移、真实 MySQL/Redis、浏览器登录态和 Bridge/MT 仍未启动。
- 宏观看板 M0 和 M1 源码合同、双库结构演练已完成，见 [M1 宏观合同记录](./stage-m1-macro-contract-and-schema-implementation.md) 和 [中断恢复验收](./stage-m1-interrupted-schema-recovery.md)。独立基础安装及 001～017 在两个旁路库全部通过，各 102 表/18 文件，结构指纹和风控种子业务内容一致；011 采用追加纠正，A/B 原始失败证据永久保留，重复 apply 不重放。源库、应用连接和交易终端均未修改。2026-09-05 已完成 [源备份、字段映射与回填对账方案](./stage-m1-data-backfill-and-reconciliation-plan.md) 及 165 表只读差异盘点，源共 271,007 行；纠正旧矩阵重复条目和过时迁移目录。[B1 首批只读/离线工具](./stage-m1-b1-backup-verification-tooling.md) 已加入源码，包含观测、参数预览、文件 hash 和恢复观测比较；不是备份/恢复执行器。下一步确认存储、加密保管及精确恢复源库，随后实施并单独授权真实导出/恢复，不向 A/B 写业务数据。M1 全量旧数据迁移、供应商采集、应用切流、旧表清理仍未完成。
- B1 后续已完成 [服务器备份条件只读核验](./stage-m1-b1-server-backup-readiness.md)：MySQL 8.4.8 客户端、GPG/gzip、相关权限可用，当前约 37.85 GiB 空闲；备份/口令新目录及恢复源库均未创建。待用户确认精确路径、同机分离保管和无 DDL 窗口后，实施导出/恢复适配器与隔离演练；本轮无备份、建库或数据回填。
- 用户确认后，B1 [加密备份与独立恢复演练](./stage-m1-b1-encrypted-restore-rehearsal.md) 已实际执行到 SQL 审查门：工具源码 `96e18216`，15 files/115 tests 通过；165 表源备份加密为 177649476 B，解密 SQL 682966225 B 与导出 hash 一致。首版审查器不支持合法 DEFAULT/生成表达式，且两列 STORED GENERATED INVISIBLE 需要准确处理 INSERT 省略，故在建库前保护性停止。新恢复库仍不存在，A/B 未写，全部现场保留。下一步确认生成列适配及续接现有备份恢复；不重新导出/覆盖、不提升为 B1 完成或全量迁移完成。
- 用户再次确认后，B1 [冻结备份续接恢复](./stage-m1-b1-retained-backup-continuation.md) 已通过：工具源码 `c3ddf32b`，16 files/129 tests 通过；最终 SQL 全量审查后创建唯一隔离镜像 `dev_vue_m1_source_20260905_01`，165 表/271007 行完整结构指纹、逐表普通列 INSERT hash 和独立真实 COUNT 全部一致。原备份/失败记录/工具不覆盖，源未再导出、A/B 未回填、应用及终端未触碰。**B1 完成，下一确认门为 B2 精确字段映射与目标缺口复核**；异机容灾、V4 业务迁移、应用切流和旧表删除仍未完成。
- B2 [首批身份/账户映射评审](./stage-m1-b2-identity-account-mapping-review.md) 已完成：读取冻结镜像与目标 A 元数据，11 表/148 列显式处置清单及纯离线校验已加入。发现 274 段归属历史不能压入当前授权 PK、7 条旧终端缺安装标识且新账户读取依赖档案、公开观摩受众不能仅复制空 assignments，以及会员/验证/推荐等字段缺承接。没有回填或修改 schema/应用/终端。下一确认门为这些缺口的结构与读写合同设计；其它 154 张源表仍需后续 B2 精确审查，不能直接进入全量 B3。
- B2 [首批目标结构/读写合同设计](./stage-m1-b2-identity-target-contract-plan.md) 已完成两轮复审：沿用 users 与账户主干，追加独立状态/归属区间/观摩 source；明确当前交易、本人历史和观摩三类授权，补发现历史查询依赖当前 owner、collector 使用连接用户归因以及真实档案登记流程缺口。分为 P1–P5，下一确认门为 P1 用户补充结构与离线验证。此轮仅文档，无新增 SQL、依赖连接、回填或运行修改，B2 全域/B3 仍未完成。
- 用户补充确认 Telegram 不需绑定：取消其新表、绑定入口及绑定回调计划，旧字段仅按退役历史方案保留来源证据；P1 为用户状态字段、推荐账户结构及离线测试，不新增用户必填资料，仍待实施确认。
- P1 [用户状态与推荐账户结构](./stage-m1-b2-p1-user-state-schema-report.md) 已完成源码与离线验证：018 仅追加 users 八列和推荐账户表，无 Telegram、无 DML、无默认余额；十个旧状态/推荐字段有明确来源映射，旧迁移及冻结清单未改。主代理回归 18 files / 148 tests 通过，包含真实文件计划中 018 半成功后禁止普通重放。当前代码计划为 19 文件/145 语句，不代表 A/B 已升级；未连接数据库、执行迁移或回填。下一确认门为 P2 归属区间、用户账户设置和分用途授权合同。
- P2 [归属历史、账户设置与分用途授权](./stage-m1-b2-p2-account-ownership-report.md) 已完成源码与离线验证：追加 019 四条结构语句，保留每段历史归属和用户私有设置；当前账户、本人历史、执行准入、观摩发布分别校验。迁移相关 19 files / 158 tests、服务端 41 files / 267 tests、类型检查与构建通过。代码计划现为 20 文件/149 语句，真实 A/B 安装证据仍只到 017。没有执行 DDL/DML、接线历史接口或访问终端；下一确认门为 P3 离线账户读模型与历史记录归属防护，查询和采集必须同步调整。
- P3 [离线账户与历史归属防护](./stage-m1-b2-p3-offline-accounts-history-report.md) 已完成源码与离线验证：current/history 账户读取分离、nullable 档案合同、精确租约在线校验、私有投影来源、完整区间历史归属及 HTTP/WS 隔离，追加 020 两条结构语句。迁移 20 files/165 tests、服务端 45 files/292 tests、前端 27 files/121 tests，类型检查与构建通过。计划为 21 文件/151 语句，23 个旧 SQL/冻结产物不变；真实 A/B 仍到 017，未迁移、回填、部署或访问终端。下一确认门 P4 观摩发布与统一动态授权；运行切换仍依赖后续 P5/B3。
- P4A [观摩结构与统一读取鉴权](./stage-m1-b2-p4a-observer-read-authorization-report.md) 已完成源码与离线验证：追加 021 三条 DDL，统一动态受众/显式授权、发布白名单、读取后归属版本复核、WS 短期授权及脱敏失效事件、客户端回读防串号。迁移 21 files/172 tests、服务端 50 files/347 tests、前端 30 files/133 tests，类型检查与构建通过；计划 22 文件/154 语句，24 个旧 SQL/冻结产物不变。未执行真实依赖/迁移/交易/部署；P4 尚未整体完成，下一确认门 **P4B 管理配置与授权写入、审计/outbox 及时失效传播**，P5 非 owner 源能力和 B3 回填仍后续处理。
- P4B [观摩管理与授权写入](./stage-m1-b2-p4b-observer-management-report.md) 已完成源码/离线验收：独立 admin 身份、配置/CAS/幂等审计、默认频道、显式授权与 outbox 内部失效通知，补主动退出和晚返回防串号。服务端 54 files/392 tests、迁移相关 23/183、前端 30/136，类型检查/构建通过；计划 23 文件/157 语句，25 个保护产物不变。022 未执行，无 DB/Redis/Bridge/终端连接、迁移、部署或交易；管理视觉页面未实现，Pro 无桥接订阅仍待后续策略/订阅 B2。下一确认门为 P5 真实设备登记与只读发布能力，先做源码与离线验证。
- P5A [设备登记与会话归属复核](./stage-m1-b2-p5a-device-registration-report.md) 已完成源码/离线验收：复用已认证 hello 登记 owner 档案，同绑定重连不重复、换号保留历史；凭据/归属冻结与持续复核、投影事务复核和命令 user/profile 防串号。修正 `default` ID 及按 profile 的 epoch，追加 023 替换旧终端级数字编号索引，不改历史数据。服务端 57 files/419 tests、迁移相关 24/185、类型检查/构建通过；计划 24 文件/158 语句，26 个旧 SQL/冻结产物不变。023 未执行，无真实依赖/终端/部署；新配对、首次凭据/账户认领和旧 profile ID 碰撞仍需后续处理。下一确认门 **P5B 非 owner 观摩源只读发布与命令隔离**；Pro 无本人 Bridge 的分析订阅仍待后续策略/订阅 B2。

### 终端时间链路休市展示进展（2026-09-06）

按用户确认落实默认UTC+3显示，并标记待校准；提供的账户最近偏移继续沿用。首页报价/K线标签、交易员和风控当前工作区已接入统一展示工具；不改UTC数据、历史冻结时区或服务端风控依据。trade 90项测试、类型检查、构建及前端边界检查通过。可信校准落库/缓存/变化通知和浏览器实机验收仍待完成，详见[阶段记录](stage-terminal-clock-chain.md)。


2026-09-06后续补齐账户投影实时消费：account.metrics.changed携带已提交快照的时区/状态，首页与交易员原子应用，兼容旧事件、显式NULL清空和revision防回退。trade累计102项、contracts累计46项、服务端竖切16项通过，相关类型检查与构建完成；未新增数据库/Redis写入。terminal.clock可信采样保存与仅变化通知尚未接通，详见[终端时间阶段](stage-terminal-clock-chain.md)。


2026-09-06休市持久保留规则已接入受信账户投影事务：同账户/归属/终端/epoch的最近可信偏移在采样不可用时保存为stale，来源不匹配保持NULL；事件使用实际提交结果。复用003与025既有快照/来源表，无迁移变更。服务端39项定向测试、类型检查和构建通过；真实MT5可信来源及terminal.clock接线仍未完成，未执行真实数据库写入或部署。


2026-09-06用户补充确认Python MT5返回终端时间，后续沿Python tick时间与UTC对比路径推进，取消MQL5诊断方案。休市不校准，真实新鲜样本验证延期至2026-09-07开市后；当前继续使用最近偏移或标记的默认UTC+3。详见终端时间阶段最新用户决策。


2026-09-06用户收敛为开发期当前dev_vue同库直接升级，不涉及公网、不要求改连空库。[第一批已实施](migration/dev-vue-inplace-stage1-20260906.md)：.env只读实查165表/2598列，对照113张SQL计划业务表，12张同名冲突及旧schema_migrations需专门兼容。生成实际元数据、覆盖产物和可重复校验器，3项测试通过；没有DDL/DML。下一批优先同库journal与users/bridge_refresh_sessions完整差异预检，沿用同库增量路线。


同库升级执行核心第二批见[报告](migration/dev-vue-inplace-stage2-20260906.md)：独立journal及首批9列脚本、逐步恢复核心和MySQL只读预检已实现，38项测试通过。尚未开放实际apply、未执行DDL/DML；最终自动升级和已替代结构删除目标保持进行中。

同库升级第三批见[真实演练报告](migration/dev-vue-inplace-stage3-20260906.md)：新加密备份恢复至隔离库，165 表/271007 行逐表核对通过；九列增量实际断线恢复与重复运行通过，全部原有列内容哈希保持一致。源 dev_vue 仍为九步 pending。下一步绑定本轮备份/演练证据并开放实际升级入口，再推进其余结构与数据转换；整体规范化和删除尚未完成。

同库升级第四批已在当前 dev_vue 执行，见[源库升级报告](migration/dev-vue-inplace-stage4-20260906.md)：首批九列及独立日志完成；165 张原表/271007 行原有列完整哈希保持一致，旧 214 条迁移记录保留，第二次执行全部跳过 DDL。当前只完成首批兼容增量，整体规范化、新表承接、业务转换和删除仍在进行。

同库升级第五批见[新表依赖报告](migration/dev-vue-inplace-stage5-20260906.md)：101 张目标专有表中，68 张被账户/订阅/快照主键类型及任务主键名称四项根冲突阻断，33 张为后续约束与初始化检查候选。检查器区分 CREATE 与后置 ALTER 外键，13 项定向回归通过；本批没有新数据库写入，下一步优先身份认证/容量/迁移账本新表及根实体转换。

同库升级第六批已落库，见[身份与迁移基础表报告](migration/dev-vue-inplace-stage6-20260906.md)：九张身份认证/终端档案/容量/迁移账本表完成，当前175表、18步完成回执、旧214条迁移记录保留；副本真实断线恢复、源库重复执行与165张原表271007行原有数据哈希核对通过。新表仍为空，下一步必须进入账户、归属及额度事实转换；全域标准化、可用性和旧结构删除仍未完成。

同库升级第七批见[归属转换报告](migration/dev-vue-inplace-stage7-20260906.md)：归属纯转换器与6项新回归完成，已有12项候选/源审查回归通过；真实只读复核4账户、7终端、3绑定、274区间（3开放）形成3个账户实体候选。尚未确认历史DATETIME时区、未分配目标ID或回填业务；继续账户实体与映射工作，时间转换等待用户对UTC+8来源的答复。

同库升级第八批见[稳定账户ID映射报告](migration/dev-vue-inplace-stage8-20260906.md)：全冻结集合的数值代表ID规则已实现，4个旧ID映射到3个计划实体并保留4个来源设置键；274条真实区间身份匹配通过，实时重读后映射摘要一致。5项新增映射测试及6项归属回归通过。目标ID尚未持久分配、业务未回填；历史时区问题仍待答复，继续同库账户实体与设置结构承接。

同库升级第九批已落库，见[账户暂存结构报告](migration/dev-vue-inplace-stage9-20260906.md)：四张同库账户实体/归属/授权/个人设置暂存表完成；副本真实断线恢复及源库独立重复执行通过，原165表271007行原有数据哈希一致。当前179表、22条完成记录、旧214条迁移记录保留，8项定向测试通过。业务尚未回填或切换，继续全字段转换与持久化映射；最终自动升级和旧结构清理仍未完成。

同库升级第十批见[账户全字段转换报告](migration/dev-vue-inplace-stage10-20260906.md)：纯转换器覆盖旧账户15字段，真实只读复核4条来源形成3实体/4个人设置，完整重读摘要一致。删除标记保持个人作用域，公共保证金模式冲突拒绝转换；10项定向测试通过。业务时间仍未转换、没有回填，继续事务回执、ID映射持久化与权限对账。

同库升级第十一批见[事务存储模式报告](migration/dev-vue-inplace-stage11-20260906.md)：现有事务回填框架新增显式账户同库模式，固定四张物理暂存表路由；专用适配器验证22步升级日志及实际结构，避免读取旧schema_migrations的新布局。当前库真实只读两次身份摘要一致，32项定向测试通过。业务writer、持久化回填和真实提交恢复尚未接通，继续专用批次与事务写入实现。

同库升级第十二批见[账户批次写入器报告](migration/dev-vue-inplace-stage12-20260906.md)：全集合转换后分页的专用writer已接入事务runner，精确回读共享实体和个人设置；6项新增测试及19项既有测试通过，包括后续行失败时业务/映射/回执一起回滚及重复提交无变化。此为离线证据，无真实写入；完整来源持久化、真实副本演练和业务回填仍待完成。

同库升级第十三批见[真实账户writer演练](migration/dev-vue-inplace-stage13-20260906.md)：在既有恢复副本使用2条合成来源，真实MySQL中途失败回滚、提交后响应丢失恢复及重复执行通过；1实体/2设置/2映射/2回执精确回读后按范围清理，原165表271007行哈希一致。首轮控制连接UTC遗漏已修复并完整重验，工具哈希已绑定。源dev_vue未写入，历史业务回填、来源持久化和全域自动升级继续进行。

同库升级第十四批见[归属授权writer报告](migration/dev-vue-inplace-stage14-20260906.md)：完整历史先计算最终授权再分页，归属/授权writer与账户writer共享精确存储器；32项测试通过。恢复副本真实组合写入1实体/2设置/3区间/2授权，回滚、提交恢复、重复执行及清理通过，原165表271007行哈希一致。源库未写，完整来源持久化和历史业务回填仍待完成。

同库升级第十五批见[来源证据存储报告](migration/dev-vue-inplace-stage15-20260906.md)：新增来源证据表和显式v2事务模式，完整原字段与逐源回执关联并同事务回读校验。副本建表中断恢复及源库apply/repeat通过，当前180表/23步完成/旧214条记录，原165表271007行哈希一致；34项测试通过。来源表为空，v2真实事务演练与历史业务回填继续进行。

同库升级第十六批见[完整来源真实事务验收](migration/dev-vue-inplace-stage16-20260906.md)：恢复副本v2合成演练通过来源INSERT失败回滚、账户/归属批次回滚、提交响应丢失恢复和重复执行。5条完整来源逐字段/哈希回读一致，合成数据已清理，原165表271007行哈希保持。源库未写入业务；真实冻结清单、时间依据与权限交叉核对继续推进。

同库升级第十七批见[真实归属交叉核对](migration/dev-vue-inplace-stage17-20260906.md)：4账户/274区间/3绑定/3开放区间一致，3个当前实体与4个历史用户账户组合无归属冲突。识别1个旧switched连接状态，核实旧写入语义后保留说明，不误撤销所有权或恢复交易许可；5项测试及真实重复读取通过。无数据库写入，时间依据、冻结清单和业务回填仍待完成。

同库升级第十八批见[真实账户冻结清单](migration/dev-vue-inplace-stage18-20260906.md)：278条来源的备份/结构/映射/执行文件摘要已冻结，真实原165表271007行与备份一致，重复读取清单稳定；4项测试通过。清单明确不可执行，列出历史时区、平台币种历史覆盖、权限投影和真实业务演练四项未完成条件；全域升级目标保持进行中。

同库升级第十九批见[历史币种平台核查](migration/dev-vue-inplace-stage19-20260906.md)：真实21条业绩日记录和3条汇总仅覆盖3/274段归属，已有币种/账户引用无冲突；271段无此证据。旧写入允许当前币种回退与覆盖，不能据此证明历史不变；平台也无逐区间列。3项测试及重复只读核对通过，无数据库写入，后续保留未知历史并区分当前属性与历史事实。

同库升级第二十批见[历史金额单位缺口](migration/dev-vue-inplace-stage20-20260906.md)：发现V4历史交易缺币种列、汇总无单位检查且前端使用当前币种标签。已追加未执行的026兼容字段迁移及两轮联动设计，34项schema runner测试通过。采集/投影/SQL/API/前端尚需继续修复，不能宣称问题已解决；当前库无写入。

同库升级第二十一批见[历史币种采集与投影](migration/dev-vue-inplace-stage21-20260906.md)：MT4/MT5逐条明确币种证据接入原始成交和交易投影，缺失保留未知、冲突标记conflicted；35项定向测试和服务端类型检查通过。026未执行，汇总SQL/API/前端尚待接通，本批无数据库写入或部署，完整金额问题尚未验收。

同库升级第二十二批见[历史金额查询与页面](migration/dev-vue-inplace-stage22-20260906.md)：完整筛选范围检查币种，未知/混币/空态金额NULL；HTTP/OpenAPI/共享合同及交易页同步，停止使用当前币种贴历史标签。42项服务端、49项共享合同、104项交易端测试及相关类型/构建检查通过，MySQL只读合成CTE5组聚合通过。026仍未执行，真实历史表承接、业务回填与全域自动升级继续进行。

同库升级第二十三批见[多步DDL状态恢复](migration/dev-vue-inplace-stage23-20260906.md)：为策略/版本双向外键补充同一表CREATE后ALTER的状态链执行器，复用既有journal，识别DDL前后结构并恢复丢失响应；12项定向测试通过。尚未接入具体策略迁移或执行真实DDL，继续准备恢复副本承接及业务转换。

同库升级第二十四批见[策略表真实承接](migration/dev-vue-inplace-stage24-20260906.md)：新增并执行inplace006，恢复副本两处DDL响应丢失恢复通过，源dev_vue执行3条DDL、独立重跑0条。当前182表/26步，新策略和版本表为空；原165表271007行哈希保持。12项定向测试通过，88个演练工具摘要一致；策略业务转换、其它根冲突和全域升级继续推进。

同库升级第二十五批见[策略订阅来源核对](migration/dev-vue-inplace-stage25-20260906.md)：真实3策略/5订阅共44字段已读取并重复验证，确认NULL品种继承与显式空的区别、当前版本44/1/11不可虚构历史。5项测试通过，基础来源检查无冲突；输出合同、配置、历史时间、权限及版本证据仍需转换落实。本批无数据库写入。

同库升级第二十六批见[订阅品种转换](migration/dev-vue-inplace-stage26-20260907.md)：保留NULL继承/显式空，分别核对旧调度和分发的品种标准化/交集，分歧时阻断；真实5条订阅两路均为XAUUSD，3继承/2显式模式保留。15项测试与真实重复读取通过，仅品种部分可转换，账户/权限/策略版本关联和全域升级继续进行。

同库升级第二十七批见[账户订阅候选](migration/dev-vue-inplace-stage27-20260907.md)：完整账户映射与逐品种结果组合，真实4旧账户→3实体/5候选/2潜在执行占用，无本轮结构冲突。20项测试与真实重复读取通过，不将旧开关升级为运行许可；策略角色版本、归属和配置转换继续进行，本批无数据库写入。

同库升级第二十八批见[订阅时段转换](migration/dev-vue-inplace-stage28-20260907.md)：真实5条配置转换候选通过且均未启用时段限制；保留跨午夜、排除结束分钟、窗外只分析及终端时区语义。27项测试与来源重复验证通过。发现V4调度尚未消费窗口，运行链路接入和时钟可信策略仍为启用前必过项；本批无数据库写入。

同库升级第二十九批见[分析调度时段接入](migration/dev-vue-inplace-stage29-20260907.md)：到期查询读取窗口，在分组前按有来源账户时钟过滤，关闭窗口推进下次正常cadence；20项测试、服务端类型和构建通过。排队后推理、交易员分发与最终执行检查仍待接入，未启动运行角色或写入数据库。

同库升级第三十批见[排队分析时段复查](migration/dev-vue-inplace-stage30-20260907.md)：准备前与每次模型调用前重新核对原账户有效订阅、当前窗口和时钟，覆盖重试跨窗；19项测试、服务端类型和构建通过。交易员分发、处理与最终执行仍需接入，本批没有数据库写入或真实模型调用。

同库升级第三十一批见[交易员分发窗口](migration/dev-vue-inplace-stage31-20260907.md)：分析完成事务按当前订阅窗口与有来源时钟决定是否生成交易员任务，窗外只分析不生成账户动作任务；21项测试、类型和构建通过。新增SQL真实V4运行验证、交易员处理和最终执行仍待完成，本批无数据库写入。

同库升级第三十二批见[交易员排队与返回复查](migration/dev-vue-inplace-stage32-20260907.md)：准备、每次模型调用及返回落库前按精确订阅版本复查窗口，23项测试、类型和构建通过。窗口版本冻结、决策完成/最终执行同事务复查及真实SQL验证仍待完成，本批无数据库写入。

同库升级第三十三批见[风险决策执行窗口](migration/dev-vue-inplace-stage33-20260907.md)：风险决策意图创建和首次dispatch事务检查当前订阅窗口，明确拒绝时无socket发送并释放预留；31项测试、类型和构建通过。分发来源、窗口版本冻结、真实SQL演练与全量升级仍待完成。

同库升级第三十四批见[时段SQL真实结构验证](migration/dev-vue-inplace-stage34-20260907.md)：开发V4参考库6条EXPLAIN和SELECT通过并独立复跑，修复真实MySQL/mysql2 LIMIT数值绑定ER_WRONG_ARGUMENTS；17项测试及类型检查通过。验证限于真实结构和空结果执行，不替代有数据场景与全量迁移验收，无业务写入。

同库升级第三十五批见[订阅者手动单分发窗口](migration/dev-vue-inplace-stage35-20260907.md)：策略分发子命令创建和首次发送按冻结目标复查接收窗口；39项测试、类型和构建通过，7条SQL真实V4参考结构EXPLAIN/空结果执行通过。分发平仓遵循原outcome精确归因规则，不新增接收窗口限制；有数据演练、窗口版本冻结与全量升级继续进行。

同库升级第三十六批见[时段配置内容冻结](migration/dev-vue-inplace-stage36-20260907.md)：区分配置内容与调度游标revision，将窗口摘要冻结到推理快照，开始/完成事务核对，历史缺证据保持stale。28项回归、类型和构建通过；最终执行读取端、分发快照及有数据验证继续推进，无业务写入。

同库升级第三十七批见[最终执行核对冻结窗口](migration/dev-vue-inplace-stage37-20260907.md)：风险决策意图创建/首次发送校验完整输入快照及配置摘要，缺证据或配置变化在发送前拒绝；29项测试、类型和构建通过，8条SQL真实V4结构/空结果验证通过。分发快照与有数据验证、全量迁移继续推进，无业务写入。

同库升级第三十八批见[分发目标冻结窗口证据](migration/dev-vue-inplace-stage38-20260907.md)：分发快照包含窗口摘要并由目标完整性hash覆盖，子命令创建/首次发送同时核对完整性与当前配置。22项测试、类型和构建通过，9条SQL真实V4结构/空结果验证通过；有数据/并发验证与其余字段转换、全量升级继续进行，无业务写入。

同库升级第三十九批见[有数据时钟归属查询验证](migration/dev-vue-inplace-stage39-20260907.md)：真实MySQL只读CTE合成16场景，验证归属/终端/投影/epoch不一致拒绝和时钟可信边界，独立verify一致。不是物理表并发锁证明，无业务写入；其余冻结证据和迁移字段转换继续推进。

同库升级第四十批见[订阅配置源语义检查](migration/dev-vue-inplace-stage40-20260907.md)：5条订阅的记忆/止盈模式、风险与冲突引用、生成键已只读核查并独立复采一致，8项定向测试通过。源语义识别不代表V4目标承接；配置消费者、字段writer与全量升级继续推进。

同库升级第四十一批见[订阅止盈偏好存储](migration/dev-vue-inplace-stage41-20260907.md)：追加027及新订阅同事务初始化，缺失历史配置不补默认；50项定向测试、服务端类型和构建通过。迁移未执行，最终止盈选择及同库回填继续推进。

同库升级第四十二批见[止盈偏好确定性选择](migration/dev-vue-inplace-stage42-20260907.md)：完成决策按显式冻结偏好生成最终价格并记录选择证据，保持十进制精度和管理动作边界。57项定向测试、类型和构建通过；生产配置读取/事务冻结、最终执行核对及同库回填未完成，无数据库写入。

同库升级第四十三批见[止盈偏好冻结和执行核对](migration/dev-vue-inplace-stage43-20260907.md)：生产Worker读取偏好，开始/完成事务和风险决策首次执行核对快照，缺失/变化明确拒绝。71项定向测试、类型和构建通过；027实际结构、回填及新SQL演练未完成，未启动服务或写库。

同库升级第四十四批见[偏好结构与真实SQL演练](migration/dev-vue-inplace-stage44-20260907.md)：V4参考库顺序026/027升级及重复跳过通过，10条SQL真实结构执行两轮一致，8项偏好有数据CTE检查两轮一致；dev_vue源库无DDL/DML。非空旧行转换、物理并发及全量自动升级继续推进。

同库升级第四十五批见[订阅构建表与真实预检](migration/dev-vue-inplace-stage45-20260907.md)：基于真实参考结构追加3张构建表，复用有序迁移与恢复工具；15项测试通过，dev_vue真实只读前置检查三步pending。尚未实际建表/回填，下一步恢复副本演练。

同库升级第四十六批见[订阅构建表实际升级](migration/dev-vue-inplace-stage46-20260907.md)：恢复副本和dev_vue实际完成3条CREATE，修正MySQL约束展示顺序比较，两个断连点恢复且无重复DDL；165张原表、271007行旧列证据一致。源库185表/29完成步骤，新构建表为空，业务回填和全量升级继续推进。

同库升级第四十七批见[策略基础字段转换](migration/dev-vue-inplace-stage47-20260907.md)：3条策略基础候选转换及只读复采一致，保留原版本/文本/归属，不猜测角色与UTC时间；11项定向测试通过。配置和角色映射、回填及全量自动升级继续推进，无数据库写入。

同库升级第四十八批见[逐周期市场数据计划](migration/dev-vue-inplace-stage48-20260907.md)：保留3条旧策略的主周期/不同K线数量，编译与读取接线完成，27项测试、类型和构建通过；源只读复采一致。底层SQL、指标和角色映射及回填继续推进，无写库。

同库升级第四十九批见[K线SQL兼容与取数验证](migration/dev-vue-inplace-stage49-20260907.md)：修复虚拟机mysql2数值LIMIT错误；11条真实结构查询两轮通过，6个CTE取数/精度场景两轮一致，18项测试及类型/构建通过。指标、角色和回填继续推进，无数据库写入。

同库升级第五十批见[入场方式与指标核查](migration/dev-vue-inplace-stage50-20260907.md)：入场方式接入交易员配置/冻结输入/结果校验，3条策略规则只读复查一致；35项测试、类型和构建通过。道诚缠论与EMA数据提供尚待承接，策略回填及全量升级继续推进，无写库。

同库升级第五十一批见[EMA34算法承接](migration/dev-vue-inplace-stage51-20260907.md)：新计算核心与旧算法有效历史逐项一致，9项回归及类型/构建通过。实测声明为M1、34根最小/60根预热，而展示窗口30根；独立指标历史读取尚待接入，无写库。

阶段 52：完成显式 EMA34 分析证据及独立预热窗口接入，23 项定向测试、服务端类型与构建通过；未回填业务数据，见 [阶段 52](migration/dev-vue-inplace-stage52-20260907.md)。

阶段 53：新增跨批次统一结构协调器，45 项测试通过；当前 dev_vue 两次只读核验确认 29 步完成、原 165 表及 271007 行旧数据对账一致。写入协调仍待恢复副本演练，完整自动升级未完成，见 [阶段 53](migration/dev-vue-inplace-stage53-20260907.md)。

阶段 54：新恢复副本实际完成协调器全部 29 步 DDL，6 次断连精确恢复、重复 0 DDL，原 165 表及 271007 行旧数据对账一致。101 个工具文件与回执已核验；全域业务回填、切换和旧结构清理仍未完成，见 [阶段 54](migration/dev-vue-inplace-stage54-20260907.md)。

阶段 55：统一开发库结构命令 `upgrade:dev-vue-schema` 已接入演练证据门，56 项测试通过；当前 dev_vue 实际 plan/apply 验证 DDL 0、日志写入 0、原数据对账一致。尚未挂入部署及完成业务转换，见 [阶段 55](migration/dev-vue-inplace-stage55-20260907.md)。

阶段 56：账户/归属事务适配完整 29 步结构身份，50 项测试通过；来源 v3 两次只读核对与原事实一致。历史时间依据、真实业务回填及全域升级尚未完成，见 [阶段 56](migration/dev-vue-inplace-stage56-20260907.md)。

阶段 57：按计划规范复盘查询，移除 5 处通配符、锁查询限定 6 个消费字段，保留作用域与行锁；15 项测试及类型/构建通过。无数据库写入或性能提升结论，见 [阶段 57](migration/dev-vue-inplace-stage57-20260907.md)。

阶段 58：复盘真实 SQL 探针发现并修复两处 LIMIT 参数兼容问题，6 条查询在 V4 参考库两次 EXPLAIN/SELECT 一致；16 项回归及类型/构建通过。空参考表不构成负载验收，见 [阶段 58](migration/dev-vue-inplace-stage58-20260907.md)。

阶段 59：宏观/经济日历 11 表最终结构进入 008 增量迁移，恢复副本真实建表及 3 次断连恢复通过，原 165 表/271007 行一致；60 项测试通过。当前 dev_vue 为只读预检、尚未执行新增 11 步，见 [阶段 59](migration/dev-vue-inplace-stage59-20260907.md)。

阶段 60：当前 dev_vue 已执行宏观 11 表增量，现 196 表/40 completed；独立重跑 0 DDL/0 日志写入，原 165 表/271007 行一致。统一结构入口接管 40 步，71 项测试通过，业务转换与最终清理未完成，见 [阶段 60](migration/dev-vue-inplace-stage60-20260907.md)。

阶段 61：宏观来源只读复核两次一致，登记 11 表/189 字段和 75 个配置键的筛查结果；不将没有专用旧表等同于没有嵌入历史内容，初始化与运行合同仍待完成，见 [阶段 61](migration/dev-vue-inplace-stage61-20260907.md)。

阶段 62：用户 5 项默认值 NULL 规范化进入 009；隔离副本真实执行、3 次断连恢复、原 165 表/271007 行一致，57 项测试通过。当前 dev_vue 尚未执行，见 [阶段 62](migration/dev-vue-inplace-stage62-20260907.md)。

阶段 63：当前 dev_vue 已执行 009，五项用户默认值改为 NULL、现有行不变；完整 45 步完成，独立重跑 0 DDL/0 日志写入，41 项测试通过。整体迁移与清理仍未完成，见 [阶段 63](migration/dev-vue-inplace-stage63-20260907.md)。

阶段 64：推荐账户转换与逐用户金额对账已实现，13 项测试通过；当前库 25 个用户两次只读转换一致、原 165 表/271007 行指纹匹配。推荐表尚未安装，事务回填与真实目标回读待完成，见 [阶段 64](migration/dev-vue-inplace-stage64-20260907.md)。

阶段 65：推荐表已进入 010 增量，恢复副本真实断连恢复通过；当前 dev_vue 197 表/46 completed，独立重跑 0 DDL/0 日志写入、原 271007 行一致。推荐表尚无业务回填，见 [阶段 65](migration/dev-vue-inplace-stage65-20260907.md)。

阶段 66：推荐域事务回填 writer/runner/46 步 adapter 已实现，33 项领域测试与 8 项共用事务测试通过；真实业务回填尚未执行，下一步副本提交未知恢复及独立回读，见 [阶段 66](migration/dev-vue-inplace-stage66-20260907.md)。

阶段 67：恢复副本真实回填 25 条推荐账户，提交后断连通过持久回执恢复；独立再运行 0 业务 INSERT，25 条目标/回执/源证据与原 271007 行一致。当前库回填及映射/检查点独立回读待完成，见 [阶段 67](migration/dev-vue-inplace-stage67-20260907.md)。

阶段 68：补齐副本 25 条映射/3 批/检查点独立核验，并完成当前 dev_vue 25 条推荐账户真实回填；独立重跑 0 业务 INSERT，原 271007 行一致。应用读写切换及财务写协议仍待完成，见 [阶段 68](migration/dev-vue-inplace-stage68-20260907.md)。

阶段 69：核对旧版下单抵扣、取消/过期返还、佣金审核行为；新增 V4 精确金额规则，14 项测试与类型检查通过。明确旧 pending 订单导入不得重复扣款，账本/订单义务仍待结构实现，见 [阶段 69](migration/dev-vue-inplace-stage69-20260907.md)。

阶段 70：真实核验 12 笔订单、0 条佣金、4 条规则，两次只读一致；4 笔已支付抵扣合计 20、无 pending 抵扣义务，规则周期为 monthly/yearly。历史导入不得重扣当前余额，见 [阶段 70](migration/dev-vue-inplace-stage70-20260907.md)。

阶段 71：订单抵扣分类器已实现，11 项测试及两次真实只读检查通过；4 条历史消费/8 条无抵扣，迁移余额变动固定为零，退款需独立证据，见 [阶段 71](migration/dev-vue-inplace-stage71-20260907.md)。

阶段 72：推荐账本 011 待演练 SQL 与期初生成器已实现，5 项测试通过；期初不伪造历史入账、不更新余额，SQL 尚未执行或注册，见 [阶段 72](migration/dev-vue-inplace-stage72-20260907.md)。

阶段 73：011 已在开发参考库真实建表，2 条合法写入/10 类拒绝及完整回滚通过；当前 dev_vue 尚未安装，待第 47 步恢复演练，见 [阶段 73](migration/dev-vue-inplace-stage73-20260907.md)。

阶段 74：账本第 47 步在恢复副本和当前库完成，真实断连恢复与独立重复通过；当前 198 表/47 completed，25 条推荐账户及原 271007 行不变，账本期初待写入，见 [阶段 74](migration/dev-vue-inplace-stage74-20260907.md)。

阶段 75：期初账本事务 writer 和 verifyOnly 恢复核验已实现，20 项测试通过；只 INSERT opening、不 UPDATE 余额，真实期初回填待演练，见 [阶段 75](migration/dev-vue-inplace-stage75-20260907.md)。

阶段 76：恢复副本真实写入 25 条 opening，提交后断连经 verifyOnly 确认；独立重跑 0 INSERT，余额和原 271007 行不变。当前库期初仍待写入，见 [阶段 76](migration/dev-vue-inplace-stage76-20260907.md)。

阶段 77：当前 dev_vue 写入 25 条 opening，独立重跑 0 INSERT/0 余额更新；原 271007 行一致，21 项测试通过。推荐应用财务事务与全库迁移仍待完成，见 [阶段 77](migration/dev-vue-inplace-stage77-20260907.md)。

阶段 78：运行账本/余额同事务写入原语已实现，19 项测试、类型检查与构建通过；尚无业务接口接入或真实财务变动，下一步 MySQL 事务/并发验证，见 [阶段 78](migration/dev-vue-inplace-stage78-20260907.md)。

阶段 79：开发参考库真实验证同事件并发仅一次入账、账本插入后故障整事务回滚及返还；独立测试行已清理，当前库未执行财务操作，见 [阶段 79](migration/dev-vue-inplace-stage79-20260907.md)。

阶段 80：orders 全部 23 字段/12 行完成精确来源检查，两次只读一致、4 项测试通过；12 行历史时间基础尚待明确，完整订单目标转换未完成，见 [阶段 80](migration/dev-vue-inplace-stage80-20260907.md)。

阶段 81：支付订单 23 字段转换合同与候选生成器已完成，21 项测试通过；保留时间未知、独立金额与支付关联依赖，禁止导入副作用。本轮未写数据库，下一步核对监听关联并冻结目标 DDL，见 [阶段 81](migration/dev-vue-inplace-stage81-20260907.md)。

阶段 82：真实核验 8 条监听与 12 笔订单，最终两次读取一致，16 项测试通过；8 条取消/过期监听关联匹配，4 笔已支付抵扣订单无监听，TIMESTAMP 与 DATETIME 分别处理。未写数据库，见 [阶段 82](migration/dev-vue-inplace-stage82-20260907.md)。

阶段 83：修正 payment_side_effects 直接映射会员激活的错误假设；真实核验 4 条 completed 投递记录，15 项测试及两次读取通过。历史不重放，不生成新权益，目标 DDL 尚未执行，见 [阶段 83](migration/dev-vue-inplace-stage83-20260907.md)。

阶段 84：payment_orders 012 SQL 已在开发参考库真实建表，3 条合法写入/14 类拒绝及金额回读、事务回滚通过；当前库尚未安装，第 48 步协调与恢复演练待接入，见 [阶段 84](migration/dev-vue-inplace-stage84-20260907.md)。

阶段 85：订单第 48 步在恢复副本与当前 dev_vue 完成，建表断连恢复、独立重跑均通过；当前 199 表/48 completed，旧 271007 行及推荐余额/账本保持一致，订单表尚为空，见 [阶段 85](migration/dev-vue-inplace-stage85-20260907.md)。

阶段 86：支付交易/匹配两表在开发参考库建成，2 条交易观测/3 条匹配、18 类拒绝及金额差回读/回滚通过；当前库尚未安装，第 49/50 步协调待接入，见 [阶段 86](migration/dev-vue-inplace-stage86-20260907.md)。

阶段 87：支付第 49/50 步在恢复副本与当前 dev_vue 完成，双断连恢复及独立重跑通过；当前 201 表/50 completed，旧行及推荐余额/账本一致，支付三表仍空，见 [阶段 87](migration/dev-vue-inplace-stage87-20260907.md)。

阶段 88：订单最终物理行转换及逐行时间依据绑定已实现，27 项测试通过；真实历史时区尚待确认，本轮不写库，继续保留完整支付业务转换门，见 [阶段 88](migration/dev-vue-inplace-stage88-20260907.md)。

阶段 89：订单逐行 writer、独立 15 字段源事实对账已实现；17 项测试与参考库真实插入/重复/冲突拒绝/回滚通过，真实订单仍未回填，完整迁移事务待接入，见 [阶段 89](migration/dev-vue-inplace-stage89-20260907.md)。

阶段 90：订单批次 contract/runner、50 步 MySQL 适配及同事务源证据已接入，42 项测试通过；整批真实 COMMIT 后断连演练待执行，真实订单回填仍受历史依据和领域门约束，见 [阶段 90](migration/dev-vue-inplace-stage90-20260907.md)。

阶段 91：恢复副本两条合成订单完成真实整批 COMMIT 后断连查证、证据失败原子回滚、映射/回执/源证据核验和重复；合成记录已清理，原 271007 行一致，真实订单未回填，见 [阶段 91](migration/dev-vue-inplace-stage91-20260907.md)。

阶段 92：无认领交易的历史匹配最终行及时间/资产依据绑定已实现，29 项测试通过；完整来源保留，不补造交易、不启动监听，真实匹配回填仍待证据与事务接入，见 [阶段 92](migration/dev-vue-inplace-stage92-20260907.md)。
