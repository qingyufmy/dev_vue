# 后端核心剩余范围核对

日期：2026-09-11。此页更新完成证据，不删除总体功能矩阵中的需求，不将前端页面和公网验收全部设为当前核心门槛。

## 2026-09-12 当前结论与执行顺序

本轮包 1、2 已推进：完整 Chan v8/Worker 接入、旧配置转换已实现；现库 56 条关键外键及旧信号/执行 5 组关联只读通过。当前准确状态、证据与未完成项以 [包 1、2 实现记录](backend-packages-1-2-progress-20260912.md) 为准。历史归档 API、真实长窗口休市证据、旧角色语义尚未完成，不能把下方历史计划中的“下一次从引擎开始”继续当作当前待办。

本节优先于下方按时间追加的进展。此次重新核对源码和已有报告，未重连数据库、未重跑依赖测试。架构底座/合同规范已有实现，交易核心部分开发依赖链已通过；完整旧策略运行和全域产品功能均不能宣称完成。不以单函数数量或测试项数计整体完成率。

| 顺序 | 一个完整交付包 | 具体范围 | 结束标准 |
|---|---|---|---|
| 1 | 旧策略进入新分析链 | 补齐最终背驰/候选映射与结果选择、完整computeChan及历史窗口策略；经market公开业务入口接AnalysisMarketSource/ContextBuilder和Worker；核对角色提示词及其它旧配置阻断 | 冻结行情下完整引擎新旧对比通过；实际Worker的模型输入含所需结构/能力字段；关闭功能时不注入；不扩大浏览器/模型K线窗口；迁移支持项与实际运行一致，未支持项明确拒绝。验证不自动激活现有策略/订阅 |
| 2 | 核心数据与运行接线收口 | 复用既有非空参考组合SQL/Redis证据核对主链生产组装；确认关键外键约束；核对旧推理、信号、执行记录的归属/ID映射、读取去向及必要数量/金额对账 | 写清每项为当前API可读、历史归档可读或尚缺迁移/接口；缺失处补实现，不仅写说明。对实际改动跑一次必要整链验证；已通过复盘链不反复扩大样本 |
| 3 | 前端按功能落地 | 先trade登录/会话与账户切换、Bridge状态和页面骨架，再分析/策略、交易员/风控、复盘/历史，随后对应设置管理 | 每个功能同时检查API可用性、权限、账户作用域、时间显示和异常状态；相关后端缺口随功能补齐。模型配置管理等未实现能力必须真实开发，不伪装成可用页面 |

当前即可基于稳定合同开展登录/账户等前端，不以包1/2全部完成为硬前置；若保持单线后端优先，则先连续完成包1与包2，再进入前端。默认下一次实施从包1剩余完整引擎开始，不再把每个内部函数当作独立阶段。

后置但保留：Bridge实机与更新发布、自动月周期及单笔生产触发的实机联调、长稳/容量/公网切换；支付/会员激活、课程后台、社区、通知、站点内容和完整平台管理按对应产品功能建设。它们不是已经完成，也不全部阻塞交易前端起步。数据库继续使用虚拟机现有dev_vue，追加兼容升级，保留历史；当前没有必要再次全面改表或清库。

本次顺序复核：范围上优先既有策略可运行，避免把原算法移植扩大为策略优化；数据上保留未验证读取和外键项，不把表数/API数等同于无损升级验收；启动前端不等于后台能力全部完成。

## 已取得当前证据

- 最新既有 dev_vue 升级报告：314 表、266 条 completed 迁移（memory-completion-current-v1-20260911.json）；4418 列来自升级前目录，不作为最新列数；117 个静态目标及两个额外动态目标全部存在。见 `architecture/current-database-catalog-20260911.json`。本次仅检查目录和迁移状态，没有重做数据逐行对账或比较迁移文件 checksum。
- 当前源码写入清单及间接调用已分类：见 `architecture/data-write-ownership-review-20260911.md`。不是数据库触发器、旧代码或全程序数据流证明。
- 当前 104 个 HTTP 合同与实际路由一致；运行合同和消费者类型生成检查通过。
- 分析到交易员、风险、执行、模拟回执及 API 回读的同链证据已建立，替身及生产派发差异见 `backend-main-flow-evidence-20260911.md`。

## 核心不能关闭的项目

记忆日志结构阻断已解除：078 已应用当前 dev_vue，266 步 completed，314 表旧数据/原迁移 checksum 对账一致；升级后直接克隆当前表结构的非空记忆主链通过。ReviewWorker 生成→确认→接受→后续交易员使用已在同链通过；非空自动日周期采集→case/job→ReviewWorker→HTTP接缝本轮已通过；与确认/记忆链为独立验证。

| 工作 | 具体缺项 | 如何关闭 |
|---|---|---|
| D3 生产组装一致性 | 同链探针已使用实际策略、品种读取及执行准备消费者；交易员上下文已使用实际账户、来源、偏好、风险、品种和记忆读取；策略参考组合已通过实际 SQL/Redis 验证空库存及频道撤权；Bridge 派发已使用生产共用处理器与服务（网络transport仍模拟），调度 tick 和分析开始事务已通过；分析 Worker/上下文/窗口及队列已贯通；模型网关/用量结算/共享额度并发和恢复已验证；模型 catalog/解密/归属校验已进入主链（fetch 为测试替身）；非空参考和外键证据尚未闭合 | 替换为生产适配器或用独立实际依赖证据覆盖其输入/输出接缝，验证权限、重放和未知结果；不要求真实账户下单 |
| D2 必要自动复盘 | 非空自动日周期采集→任务→生成→HTTP已通过；单笔收集由探针显式调用，月周期writer/Worker与确认后记忆使用为独立证据 | 基础接缝已验证；生产单笔触发、自动月边界及真实终端在对应功能联调核对，不把细节持续扩为前端起步门槛 |
| D8 历史数据和读取切换 | 表目录全部存在不能证明历史字段语义和全部读取已切换 | 汇总已有迁移/回填/恢复报告，逐域对应读写入口和原需求，缺证据处补验证 |
| D8 完整功能去向 | 现有接口清单只包含已声明实现，未声明的旧业务不会被路由对账发现 | 以 full-project-function-migration-matrix 第 8 节及原业务盘点为上游，分别标记已有实现、待实现、前端/Bridge 阶段配套及上线验收 |

## 已确认仍不能宣称完整交付的业务域

`commerce/README.md` 明确现有范围为学习权益读取、推荐规则管理和钱包读取。它不能代表原矩阵的订单、TRC20 支付、权益激活及完整返佣对账已经实现。`learning` 当前课程读取和完成事务也不能代替全部内容、测验和后台管理。

当前 14 个模块目录及 HTTP 域源未提供独立 community、notifications、site-content 的完整能力；不能靠目录名缺失断言功能不存在，但本次现有路由清单没有给出这些全流程的完成证据。它们仍保留在统一清单 D5/D6，后续应随对应产品重构落实服务端用例，不能写成“后端无需再做”。

identity/profile/membership/models 等业务职责可由现有模块承接，不要求为匹配原矩阵名称新增空模块；需要核对具体能力，如 MFA、资料修改、模型管理和权益，而不是机械对齐目录数量。前端与 Bridge 细节继续按用户指定顺序后置。

下一步按上表核实实际接线与既有证据，优先关闭 D3/D2 必要流程和 D8 数据切换，避免把新文档数量当作交付进度。

## 全域需求对账更新

[22 域逐项对账](backend-domain-reconciliation-20260911.md) 已覆盖原功能矩阵第 8 节全部行，并附从合同源提取的 104 个操作。已区分当前模块承接、缺少的管理/业务能力与交付归属；不再用路由集合一致证明原需求全覆盖。D8 功能去向完成首轮源码/合同核对；逐域历史数据语义及读写切换仍未关闭。下一步优先汇总这些既有迁移证据，避免继续扩大复盘边缘验证。

## 历史承接实际读取更新

[当前核心数据读取核对](backend-core-data-read-reconciliation-20260911.md)：应用账号只读验证策略/订阅/4个记忆库、风控归属及183个历史案例通过，业务写入0。明确新增已证实缺口：旧策略 use_chan_analysis 仍为 chan_runtime_mapping_required，角色提示词 semanticAcceptance=pending，迁入草稿/暂停不能视为运行语义完成。此项归D3/D8核心；下一步核对旧输入并承接通用数据能力，不能简单解除阻断或自动启用。

缠论承接已定位实际 v8 算法与独立历史窗口；内部K线读取上限已支持2000，真实dev_vue只读1000/1800/2000根验证通过，2001拒绝。模型/浏览器窗口未扩大，计算和投影仍待承接，迁移策略仍未激活。详见核心数据读取核对的最新章节。

## 前端并行起步与当前收口（按用户最新要求）

用户明确前端阶段逐功能测试，不将全页面/实机细节作为当前前置门槛。现有trade消费者已包括bridge、analyst、strategist、risk、reviewer、trades、audit、trader各自API模块，可以基于稳定合同逐模块开展前端；“可开始”不表示该页面已验收或相关自动业务完整。

- 可以先做：账户/Bridge连接信息、分析列表/详情、策略与订阅编辑、风控政策/决策、复盘历史和记忆读取、交易历史、用户审计。相应消费者位于 frontend/apps/trade/src/features/<功能>/api/。
- 功能仍须配套补齐：旧策略缠论完整计算/角色语义、自动复盘实机联调、非空参考组合、模型配置管理及其它未声明功能。保留在D2/D3/D8或对应产品阶段，不伪造运行成功状态。
- 派发主链的一项实质缺口已关闭：生产处理器→实际持久化→模拟send→重放不二发→结果/API通过，见 bridge-dispatch-main-flow-v1-20260911.json。真实网络/终端验证留给Bridge联调。

接下来按影响用户流程的缺口收口，不再持续扩大纯算法测试样本或以内部函数数目报告阶段。后端核心整体仍未全部完成。

## 本轮收口

非空自动日复盘已获得实际SQL/Redis/Worker/HTTP证据，见 `architecture/period-nonempty-workflow-v4-20260911.json`，并保留上述证据边界。后续核心实现优先旧策略完整运行接入（缠论引擎/窗口/Worker和角色语义），随后整理历史推理/信号/执行读取与生产参考组合接线。稳定API对应前端可开工，不等待全部模型、支付、内容管理功能及实机细节一次性完成。

### 2026-09-12 旧策略运行接入进展

完整v8单窗口组装已落入market域，数据质量/时钟/结构能力保持旧语义，320组对比通过，严格类型检查通过。本轮无数据库连接/变更，无运行进程启动。尚缺跨窗口共识、完整引擎与Worker接线，角色语义和历史读取切换也未关闭；不能将单窗完成写成旧策略已能运行。前端起步边界维持不变。

跨窗口结构投票与首次结构稳定性判断已迁入，475项旧版对比及服务端构建通过。当前仍未完成的是最终背驰/入场候选证据映射、最终结果选择、完整历史窗口策略和Worker组装；没有启用旧策略，也没有改动数据库。此处是原缠论能力的分层承接，不是新增交易规则。
