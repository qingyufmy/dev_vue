# 策略配置与自动分析联调记录

## 已验证范围

本次为本地 V4 服务与开发库 dev_vue 的实际依赖验证，不是公网发布或交易执行验收。

- 道诚实战精选策略（strategy_id=1）通过当前交易端编辑、保存并发布 v45（version_id=7）。
- 配置：每 5 分钟，主周期 H1；H1 150、H4 100、M15 150、M5 100、M1 100 根；开启缠论、价格行为及 M1 EMA34。
- MT4 8950701（account_id=7）订阅 ID6：仅分析，不启用交易员或发送交易指令。
- 自动分析运行 268aaa71-e186-4591-aa17-7e27b522ba11 首次遇到选源校验错误，修复后重试原队列任务成功。
- 下一轮自然运行 15aef83b-9cca-4414-8e8d-5cd161b1549f 成功；model_task_id=e18e6b7d-44bc-41b4-aa82-9c22c1107dab。
- 冻结快照 ad0d8728-e546-4ed1-901c-aa1e5ac21527 确认上述周期数量；公共源为 MT5 account_id=1、generation=342，分析请求账户仍为 MT4 account_id=7。
- 结果 429693e2-229f-4ead-8af4-cedcc53977eb 已在 AI 分析师页面显示；长结论以正文展示，不再占用大标题。

## 修复

公共/私有受管行情源已由选源服务鉴权并校验来源版本，不能再强制要求来源账户等于发起分析的交易账户。保留旧模式的账户一致性校验，受管来源必须具备有效来源标识及版本。

准备分析时发生确定性 InferenceError，记录失败原因并结束队列处理，避免运行一直停留在 queued；版本冲突和未知异常继续交给原重试机制。

本地分析启动脚本支持 scheduler-analysis。

## 验证结果

- 定向行为回归 21 项通过：调度/工作器、输入事务边界、行情选源。
- 服务端构建通过，交易端类型检查通过。
- 浏览器实际查看最新结果并检查排版。
- 本轮没有发送终端交易指令；交易发送保持关闭。

## 未完成与下一步

1. 最新结果为观望，不代表策略数据已就绪。H1/H4 缠论返回 chan_history_gap_unresolved；价格行为存在 event_history_gap_unresolved 和 event_clock_unavailable。需区分正常休市断档与真正历史缺失，并核对公共时钟在计算上下文中的使用，不能直接删除校验。
2. 当前页面顶栏显示“自动分析已关闭”，账户订阅页却显示运行中；同时连接状态待确认。需核对无当前在线账户时的状态表达，不能把未选中账户等同于订阅已关闭。
3. 数据库时间与快照/页面时间显示存在差异；此记录以运行 ID 为关联依据，尚未完成时钟一致性审查。
4. 工作区含大量继承改动，本轮文件与既有修改交织，未做整批提交或推送。

本轮证明了配置保存、自然调度、模型调用、结果落库与页面展示链路；指标数据可用性仍需下一轮处理。

## 后续推进：公共校准与账户状态

- 指标输入改为读取已授权管理员的公共校准记录，有效期 25 小时；终端心跳中的 stale 不再覆盖公共确认。读取前后核对管理员资格，记录不存在时保持不可用。
- dailyCalibration 随缠论/价格行为输入归档；旧快照未标记时仍执行原五分钟规则，避免改变历史重放结果。价格和 K 线时效检查保留。
- 实际冻结快照复算：M1、M15 的价格行为从 event_clock_unavailable 恢复 ready；M5 转为 event_source_stale，H1 仍为 event_history_gap_unresolved。因此不能把全部数据宣称为可用。
- 顶栏并非订阅数据更新失败：浏览器确认当前控制账户为 MT5 860058，其订阅 1、3 已暂停；运行中的是 MT4 8950701 的订阅 6。增加当前账户 title 提示，无账户时显示待选择账户。
- 服务端 25 项相关测试、前端 4 项测试通过；服务端构建和交易端类型检查通过。已重启本地分析工作器。
- 纠正前述时间疑点：诊断 MySQL 客户端未指定 timezone 时把 UTC DATETIME 按本地时区解析，产生 8 小时偏差。指定 timezone=Z 后运行创建时间为 16:40 UTC，与快照一致；此前 08:40 是诊断显示偏差。

### 历史连续性剩余工作

实际历史：H1 的 1800 根有 78 个非连续间隔，H4 的 1000 根有 34 个；既包含每日维护/周末形态，也存在 9 月 7 日较长间隔。旧版采用匹配具体来源的休市规则，而 V4 目前只保存即时开休市状态，没有可重放的历史休市规则。即时“开市”不能解释过去每一个缺口。本轮没有猜测假期时间或直接取消缺口检查，历史连续性尚未完成。

另记录旧工作器 16:45 UTC 运行 136180cf-5134-4f1b-9408-f9fbc6b931d9 因 analysis_text_invalid 失败，需独立检查模型输出必填文本；未通过放宽合同掩盖。

### 新工作器自然运行

16:50 UTC 自动运行 0d30c41f-53d8-4fd3-9e88-a7ee89bc75bf 已进入模型调用，输入快照 51802492-0343-43bd-b739-86f8987bd037：dailyCalibration=true，读取 16:29:01 UTC 管理员确认的 UTC+3；M15 events ready，M1/M5 event_source_stale，H1/H4 event_history_gap_unresolved。此证据确认修复被实际工作器使用，不能代替整套数据就绪验收。

## 整点收盘等待修复

16:50 快照的 M1 最后收盘为 16:48、16:49 仍标记 forming；M5 最后收盘 16:40、16:45 仍 forming。16:52 实时数据库已经更新至 M1 16:51、M5 16:50，证明采集持续工作，问题发生在分析抢先于收盘推送。

- 读取到理论上已结束但终端尚未确认 closed 的尾部 K 线时，返回 market_candle_close_pending。
- 分析工作器每 5 秒重新构建一次快照，最多 3 次等待；每次刷新捕获时间并复核执行窗口。仍未确认则记录失败，不调用模型，不把 forming 本地改成 closed。
- 重读仅访问已有数据库和选源状态，不额外请求终端历史；报价频率和历史页大小保持原值。
- 39 项相关测试通过，服务端构建通过，已重启分析工作器。
- 自然运行 d5b66407-8392-4590-acf8-f7b9ed219b30 于 16:55:00.459 UTC 创建，快照 2afa13ff-c191-4a25-b547-3bb1a9678b2a 的 capturedAt=16:55:06.302 UTC。M1、M5、M15 events 均 ready，H1/H4 仍为 event_history_gap_unresolved。
- 历史休市识别仍未完成：当前旧版与 V4 本地配置都未设置明确的来源休市策略。没有通过通用周末猜测或删除连续性检查来绕过此项。
