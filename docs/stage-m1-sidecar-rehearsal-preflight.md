# Stage M1 旁路数据库演练前置检查

> 日期：2026-09-05
> 状态：用户追加授权 SSH 建库，两个空目标库已创建；仅执行建库 DDL，基础迁移链与数据演练仍未完成
> 检查基线：`b1246662`，分支 `dev_vue`

## 1. 已确认范围

- 源为 `server/.env` 配置的 `dev_vue`，始终只读。
- 计划使用两个独立空库重复演练，不在源库运行 V4 SQL。
- 比较结构、迁移 checksum、逐用户/账户计数、稳定主键、关键金额和大载荷哈希；验证中断恢复与回滚。
- 不切换应用连接，不启动运行时、模型或交易 Worker，不访问 MT5 交易接口。
- 不删除旧表，不自动删除演练库；清理由独立确认门处理。

## 2. 本次真实只读证据

数据库查询均位于 `START TRANSACTION READ ONLY` 中，结束执行 rollback 并关闭连接；未输出凭据或用户行内容。

| 检查 | 结果 |
| --- | --- |
| 当前数据库 | `dev_vue` |
| MySQL 版本 | `8.4.8` |
| 可见源表数 | 165 |
| 源迁移 ID 降序首项 | `201_risk_reset_baseline_and_semantic_version` |
| 当前连接账号授权 | `USAGE ON *.*`、`ALL PRIVILEGES ON dev_vue.*` |
| 目标库创建/访问授权 | 当前授权中没有独立旁路库权限 |

不能因为账号可以修改源库，就用源库做演练；也不能尝试扩大账号权限。本次未发送 CREATE、ALTER、INSERT、UPDATE、DELETE、DROP 或 GRANT。

## 3. 迁移链缺口

按现有 17 份 SQL 的声明顺序静态检查，共声明 93 个新表，但仍依赖以下外部基础表：

| 缺失基础表 | 首次使用 | 当前源 ID 类型 |
| --- | --- | --- |
| `bridge_refresh_sessions` | 001 直接 ALTER | `bigint` |
| `users` | 002 外键引用 | `int` |
| `ai_model_profiles` | 012 外键引用 | `int` |

该检查只识别建表、ALTER 与外键依赖，不是 MySQL 语法或完整列兼容性验证。结论是：现有 001～017 **不是独立的从零建库链**；空库预检通过也不代表这些 SQL 可执行。

在 `server/src` 与 `scripts` 的相关源码检索中，尚未找到 V4 SQL 文件执行、checksum 写入、部分 DDL 中断恢复的完整迁移器。现有 `verify-v4-migration-target.mjs` 只读检查目标；`rehearse-trade-history-migration.mjs` 比较调用者提供的对账输入，并不负责真实数据库搬迁。

当前 165 表迁移矩阵仍是设计基线，不能用复制三张基础表或只执行宏观 DDL，冒充全量数据迁移完成。不得直接复制旧 `inference_snapshots` 等同名异构表到 V4 活动结构。

## 4. 恢复演练所需条件与执行顺序

1. 在宝塔新建两个空 MySQL 库，建议名 `dev_vue_m1_a`、`dev_vue_m1_b`。如果名称已有数据，必须另选名称，禁止覆盖或清空。
2. 提供仅针对这两个目标库的迁移身份与连接配置；现有源连接仅用于只读查询。不需要向源应用账号授予全局权限，不在聊天或 Git 中记录密码。
3. 在真实目标 DDL 前补齐并复审明确的基础 schema 安装产物、文件迁移器、checksum/执行记录和部分 DDL 失败关闭规则。现有 001～017 不重排、不删除；基础 schema 作为显式前置阶段管理，不伪造执行历史。
4. 生成可恢复的冻结源备份与机器可校验的逐字段处置清单；识别旧 DATETIME 时区语义、孤儿引用与同名异构，不猜测转换。结构演练与全量业务数据搬迁分开验收。
5. 对两个空目标预检，依序执行结构迁移，再从同一冻结源进行数据迁移和对账。目标不一致、中断状态不明确或哈希变化时停止，不自动重放未知 DDL。
6. 完成恢复验证后保留两个目标及脱敏验收记录，不切流、不删源。

## 5. 两轮复审

### 第一轮：范围、需求与过度设计

- 原先把“17 份 V4 SQL”视为可直接在空库执行的路径不准确，已明确三张基础表缺口。
- 宏观新增表的结构验收不等于全部用户数据迁移；两者分别出具证据，不能缩减用户的数据保留要求。
- 不为绕过权限而在源库建前缀表，也不启动另一个临时数据库来冒充已配置 MySQL 的验收结果。

### 第二轮：权限、数据、异常与回滚

- 当前授权仅覆盖源库，停止所有目标写操作；目标库权限由用户配置，不尝试提权或读取其它项目凭据。
- MySQL DDL 不可依靠普通事务 rollback 撤销；恢复需要迁移执行记录、已完成步骤识别与隔离目标，不能宣称本次只读 rollback 已验证 DDL 回滚。
- 尚无备份恢复、真实 DDL、双次数据对账、并发/锁、查询性能证据。这些验收项均保持未完成。

## 6. 首次前置检查结果

完成了实际连接权限、源库版本/表数及迁移基础依赖检查；没有创建目标库或搬迁任何数据。M1 继续保持未完成，权限配置和迁移基础链补齐后再恢复真实演练。

## 7. 用户追加授权后的 SSH 建库结果

用户确认两个库为测试用途，并授权直接通过 SSH 新建。已核对 SSH 主机与本地 `server/.env` 的 MySQL 实例 UUID 相同，版本为 8.4.8；先检查两个目标均不存在，再执行两条显式 CREATE DATABASE，不使用覆盖、清空或 `IF NOT EXISTS` 掩盖同名冲突。

| 目标库 | 字符集 | 排序规则 | 建库后表数 |
| --- | --- | --- | --- |
| `dev_vue_m1_a` | `utf8mb4` | `utf8mb4_unicode_ci` | 0 |
| `dev_vue_m1_b` | `utf8mb4` | `utf8mb4_unicode_ci` | 0 |

- 通过宝塔自身配置读取接口在远端内存中取得 MySQL 管理凭据；未导出、打印或写入 Git。直接读取旧配置不能正常认证，采用面板当前的配置读取/解密路径后连接成功，没有重置任何密码。
- 未创建数据库用户，未执行 GRANT，现有应用身份仍仅访问源库。后续目标演练可在 SSH 内执行，无需扩大应用账号权限。
- 没有修改 `server/.env`、运行时开关或应用数据库指向；没有启动/重启网站、Worker 或 Bridge，没有发送任何 MT4/MT5 指令。
- 未向两个空库执行表结构迁移或数据导入，未修改源 `dev_vue`。仅直接创建 MySQL schema，未修改宝塔数据库列表元数据。
- 建库权限阻塞已通过用户授权的 SSH 路径解除。下一步仍需先补齐第 3 节的基础 schema 与完整迁移器，再验证迁移；两个空库的存在不代表数据迁移已完成。
- 两个目标保留供测试，不自动删除；删除必须另行确认。
