# 分析中文输出

范围：通用分析输出语言约束、已知历史市场状态中文映射、原文按需展开。没有修改策略方向、阈值、证据或历史数据库内容。

复核一：只约束面向用户的说明，不翻译 JSON 字段、机器枚举、事件标识；策略仍是判断权威。复核二：不加入语言失败重试门，不伪造历史中文推理；旧正文保留原样，已知状态精确映射，未知内容保持原文。

验证：交易端类型检查、12 项模型网关相关测试、服务端构建和边界检查通过。浏览器确认历史市场环境为中文且原文可展开。

运行边界：本次未重启运行中的分析 Worker，未发起模型调用。新提示已进入构建产物，待 Worker 重启后生效；不能宣称已有真实新结果符合中文要求。历史正文仍可能包含英文。

下一步：让分析 Worker 加载新提示并验证下一条分析的中文结果；随后推进风控师功能验收。继承工作区未安全拆分，本轮不整体提交推送。


## 运行验证补充

2026-09-15 已确认无排队/运行任务后重启本地 worker-analysis，新进程 63368，/health/ready 返回 ready/accepting。一次手动分析 4409af00-813f-493c-97dc-b2ddad98b2f2 因 daily_token_limit 失败，未生成新结果。未调整额度、未重复请求；提交前确认无 active + analysis_enabled + trader_enabled 的订阅。

新增失败原因读取：仅任务失败时通过公开审计接口读取一次，中文解释额度上限，未知错误不展示机器码；晚返回结果验证用户和任务版本。9 项定向测试、类型及前端边界检查通过。实际中文模型输出仍待额度恢复后验证。

风控页面初步清理：未知规则与解除条件不直出代号，移除策略哈希和内部交易决定编号；参数结构化展示仍待后续完善。
