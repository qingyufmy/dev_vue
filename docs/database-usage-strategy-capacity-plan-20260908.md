# 用量审计策略引用容量

144步库ai_model_usage_logs.strategy_id目前INT NULL，目标BIGINT UNSIGNED NULL。12139行中负数0、NULL1，非空值1–3。该列无入站或出站物理FK；只扩大整数容量，保持旧ID和NULL，不切换策略权威或重映射旧引用。

第一轮复审：字段属于审计记录，扩大容量不改变既有记录含义；目标空库本就将其作为不可变审计引用。既有V4用量writer接受字符串策略ID，读者无需因容量调整改变结果。未来大ID仍须满足服务端策略合同，不由此次DDL授予写入能力。

第二轮复审：追加1条MODIFY，以完整表定义串联之前索引/FK步骤，保留旧SQL和checksum。恢复副本DDL后异常恢复、重复0DDL，全部业务表逐行摘要/其它结构/自增值保持后执行当前库。该列按十进制文本对账以避免驱动INT与BIGINT类型表示不同；不把表示变化误认为数据变化。
