# 模块化 P0 第一批进度

2026-09-08，按[全栈模块化方案](full-stack-modularization-plan-20260908.md)持续实施。P0 尚未完成，P1–P7 保持原范围。

## 已实现

- 收窄认证测试辅助函数的 payload 为对象记录类型，保留额外非法字段的拒绝测试；完整 `typecheck:server` 通过。
- 新增 TypeScript AST 依赖解析器，支持静态导入、类型导入、转导出、字面量动态导入、require、别名和 Vue script；计算型动态导入显式列为未解析。
- 新增 `inspect:server-boundaries` 与 `inspect:frontend-boundaries`，以非零退出状态报告现有问题；没有自动生成或接受豁免。
- 后端检测跨域内部引用、公开入口经 barrel 暴露 infrastructure/transport、domain 与 application 反向依赖、受限组装入口和源文件依赖环。
- 前端检测显式源码导入的跨应用、feature 内部引用、共享包反向导入应用和依赖环，保留原有前端检查不变。

## 当前证据

| 范围 | 扫描结果 |
| --- | --- |
| 后端 | 232 个源码文件，60 条跨域内部引用、77 条公开实现导出、11 条 domain 依赖候选、1 个源文件循环分量 |
| 前端 | 322 个源码文件，25 条 feature 内部引用、9 个源文件循环分量；配置解析错误 0 |

明细：[后端](migration/server-module-boundaries-20260908.json)、[前端](migration/frontend-module-boundaries-20260908.json)。各规则结果可重叠，不能相加解释为独立故障数。类型依赖也参与检查；循环分量不是运行时循环故障的直接证明。

`pnpm run typecheck:server` 通过；解析器 9 项正反例及认证 10 项行为测试通过；`git diff --check` 通过。两个 inspect 命令发现真实存量差异，预期退出 1，不能表述成架构验收已通过。

## 仍需继续

1. 复核导入及转导出来源，区分具体实现与允许的公开契约；完善模块层之外的共享/组装依赖判定，增加模块级依赖环与运行/type-only 区分。
2. 检测器当前扫描 Vue script，不分析 template 自动组件和 Nuxt 自动导入所有权；补齐这些入口后才能作为完整前端模块门禁。
3. 对已复核债务建立精确例外清单，记录 source/target/rule、模块、理由与消除阶段，拒绝新增和陈旧例外；不能只用总数防回归。
4. 建立模块/表写入所有权清单，确认合同编译方案；接入方案要求的 verify 命令，而非把 inspect 改名后直接宣称完成。
5. 完成 P0 后进入账户业务样板，数据库、API 与前端同步推进。

本批未启动服务、连接真实依赖或执行数据库迁移。此前方案及审查文件保留为设计历史，其中“类型检查失败”描述的是实施前基线，以本记录为修复后的状态。

## 第二批：依赖图精度与 Nuxt 自动导入

已区分 type-only、混合和运行导入；支持 import-equals，解析失败直接报错。新增模块级强连通分量检查：即使不同文件之间没有闭环，跨模块依赖环也会报告完整来源证据。

[后端 v2 扫描](migration/server-module-boundaries-20260908-v2.json)显示 bridge/execution/inference/risk/trading 形成模块级循环分量，其中存在运行导入边；risk 两个领域文件的源文件循环仅包含类型反向边，不再把它描述为运行循环。这里的 runtime 指静态运行导入图，不代表已经启动或观察到实际运行故障。

新增 Nuxt app 自动依赖检查：读取重新 prepare 后的 imports/components 声明，解析 Vue 模板编译结果和脚本作用域，显式导入、参数遮蔽和属性名不会被错误算作自动导入；局部声明缺失或目标不存在直接失败。复用了已安装 Vue 的 compiler-sfc，没有新增依赖或修改 UI。

本轮 `pnpm --filter @aurum/www exec nuxt prepare` 成功；15 项检测器测试与语法检查通过。[前端 v2 扫描](migration/frontend-module-boundaries-20260908-v2.json)仍为25条内部路径引用和9个源文件循环分量；自动边为0，经核实主站 learning composable 使用显式导入。该结果不表示没有其它架构债务。

剩余：Nuxt server 自动导入、动态组件表达式、声明新鲜度的自动保证仍需补齐；当前人工先 prepare 再 inspect。精确例外与完整模块/表写入所有权清单尚未创建，verify 门禁和 API 合同编译方案尚未收口，P0 保持进行中。

## 第三批：会话模块实际封装

为 trade/admin auth 模块定义公开入口和职责说明。消费者改用公开能力，session ref、嵌套身份对象和 load 返回值均为只读；加载与退出仍由模块拥有。登录页通过公开的动态加载函数提供，不因公开入口而提前打包进主入口。

实时消费者改用 `TradeSessionSnapshot` 只读合同，Bridge 配对接收只读 Ref。没有通过断言把只读状态伪装成可写状态，也没有将整个内部实现转导出到入口。其它模块不能再直接修改共享会话事实。

两端类型检查、构建和原有127项测试通过；新增2项公开会话行为测试通过，验证跨消费者无法改写 ref/用户ID、加载返回值只读、正常退出可清空状态及 admin 权限筛选保持。[前端 v3 扫描](migration/frontend-module-boundaries-20260908-v3.json)显示 feature 内部路径引用从25条降到8条，auth 相关为0；共享 UI 的9个循环分量尚未处理。未进行浏览器视觉或真实 SSO 验收。

剩余8处涉及 home 的共享交易状态和路由私有页面入口，其中状态所有权应按账户流程拆分，不能简单转导出可写 ref。P0 的其它工作及 P1–P7 仍保持原范围。

## 第四批：账户目录与交易上下文所有权

核实 home-runtime 只是通用 lib/trading-runtime 的转导出后，将账户目录、交易上下文、观摩频道目录移到独立 `trade/features/trading-context` 模块。内部 ref 不导出；模块公开只读投影及应用服务端响应的操作，写入时复制 DTO，阻止调用方通过原对象引用改写内部事实。home、risk、trader、应用壳层和时间读取已接入公开入口。

保留消费者已有请求代次、用户和账户作用域检查；没有把本地 apply 操作当作服务端授权。该阶段只收口投影所有权，未替代账户切换 HTTP 用例和全部缓存清理协调。行情、账户快照、持仓及实时 revision 仍待迁入对应模块，避免把整个通用状态对象改名后算完成。

同时为 home/shell/overview/settings 提供页面按需加载入口，路由不再直接导入私有 Vue 文件。现有页面与占位功能覆盖不变，公开 loader 不代表占位功能已实现。

验证：trade 原有114项测试通过；新增2项所有权测试及观摩/迟到响应/时间共19项定向测试通过；两端4项路由测试通过；trade/admin 类型检查与构建通过。最终[前端 v4 扫描](migration/frontend-module-boundaries-20260908-v4.json)剩2处 feature 内部路径引用、9个 UI 源文件循环分量，没有新增模块循环。未运行真实服务或数据库迁移。

下一步继续快照/持仓/行情的写入能力分离，以及 P0 精确债务、数据表所有权和 API 合同检查；不会以当前扫描数量降低替代整个方案验收。

复核还发现 risk 的 selectAccount 响应应用发生在 generation 检查前，这是原有顺序，应在接续账户请求协调时补迟到响应回归并修复；本批不宣称所有调用方的异步保护已正确。

## 第五批：风控账户响应隔离

新增行为回归先复现3种共享上下文覆盖：并发选择的旧响应、卸载后的响应、被新load替代的默认账户选择。修复为先检查 generation，再应用服务端上下文；过期初始化不继续读取旧账户风控数据。

继续复现并修复旧账户规则保存/手动解除完成后污染新账户页面的问题：写操作固定发起时的 accountId、userId 和 generation，响应、错误及后续刷新只作用于仍有效的范围；新增函数内重复写入保护。被忽略的旧响应不表示撤销了服务端已经受理的写入，也不会自动重新发送。

6项范围回归覆盖上述5种失败以及当前账户正常保存/重复写入拒绝；连同风控展示和上下文所有权共15项定向测试通过，trade 类型检查通过。未调用真实风控接口或执行交易。此前第四批指出的 selectAccount 顺序缺陷已修复；整个账户流程的统一协调、剩余共享状态、后台模块边界与 P0 其它条目仍需继续。

## 第六批：服务端审计业务入口与组装隔离

audit 的 index 只公开 AuditReadApi、查询/结果合同及错误类型；数据库实现、具体服务和路由移出公开导出。composition 工厂创建查询服务并提供绑定依赖的 HTTP 插件，由 API entrypoint 组装，route registrar 只挂载插件。同步细化后端目标文档的业务/组装入口定义。

审计标识符校验不再跨域导入 trading 内部函数，保留原格式、长度、修剪及审计错误语义。新增非法标识符不会进入 repository 的回归。旧全路由测试以空服务注册已无法满足认证初始化，修正为明确的注册夹具，并加入真正审计插件的路径和账户权限拒绝检查，未用空插件掩盖覆盖缺口。

完整 server 类型检查通过；审计、HTTP注册/实时及运行组装共21项定向测试通过。[后端 v3 扫描](migration/server-module-boundaries-20260908-v3.json)审计相关违规为0，总计59条跨域内部引用、75条公开实现导出、11条domain依赖候选及原有循环分量。audit 跨域 SQL 是待审查的只读投影，表所有权、真实MySQL及全API合同仍未验收。P0及整个方案保持进行中。

## 第七批：API 实际注册对账

新增 inspect:api-contracts，构建后捕获真实 Fastify 注册路径并与 OpenAPI 双向对账；不启动监听或真实依赖。93项合同操作与87项业务注册中匹配85项，8项缺失、2项认证中心路由未声明、29项参数名差异，完整处置见[API 差异报告](api-registration-gap-review-20260908.md)。5项比较器测试和V4构建通过。检查如实退出1，不隐藏缺口，也不把注册匹配当作请求/响应及业务验收。合同编译、消费者覆盖和其它P0事项仍待收口。

## 第八批：路由参数对齐与认证中心合同

29项操作的路径参数统一到合同命名，同步修改 Params 类型及 handler 取值，实际 URL 形状和应用用例输入保持。涉及 audit、execution、inference、reviews、risk、strategies、trade-history、trading；对应注册检查同步更新。参数改动后服务端96个测试文件720项测试通过。

补齐 GET /auth/session 与 POST /auth/logout 的身份中心合同：单独声明 auth cookie、host、CSRF/Origin、会话响应、当前会话退出及重复退出401；不复用含 app/permissions 的应用会话响应。现有认证错误媒体类型是 application/json，合同按实际行为声明；统一 problem 媒体类型仍需接续处理。

新增两种 Cookie 配置的真实 Fastify inject 行为测试，以 JSON Schema 2020-12 校验实际响应；覆盖额外应用字段/非法时间拒绝、错误 host、缺 Cookie、错误 Cookie 名、缺 CSRF、错误 Origin、退出清 Cookie、会话失效及其它会话不被撤销。认证与比较器合计17项测试通过，完整服务端类型检查和V4构建通过。前端目前没有这两个路径的消费者，不能据此宣称已完成生成客户端或浏览器认证验收。

[最新对账](migration/api-route-coverage-20260908-v2.json)：95项合同、87项业务注册全部匹配；未声明路由0、参数命名差异0，仍有8项合同路由缺实现，检查继续退出1。合同同源编译、域拆分、模块/表所有权、精确例外和正式自动门禁仍未收口，P0及P1–P7继续。未启动服务、连接真实依赖或执行数据库操作。

## 第九批：Schema 编译与语义验证

新增 inspect:api-schemas，使用明确的JSON Schema 2020-12编译器检查当前合同827个Schema使用位置，全部通过；包含结构引用后的请求/响应和响应头，不把示例数据里的schema字段误当约束。7项正反例通过，验证引用错误、无效关键字/约束、联合/空值、额外字段、日期与不修改输入。Ajv及格式库显式固定开发依赖；锁文件仅新增根依赖声明，冻结离线检查通过。

范围和接入决定见[编译接续记录](api-contract-compilation-20260908.md)。当前未完成域源拆分、客户端类型生成或Fastify运行时挂载，不把可编译性当作API合同验收。P0与全量模块化目标继续。

## 第十批：域合同源与确定性聚合

HTTP合同拆为13个业务域与common公共源，按当前服务端职责明确拥有路径与组件；域文件由manifest显式登记。公共源只有13个通用模型及共享参数/错误结构，没有业务路由。职责和维护入口见[合同源说明](../contracts/http/README.md)。这不是数据库表写入所有权验收。

openapi-v4.json改为生成产物；新增generate:api-contract和verify:api-generated，并将生成漂移检查接入两项API inspect命令。生成器拒绝未登记文件、重复域、重复路径/组件、缺失/外部引用，排序对象键但保留数组顺序。已与上一提交的聚合合同逐字段深比较相等，全部业务定义保持，8项未实现合同路由仍然存在。

验证：生成器/Schema/注册比较器16项测试通过；真实CLI在聚合文件多一个换行时拒绝，恢复后通过；827个Schema使用位置编译通过；服务端与相关合同738项测试通过；前端合同包51项测试与类型检查通过。未执行数据库迁移或真实服务调用。

域源拆分和聚合漂移检查已落地；前端现有类型仍需从域合同生成并替换，服务端校验尚未同源挂载，完整OpenAPI结构检查、模块边界/所有权与P1–P7保持未完成。

## 第十一批：生成传输类型并接入认证消费者

固定openapi-typescript 7.13.0，新增generate:api-types及verify:api-types，从同一聚合产物生成完整路径、操作和模型类型；类型检查先验证聚合与类型产物均未漂移。依赖锁文件只增加该工具所需17项依赖，不夹带既有Nuxt/Vue依赖快照变更，冻结离线检查通过。

现有AuthLoginRequest/AuthLoginResponse/AuthorizationRequest/SessionResponse/SessionSummary改用生成类型；API客户端登录与会话方法显式采用生成输入/输出，现有应用消费者沿用公开名称。保留运行时Zod校验和其它业务转换，避免以类型替代实际校验或把snake_case传输字段直接替换camelCase页面模型。

验证：新增编译期双向类型兼容及非法身份surface/数值用户ID/身份中心permissions反例；完整前端9个工作区类型检查通过，合同包53项、API客户端22项测试通过。实际CLI拒绝被改动的生成类型，恢复后通过。生成产物无运行时代码，未启动真实服务或访问数据库。

生成类型已进入认证实际调用链；其余域消费者、统一运行时校验、边界门禁、表所有权和P1–P7仍未完成。

## 第十二批：审计接口服务端同源校验

新增显式运行操作清单及Schema闭包生成器，两个审计GET从域合同生成参数/成功响应校验；产物随server构建，Ajv和格式库转为生产依赖。服务端类型检查和构建加入运行产物漂移门。适配范围限定GET path/query和200 JSON，未支持写操作或其它媒体类型时明确拒绝生成。

审计路由认证后校验输入，用例完成后校验DTO；规范整数query仅在校验副本转换，不改变请求。对齐分页HTTP上限为实际100，拒绝非法分页而非静默截断；修复认证错误误报503，错误媒体类型对齐problem+json并补齐合同状态码。

20项审计/注册测试和2项运行校验测试通过，覆盖查询前拒绝、认证顺序、重复query、错误日期、缺路径参数、规范整数、不修改输入及非法响应不泄露。服务端完整类型检查/构建、前端合同类型检查/生成检查通过；全部834个Schema使用位置编译通过。真实CLI拒绝运行产物漂移和不存在的操作，恢复后通过。

当前是路由内显式同源校验，未挂Fastify默认schema；错误响应仍由固定problem适配器构建，未统一按状态码校验。其它业务接口、写操作、客户端运行时迁移、P0其余项与P1–P7继续。未连接数据库或真实服务。

## 第十三批：审计错误响应按合同校验

生成器改为提取所有明确状态码及JSON/problem媒体类型，运行校验按两者选择Schema，并验证problem正文status与HTTP状态一致；未声明状态或媒体类型拒绝通过。审计两个接口的失败分支同步使用校验器，非法错误正文替换为经过校验的固定503并生成新关联ID，不携带原始值、不递归重试。

24项审计/运行校验/注册测试通过，覆盖错误Schema、媒体类型不匹配、未声明状态、正文状态不一致、额外内部字段和失败兜底不泄露；服务端构建通过。生成范围仍限定GET的path/query和JSON/problem明确状态响应，写接口、空响应、其它域和P0其它工作继续。本批没有数据库或真实服务操作。

## 第十四批：学习完成写接口同源校验

生成器和运行适配器支持JSON写请求体及header，写入体须禁止未知字段；header按Fastify小写键处理。各模块只编译自己登记的操作，避免审计连带编译其它模块。学习完成新增运行登记，先完成会话与CSRF校验，再校验路径、幂等键及严格body，用例的revision范围、权限重查、幂等与原子事务保持。

成功结果按合同校验；若保存后响应无效，返回learning_commit_unknown/503并隐藏原始值，保留原key/body恢复路径。错误和错误host补齐problem字段及媒体类型，正常机器错误码保持，未知错误兜底不返回原始内容。

48项服务端定向测试通过，包括既有11项事务/幂等/提交未知测试、两种Cookie的实际HTTP链路、非法键/类型/额外字段不会调用保存、保存后响应失败返回提交未知；主站学习完成7项测试通过。服务端完整类型检查和构建通过，生成门通过。没有连接真实MySQL、执行迁移或启动服务。

目前3个操作接入同源运行校验，非全部API。其它业务、空响应、统一错误适配器、客户端运行时迁移、P0其余条目及P1–P7继续。

## 第十五批：服务端精确边界债务门

重新扫描235个源文件，147条存量记录按source/target/rule、导入种类、类型属性和次数登记到[精确债务](architecture/server-boundary-debt.json)，并标记模块、消除阶段与处理理由，见[复核说明](architecture/server-boundary-debt-review-20260908.md)。补充源码循环内部边证据及所有循环边的导入种类，避免只比循环成员漏掉扩张。

新增verify:server-boundary-delta并接入服务端类型检查/构建。新增、类型转运行、同边次数增加或陈旧记录均失败，禁止通配符、重复配置、未知规则豁免和自动重建基线。15项检测器测试通过；真实临时内部导入被CLI拒绝，删除探针后完整类型检查通过。

存量147条没有被“修复”或称为合法；严格inspect继续退出1。当前只防止新增已检测规则的服务端越界，前端精确债务、检测覆盖完整性、表所有权及P1–P7继续。未启动服务、调用数据库或更改业务行为。

## 第十六批：前端精确边界门与Nuxt声明刷新

重新扫描331个前端文件，2条feature内部依赖和9个共享UI循环逐条登记，附消除阶段及理由，见[前端复核](architecture/frontend-boundary-debt-review-20260908.md)。补齐非循环前端依赖的导入种类和类型属性；共享比较器区分前后端规则，禁止跨应用/共享包反向依赖豁免。

新增verify:frontend-boundary-delta，每次先用项目安装的Nuxt CLI prepare，再扫描；准备失败、配置错误及新/陈旧债务均失败。新门接入既有前端边界命令，保留原检查，并进入测试、构建和类型检查流程。

20项检测器测试通过，实际临时内部引用被拒绝，探针已删除；全部前端9个工作区类型检查通过，服务端增量门保持通过。11条前端存量仍需真正消除，Nuxt server/动态组件等覆盖与表所有权仍待完成；P0及P1–P7继续。没有修改UI行为或调用真实依赖。

## 第十七批：消除共享UI循环

按shadcn-vue现有组件结构，将alert/avatar/badge/button/empty/field/sidebar/tabs的variants与类型迁到各自独立文件；公共index保留组件、variants及类型名称的导出，组件内部直接读取定义文件。SelectContent改为直接导入同目录滚动按钮，不再经自身index循环回读。

源声明逐项比较确认8组定义完全一致，10个变更Vue文件的template及其后内容保持；四应用构建、全部前端类型检查与增量门通过。没有重设计UI或进行浏览器视觉验收。

扫描339个前端源文件，9个UI源码循环实际清零，只删除这9条已解决的精确例外；剩2条App/trader读取home共享交易状态的内部引用，按P1状态所有权继续处理。P0其它项及整体方案尚未完成。

## 第十八批：SQL 写入证据与账户事务所有权核查

新增只读 inspect:sql-writes 工具，使用 TypeScript AST 提取 SQL 字面量及间接 execute/query 调用。扫描235个源文件，264个SQL候选、92张静态目标表、8张多模块写入表；5处未解析语句和114处间接调用单独保留，结果不视为数据库全表或运行所有权证明。

[所有权核查](architecture/data-write-ownership-review-20260908.md)记录账户根虽只有bridge写入仍应归trading，以及认证撤销Bridge凭据、风险决策、预留、订阅调度和outbox的事务协作要求。五处未解析SQL已人工分类，原始证据不被静默覆盖；间接调用与数据库全表对齐继续。账户首认领的锁序、BIGINT身份及原子性明确列为P1迁移条件。

6项检测器测试通过，未改业务行为、数据库或配置。工具为源码清单而非自动所有权门禁；P0及全栈方案仍在实施。

## 第十九批：Bridge 凭据撤销写入所有权收口

将设备撤销SQL实现从auth迁到bridge，保留用户锁、两张凭据/配对表同一事务和错误传播。auth创建工厂移至composition并显式接收既有BridgeDeviceRevoker端口；API运行入口从bridge/composition创建实现后注入。浏览器实时入口同步使用auth/composition；公开业务index不再导出组装工厂、配置及撤销实现，没有新增跨域内部依赖。

33项Bridge配对/认证SSO定向测试通过，新增凭据更新失败回滚且释放连接、设备撤销失败不提前退出网页会话的回归。完整服务端类型检查/构建通过。扫描236文件，边界债务由147减为144，仅删除3条已消除的例外；更新SQL清单后多模块写入表由8减为6。

未改变注销权限、HTTP合同、数据库结构或运行配置，未执行实际撤销。该改动完成一个明确的写入职责归位，不代表认证或Bridge全域模块化完成；账户根事务、其它跨域写入、间接SQL及P0剩余项继续。

## 第二十批：账户首次登记事务端口

[账户登记设计](architecture/account-registration-transaction-design-20260908.md)完成职责及事务异常两轮复审。将账户实体、归属区间及当前归属SQL写入从Bridge迁到trading，通过运行入口注入绑定当前事务连接的登记工厂；应用端口无MySQL类型，无独立提交或默认旧实现。保持hello和凭据校验、锁序、后续绑定/会话失败整体回滚、BIGINT字符串及原Bridge错误码。

原有登记测试全部经新的组装工厂运行；26项登记链路和3项适配器测试通过，覆盖结构化端口失败、后续SQL失败、并发冲突、不接管其它用户/撤销/删除身份、异常行数及BIGINT。完整服务端类型检查、构建和边界增量门通过；239文件存量143条，仅删除1条消除的具体仓储公开导出例外。

更新SQL清单，账户三张表的静态写入者均为trading。此批完成首次登记写入归位，账户读取和跨域授权查询、全表所有权、间接SQL及P0/P1其余验收继续；未连接MySQL、注册真实账户或执行迁移。

## 第二十一批：账户锁定查询与会话证明投影

账户精确身份查找和当前归属锁定从Bridge仓储迁入trading事务端口；登记、激活及心跳使用同一连接上的公开能力，保留BINARY身份、软删除、区间与revision条件和账户先于凭据/会话的锁序。两条SQL规范化空白后与迁移前完全一致。

补充[Bridge会话证明只读投影合同](architecture/bridge-session-proof-read-model.md)，明确来源表所有者、最小输出、精确冻结route、资格条件、加锁与非加锁读取差异及实际执行计划待验收。没有借投影声明开放任意跨域SQL或把独立读取表述为可串行化授权快照。

31项登记/账户适配器测试通过，完整服务端类型检查与构建通过，存量143条无新增。SQL清单刷新行号，写入统计未变。未连接真实依赖；全表所有权、间接SQL、账户其余链路和整体P0/P1验收继续。

## 第二十二批：认证公开入口与HTTP组装封装

auth/index移除MySQL、Redis、密码校验、签名器、客户端注册、清理器及HTTP实现导出；保留其内部实现。composition新增createAuthHttp，API运行入口注入完整SSO插件，实际路由清单和离线注册fixture采用同一工厂。交易/观摩适配器及Bridge配对、推荐、观摩路由的5处auth内部引用改走既有公开能力。[模块README](../server/src/modules/auth/README.md)记录职责、数据、端口与未验证范围。

认证/浏览器运行/Bridge配对41项和观摩/推荐HTTP22项测试通过，完整服务端类型检查、构建通过。实际注册仍为87个业务路由；与先前清单按method/path排序后，8项缺失、未声明、参数差异及6个协议路由逐项一致。未以协议封装隐藏已有API缺口。

239源文件边界检查128条存量，无新增，仅移除15条本批实际解决的例外。没有修改认证权限、会话规则或数据库，也没有新增清理定时器。其它模块、用户资料及API同源校验、全表所有权与真实依赖验收继续。

## 第二十三批：学习模块HTTP与事务组装

learning新增composition入口，查询服务、完成写入服务和HTTP插件在运行入口创建；会员读取及同一连接事务工厂显式注入。公开index移除SQL/路由实现导出，保留用例、端口、DTO和错误；全局路由登记器只接收learningHttp，主站域名和Cookie策略归模块插件配置，不再保留无效的重复全局配置。

SSO的两种Cookie课程解锁与完成写入用例改经实际HTTP工厂；学习查询/事务、认证及浏览器运行41项测试通过，完整类型检查与构建通过。实际87个业务路由保持，8项缺失、未声明/参数差异及协议路由按method/path逐项一致。原MySQL事务和提交未知处理未改；只读开发库探针更新工厂导入后仅做语法检查，未执行探针。

[Learning README](../server/src/modules/learning/README.md)明确进度两表写入归属、内容读取及会员依赖。240文件边界存量124条，只删除4条已消除例外；SQL清单只刷新文件数。真实数据、课程管理全功能、剩余模块与P0/P1–P7验收继续。

## 第二十四批：设置模块统一公开入口

移除settings/management第二入口，index仅公开领域读取类型/端口和管理用例；composition绑定内部读取、管理读写和HTTP，生产工厂固定保留validateSettingMenu。API入口注入既有管理员认证适配器，管理路由仍位于精确admin host检查下。读取类型从MySQL文件迁入domain，写入适配器同步引用，无SQL或事务行为变更。

39项读写、值/菜单策略、管理HTTP及运行注册测试通过；完整服务端类型检查和构建通过。实际87个业务接口及既有8项缺失、参数/未声明差异和协议路由逐项保持。Secret遮蔽、NULL/空字符串区分、管理员资格、revision、幂等和提交未知继续由既有行为测试验证。

[Settings README](../server/src/modules/settings/README.md)登记三表写入、事务锁序、内部读取与HTTP权限差异。241文件存量123条，无新增，只删除1条已检测的index导出例外；原management导出未计入旧扫描，明确保留多入口覆盖缺口。未修改真实配置或连接数据库；整体方案继续。

## 第二十五批：第二入口与模块外消费者门禁

扩展server边界规则：全局transport/outbox/bootstrap等模块外消费者不能访问第二入口或内部文件，只能消费index；composition仅bootstrap/entrypoints可访问，同模块反向依赖也拒绝。这两类规则不允许加入存量例外。允许模块内部私有辅助文件，未按文件名强制删除合法内部实现。

实查发现此前漏检的outbox→trading三处内部引用，已改为公开事件协议。通道常量迁入无Redis依赖的应用协议文件，发布者/订阅者复用原值；观摩失效结构与校验函数从公开入口提供，没有重新导出具体订阅实现。

20项边界测试、16项观摩失效与实时运行测试、完整服务端类型检查和构建通过。真实CLI拒绝全局类型导入经第二入口取得数据库类，临时两文件已删除。242文件原123条债务保持，没有更新例外基线；SQL清单仅刷新文件数/行号。未连接真实依赖；符号级导出分析、全表所有权及其余P0与业务阶段继续。

## 第二十六批：导入后的本地别名导出检查

新增顶层导入绑定与直接别名解析，识别改名导入、常量链、命名空间成员、类型别名、断言包装、默认实例及import-equals导出。公开index可沿中间文件的本地导出继续定位infrastructure/transport/composition泄漏。合成导出记录独立于物理依赖边，避免重复计算循环证据；严格扫描输出单列localReexports数量。

27项边界相关测试通过（原26项通过后，新增包装场景并复跑7项本地导出测试）。真实CLI拒绝auth/index导入MySQL类、经常量改名后公开导出的探针，原文件已恢复。前后端增量检查通过，123条服务端与2条前端存量未变化，没有调整基线。此次修改仅涉及检查工具、测试和文档，未连接数据库或启动服务。

该解析不覆盖完整数据流：函数返回、任意调用、对象展开/解构、可变别名及Vue本地导出仍需后续覆盖；不得将本批称为完整符号分析。全表写入所有权矩阵、API全量校验和账户全栈样板仍按主方案继续。

## 第二十七批：间接SQL调用的语义核查

逐项核查原清单114处间接execute/query调用，定位固定查询常量、函数返回、封闭映射和业务端口的来源。107处为SELECT（部分带锁），6处业务写端口分别追到当前MySQL适配器，1处为Bridge历史查询端口；没有将业务写入端口判作只读，也未从原始候选清单删除记录。

新增[语义记录](architecture/sql-indirect-call-review-20260908.json)，记录23个文件的源码摘要、每处调用行/方法和分类依据。实际重新扫描核对114处集合与文件摘要一致；本批没有修改运行代码，因此不运行复述文档的业务测试。未连接MySQL、Redis或终端。

完成的是当前清单的间接调用核查；全表所有者矩阵、六张多域写表的事务端口迁移、只读投影合同及P0其它事项仍继续。源码、辅助函数或组装关系变化需重新核查，记录不作为所有权自动豁免。

## 第二十八批：全表所有权矩阵底稿

只读查询env指定开发库的表名并核对数据库身份，确认222表；与v10的69张延期表、旧165表和源码写入清单对齐，形成291表矩阵。114张目标字段表全部有建议所有者；140张提出明确所有者建议，151张旧源承接仍待定。7张构建表明确归迁移用途，不冒充正式业务表。

92个静态写入目标与2个动态快照表均进入矩阵；其中66张当前不存在，明确保留真实运行缺口。矩阵覆盖、唯一性及当前源码写入清单一致性通过离线核对。两轮复审记录在[矩阵说明](architecture/table-write-ownership-matrix-20260908.md)，本批不执行DDL/DML，不修改服务端运行行为。

完整所有权验收仍需要151张旧表逐域定责、140项建议的端口/事务落实、字段兼容与真实业务验证；下一批接续账户根合同及其数据库配合项。

## 第二十九批：账户根当前预检与接续合同

新增review-account-root-cutover-local.mjs，仅支持read-only和新绝对路径回执；校验数据库身份、同连接锁、只读事务和日志，复用已有账户映射/UTC转换/归属一致性规则。当前4旧账户映射3实体、保留4设置，274历史和3当前绑定一致；4张构建表仍空，旧订阅存在1条指向旧账户根的外键。

完整结构协调器因历史学习回执冻结package.json而拒绝当前工具摘要，回执明确schemaVerification=false、readyForCutover=false；没有修改原验证条件或执行DDL/DML。只读预检独立完成账户事实采集，不把它冒充迁移验证通过。失败过程的临时回执已清理。

[账户根接续设计](architecture/account-root-cutover-design-20260908.md)完成两轮复审，明确旧根保留、3实体/4设置、旧订阅引用、同事务归属及新增写入后的恢复边界。下一步修复历史工具证据与当前执行版本的耦合，再推进新波次和恢复副本演练；整体方案继续。

新入口语法检查及15项账户ID映射、字段转换、归属一致性测试通过。真实只读报告已生成；不以这些结果抵消完整结构校验失败。

## 第三十批：历史package证据与当前工具兼容

从历史Git对象恢复学习Schema验证时package.json原字节，按原回执SHA-256校验独立归档。当前package允许脚本调整及依赖新增，但原依赖版本/条目、模块类型、Node约束、包管理器及其它顶层配置必须保持。其它工具、原SQL、回执、步骤checksum未修改；旧演练执行器与完整工具清单保持冻结。

当前开发库147步结构/日志检查已只读通过，账户转换摘要及归属事实与上一批一致，见[v2回执](architecture/account-root-cutover-review-20260908-v2.json)。移除只读入口对旧已知摘要错误的临时继续采集分支，后续结构失败直接拒绝。

两轮复审见[证据兼容设计](architecture/migration-package-evidence-design-20260908.md)。本批只修复当前协调器使用历史package证据的方式，不代表其它历史演练的全部源文件已适配，也不证明当前安装依赖/运行环境相同。新的回填波次仍须冻结当前工具并在恢复副本验证；未执行数据库写入。

51项package证据、学习Schema、协调器异常恢复及订阅外键测试通过；新旧账户回执的源/映射/转换摘要、归属、外键及构建表计数逐项一致。原迁移SQL与回执没有改动。

## 第三十一批：当前账户回填适配器版本

新增mysql-account-backfill-v2.mjs，固定使用147步当前协调器；每个事务核验结构、日志和源表定义，以新版本加入目标schemaHash。保留inplace-account-v2构建表路由与源行证据，并复用原事务/提交未知协议。旧coordinated-account/v1和历史波次保持不变。

16项新旧适配器及账户写入测试通过，覆盖完整步骤/缺日志/待恢复拒绝、checksum改变和版本区分、失败回滚及提交未知不重放。通过[v3回执](architecture/account-root-cutover-review-20260908-v3.json)在真实dev_vue只读验证新身份，当前4源账户→3实体/4设置及归属事实保持。未运行真实回填；下一步冻结新波次及恢复副本演练。

另20项通用回填批次/恢复与归属写入测试通过，合计36项；新入口语法和差异检查通过。

## 第三十二批：本机程序真实恢复副本回填演练

新增rehearse-account-wave-local.mjs，固定目标为dev_vue_m1_source_20260907_02，凭据仅从stdin接收，本机执行，通过临时SSH隧道访问虚拟机MySQL。身份、147步结构、空账户构建表及与当前dev_vue账户摘要一致均在写前校验。归属转换使用UTC零偏移，源行未输出到公开回执。

首次预检发现副本原账本不空，在任何本次写入前停止；未清库，改为检查本轮账户映射不存在，并冻结旧账本及其它212表。成功演练7个批次，得到3个账户、4条设置、274条区间、4条归属；映射、回执及源证据各新增278条，原有46条记录保留。212表全部行摘要前后相等，原账本逐行相等。

真实写后注入异常并验证回滚；实际commit成功后模拟响应丢失，确认返回backfill_commit_unknown，再由恢复查询确认committed；重复每批均不新增业务数据。当前工具摘要在执行前后核对相等，回执冻结工具、目标身份、源/转换摘要及run ID。临时失败回执已清理，SSH隧道已关闭。

语法检查与真实MySQL演练通过，见[演练证据](architecture/account-wave-local-rehearsal-20260908-v3.json)。本批只写恢复副本4张构建表与迁移账本，没有写当前dev_vue或进行表提升；下一步核对完整目标行值与权限投影，再实现正式根切换及故障恢复。

## 第三十三批：回填目标全字段与所有者投影

新增verify-account-wave-projection-local.mjs，只读访问固定恢复副本；验证演练工具摘要、目标身份和两条转换流transformHash，重新由完整旧源生成期望值，再与四张构建表全部目标业务列比较。3账户×10列、4设置×14列、274区间×11列、4归属×7列均一致，整数/UTC毫秒按精确值比较，不只比较数量。

从当前MysqlAccountRegistration.lockCurrentOwnership提取SQL条件，构建表替换及移除FOR UPDATE后执行75组用户/账户组合，3组允许、72组拒绝，返回区间和归属revision匹配。该验证不覆盖行锁并发、Bridge凭据、额度和其它API权限，未把只读投影等同完整授权链路。

[真实只读回执](architecture/account-wave-projection-verification-20260908.json)和脚本语法检查通过。无数据库写入，临时SSH隧道已关闭。下一步继续恢复副本的正式根提升/旧引用保留及异常恢复，当前dev_vue尚未切换。

## 第三十四批：恢复副本原子根提升与独立恢复

新增固定5表重命名协议、全库DDL/行快照及本机演练入口。先持久化带工具摘要的前后状态计划，再在dev_vue_m1_source_20260907_02执行原子RENAME；模拟成功后响应丢失，按222表实际状态确认完成，不重复DDL。

另一本机进程加载落盘计划并恢复全部表名；全库DDL（含自增值）和数据摘要必须精确一致，不能在新增写入后盲目回退。真实提升1DDL、恢复1DDL、再次恢复0DDL，旧strategy_subscriptions外键提升时指向legacy根，恢复后指回原根。副本最终恢复原状态，回填构建数据保留；临时SSH隧道已关闭。

7项状态/冲突/未知结果/重复恢复测试及真实MySQL演练通过。最终证据为[提升](architecture/account-root-promotion-rehearsal-20260908-v2.json)、[恢复](architecture/account-root-restoration-rehearsal-20260908-v2.json)、[重复恢复](architecture/account-root-restoration-repeat-20260908.json)与对应plan文件。早期中间演练回执已清理。

这仍是恢复副本实验，未改当前dev_vue、未追加迁移记录，也未启用运行服务。下一步把已验证的多表转换纳入正式追加迁移与提升后结构检查，保持旧147步checksum及旧引用可追溯。

## 第三十五批：提升后历史结构只读适配

新增account-root-historical-schema.mjs，将固定5表的元数据读取映射到提升后的物理表；只还原SHOW CREATE表头及外键目标，其余列、约束、表达式和字符串保持。连接适配仅允许固定元数据读取，所有DDL/DML、任意查询和日志写入均拒绝；要求旧日志完整且checksum正确，不过滤未知记录。

在恢复副本再次实际提升后，通过[完整147步检查](architecture/account-root-historical-schema-verification-20260908.json)，不是仅模拟表名或检查存在性。旧订阅外键实际指向legacy根；随后独立进程[恢复](architecture/account-root-historical-validation-restoration-20260908.json)并核对222表完整DDL/数据摘要。此次[提升回执](architecture/account-root-historical-validation-promotion-20260908.json)引用同一冻结planHash，重复计划文件核对完全一致后删除。当前dev_vue未改，SSH隧道已关闭。

8项新适配测试、36项原协调器测试、4项订阅外键测试通过；另7项提升/恢复测试通过。两轮复审见账户根设计，验证覆盖未知SQL拒绝、混合布局、陌生或错误日志和字段漂移，未修改旧147步SQL/checksum。

这完成正式切换的历史结构校验前置能力；正式追加步骤、完整新日志校验、停写与软引用审查，以及当前库应用和全栈账户流程仍未完成。

## 第三十六批：正式账户根步骤与148步日志状态机

追加035_account_root_promotion.sql，独立加载器只接受固定5表的单条原子RENAME；148步注册表保留原147步对象及checksum，其摘要仍为6bd9b37a155323eeda3bc8e36cfb0e7f8beaf7743a6e92cbe3f9dc7e3040ef59。通用SQL白名单与旧协调器未改动。

新状态机先校验完整日志再分出精确历史集合；证据绑定目标身份、旧注册表、新步骤及工具。迁移日志行改由完整日志校验，日志表结构继续保护；其它表在迁移未完成期间必须匹配完整前/后快照。started、DDL、completed响应未知立即停止，由下一次读取实际状态确认，不在同次调用重试。

完成后的重复检查允许普通业务行和自增增长，仍核对全部结构及legacy账户根数据；无日志提升、已完成但旧布局、混合状态、历史checksum/证据错误和未完成期间新增数据均拒绝。表名预测只改表头/外键目标，不修改列名、约束名或字符串。

17项新状态机测试通过，包含新建store从持久化事实恢复、三处响应未知与前后置漂移；另44项历史适配及原协调器测试通过。语法与差异检查通过。两轮复审见账户根设计。

本批没有连接数据库或执行035。当前交付是正式步骤加载器与状态机，持久化计划入口/MySQL适配、真实148步恢复副本演练和软引用审查仍待接入，不能称当前dev_vue已经148步或账户样板完成。

## 第三十七批：正式148步真实恢复副本演练

新增持久化计划/MySQL适配器与固定恢复副本入口。计划独占创建并fsync；入口准备时复核旧147步、212表保护摘要和完整账户字段投影。适配器要求正确数据库/UUID、UTC/autocommit、有效日志结构及同连接升级锁，每次写操作再次核对锁；只有固定步骤及SQL可执行。完整工具集合不能删项、重复或通过路径穿越读取其它文件。

实际由五个独立本机进程执行[准备](architecture/account-root-registered-prepare-20260908.json)、[DDL响应丢失注入](architecture/account-root-registered-lost-ddl-20260908.json)、[只读识别](architecture/account-root-registered-inspect-unknown-20260908.json)、[恢复日志](architecture/account-root-registered-reconcile-20260908.json)与[重复执行](architecture/account-root-registered-repeat-20260908.json)。整个流程只执行1次RENAME；新进程识别reconcile后只补完成日志，再次返回completed/0DDL。

恢复副本dev_vue_m1_source_20260907_02现在保留148步与提升后的正式表名，没有删除日志或反向伪造原状态。[最终只读核对](architecture/account-root-registered-result-verification-20260908.json)证明原147条日志的全部列摘要不变，222表的非日志数据/DDL与提升计划一致。当前dev_vue另行只读核验仍为147步、旧账户4行、四张账户构建表均0行。

本批4项新持久化/连接锁/工具清单测试和17项状态机测试通过，真实MySQL跨进程恢复通过，SSH隧道已关闭。计划与回执永久保留用于恢复和审查，测试临时文件已清理。新增读取验证器只读检查副本与当前库，不输出业务原始字段或凭据。

当前库应用仍需旧根软引用/JSON引用及本地运行写入口调查，并准备当前库自己的回填与提升计划；不能把副本148步视为当前dev_vue升级完成。账户API与前端完整流程、其余P0–P7继续。

## 第三十八批：旧账户引用的当前库只读核查

新增字段/JSON与SQL字面量检查器，当前147步与账户映射摘要保持。38个列候选中28列有数据，10列共236处合并ID引用，逐表处置见[引用处理说明](architecture/account-root-reference-disposition-20260908.md)。旧订阅保留旧外键；执行/风控/历史按所属域迁移，禁止统一原位替换。

179个JSON/文本候选列、233,395个非空值完成有界读取，包含按旧格式解码的789个压缩快照；最终无大小或遍历限额遗漏。明确键匹配271处，其中262处匹配旧ID、0处匹配合并ID；17,411个非JSON文本及任意语义/其它编码仍有范围限制，不作全库隐式引用清零结论。

V4源19文件40个直接根SQL字面量为39SELECT/1INSERT。旧账户注册、风险状态及用户删除代码仍含旧根写入；静态候选不等于真实运行写入，亦不把JOIN根的其它表UPDATE归为根写。5项解析测试、当前库只读检查及来源/工具摘要核对通过。早期限额中间回执已清理，只保留最终[v4证据](architecture/account-reference-review-20260908-v4.json)。本批未执行DDL/DML或停启服务。

下一步落实本机停写条件、账户用例依赖表和模块就绪条件，再准备当前库自己的回填/提升证据；整体模块化尚未完成。

## 第三十九批：交易模块组装与 HTTP 入口封装

六类进程通过受限 composition 获取交易查询、账户/观摩服务、Bridge 投影及浏览器订阅能力，不再各自构造交易域的 MySQL/Redis 实现。认证端口移入 application，保留 trade/admin 会话与 CSRF 作用域；API 总注册器只接收账户和管理观摩的 HTTP 插件，固定路由前缀由模块内部组装，总注册器继续执行精确域名隔离。

移除8个基础设施与2个HTTP公开导出，精确核销10条记录；服务端检查结果113条，added/stale均为空，没有增加例外。单元测试可以直接读取本模块实现，生产调用方经公开端口和受限组装入口协作。补充模块README，明确实际写表、同事务注册、行情与风险预留跨域写入等剩余工作。

完整服务端类型检查和构建通过，9个文件67项定向测试通过。新增组装行为验证区分认证作用域、额度读取与实时订阅关闭；实际API注册测试补充账户/管理观摩的错误域名421拒绝。无需监听或真实依赖的路由对账与既有v2证据逐字段比较一致（数组按集合规范化）：95合同、87注册、87匹配，8项既有缺失未变化，schema注册及协议例外亦未变化。该对账仍因既有缺口返回1，不能称全量API覆盖通过。

职责复核：具体数据库/Redis及HTTP组装留在模块，运行角色负责生命周期，保留共享repository与原事务。异常复核：认证、域名隔离、实时错误回调和关闭顺序保持；不改变数据库或交易语义。本批未连接数据库、启动服务或部署。事务账户时钟SQL、实时hub/session公开导出和跨域写表仍待收口，账户完整模块与全栈验收尚未完成。

## 第四十批：事务账户时钟公开能力

新增AccountClockReader公开端口及createTransactionAccountClock受限工厂，移除业务index的时钟SQL导出。推理repository/window guard与执行、用户命令、Bridge命令repository显式接收工厂；六类运行入口注入具体实现。任务生成、快照冻结、完成复核、风险订单/订阅分发规划与派发前窗口复核均沿用调用方当前连接与事务。

时钟SQL、FOR SHARE及账户/归属/绑定/投影来源条件未改动，工厂不会申请连接或提交。缺失或不可信时钟仍拒绝账户动作，关闭窗口无需时钟读取。新增测试通过实际组装工厂证明时钟读取发生在同一活动事务，成功提交、缺失拒绝及异常回滚后均释放连接。

服务端完整类型检查、构建及9文件45项定向测试通过，包括任务窗口、证据、执行窗口、用户命令、Bridge持久化/fencing、执行持久化及推理流程。精确越界记录113→112；原五域组的两条运行时钟依赖改为类型依赖，并增加六条工厂签名所需的公开类型边。核对这些具体变化后更新原有环证据，runtime从true变false，类型环保留；不是删除所有环或新增通配例外。

职责复核保持数据读取归交易模块，消费方负责短事务及业务窗口决定；异常复核保留原失败码、窗口/偏好冻结摘要及派发前检查。本批未连接数据库或运行终端。旧只读审计中的源码hash是当时快照，涉及本批修改的消费文件在下一次当前库切换前须重新核对，冻结历史回执不重写。后续继续实时边界及账户前端，当前库升级和全栈验收仍未完成。

## 第四十一批：实时会话端口与关闭竞态

新增application层的sink、connection、sessions及publication端口。WebSocket只负责认证、帧限制、顺序和网络生命周期，通过组装传入的sessions创建会话；Redis订阅器只依赖publication端口，不再反向引用实时hub类型。移除trading/index的hub/session导出，生产实现只由受限composition组装，单元测试继续可直接验证模块内部实现。

修复关闭期间异步订阅迟到的泄漏：会话close标记终止并释放现有订阅；尚未完成的查询返回stop时立即释放，忽略后续排队消息。unsubscribe增加代次，拒绝旧代次消息/关闭回调并释放迟到订阅，但允许之后重新订阅。网络close/error统一调用终止操作，保持已有事件顺序、帧限额及认证协议。

完整类型检查、构建、9文件82项定向测试通过，包含2项关闭/取消订阅迟到回归，以及真实本机WebSocket测试、观摩授权、历史账户范围、宏观与审计事件。数据库和Redis使用测试适配器，本批未连接开发库或启动业务服务。服务端越界记录112→110，只移除两条确切公开实现导出，added/stale为空。

职责复核确认网络传输、会话订阅和事件发布分属各自端口；异常复核覆盖会话终止幂等、迟到清理和取消后重订阅。交易模块公开入口现已无基础设施/传输实现导出；跨域写表、类型依赖环、账户前端和真实开发库完整流程仍未完成，不能据此宣告账户样板或整体重构完成。

## 第四十二批：账户前端投影与工作区状态归属

将账户快照、当前账户显示与首页连接状态移入trading-context公开入口，暴露只读投影及显式应用操作。账户ID、观摩频道、模式变化或退出时清空快照/连接状态；同一作用域revision更新保留快照。壳层currentAccount拒绝显示其它账户快照，时间显示继续经当前账户匹配与既有可信度规则处理。

首页行情、持仓、挂单和revision留在home内部；交易员持仓、挂单及revision改为每次useTraderWorkspace创建的局部状态。删除lib/trading-runtime共享可写状态文件并更新全部消费者。App和trader不再穿透home内部，精确删除原两条前端债务，当前检查finding/debt均为0。检查仍不完整覆盖Nuxt server隐式导入和动态组件表达式，不能当作全部前端架构验收。

trade类型检查和生产构建通过；原28文件122项测试通过，新增2项账户投影/作用域行为后定向17项通过（总用例现124项）。验证包括只读字段、调用方修改副本、错误账户快照、账户/频道/模式清理、观摩迟到响应与终端时间。此次未修改页面布局，未进行浏览器连开发库验收。

职责复核避免通过扩大home公开入口保留跨功能共享可写状态；异常复核保留调用方用户/代次检查和时区可信度，明确投影setter本身不是授权或请求协调器。账户流程的集中请求协调、实时作用域及当前库升级后的完整页面流程仍需继续，不将静态越界清零当作全栈完成。

## 第四十三批：账户请求与实时回调代次

新增trading-context公开请求作用域工具，以调用方的用户/会话/工作区生命周期键及独立通道识别当前请求。交易员工作区、决策列表、详情、刷新和实时回调接入；同账户连续刷新不再接受较早请求的迟到结果。停止实时先失效回调，再关闭控制器；Bridge更新同时核对当前账户ID，退出或身份切换同步清空目录/上下文/资源并拒绝旧结果。

首页最新分析与策略读取也使用独立通道，避免旧分析返回覆盖最新状态，同时避免分析刷新使独立策略目录请求失效。工具不执行网络取消、不授予访问权限，不改变服务端revision和幂等合同。首页账户/行情的局部代次与停止后的错误处理仍需继续收口。

trade完整29文件126项测试、类型检查、生产构建和前端边界检查通过（finding/debt仍0）。新增挂载真实composable的两项回归，验证同账户刷新倒序完成，以及退出期间迟到HTTP与已关闭实时控制器回调；API和实时连接使用测试适配器。本批未连接数据库或执行浏览器真实依赖验收。

职责复核将请求有效性机制归账户公共能力，数据提交仍由对应feature负责；异常复核覆盖成功/错误/finally、登录身份变化、旧切换收尾和取消后回调。当前全栈账户样板、数据库升级及其余模块的完成条件仍保留。

## 第四十四批：首页生命周期与过期错误隔离

首页context、account-load、snapshot和market改为统一请求通道，保留账户/品种/周期额外检查。内部loadAccount不再重复推进父流程代次，外部load、上下文切换和stop统一控制生命周期。成功、错误、finally均检查请求有效性；停止后的旧快照/行情异常不再清空其它页面刚发布的账户状态，当前请求失败仍显示并清理无效快照。

登录身份变化同步停止实时、清空账户/观摩目录和上下文、行情、最新分析及本地选择；新身份存在时重新读取。停止时复位本地加载状态，旧分析响应不再写回。新增4项回归覆盖退出期间同步、停止后上下文失败、嵌套快照迟到失败与当前失败、停止后的分析结果。

trade完整29文件130项测试、类型检查、生产构建和前端边界检查通过，finding/debt保持0。职责复核保留feature的数据提交职责与账户公共请求工具；异常复核区分过期错误忽略和当前错误反馈，没有吞掉当前用户操作失败。无数据库连接或部署，浏览器真实开发依赖流程与整体模块化仍未验收完成。

## 第四十五批：当前库账户读取结构探针

新增probe-account-readiness-local.mjs，使用当前构建的公开查询工厂及空结果适配器捕获读取SQL，开发MySQL仅执行EXPLAIN与结构元数据读取。固定核对当前dev_vue身份，读取事务结束并关闭连接，未读取业务行或执行DDL/DML。14操作捕获14查询全部被结构问题阻止；16个查询依赖表中12缺失，market_candles同名但缺V4账户键。明确说明见[就绪核查](architecture/account-readiness-20260908.md)。

最终[v2证据](architecture/account-readiness-probe-20260908-v2.json)保存查询、来源/构建摘要和实际列元数据；首次13查询中间报告清理。语法检查和实际只读探针完成；此结果是已验证的未就绪状态，不能称API联调通过。探针不穷举非空/有效授权分支，不覆盖所有写表和事务条件。

下一步按终端绑定、交易上下文、账户行情投影和观摩分组落实增量结构及初始事实，核对148步后历史校验衔接，再推进当前库独立回填/提升。前端与模块边界成果保留，账户完整流程仍不具备当前库验收条件。

## 第四十六批：终端路由增量结构与150步定义

追加036_terminal_route_tables.sql，定义terminal_account_bindings和bridge_connection_sessions，外键指向正式V4账户根及已有users/terminal_profiles。会话表采用当前档案级numeric epoch普通索引和终端/opaque epoch唯一键，未恢复已废弃的终端/numeric唯一约束。两表初始为空，V3历史路由不伪造为V4在线连接，注册写入口继续归Bridge同事务用例。

新增loadTerminalRouteMigration，复用完整148步并追加两项独立checksum步骤。注册表兼容测试通过，前148步完整对象保持一致、步骤ID唯一且重复加载确定。SQL经既有分句器解析，不代表已经过真实MySQL DDL验收；本批未连接数据库。

[增量合同](architecture/terminal-route-incremental-contract-20260908.md)记录两轮复核及历史校验衔接：原035完成检查保护完整表集合，后续需先校验精确新增表与完整日志，再对原集合验证原148步，不能忽略任意新表或日志。执行协调器、持久化计划和恢复副本演练尚未实现，当前dev_vue未到150步。下一批接入这些恢复能力。

## 第四十七批：终端路由状态机与原148步验证复用

新增terminal-route-coordinator.mjs。先验证持久化计划及完整150步历史，拒绝未知日志、缺口和未完成旧步骤；再检查新增两表的结构/行数/日志关系。仅精确登记且已单独核验的新表可从旧快照分离，原148条日志交原035协调器执行只读完成验证，保留其旧表集合、schema、legacy行与工具摘要检查。

默认只读。每张表开始、DDL及完成记录前后重新检查；三处响应未知即退出，下次依据日志与实际结构恢复。无日志同名表、未完成表已有业务行、完成但缺表或漂移均拒绝。适配器还须验证冻结计划、持有同连接锁，并在未全部完成时核对旧业务行保护摘要；本状态机不把这些待实现的MySQL职责当作已经验证。

15项新状态机测试及17项原根迁移测试通过。覆盖已落地/未落地DDL异常、重入零DDL、旧数据在DDL期间变化、新表完成后普通数据增长，以及陌生表仍被旧验证器拒绝。语法与差异检查通过。此次未连接或修改数据库，MySQL适配、规范DDL和持久化计划及恢复副本真实演练继续待做。
