# 模型归属与额度单例约束

在当前131步库追加两个既定目标CHECK：平台模型owner_user_id=0、用户模型owner_user_id>0；平台额度配置id=1。冻结目标来自bootstrap/v4-foundation-v1.sql。当前6个模型、1条配置均满足；执行前再次只读检查。保持已有ID、状态、额度、时间、默认值、唯一键和所有历史行。

第一轮复审：这两项是数据库可独立完成的完整性约束，V4模型解析器已按scope/owner过滤，额度读取明确p.id=1。不增加用户0或owner外键，不引入新配置，不代替服务端权限检查。

第二轮复审：复用原升级锁、checksum、journal和中断恢复。追加步骤绑定完整前后SHOW CREATE；恢复副本先执行并逐DDL模拟丢失确认，确认重复0DDL、所有业务行/其它结构/自增值不变，再冻结回执开放dev_vue执行。CHECK真实拒绝能力在恢复库用事务内无效UPDATE验证，必须回滚且对账一致；不以Mock证明MySQL强制约束。

剩余风险：MySQL实际CHECK表达式渲染必须和严格后置指纹一致，否则保留started日志并核对真实DDL，不重放已执行ALTER。完整全库索引/关系差异与根实体切换仍须继续处理。
