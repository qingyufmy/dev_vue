# 量见智桥图标、托盘与关闭选择

- 复用 bridge/assets/liangjian-bridge.ico，正式构建脚本把图标写入桌面 EXE、Launcher EXE；桌面另内嵌托盘资源，不依赖工作目录的散落文件。
- 主窗口常驻 NotifyIcon，双击或菜单“打开量见智桥”恢复窗口；菜单“退出软件”沿用原有安全关闭逻辑。
- 标题栏关闭及 Alt+F4 提供最小化到托盘、退出软件、取消。默认焦点为托盘选项，Esc 取消。托盘隐藏只隐藏界面，连接与额度刷新继续运行。
- 系统关闭、更新退出及已经开始的清理不弹重复选择框。重复启动通过原有单实例通知恢复窗口，并恢复任务栏入口。
- 真正关闭时释放 NotifyIcon 和菜单。账号请求进行中仍保持原有关闭保护，选择未执行时不保留显式退出标记。

验证：.NET x86 checked/warnings-as-errors 编译和 diff 检查通过；独立对话框渲染检查无裁切，默认/取消 DialogResult 以及 EXE 图标提取检查通过。修复版已替换并启动。真实用户托盘点击和系统关机未自动操作，不将其表述为已验收。

本地候选及更新前备份位于 D:\dev_codex\.local-runtime\bridge-tray-20260914；未发布安装器、未提交混合工作区、未发送终端交易指令。
