# AI 交易员工作区调整

目标：压缩账户摘要，按 AI 决策、持仓挂单、执行记录组织页面，修正表格与标题错位。沿用现有组件、API 和账户隔离逻辑，不增加配置或后端状态。

第一轮复审：保留所有查看与操作入口；手动下单降为次级操作；三个页签减少长页面堆叠。无新增依赖。

第二轮复审：保留账户切换后的请求隔离和弹窗清理；确认按钮仍走现有交易流程。验证只查看，不发送交易指令。测试现有交易员行为，执行类型检查并检查桌面及窄屏布局。回滚限本轮组件修改。

剩余限制：真实交易执行不在本轮验证范围，空决定列表不代表链路已验证。

## 验证结果

- 交易端类型检查通过；交易员展示、命令组件、命令构建、请求隔离和权限回归共 21 项通过。
- 本地浏览器确认当前 MT5 持仓、挂单及三个页签切换。375 / 768 / 1024 / 1440 宽度均无页面横向溢出，完成后恢复原尺寸。
- 自动交易保持关闭，未提交交易。无决策时显示空状态及账户策略订阅入口。
- 首次测试参数未收窄，额外运行全部交易端用例（240 通过、5 失败）。其中到期时间测试仍注入账户时区，但现有页面读取公共时区。本轮未修改该业务，不将全量测试报告为通过；旧用例需另行统一。暂不提交推送。
- 执行记录仍为本次会话操作，不代表完整历史执行链已完成。
