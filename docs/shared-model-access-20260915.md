# 共享模型限额调整

用户明确要求取消共享模型每日限额，仅限制手动分析每 5 分钟一次。

移除 V4 用量入口的每日请求数和词元总量阻断，保留账户有效、会员共享权限、模型验证、用量记录及结算。历史策略表中的限额字段保留但 V4 不再用于阻断，不修改数据库或历史用量。

手动分析服务端冷却 300 秒，按 user_id 加锁；同幂等键返回原任务，不重复占用冷却，自动调度不受手动冷却影响。前端冷却及提示同步改为 5 分钟。变更只影响后续接受的手动任务，既有冷却记录不回写。

定向验证：服务端 13 项测试、前端 7 项测试通过；服务端构建与前端类型检查通过。首次扩大验证发现旧 vertical-slice 合同断言已落后于当前 API，未混入本次修复，不声称全套测试通过。API 和 worker-analysis 已于 2026-09-15 04:47 UTC 更新，健康检查通过；未手动触发收费请求，下一自然周期结果尚待验证。
