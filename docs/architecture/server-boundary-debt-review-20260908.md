# 服务端精确边界债务与增量门禁

第十九批更新：auth组装工厂移至受限composition入口，Bridge设备撤销实现归bridge并由运行入口注入；auth/index不再暴露这些具体实现。当前236个源文件、144条存量记录，无新增越界，仅删除3条实际消除的公开实现导出例外。以下147条为原始基线。

源码基线67262703，2026-09-08重新扫描235个源文件。仍有147条记录：59处跨域内部导入、75处公开入口暴露实现、11处领域层依赖、1个源码循环及1个模块循环。每条保存在server-boundary-debt.json，包含具体source/target/rule、导入种类、类型属性、次数、模块、消除阶段和处理理由。

## 复核与处置

- 跨域内部导入按实际目标路径登记：Bridge/execution共享命令、inference依赖策略与交易内部服务、trade-history依赖Bridge查询类型/传输、交易认证适配器访问auth内部。应按所属域改为公开端口及组装注入，不能把全部内部实现重新导出规避检查。
- 公开实现导出按index转导出的具体infrastructure/transport目标登记，含同一路径的纯类型与运行导出；类型定义位于基础设施目录同样应迁到公开业务契约，不豁免整个文件。
- 领域层依赖主要在Bridge/execution/risk及历史投影之间；保持现有业务规则，先明确基础契约与应用端口后迁移，不能删除行为来打断依赖。
- risk领域源码循环及bridge/execution/inference/risk/trading模块循环均保留完整内部边证据。runtime标志描述静态运行导入边，不代表已观察到运行故障。

消除阶段按原方案：trading为P1，strategies为P3，inference为P4，执行/Bridge/风控/复盘/历史为P5，认证/商业/学习/设置为P6。模块循环由execution协作流程负责收口。此登记允许按批准顺序迁移既有债务，不把这些依赖认定为目标架构合法。

## 门禁行为

`pnpm run verify:server-boundary-delta`先运行真实静态扫描，再对比精确多重集；新增记录、同一边次数增加、类型改运行、导入改导出均失败。减少债务后必须同时移除陈旧记录。循环比较全部内部边，不仅比较成员或循环总数；行号移动不产生假差异。

配置必须有模块、阶段和理由，禁止通配符、重复记录及将未知规则/无法解析依赖加入豁免。没有自动更新基线命令。将新增问题手工加入JSON依然属于需要代码审查的规则变更，脚本不能代替该审查。

15项检测器测试通过，包括同数量替换、陈旧例外、运行属性变化、重复边、循环内部扩张及配置拒绝。实际在audit/application下创建一条指向trading/domain的临时依赖，CLI正确拒绝；探针已删除，之后类型检查通过。门禁已接入服务端类型检查和构建。

## 尚未完成

本次只冻结当前扫描器检测到的服务端债务。前端精确债务、Nuxt剩余隐式依赖、模块层之外的依赖规则、所有公开导出形式及别名解析完整性仍需补齐；表所有权和全量业务迁移也未完成。严格inspect:server-boundaries继续如实报告存量并退出1，增量门通过不能用作全量模块化验收。
