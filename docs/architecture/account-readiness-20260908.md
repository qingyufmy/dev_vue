# 账户样板开发库就绪核查

依据：[实际只读探针](account-readiness-probe-20260908-v2.json)。目标为server/.env指定的dev_vue，工具校验数据库名、账号范围与server UUID；没有读取业务行、执行DDL/DML或启动服务。

当前账户样板不能直接连接此库完成流程。14个读取操作捕获14条SELECT，开发MySQL的EXPLAIN全部被结构问题阻止。16个捕获查询涉及的表中12个缺失、4个存在；存在不代表字段和数据语义兼容。market_candles实际返回缺少trading_account_id，其余查询首先遇到缺表，后续字段问题尚未展开。

| 依赖组 | 当前发现 | 接续处理 |
| --- | --- | --- |
| 账户根与所有者 | trading_accounts存在；正式ownerships、ownership_intervals及user_trading_account_settings缺失 | 沿用账户构建表回填与正式提升协议，当前库独立生成计划，不复用副本计划 |
| 终端路由 | terminal_profiles存在；terminal_account_bindings、bridge_connection_sessions缺失 | 核对profile现有列与V4查询字段；明确旧Bridge绑定到新账户/档案的映射及新会话为空的启动语义 |
| 账户上下文 | trading_contexts缺失 | 追加兼容结构与用户默认选择规则；不能凭旧账户ID直接生成新上下文 |
| 账户与行情投影 | account_runtime_snapshots、market_quotes、trading_projection_revisions缺失；market_candles同名但不兼容 | 区分可重新同步的投影与需保留的历史行情，旧表保留并明确来源键/账户键迁移；不能直接在旧表加默认账户ID |
| 观摩 | observer_sources、observer_channels、observer_channel_accesses缺失 | 明确配置、发布权限和用户授权的初始事实；没有已验证来源时不得默认开放观摩 |
| 用户 | users存在 | 此探针未证明全部身份/权益字段和权限条件就绪，仍需真实只读用例核验 |

恢复副本148步只覆盖账户根正式提升，不能作为以上全部依赖表完成的证据。空库专用20260903_003等SQL是设计参考，禁止直接对现有dev_vue重跑。

下一步先完成终端/上下文/投影/观摩各组的增量结构与初始事实合同，核对追加迁移与148步历史校验的衔接。当前库切换仍须停写和源摘要核对；结构补齐后重跑探针，并继续实际查询、权限、事务与前端流程验收。即使EXPLAIN全部通过，也不能替代这些验收。

探针限制：使用空结果测试适配器捕获当前读取方法实际到达的SQL，不穷举有账户、有在线路由或有效观摩授权后的分支；因此未捕获的投影来源证明、持仓载荷、连接额度等依赖必须另行补齐。报告保存查询文本、摘要、实际表列元数据及源码/构建摘要，不保存用户记录或凭据。首次使用无效用户0使观摩分支未进入，已改为有效格式的测试ID1重新捕获，仅保留最终v2报告；ID只用于EXPLAIN参数，没有执行真实业务查询。

第一轮复核：将账户根迁移与配套业务结构分开，保持既有模块归属，不用空库初始化替代增量升级。第二轮复核：确认只读边界、目标身份和查询范围，标明首次报错遮挡的后续缺口；旧行情和旧账户引用不作无依据替换。
