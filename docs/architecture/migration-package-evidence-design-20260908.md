# 迁移历史package证据与当前工具兼容

历史学习Schema回执冻结package.json全文，当前新增边界/API脚本及Ajv、openapi-typescript依赖后，协调器无法读取既有147步计划。差异只涉及scripts和依赖新增；已有依赖版本、模块模式、Node约束和包管理器未变。

处理：从Git提交1cd6a89fa20d9060920175d094e987a7ffe8a556取回原package字节，保存独立历史证据文件，并按原回执中的SHA-256验证。当前package允许scripts变化和新增依赖，但所有原dependencies/devDependencies条目必须保持相同值；其余顶层配置精确一致，新增overrides等配置不自动通过。历史SQL、回执及步骤checksum保持不变，其它4个工具仍按原路径和摘要验证。

这项检查只负责历史声明与当前清单兼容，不证明实际安装依赖或执行环境相同。正式新增迁移仍要冻结当前工具与环境并在恢复副本演练，不能将旧回执升级为当前运行验证。旧演练执行器及其完整工具清单不修改。

第一轮复审：区分历史package证据和当前工程配置，允许明确不改变已有依赖的增量；不删除工具校验，不自动接受新摘要，不改变历史SQL。

第二轮复审：归档文件改动、已有依赖删除/换版本、模块/引擎/包管理器变化或新增顶层配置均拒绝；恢复旧schema加载能力后，先只读核对147步实际结构及checksum。未执行任何新迁移前，不据此声称迁移演练完成。
