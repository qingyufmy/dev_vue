# 观摩管理初始行补正

发现基线：dev_vue / 4b034a50。真实开发库 observer_management_registry 的 id=1 不存在，合成运营者创建观摩源返回 observer_management_storage_unavailable；该运营者没有源或管理操作回执。标准迁移 20260905_022 包含 INSERT (1,0)，原地迁移 025 仅纳入结构语句，未初始化必需行。当前元数据就绪检查没有发现这一缺口。

补正采用新 042 步，保留旧 165 步、迁移账本和 321 个冻结输入。协调器持有既有数据库升级锁，并在 RR 事务中锁定 registry 的 id=1；只有 registry 缺失且源、频道、授权、管理操作及相关 outbox 全空，才初始化 revision=0。已有合法 revision 保留原值，追加账本；已有完成账本但行缺失、历史不空而行缺失或 revision 不可用时拒绝修补，不能重置历史。

seed 与新迁移账本使用同一 InnoDB 事务提交。实际升级只涉及这两行，不更改账户/用户/时间/业务历史。就绪检查只读必需 singleton，缺失或耗尽版本时拒绝就绪，不在启动时创建数据。用户授权范围为持续升级当前开发库，应用与测试保持本地。

第一轮复审（需求与职责）：问题属于 trading 观摩协调能力及原地升级完整性，不通过测试脚本临时插行掩盖。新建来源仍经管理用例的停用→启用流程；合成运营者独立且不保留可登录密码，频道仅向明确测试用户开放。原地升级之外的标准迁移已经包含初始行，不重复更改。

第二轮复审（兼容、并发与恢复）：新增步骤保留全部旧 checksum；验证表为 InnoDB、主键/列及无触发器。registry 行/间隙锁使管理事务与初始化串行；seed 和 ledger 一起提交，提交确认丢失时仅检查，不自动重放。先执行失败注入并真实回滚，对比前后，再正式应用和重复检查。已成功应用后不删除 singleton、不回退递增 revision；应用代码可回退而数据保留。若已有历史却缺行，必须另行恢复核对，当前脚本失败关闭。

验证计划：迁移协议行为测试、就绪检查回归、实际 --inspect/--rehearse/--apply/--inspect 回执、类型与构建检查；随后按持久意图和精确操作回执恢复观摩夹具。首次创建的合成运营者/账户及失败报告保留，不能重复创建。真实终端及浏览器拦截均不由本次补正证明解决。
