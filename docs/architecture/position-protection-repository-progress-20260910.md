# 保护命令仓库接入进度

2026-09-10，本地源码接入，尚未启用运行入口。

- MysqlBridgeCommandRepository 新增显式保护命令 provider：新命令在同一事务内复核当前风险、检查对应快照、写命令及绑定，再写事件/outbox；已有命令使用保存的绑定复核，不生成新审核。
- bootstrap 在事务前捕获一次 Redis 路由；连接故障延迟到新审核时处理，已有绑定恢复不依赖 Redis。捕获对象限制命令、用户和账户范围。
- position_workflow 的 modify_position 候选允许当前持仓版本高于原准备版本，但拒绝回退和非安全整数；最终授权仍由命令事务完成。普通命令保留精确版本要求。
- 命令请求可早于事务内审核；新绑定使用数据库当前时间，重试使用已保存的绑定时间校验，不能借重试延长期限。

验证：83 项定向测试通过；服务端类型、构建、模块边界和 98 项运行合同生成检查通过。新增 provider 测试隔离 SQL reviewer，验证路由捕获和失败恢复边界；不构成完整仓库 SQL 证明。首次测试使用无效 child ID 提前触发真实来源校验，已调整为明确隔离的 provider 单测。

strategy-write-reference-v124-20260910.json 真实隔离 MySQL/Redis 参考通过，随机库已删除，existingDatabaseWrites=0。其中绑定写口仍由 fixture SQL 创建外围命令，不能将其当作实际仓库新接入路径的联合验收。

下一步：补实际 MysqlBridgeCommandRepository 创建/回滚/提交确认丢失/重放联合验证，再完成发送时复核、结果归并与运行入口。056/058/059 当前库升级、完整恢复演练、旧业务回填及 canonical 切换等仍待完成。发送门禁未解除，没有启动服务、当前库迁移或真实交易。
