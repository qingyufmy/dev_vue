# 订阅归属引用约束

145步当前库5条订阅的user_id和trading_account_id均可找到父记录。补齐目标FK策略订阅→用户/交易账户，沿用现有INT父子类型，不修改ID、默认、开关、时区、原索引或数据；既有复合索引覆盖子键，无额外辅助索引。目标账户/订阅BIGINT根迁移仍延期，这两个引用约束不能证明根迁移完成。

第一轮复审：用户/账户引用含义和现有值明确，可独立增强引用完整性；没有其它根字段并非跳过有效引用约束的理由。目标默认RESTRICT/NO ACTION，禁止级联删除，订阅软删除字段保持。

第二轮复审：同表两个约束按MySQL名字排序构造完整前后状态；保留前145步checksum。恢复副本逐DDL后中断恢复、重复0DDL与全表行/结构/自增对账，再执行当前库。约束只是引用完整性，不校验账户当前归属用户，真正授权仍由版本化归属与服务端完成。
