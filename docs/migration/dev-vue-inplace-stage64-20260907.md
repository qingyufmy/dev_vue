# 阶段 64：推荐账户转换与真实来源核对

## 本次结果

实现 `scripts/lib/v4-referral-conversion.mjs`，承接既有 018 合同：每个源用户生成一条推荐账户候选，保留用户 ID、推荐码、推荐来源码和 DECIMAL(20,8) 余额。零余额、NULL、空串、负余额和重复推荐码均不丢弃；不把推荐码解释为用户 ID。

目标 `revision=1` 是新的并发基线。`updated_at_utc` 是冻结在迁移清单中的目标登记时间，依据 `docs/stage-m1-b2-p1-user-state-schema-report.md` 2.2；不是旧业务时间的 UTC 推算。旧 created_at/updated_at 原值与源投影 hash 保留在转换证据中，未来 writer 必须将证据纳入同批持久回执。本次尚未写入数据库回执。

金额计算和汇总使用 BigInt 定点单位；独立对账函数从来源事实与目标回读值逐用户比较，检查遗漏、多行、码值、金额、revision 和登记时间。总额相同但用户金额调换仍失败。

## 当前库只读证据

`scripts/review-dev-vue-referral-source.mjs --write` / `--verify` 两次结果一致，见 `dev-vue-referral-source-review-20260907.json`。检查绑定 dev_vue 实例、完整 45 步结构日志、五项默认值规范化后的原结构以及原 165 表/271007 行指纹。

| 项目 | 结果 |
| --- | --- |
| 源用户 / 转换候选 | 25 / 25 |
| 推荐余额合计 | 80.00000000 |
| NULL 推荐码 / 空推荐码 | 20 / 0 |
| NULL 推荐来源码 | 25 |
| MySQL 实际排序规则下重复码组 | 0 |
| 非空来源码找不到 / 多义 | 0 / 0（当前无非空来源码） |
| user_referral_accounts 已存在 | 否 |
| 本次数据库写入 | 0 |

报告不保存原始推荐码。候选登记时间仅用于这份只读转换证据；真实回填需另行冻结运行清单并贯穿恢复重试。MySQL 排序规则负责重复检测，未以 JavaScript 大小写转换代替。没有当前重复不构成未来加唯一约束或启用结算的依据。

## 两轮检查与验证

1. 合同检查：018 和用户拆分报告明确登记时间来源，不需要等待旧账户 DATETIME 时区确认；保留重复码和负金额，不擅自添加财务默认值或新权限。
2. 完整性检查：独立对账不接受仅总额相等；真实源读取显式列清单、只读一致快照，额外字段/重复 ID/无效编码/有损精度均失败。不将合成目标对账当作真实目标回读。

13 项定向测试通过，覆盖精度上限、负最小单位、超精度、数字/NULL 输入拒绝、重复 ID、无效 Unicode、时间验证、来源顺序确定性，以及每用户金额调换。

## 下一步与未完成项

追加推荐表的同库结构步骤，先在恢复副本完成真实建表和断连恢复；然后接入现有事务回执、源证据、检查点及提交未知恢复协议，在恢复副本完成 25 条真实回填和独立回读后应用到当前库。财务新写入仍需账本与余额原子协议；旧 users 推荐字段在切换与对账完成前继续保留。全库规范化、业务回填、最终清理均未完成。
