# 阶段 161：课程核心增量恢复演练及当前库升级

恢复镜像 dev_vue_m1_source_20260907_02 已从 58 步升级到 62 步。每项实际 DDL 后、完成日志前注入一次异常；四次都先只读识别 reconcile，再补齐日志继续，DDL 总计恰为 4。再次运行零重复 DDL。原始 271,007 行和之前新增表的摘要前后一致。

演练回执 dev-vue-learning-core-upgrade-rehearsal-20260907.json，SHA-256 d53d3c8cd3508b15a166b61b1db84f64ee011b60fe375a269778b1fe77007f88，201 项工具摘要核对通过。证据目录 /www/backup/aurum-v4/m1/20260906-01/learning-core-upgrade-rehearsal-01。

## 当前 dev_vue 实际执行

upgrade-dev-vue-schema.mjs 提升为 v14，使用 62 步协调器及固定学习升级演练证明；旧证明切片仍保持原长度/校验值。新增四表的升级前后行保护：不存在只能变为空表，已有数据必须保持完整摘要。

预检只报告四张 learning_ 表待创建，0 DDL/0 日志写入。随后当前库 apply 验证成功，4 DDL、8 次日志写入，62 步全部完成。独立再次 apply 为 0 DDL/0 日志写入，原始 271,007 行不变，先前权益/返佣/配置等目标数据保护通过。无业务回填。

首次回执：dev-vue-schema-upgrade-597ef5a9-df97-4cd4-b49d-1ddadf239001.json。

独立重复回执：dev-vue-schema-upgrade-a395b941-82eb-4a3f-ac6e-d8ff7d600d3f.json。

## 适配与验证

追加 mysql-settings-backfill-v3.mjs，配置生成/执行 CLI 现在使用 62 步身份指纹；保留历史 v1/v2 适配器，不修改其演练源。新的实际只读身份校验通过，schemaHash ced24d5b3b2ba9c6e17a649240b497d1befa14993a39487e369809e8b6de6046。旧 58 步 manifest 不可伪装为新结构 manifest；当前库没有已导入配置运行需要迁移其绑定。

四项学习 proof/协调器测试通过，两项 CLI 语法检查通过，真实当前库重复升级与新版配置身份读取通过。完整配置 CLI 在 62 步环境的回填尚未重演，不把适配器读取通过说成完整命令链复验。

定向复核：DDL 与日志分开，注入发生在实际 DDL 后；不依赖事务撤销表创建。独立回执预先保留，原数据和新表都受保护。SQL 022 未修改，历史 58 步未重排。当前新增四表为空，课程内容、媒体、附件和学习进度还没有回填；历史时间与媒体可用性仍是数据转换门。

下一步实施课程完整行转换及独立对账，并继续资源/文件等剩余来源表。全域自动升级、业务切换和旧结构删除仍未完成。
