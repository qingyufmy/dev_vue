# 同库升级第十六批：完整来源真实事务验收

已在既有恢复副本 dev_vue_m1_source_20260906_01 完成 inplace-account-v2 模式真实 MySQL 演练。范围为合成账户与归属记录，不转换真实历史日期，不写源 dev_vue。

## 验收结果

- 在业务行、映射和回执 INSERT 后，故意让来源证据 INSERT 失败。实际事务回滚，四张工作表和批次/映射/回执/来源证据均无残留；仅保留已单独提交的准备记录。
- 账户批次第二条、归属批次第三条写入后故意失败，分别验证整批回滚；归属失败不改变已经提交的账户批次及其两条来源。
- 账户与归属批次实际 COMMIT 后主动断开连接，并向调用方隐藏成功响应。新连接按账本确认 committed；重复执行不调用 writer。
- 提交结果：1实体、2设置、3归属区间、2授权投影，5条映射、5条回执、5条完整来源。业务字段通过 writer 精确回读；完整来源逐条比对全部原始字段、源内容哈希和主键哈希。
- 按本次固定runId先删除来源证据，再清理回执及合成业务，六张迁移账本表和四张工作表恢复为空。
- 副本原165张表271007行原有列哈希与备份一致，当前账户回填结构摘要未变。

证据：[真实演练回执](dev-vue-source-writer-rehearsal-20260906.json)、[80个执行文件哈希](dev-vue-source-writer-tools-20260906.json)。收集时验证本地对应文件哈希与远端实际工具一致。未新增DDL，未修改业务源库或运行配置。

## 复核边界

第一轮沿用账户、归属及来源存储既定语义，完整来源必须与业务结果同事务存在，不能用只保存摘要替代。第二轮专门验证来源写入失败的位置、已有账户批次与后续失败批次的隔离、JSON精确回读、外键依赖清理顺序及原表全量哈希。

这是合成数据的真实依赖证据。历史时区仍未确认；真实4条账户和274条归属尚未回填。最终迁移清单还必须绑定完整时间依据内容及证据ID，来源表中的时间依据哈希本身不能代替这些内容。下一步建立真实业务回填的冻结清单、权限/归属交叉核对及历史时区依据，再推进副本和源库回填。正式切表、全域自动升级和旧结构删除均未完成。
