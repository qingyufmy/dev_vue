# 阶段 147：历史时间日志可用性核对

本轮重新核实当前源码775afe92及干净工作区。已重读既有U2时间合同，明确UTC+8是有代码支持的候选解释，不能重复描述成毫无线索；代码作者时间仍非历史部署/导入记录证明。

## 当前虚拟机只读证据

连接dev_vue核验MySQL8.4.8和既有服务器UUID，完成日志配置与binlog目录元数据读取。当前general_log=OFF、log_bin=ON、binlog_format=MIXED、过期秒数600000，58条升级步骤完成。system_time_zone=CST/time_zone=SYSTEM只描述当前配置，不用于推断历史偏移。

SHOW BINARY LOGS列出000041–000052，其中前七项报告0字节。对五个非空日志按快照长度逐事件跳读，只解析19字节事件头，不解释/输出SQL正文、行事件数据或配置值。

可用事件头范围：000048从2026-09-03T03:42:24Z开始，000049从9月3日至4日，000050从9月4日至6日，000051–52覆盖9月6日至7日。现有文件没有6—8月的事件头范围，不能拿9月导入/恢复事件替代源数据当时的写入时区证明。事件头时间本身也不等于DATETIME字段的时区证明。

## 交付与限制

新增review-mysql-time-evidence-host.py及review-mysql-binlog-coverage-host.py，凭据仅经匿名内存FD传入Node，输出仅允许的日志元数据。两个实际报告分别为dev-vue-mysql-time-evidence-availability-20260907.json和dev-vue-mysql-binlog-coverage-20260907.json，绑定各自工具SHA256。

本轮没有DDL/DML、配置更改、日志刷新/删除、服务启动或公网访问。没有声称覆盖服务器外部归档备份或已清理日志。

已向用户请求确认旧业务DATETIME是否一直采用UTC+8，允许按表/时期限定；尚未得到答复，不将等待视为批准。其他类型/业务语义审查可继续；整体目标仍活动，未因单项时间证据缺口宣称任务完成或全局阻塞。
