# 阶段 115：返佣规则管理读取与新旧核对

新增 `GET /api/v4/admin/referrals/rules`，读取当前规则 ID、plan、period、rate_bps、enabled 和字符串 revision。与写入口使用同一管理员主机分组及 admin-web 会话；GET 不要求写入用 CSRF token，仍验证登录及管理员权限。响应明确 `Cache-Control: no-store`。

应用服务验证 actor；MySQL 读取在短事务内重新检查 active、未删除的 admin 并持有共享锁，单条显式列查询按主键排序。最多读取五行以检测超出四种受支持组合的异常，不能截断后伪装完整。重复 ID/范围、未知范围、非法比例/状态或 revision 均拒绝；缺失规则保持缺失，不合成 10% 默认行。读取结束 rollback 释放锁，没有 DML。

## 真实当前库证据

`review-dev-vue-referral-rule-list.mjs --write|--verify` 使用实际构建读取器，在当前 dev_vue 两次独立只读结果一致，见 [读取回执](dev-vue-referral-rule-list-review-20260907.json)。

- 四条规则完整返回，revision 均为 1。
- 五个旧字段逐项匹配阶段 107 的来源记录，新增读取输出与直接 SQL 六字段一一对应。
- 源码、构建文件与六字段来源散列留存。
- 未执行数据库写入。此验证通过数据库当前管理员身份调用仓储，不是浏览器登录或真实会话验收。

15 项读取/HTTP/管理事务测试、3 项 OpenAPI 合同测试通过；服务端类型检查与构建通过。包含停用和零比例、大整数精度、权限先行、空集合不补默认、异常集合拒绝以及响应不缓存。

当前 dev_vue 仍为 203 表/53 步。管理员前端流程及旧写入口退出尚未验收，不能删除旧规则数据。已补齐新接口读写及底层事务的独立能力，但整体数据库标准化、其余域转换和全量部署自动升级仍未完成。
