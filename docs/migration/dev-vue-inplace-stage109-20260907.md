# 阶段 109：返佣规则同库升级协调

新增 `inplace-referral-rule-schema.mjs`，在已冻结 51 步后登记 `inplace_014_01_referral_rule_constraints`。SQL 绑定已执行参考探针的散列，完整 DDL 前后散列进入步骤 checksum，复用原 journal/协调器，不修改已验收的历史加载器。

原表 `referral_rules` 继续参与原库结构验证，不作为新增表排除。结构适配器只接受完整匹配参考前态或后态的 DDL，再恢复原态用于基线指纹。改变比例上限、revision 类型、原列默认值、索引或其它定义均不能借此隐藏；journal 决定当前允许哪一个状态。触发器或表类型异常阻断。

## 验证

- 七项关联测试通过，包括唯一一次 ALTER、重复执行零 DDL、DDL 后异常再运行补齐日志、无日志后态拒绝及原定义漂移拒绝。
- 在当前 dev_vue 执行新协调器的只读计划及全原库结构校验，结果为 51 completed / 1 pending，见 [只读回执](dev-vue-referral-rule-plan-20260907.json)。没有 DDL/DML。
- 上述异常恢复是离线 store 故障注入；不能替代完整恢复副本上的真实故障演练。阶段 108 的真实参考 ALTER 证明 SQL 约束有效，不证明 journal 恢复已在真实依赖上完成。

下一步：恢复副本执行第 52 步并注入 DDL 后完成日志故障，核对原 271007 行以及现有会员/推荐/支付数据，重复执行通过后更新正式显式升级入口、应用当前 dev_vue。尚未改变现有 `upgrade-dev-vue-schema.mjs` 的 51 步契约，当前库仍可用旧入口检查；实际升级到 52 步时必须同步入口及后续读取验证器，不能忽略新日志或关闭结构校验。
