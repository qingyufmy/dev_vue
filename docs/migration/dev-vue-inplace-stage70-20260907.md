# 阶段 70：真实订单抵扣与返佣规则来源核验

新增 `scripts/review-dev-vue-commerce-source.mjs`，使用 dev_vue 的只读一致快照，绑定实例、46 步 schema、原 165 表/271007 行指纹。取得 orders/referrals/referral_rules 实际定义与字段，读取明确的订单金额/用户关联/状态/币种投影及全部规则行。两次结果一致，见 `dev-vue-commerce-source-review-20260907.json`。

## 当前事实

| 来源 | 行数与金额 |
| --- | --- |
| orders cancelled | 4 行；订单金额合计 301，amount_confirmed 合计 1，推荐抵扣 0 |
| orders expired | 4 行；订单金额与 amount_confirmed 各合计 16，推荐抵扣 0 |
| orders paid | 4 行；订单金额合计 20，amount_confirmed 合计 0，推荐抵扣合计 20 |
| orders pending | 0 行；待承接的未完成推荐抵扣返还义务 0 条 |
| referrals | 0 行 |
| referral_rules | 4 行；plus/pro × monthly/yearly，均 enabled=1、rate_bps=1000 |

12 笔订单的 currency 存储值均为 USD。仅此不足以证明所有历史余额单位来源；保留实际字段，不以默认值推断额外金融事实。amount_confirmed 保持原字段名称与值，不在本报告将它概括成链上实际到账。

未发现金额不能精确表示、负金额、推荐抵扣超过订单金额、订单用户孤儿或有抵扣的未知订单状态。四条规则均在 0–10000 bps 范围。金额核验使用 BigInt 定点单位，报告不含用户身份、订单号、地址等原始标识。

## 对后续迁移的直接影响

1. 已迁入的 25 条余额合计 80 是当前余额。四笔 paid 历史订单的抵扣合计 20 只能作为历史订单事实导入，不能在导入时再次执行 debit。不能仅凭 80+20 推造一笔历史初始入账 100。
2. 本快照没有待支付抵扣，当前批次无需创建活动退款义务。自动升级实现仍须读取并识别这种义务，不能将本次零行硬编码为一般规则，也不能触发旧订单支付或退款。
3. 四条规则周期为 monthly/yearly，与旧部分下单调用使用 month/year 不同。源规则保留完整原值；新产品周期合同明确后用显式映射，碰撞或不一致时阻断，不能通过 TRIM/字符串替换静默合并。
4. cancelled 订单的 amount 与 amount_confirmed 不相等。不得以“二者必须相等”修补旧数据；先保留来源，结合订单/链上匹配合同确定各字段目标。
5. referrals 当前无行，不代表可删除其佣金归因/审核能力。账户期初账本与以后佣金事件分开，不伪造历史佣金补齐账本。

## 验证与后续

只读真实执行两次通过，数据库写入 0；本轮没有改服务端运行代码。完成来源、类型及金额精度核对后，下一步建立明确的期初账本事实与订单历史导入合同，再接入用户余额读写事务。订单全字段转换、V4 commerce 完整运行链、全库自动升级与旧结构删除仍未完成。
