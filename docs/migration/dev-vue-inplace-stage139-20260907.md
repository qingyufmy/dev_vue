# 阶段 139：恢复副本与当前库升级到58步

## 恢复副本

dev_vue_m1_source_20260907_02完成57→58。仅执行inplace_018_01_system_setting_requests一条DDL，DDL成功后注入异常；协调器识别结构已存在并完成日志对账，随后重复零DDL。原165表271007行与既有新增表数据一致，请求收据新表为空。

演练回执dev-vue-setting-request-upgrade-rehearsal-20260907.json绑定192文件，逐文件哈希核验通过。回执SHA256：fcf0a7808a49d23d51b6d13876dd3982ca19836ee30604ae3c6bc65519081903。新增专用runner，未改动原57步演练文件。

## 当前dev_vue

显式升级入口提升到v13，保留此前各阶段证明及行保护，新增请求收据六列完整摘要保护。执行前只读预检确认前57步完成且只剩请求收据建表。

首次apply完成58步：1 DDL、2日志，原271007行验证一致，内部重跑零变更。回执：dev-vue-schema-upgrade-4d5d2b7f-1917-4f68-a91d-b67b58d0a535.json。

独立再次apply：0 DDL、0日志，58步均完成。回执：dev-vue-schema-upgrade-221b37a0-782a-40d9-a7d5-42c0cca64a2e.json。

本轮仅新增请求收据结构和升级日志，没有回填或修改业务数据、切换消费者、启动服务或操作公网。

## 验证与限制

7项证明/协调器测试通过，覆盖原57步证明、新58步证明、缺失→空表、已有行变化/丢失拒绝、DDL异常恢复和重复零变更。新增脚本语法检查通过。

当前58步显式入口仍针对已核验的dev_vue基线；不能据此宣称所有旧版本均能部署自动升级。配置真实回填、完整业务语义检查及HTTP/消费者切换仍需完成；其他数据库域与旧结构删除门继续保持未完成。下一步推进配置管理HTTP合同，同时继续处理真实回填的时间与业务证据。
