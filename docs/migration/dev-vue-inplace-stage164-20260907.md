# 阶段 164：学习进度批次、同事务存证及独立对账

新增 learning-progress-backfill/v1 批次构造，沿用已有 PK、ID map、receipt、checkpoint 数据形状。来源按旧进度 ID 排序，批次绑定转换摘要、序号和前后游标；限制单批 500 行、2 MiB。writer 和存证回调均验证封闭的完整行，调用者改变返回对象不能绕过来源、目标或课时映射绑定。

来源存证 learning-progress-source/v1 保存原始八字段、sourceSnapshotId、登记时间、basisHash、完整课时映射及逐行时间依据。MysqlLearningProgressBackfillRepository 复用已有事务及 commit-unknown 行为，在同连接插入行回执和来源存证，并在提交前逐项回读。存证缺失/不符回滚；跨 run 拒绝；提交响应未知销毁连接且不自动重试。

新增目标身份读取器，限定 dev_vue/规定恢复副本，校验现有 62 步协调器及来源 progress 定义，使用独立 learning-progress/v1 schemaHash。没有修改已执行 SQL、历史协调器或旧域证明绑定文件。

独立 auditLearningProgressImport 不引用转换器、writer 或时长转换函数，按冻结输入重新计算精确毫秒和 UTC。核对目标全部 13 字段、源集合和映射摘要、真实用户集合、回读课时公共 ID/来源摘要、原始存证全文及身份。缺失、额外、重复行、进度裁剪、标志重算、父实体变化和存证变化均不能通过。审核结果不授予切流或删除权限。

## 验证与限制

新 audit/batch 9 项测试和 repository 3 项测试通过；进度转换及 writer 另外 7 项定向测试通过。覆盖逐一修改全部 13 字段、缺失/额外/重复数据、课时和用户变化、原始存证变化、时间证据缺失、NULL、1ms、9223372036854775807ms、封闭输入、游标顺序、存证失败回滚与 commit-unknown。

均为纯函数和 Mock SQL。62 步目标身份读取器尚未执行真实查询，本轮没有写库。批次构造和 repository 已完成；学习域专用合同/runner、真实 SQL 对账读取和课程批次尚未接通，不能将模块存在表述为完整迁移命令已可用。课程父实体必须先经存证及对账导入。

下一步接入学习域合同及批次执行/恢复入口，补课程批次和独立对账，再执行恢复副本的真实事务与故障演练。旧历史时间依据仍需按已登记证据解决，合成测试的 UTC+8 不充当真实旧行依据。全域升级和旧结构清理仍未完成。
