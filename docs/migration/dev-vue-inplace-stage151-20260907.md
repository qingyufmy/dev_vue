# 阶段 151：凭据转换计划持久化及 58 步回填适配

新增 v4-settings-credential-plan.mjs。计划绑定 runId、snapshotHash、来源 ID/配置键/完整来源行摘要/审核格式和目标密文；不存源明文或密钥。仅接受五项已登记凭据。恢复同时检查摘要、作用域和认证解密结果，重算非秘密 checksum 不能使不匹配内容通过。

persistCredentialPlan 使用受控路径独占创建、文件 sync 和回读验证。数据库事务前保存计划；重复调用先读原计划，不重新加密。空文件、部分写入、损坏内容或来源变化失败且不覆盖。批次获准后必须把返回 checksum 固定到迁移 manifest；恢复时传 expectedChecksum。此时文件缺失会失败，禁止重新生成替代密文。

文件包含密文与来源标识/摘要，仍须存于 Git 外受控目录。POSIX 创建权限为 0600；Windows 目录 ACL 由调用者配置，本实现不宣称验证 Windows ACL。文件 sync 不替代目录及备份持久性证明；丢失已绑定计划时从受控备份恢复，不能绕过 expectedChecksum。完整升级命令接入该强制参数仍待实施。

新增 MysqlSettingsBackfillRepositoryV2，在原事务/来源存证逻辑内替换目标身份读取为当前 58 步协调器。保留原 57 步适配器和历史演练绑定。新 schemaHash 使用 settings/v2，旧 manifest 无法被静默当作新版 manifest。此为可供下一批使用的新适配器，尚未改动旧回填 launcher。

## 验证

36 项凭据转换、文件计划、回填 runner/合同及 writer 定向测试通过。实际临时文件验证首次落盘、无活动加密版本的重复读取、原字节不变、错误来源/批次/预期摘要拒绝、部分文件不覆盖及已绑定文件丢失不重建；临时目录由测试 finally 清理。

在 dev_vue 的 UTC 只读一致性事务中调用新目标身份读取通过：UUID ac423207-6ef3-11f1-b302-000c29fda104，schemaHash 498e2002c8eae892723213acb59f630535711264ac400b3215d5a2b2668b3e8f。核对既有备份 UUID。没有 DDL/DML。

第一轮复核：随机转换计划先于数据库写入，目标和来源绑定；旧模块及 proof 不变。第二轮复核补充已绑定计划丢失时禁止重建，明确文件持久化/Windows ACL 的证据边界。没有执行真实凭据文件落盘、批次回填、消费者切换或删表。

下一步将该计划接入凭据专用行转换、来源保护存证与独立对账，然后在恢复镜像演练完整事务及提交响应丢失。历史时间依据仍是实际回填的待决项；自动全库升级和旧结构清理未完成。
