# 同库升级第四十二批：止盈偏好确定性选择

新增策略域 selectSubscriptionTakeProfit，并将偏好类型从基础设施移回业务域。四模式、AI缺推荐的第一档回退、所选价格缺失不换档规则来自旧 config.js 的 signalOrderPayload；价格按字符串保留，不经 Number。严格新合同拒绝数值/指数/负数等价格，并要求明确的三档与推荐值；这不宣称旧异常输入可未经适配直接迁入。

InferenceService.completeTrader 在输入显式冻结 executionPreferences 时应用选择结果，并保存 take_profit_selection（含原模型价格、档位和偏好revision）；模型提示合同同步说明所需字段。没有冻结配置的既有输入不补默认；平仓、撤单、改单和持仓保护不被开仓偏好覆盖。决策持久化使用转换结果，原模型结果对象不变。

验证57项定向测试通过：选择器17、决策转换8、偏好存储12、交易员Worker12、推理纵向流程8。服务端类型检查、构建和diff检查通过。这些是源码/离线测试，不是实际模型、数据库或终端证据。

尚未给生产Worker注入 executionPreferences：需先完成027实际结构准备、偏好读取与输入快照的同事务验证，以及最终执行前配置一致性检查。暂不启用不完整的配置冻结链路。本批无DDL/DML，未删除旧结构。后续继续上述接线、旧字段适配和同库回填，自动升级目标未完成。
