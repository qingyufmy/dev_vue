# dev_vue 同库升级阶段 60：宏观结构实际升级

当前 server/.env 指定的 dev_vue 已执行阶段 59 的 008 增量 SQL。MySQL 实例 UUID 与备份一致，完成后独立查询确认 196 张表、40 条 completed 升级日志；新增 11 张宏观/经济日历表均为 0 行。

## 入口与证据门

既有 `pnpm run upgrade:dev-vue-schema --plan|--apply` 接管完整 40 步，输出版本升级为 dev-vue-schema-upgrade/v2。分开验证原 29 步物理演练和后续 11 步宏观演练，不修改任何已执行 SQL、旧协调器或已冻结演练工具。

新增 `inplace-macro-coordinator-proof.mjs`，核验完整 40 步、11 次 DDL、3 个物理恢复点、原始数据 hash、目标实例、备份标识和 106 个工具文件；原 101 文件证据门同时保留。

## 实际执行

| 操作 | DDL | 日志写入 | 旧数据对账 |
| --- | ---: | ---: | --- |
| 首次 apply | 11 | 22 | 原 165 表、271007 行一致 |
| 首次内部重跑 | 0 | 0 | 40 步 completed |
| 独立第二次 apply | 0 | 0 | 原 165 表、271007 行一致 |

首次回执：`dev-vue-schema-upgrade-9b7c80ba-65ea-4ca7-bbad-838468e8b92e.json`。

独立重跑回执：`dev-vue-schema-upgrade-cf1b304c-f3d6-4828-84f4-92ada8bda1a6.json`。

没有写入宏观业务数据、改变旧行、重命名或删除旧表；没有启动服务、采集、调度器、模型或交易。新增结构为数据承接提供目标，不能据此声称历史宏观数据迁移完成。

## 验证与后续

71 项定向测试通过：新增宏观证据门 11、宏观结构/断点 13、原证据门 11、原协调器 36。实际 plan、首次 apply 和独立第二次 apply 均通过。

早期阶段的固定 29 步审查/账户回填适配器仍是冻结版本，会拒绝新的 40 步日志。当前结构操作使用本批统一入口；下一次业务回填须显式采用完整 40 步目标身份，不能过滤掉未知日志继续运行旧入口。

历史时间依据、全域字段语义转换、业务回填与可用性验收、自动部署切换和旧结构删除仍未完成。当前 40 步成功仅代表已实现结构范围完成，fullNormalizationComplete 继续为 false。
