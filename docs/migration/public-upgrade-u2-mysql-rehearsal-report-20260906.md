# U2 A库真实MySQL事务演练结果

2026-09-06，用户在[明确范围](public-upgrade-u2-mysql-rehearsal-scope-20260906.md)后回复“继续”，已在`aurum-vm`（192.168.1.254）的`dev_vue_m1_a`完成全部场景及精确清理。没有DDL、业务表写入、源库写入、部署或终端操作。B库仅在演练后只读复核。

## 实际执行结果

执行源为已提交`46db84fd`的`v4-backfill-mysql-rehearsal.mjs`及既有runner/SQL适配器，通过匿名内存FD在虚拟机运行。MySQL UUID固定为`ac423207-6ef3-11f1-b302-000c29fda104`。预检确认115表、26条完整迁移记录、预期实际结构指纹和025五表为空；凭据没有写入文件、日志或本报告。

| 场景 | 实测结果 |
| --- | --- |
| 同一批次并发两次 | 两次返回committed；合成writer只调用1次，批次、映射、回执和checkpoint只前进1行 |
| writer插入哨兵后报错 | 整批回滚；五表计数及checkpoint仍停留在第1批 |
| 确认回滚后显式重试 | 第2批完成；原子回读验证哨兵JSON、映射、回执和checkpoint一致 |
| 实际commit完成后注入响应错误 | 框架返回commit_unknown；新事务恢复确认committed，重复调用不重写第3批 |
| 实际rollback后在commit入口注入响应错误 | 框架返回commit_unknown；恢复确认not_committed，显式重试完成第4批 |

共6次writer调用，其中2次写入随失败事务回滚，最终4个批次提交。最大持久数据为18行：run主行+4个哨兵行、1个checkpoint、4个batch、4个ID map、4个receipt。每步通过独立获取的连接查询原值JSON、绑定、批次摘要、源/转换hash、目标引用与游标，而不是只比较writer返回值。

全部场景成功后，清理事务再次核对目标和主run绑定，按固定run/逻辑源/哨兵ID精确删除，逐条检查删除行数；025五表均恢复0行。

## 独立复核与边界

演练进程结束后另开新进程建立连接，只读核对A/B所有115张表计数及实际DDL指纹。A库的结构、journal指纹和所有表计数恢复到演练前；B库计数与前次安装结果相同，两库实际结构仍一致：`9bce7856c13d7b54a0042a7414d30f209e5d3f2d152d6d28d6e8bf012c4f2218`。

[机器证据](public-upgrade-u2-mysql-rehearsal-result-20260906.json)保留执行时间、目标/工具hash、各场景计数、清理结果和独立重连证据。A/B的journal hash不同源于各自迁移事件，未拿journal hash差异误判物理结构差异。

这是实际MySQL事务、SQL约束及同run锁串行行为的合成演练；提交响应错误通过连接包装器注入，没有断开真实网络、杀数据库进程或制造真实死锁。哨兵位于迁移元数据表，不能代替真实业务writer、金额DECIMAL列、业务FK和全域对账验证。旧表逐字段值未独立对账，计数一致不冒充用户数据无损迁移证明。本批未修改程序源码；此前35文件268项是离线验证历史，不表述为本批再次执行。

## 下一步

U2通用事务框架已具备这批真实MySQL证据。继续将源读取器和账户候选映射在完整冻结源上进行只读核验，解决平台来源、跨用户重复账户、历史时间及历史内容承接，然后实现身份业务writer及独立目标对账。U3真实业务回填尚未开始；公网无损覆盖仍受全域映射、内容/文件验证、恢复演练和功能门约束。
