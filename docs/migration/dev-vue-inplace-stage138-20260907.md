# 阶段 138：配置请求真实恢复与58步协调器

参考库dev_vue_m1_a已真实安装021请求收据表。执行前验证库名、服务器UUID、目标表不存在、配置/审计为空及专用夹具用户不存在；当前dev_vue未写入。

## 真实证据

使用真实MySQL管理事务提交配置、审计和收据后，故意销毁连接并抛错，调用方收到setting_commit_unknown。新连接重交原请求读取收据恢复原版本，无重复写入。这是实际提交后的故障注入，不宣称复现了任意网络故障。

两个相同新请求通过Promise.all并发执行，仅一次更新，另一请求返回replayed。配置再次更新之后，旧请求仍返回旧收据结果；改变原请求正文拒绝，撤销管理员角色后重放拒绝。最终只有两条审计和两条请求收据，版本精确递增。

三类数据库负例通过：非法摘要、请求ID末尾LF、没有对应审计版本的外键。所有合成收据、审计、配置和用户已清理，目标表为空；AUTO_INCREMENT可能推进。没有写入当前dev_vue、启动服务或切换消费者。

回执 `dev-vue-setting-management-probe-20260907.json` 绑定16个源码/构建/SQL/工具文件，取回后逐项哈希一致。回执SHA256为 `813d23cd2d6594e89cb470393f0db493cf26061375ff46ba73f060edb41d2967`。

## 升级接入

新增loadSettingRequestCoordinator，追加第58步inplace_018_01_system_setting_requests，绑定真实SHOW CREATE和原始021摘要，保留此前57步。3项测试通过：正常安装及重复零DDL、DDL成功后异常恢复不重放、未记录冲突表拒绝。Node脚本语法及服务端构建通过。

目前只是58步协调器可用；当前库显式升级器仍57步。下一步在恢复副本演练57→58与原数据保护，取得证明后再更新当前库显式升级入口。完整自动升级、所有域规范化及旧结构清理继续保持未完成。
