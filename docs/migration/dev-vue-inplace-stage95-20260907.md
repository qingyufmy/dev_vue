# 阶段 95：支付匹配整批真实恢复演练

在 `aurum-vm` 的恢复副本 `dev_vue_m1_source_20260907_02` 完成两条合成父订单和两条匹配的真实 MySQL 演练。当前 dev_vue 未写入，未启动支付监听、会员激活或余额操作。

## 真实结果

- 父订单通过订单批次迁移导入；匹配写入前核验完整父订单。
- 第一匹配批次实际 COMMIT 后销毁连接并模拟响应丢失。runner 返回 `backfill_commit_unknown`，新连接查证为 committed；目标完整回读通过。
- 第二批次在写来源证据前注入失败，目标、映射、批次、回执与检查点保持失败前状态；显式重试后成功。
- 两批重复执行无新增；最终目标、映射、行回执、来源证据、批次各 2 条，检查点 sequence=2、processed_rows=2。
- 完整目标行、父关联、映射、回执、完整来源证据和独立 9 字段源事实对账通过。
- 合成父/子数据及两次运行的迁移元数据按精确 ID/运行范围清理；原始表结构和 271007 行对账与基线一致。

## 证据与范围

回执：[dev-vue-payment-match-backfill-probe-20260907.json](dev-vue-payment-match-backfill-probe-20260907.json)。原始回执 SHA-256：`6c3c75e063e546d6157504cefa148c1c8c1606e965a9693c8302501ce33b1e81`。

回执绑定 152 个工具/依赖/历史证明文件。远端目录：`/www/backup/aurum-v4/m1/20260906-01/payment-match-backfill-probe-01`。

服务器 UUID：`ac423207-6ef3-11f1-b302-000c29fda104`。匹配运行 `ffffffff-ffff-4fff-8fff-fffffffffffa`，父订单运行 `ffffffff-ffff-4fff-8fff-fffffffffffb`；合成目标 ID 777503/777504，合成来源 ID 2147482100/2147482101。自增计数可能前进，未重置；未宣称参考副本物理字节完全不变。

22 项关联定向测试通过，演练脚本语法检查通过。真实历史时间、资产依据仍未验证；合成 UTC+8 和资产合同仅用于验证迁移机制，不能支持真实业务回填准入。

## 后续

匹配批次的真实恢复机制已验证。下一步补齐订单 crypto 快照与匹配来源的完整字段归档/对账，并推进支付业务剩余映射及真实依据核验。已有交易认领分支、业务切换、全量自动升级和旧结构删除仍未完成。
