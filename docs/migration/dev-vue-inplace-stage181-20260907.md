# 阶段181：本地学习迁移真实数据库及CLI演练

本阶段遵循用户要求：测试和迁移工具在本地运行，虚拟机只提供MySQL。未启动虚拟机网站、Worker、Bridge，也未增加数据库实例。

## 本地入口

新增 `scripts/rehearse-learning-backfill-local.mjs`，从现有 `server/.env` 读取连接凭据，目标固定为既有恢复库 `dev_vue_m1_source_20260907_02`。共享演练逻辑提取到 `scripts/lib/learning-backfill-rehearsal.mjs`，原host入口保留兼容；本阶段使用本地入口。

```powershell
node scripts/rehearse-learning-backfill-local.mjs --apply <绝对路径私有回执.json> <绝对路径历史备份.sql>
```

实际清单生成及迁移CLI新增显式 `--rehearsal` 前缀，只选择这一固定恢复库，不修改 `.env`，目标UUID、清单绑定及62步升级结构仍由原校验器核对。原始 `dev_vue --apply` 提升门尚未放开。

## 恢复后的结构校验

首次运行在写入前因旧的精确结构指纹不匹配而停止。没有改写原指纹算法、已执行迁移或旧证明。

本地读取历史SQL备份，完整字节数及SHA-256与原备份回执一致，提取165张表的DDL后重新生成的精确结构指纹也与历史基线一致。再逐表比较当前库经过已批准增量还原后的结构，使用已有结构规范化规则处理等价字符集声明及表自增当前值；历史与恢复后的自增差异单独记录，不当作结构变化或声称计数回退。

当前原表完整DDL另行冻结，演练期间不得变化；表集合、原始271,007行及新增受保护数据继续严格核验。实际类型、默认值和额外表变化仍拒绝。

## 验收

- 53项本地定向测试通过，包含真实commit后确认丢失适配、父子迁移、指纹等价/不等价、完整备份字节绑定等检查。
- 本地进程连接真实MySQL：12条课程、5条学习进度完成演练。真实batch提交后注入一次确认丢失，得到unknown；恢复查询不写库，显式继续后通过业务及控制记录对账，重复执行不改行。
- 实际子进程调用 `prepare-dev-vue-learning.mjs --rehearsal --write`，再调用迁移CLI的check、apply、recover、verify、重复apply，全部通过。
- CLI预检不改行；重复命令前后全表数据摘要一致。最终只按两个固定run及固定逻辑来源清理演练数据，恢复库211张表的全部行摘要与执行前一致。
- 开发账号仅临时增加恢复库的SELECT/INSERT/UPDATE/DELETE权限，结束后回收并核对原授权摘要；当前dev_vue连接配置不变。

最终私有回执为 `D:/dev_codex/_private-recovery/mysql-20260907-01/learning-local-rehearsal-04.json`，其同名 `.cli` 目录保存输入和各命令输出。最终回执包含代码摘要、真实恢复结果及 `cliEndToEndVerified=true`；原始课程信息、SQL和凭据不入Git。

## 范围与后续

演练使用固定的合成时间证据验证转换机制，不能证明历史课程和学习进度的真实时区；没有向当前dev_vue回填学习业务数据，也没有删除旧表。文件引用保留不等于外部资源可访问。

下一步核实历史时间与资源/访问语义、形成真实迁移清单，再提升当前库回填入口。全面规范化、查询索引验收及旧结构清理仍未完成。后续继续以本地工具执行、虚拟机仅提供数据库为默认方式。
