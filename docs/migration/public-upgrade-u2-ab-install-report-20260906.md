# U2 A/B 018–025安装结果

2026-09-06，用户对[明确安装范围](public-upgrade-u2-ab-install-scope-20260906.md)回复“继续”后，已按先A后B执行。两库安装成功并通过重新连接后的只读复核；这只完成目标结构准备，不代表源内容、业务回填、真实事务故障恢复或公网覆盖已验收。

## 执行与复核

目标为SSH `aurum-vm`（192.168.1.254）上的MySQL实例，UUID `ac423207-6ef3-11f1-b302-000c29fda104`；仅写入`dev_vue_m1_a`、`dev_vue_m1_b`。执行源为本地已提交`2b4efad1`的现有schema runner和SQL，工具通过匿名内存FD运行；凭据仅经既有宝塔支持读取路径进入内存，不写凭据文件，不改应用目录、配置或服务。

操作前，两库各102表，实际DDL指纹同为`dd799b70b93dc01526ca6220cba0b1f2f314c2d94f7df1c59d0827f9236aea93`；bootstrap及001–017完整，checksum、语句进度、既有correction/recovery事件通过runner校验。本轮没有恢复失败迁移或重跑旧correction。全部业务字段名与该前缀计划核对，涉及ALTER的7张表均为空；所有旧表计数已保存并在执行前再次比对，漂移则拒绝写入。

每库执行018–025的21条计划语句：13个CREATE、7个ALTER和022的一条控制行INSERT，外加现有runner迁移日志/进度记录。023包含已批准SQL中的索引替换，不删除会话行；其它旧数据未回填、截断、清理或重建。

重新建立连接复核：

- A/B各115表、26条完成迁移记录，待执行列表为空；所有字段名符合完整计划。
- 两库实际结构指纹一致：`9bce7856c13d7b54a0042a7414d30f209e5d3f2d152d6d28d6e8bf012c4f2218`。指纹覆盖SHOW CREATE字段、索引和约束等定义，仅排除AUTO_INCREMENT当前计数，不将journal hash冒充结构hash。
- 全部旧表行数与安装前相同，既有correction/recovery事件计数未变；新增表只有`observer_management_registry`的`id=1,revision=0`控制行，其余为空。
- 未独立回读比较旧风险控制种子的逐字段值；计数一致不能冒充逐值或金额对账。对源库没有DML/DDL，本次工具不连接源库执行回填；没有启动服务、部署应用或终端操作。

[机器证据](public-upgrade-u2-ab-install-result-20260906.json)保存时间、精确目标、工具包hash、SQL文件checksum、执行前后完整表级结构摘要和行数、独立重连的runner结果。原[安装前只读观察](public-upgrade-u2-live-observation-20260906.json)保留为历史，不覆盖其尚缺018–025的事实。

## 验证边界与下一步

本批使用现有已测试runner，未修改已执行SQL或业务源码。真实MySQL成功执行及重连后的结构/记录/计数验证是本批证据；不把此前257项离线测试重新标为本批新跑或真实业务验收。

U2接下来可针对已安装025设计有明确run ID、合成输入和影响表清单的真实事务回滚/恢复验证；真实测试行写入及清理不在本次安装授权中。源读取/身份映射/历史时间和业务writer仍需接续，U3业务回填尚未获准。公网切换还需全域映射、数据/文件对账、恢复演练和功能验收。
