# U2 真实MySQL事务演练待批准范围

当前只编写脚本和离线验证，没有执行下述测试写入。目标仅为虚拟机`aurum-vm`、UUID `ac423207-6ef3-11f1-b302-000c29fda104`的`dev_vue_m1_a`，B库保持结构参照。

第一轮复审：复用025五张迁移元数据表，不新建测试表，不写用户/账户/交易/队列等业务表。合成writer把独立“payload哨兵行”写入同一事务中的data_migration_runs，以验证目标写入与checkpoint/receipt原子性；该行不是用户或任务。此方式证明同连接事务机制，不能代替真实领域writer及FK语义验收。

第二轮复审：运行前要求固定目标、安装后物理指纹和26条完整迁移记录（bootstrap加001–025）、五表均空且指定IDs不存在。使用1个主run、1个stream、4个单行batch，最多18条持久行：runs最多5（1主run+4哨兵）、checkpoint1、batches4、id_maps4、receipts4。无DDL，不读取源库。固定逻辑源`u2-synthetic-20260906-a`；主run为`d47c6d10-0000-4000-8000-202609060001`，4个哨兵为同前缀结尾0002–0005，冲突即停止，不能覆写或清空已有数据。

场景：同一batch并发两次只写一次；writer写哨兵后报错整批回滚；确认未提交后显式重试；模拟commit完成后丢响应，恢复确认committed且不重写；模拟commit前回滚但响应报告unknown，恢复确认not_committed后显式重试。提交边界故障由连接包装器注入，实际事务/约束/锁使用真实MySQL；不是实际网络断线、断电或死锁测试。

每一步以独立连接回读5表与哨兵JSON校验批次、cursor、行数、原值/转换hash和映射；writer确认hash不充当目标回读。正常完成后，若批准包含清理，则按主run、固定逻辑源和哨兵UUID精确删除本次18行，并验证五表恢复为空。失败不自动删除、不盲重跑，保留有限合成记录以供确认恢复。该许可不包含真实业务回填、源库/公网写入、服务启动、部署或终端操作。

脚本为`scripts/lib/v4-backfill-mysql-rehearsal.mjs`，仅提供操作员内部函数，没有应用API或自动执行入口。`runMysqlBackfillRehearsal`要求明确的`scope=u2-a-synthetic-20260906,apply=true,cleanup=true`声明；该声明只能表达已获得的授权，本身不产生授权。每个读取连接和事务均核对目标身份；清理再次锁定主run并核对绑定，逐条检查删除行数，整段使用现有事务适配器处理回滚/提交结果未知。清理响应未知也不能盲重跑。

离线验证：3项新测试证明合成批次符合既有合同、固定目标仅为迁移元数据哨兵、错误授权范围在连接前拒绝、错误库/实例在元数据和DML前拒绝。它们不证明真实演练成功；现有runner/适配器回归一并通过，总计35文件268项。下一步需批准上述A库最多18行合成写入及成功后的精确清理，再执行真实MySQL验证。既有A/B安装许可没有被扩大为测试数据写入许可。
