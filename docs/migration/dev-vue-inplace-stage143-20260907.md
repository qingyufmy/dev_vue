# 阶段 143：前端配置数据合同与请求恢复状态

共享contracts新增配置范围、请求编号、更新正文和读写响应Zod合同；保持版本字符串，拒绝protected响应携带value、非秘密响应冒充credential、NULL/空串状态不一致。api-client新增getAdminSetting/updateAdminSetting，沿用same-origin凭据和CSRF，明确传入Idempotency-Key，不自动重试，读取no-store。

admin新增createSettingWriteSession作为页面待接入的数据层。一次操作绑定用户身份，复制并冻结原正文与UUID；未知提交时阻止新的提交，recover使用原内容/编号及当前CSRF。快照返回副本，外部修改不会改变待恢复请求。更换身份时拒绝恢复；恢复期间403等错误不能证明早先请求未提交，继续保留uncertain。

只有首次请求收到已识别的服务器明确拒绝才释放请求。代理408/非标准错误、无效成功响应与未知提交均保留uncertain。响应版本必须恰好为expectedRevision+1。状态仅存内存，未将配置正文或令牌写入浏览器存储。

## 验证

共享API客户端21项测试、admin6项测试通过；四应用及共享包类型检查、前端边界和四应用构建通过。Nuxt构建有依赖导出路径弃用提示，无构建失败。测试中发现旧交易历史夹具缺少现行币种字段，已按空结果/未知币种/不可比较金额补齐夹具，业务实现未改。

初次修改共享客户端出现Windows默认文本编码导致的中文字符串损坏，已从当前提交的原文件恢复原文并仅重加本轮内容，类型及构建复验通过。所有后续文本读取显式UTF-8。

## 尚未完成

本轮无UI页面改动或浏览器联调，无服务/数据库操作。页面必须持有该状态对象直到确认结果；刷新和导航后的恢复尚需明确用户流程，当前不能宣称跨刷新持久恢复。后续接入管理员配置页面及读取/恢复交互，再处理真实配置回填和旧入口退出。全域数据库规范化、完整部署自动升级与旧结构清理尚未完成。
