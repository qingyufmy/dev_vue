# 阶段 126：统一配置与变更审计结构验证

追加018_system_settings.sql（15列）和019_system_setting_changes.sql（7列），在参考库dev_vue_m1_a真实安装成功。两份SQL已经执行，不得改写，后续修正需追加迁移。当前dev_vue未安装本轮结构，仍以54步为准。

结构按[两轮复核合同](system-settings-schema-contract-20260907.md)实现：命名空间/键唯一，显式值类型和敏感级别，版本/来源及原label、sort_order保留；审计同一配置/版本唯一，同一请求/配置唯一，关联真实用户且无级联删除。

参考库接受布尔、JSON数组、不透明合成凭据三类配置，拒绝13类错误：身份尾空格、键大写、未知类型、错误布尔、非规范整数、无效JSON、非数组JSON、明文凭据、公开凭据、零版本、native缺时间、origin尾空格、legacy缺来源。审计插入成功并拒绝同请求另一版本；大整数revision和毫秒时间回读精确。测试配置、审计和临时用户均在同一事务回滚。表结构保留，表数据零行，见[真实回执](dev-vue-settings-schema-probe-20260907.json)。

这些约束只验证存储基本形状；逐键范围、枚举、地址/URL/HTML语义、凭据认证解密仍由应用负责。审计hash不提供旧值重建能力。下一步接入55/56步协调器、恢复副本演练及当前库显式安装；尚无真实配置回填、消费者切换或旧表删除。
