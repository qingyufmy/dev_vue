# 阶段131：配置批次、来源归档与独立对账

配置迁移接入既有批次框架：固定system_config/settings-v1源流和system_settings目标，同库模式限定inplace-settings-v1。批次绑定run、snapshot、转换hash、完整条目和稳定主键游标，按既有行数/字节上限校验。

MysqlSettingsBackfillRepository复用现有事务适配器，新增57步身份校验和同连接来源归档/回读。目标、映射、回执、进度和八字段原始来源同事务；提交结果未知时仅查回执，只有确认回滚的死锁允许有界重试。来源归档可能含不透明密文，不能作为公开报告或API返回值。

独立auditSettingsImport不导入转换器或writer，分别验证时间依据和语义要求，核对15列以及完整来源归档；结果只含字段级差异，不输出原值。33项关联测试通过，覆盖整批回滚、并发重复、响应丢失、映射冲突、来源归档失败、全部目标字段/来源字段篡改以及缺失/额外/重复记录。

当前dev_vue在READ ONLY事务内实际运行新身份读取器成功，57步完整；本轮无DML。写入与故障测试仍为Mock/内存事务，真实恢复副本演练待完成。时间与逐键语义依据仍须真实核验，旧配置75行保留，完整配置切换和全库旧结构删除未完成。
