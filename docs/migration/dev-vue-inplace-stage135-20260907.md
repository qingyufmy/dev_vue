# 阶段 135：配置更新真实事务与并发演练

## 本轮结果

阶段134的非秘密配置更新原语已在 `dev_vue_m1_a` 参考库完成真实 MySQL 演练。当前 dev_vue 未写入，无服务启动、消费者切换或公网变更。

两个连接均校验参考库名称和服务器 UUID，设置 UTC 会话、REPEATABLE READ 与有界锁等待。脚本先确认配置及审计表为空、专用夹具用户不存在，再提交一个合成布尔配置供并发连接使用。

1. 第二个连接先建立旧版本的一致性快照。
2. 第一个连接更新配置但暂不提交；第二个连接请求同一旧版本更新。
3. 在 performance_schema.data_lock_waits 中观察到第二个连接实际等待行锁，然后提交第一个事务。
4. 第二个连接获得锁后拒绝旧版本，证明 FOR UPDATE 当前读不会被此前的一致性快照替代。
5. 版本由9007199254740993精确递增到9007199254740994，只有一条已提交审计，原标签保持。
6. 使用相同请求ID再次更新新版本，故意让审计 INSERT 触发真实 ER_DUP_ENTRY；调用方回滚后配置值、版本及审计条数保持首次提交状态。
7. 删除本轮合成审计、配置和用户，验证两张配置表及夹具用户计数均归零。参考表 AUTO_INCREMENT 可能推进，不以此宣称所有DDL元数据不变。

## 可复核证据

- 脚本：`scripts/probe-setting-writer-host.mjs`、`scripts/run-setting-writer-host.py`。
- 回执：`dev-vue-setting-writer-probe-20260907.json`，8文件清单同时绑定运行时源码、构建产物和执行工具，取回后逐文件校验成功。
- 回执SHA256：`c4e0704b4632b138f3eafcf48e69a8c273a8610392f78a194cf7017f39342c5a`。
- Node语法检查及服务端构建通过；真实脚本返回 verified/concurrentLockWait/atomicRollback/fixturesCleaned。

## 定向复核与后续

本轮证明了锁等待、旧快照下的版本冲突，以及审计失败后的调用方回滚。没有把重复请求错误当作幂等成功；现有审计摘要不足以恢复任意历史请求，管理用例仍需独立持久化请求收据。

下一步为逐键运行时语义策略及配置管理请求幂等合同，之后接入认证授权与管理API。配置真实回填、历史时间依据和消费者切换仍未完成。数据库全域规范化、完整部署自动升级及旧结构删除门继续保持未完成。
