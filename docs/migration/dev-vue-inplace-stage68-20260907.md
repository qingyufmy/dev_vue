# 阶段 68：当前 dev_vue 推荐账户回填

当前 dev_vue 已真实回填 25 条推荐账户，余额合计 80.00000000。源 users 推荐字段保持原值，原 165 表/271007 行指纹匹配；本次新增目标行及迁移元数据，没有删除或切换应用读写。

## 补齐副本证据

`auditReferralLedger` 直接读取 run、全部 checkpoint、批次和 ID map，逐项比较冻结清单。检查批次请求 hash、序号、结束游标、处理行数、映射来源/目标/创建 run；只接受完整集合，不靠 COUNT 或 writer 返回值宣称正确。

恢复副本两次只读结果一致：1 个 run、1 个 checkpoint、3 个批次、25 条 ID map，处理行数 25。证据 `dev-vue-referral-ledger-audit-20260907.json`；9 项独立核验测试覆盖余额映射调换、缺批次、游标/行数/hash 变化、多余映射和创建 run 不符。

## 当前库执行

新增显式命令 `node scripts/backfill-dev-vue-referrals.mjs --apply`。只连接 server/.env 指定的 dev_vue，并绑定实例 UUID、原始备份及全库行 hash。执行前核验真实副本首次/重复回执、提交响应丢失证据、来源 hash、125 文件工具清单和独立台账审计；每个业务事务再次验证完整 46 步 schema 身份。

独立运行清单 `dev-vue-referral-apply-run-20260907.json` 先独占落盘并 fsync，再准备数据库 run。run ID 为 `2903fd3f-934b-4d6b-9f36-6f7d022a9485`；迁移登记时间与副本分开冻结。未知提交先查持久批次回执；当前库不注入故障。

首次执行 25 次业务 INSERT，三批完成。目标独立回读逐用户核对余额、推荐码、来源码、revision 和登记时间；25 条源证据/行回执/映射及最终 checkpoint 均通过。程序内再次执行各批次 0 DML。

另起进程再次执行同一 run：0 业务 INSERT，程序内重复 0 DML，全套回读再次通过。两份实际回执见本目录 `dev-vue-referral-apply-*.json`（含 businessInserts=25 和 businessInserts=0），不是纯源码测试。

29 项定向测试通过。真实测试范围仅数据库迁移，不包含结算、兑换、会员权益变更或前端读写。

## 下一步与保留边界

当前推荐目标已有完整期初余额，但应用权威读写切换、账本与余额原子写协议、旧字段引用清理尚未完成；不能把拆表回填等同于推荐功能整体上线。继续核对推荐域的运行接口/权益与其他身份字段处置，再按全库方案推进。自动部署时完整旧库升级、其他域业务迁移和最终旧结构删除门仍未完成。
