# 阶段 81：支付订单逐字段合同与候选转换

完成 [支付订单转换合同](payment-order-conversion-contract-20260907.md)，覆盖 orders 全部 23 个源字段，记录两轮不同侧重复审、调整及剩余风险。新增候选转换器，复用既有完整来源检查和推荐抵扣分类器，将订单主体、历史标签及支付来源证据分开输出，保留全行及 hash。

时间分别标记 absent/unresolved，不接受任意时区，不补造 UTC。amount_confirmed 保留独立金额语义；零值、取消订单金额差、原产品周期及链上期望金额均不自动修复。所有导入副作用关闭，产品/币种/支付监听/权益核验依赖显式保留。

验证：候选转换 6 项、来源检查 4 项、抵扣分类 11 项，共 21 项定向测试通过。覆盖排序稳定性及支付证据变化对 hash 的影响、零值、NULL、缺失时间、金额差、超额抵扣和退款证据门。本轮没有连接数据库或修改数据库结构/业务行；阶段 80 的 12 行来源证据没有被重新采集，不能宣称当前真实候选回填已完成。

下一步按合同核对 crypto_watch_list 与订单关联，然后冻结支付三张目标表 DDL。当前候选不是 SQL writer，订单业务回填、读写切换和全库自动升级仍未完成。
