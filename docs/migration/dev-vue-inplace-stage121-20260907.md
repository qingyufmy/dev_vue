# 阶段 121：钱包真实批次恢复与独立对账

恢复副本 `dev_vue_m1_source_20260907_02` 完成钱包批次真实 MySQL 演练。两条来源取自该副本 wallet_keys，未修改来源；仅为演练提供合成时间依据，不证明这两条记录的历史时区。当前 dev_vue 没有写入。

## 实际结果

- 两个单行批次分别写入目标、ID map、receipt、来源归档及批次日志，共各两行；一个 run 和一个 checkpoint，最终游标/处理数均正确。
- 第一批实际 commit 成功后主动断开连接并抛出错误，仓储返回 commit_unknown；恢复流程查到已提交回执，没有重发 writer。这是应用层注入的提交响应丢失，不是操作系统网络故障试验。
- 第二批在来源归档阶段注入异常，目标、映射、回执及进度全部回滚，计数与失败前一致；显式再执行成功。
- 重复两个批次后计数不变；映射、回执 hash 和来源 payload 均精确匹配。
- 独立对账器实际回读全部 13 列及两条来源归档，零差异；另以 verifyOnly 回读目标确认。
- 删除仅属于本次 run 的临时目标和迁移记录后，各项计数均为零。原 271007 行与冻结基线一致，原结构及已有新增表数据校验通过。

## 可复核证据

[完整回执](dev-vue-wallet-backfill-rehearsal-20260907.json) SHA-256 为 `ddc78ec3966a6e1469a57406bb1ce592e4b5c24fe737cbd77bbf5b99e9c3d1a6`，绑定 187 个文件。远端目录为 `/www/backup/aurum-v4/m1/20260906-01/wallet-backfill-rehearsal-02`。四个相关离线测试文件 29 项通过。

初次打包遗漏来源检查器的相对导入，Node 在模块加载时终止，未进入数据库操作。第二次按相对 import 递归收集依赖、沿用并核实前期 176 文件 manifest，使用新目录执行成功；未覆盖首个失败包或历史证明。

## 剩余工作

钱包批次技术路径已有真实依赖证据，当前四条旧钱包的真实时间依据和托管控制关系仍待解决，不能用演练的合成依据回填。运行 reader/消费者切换及旧入口退出仍未验收，因此保留 wallet_keys。继续推进运行读取与其他不依赖历史时间判定的规范化工作；全库自动升级及旧结构删除尚未完成。
