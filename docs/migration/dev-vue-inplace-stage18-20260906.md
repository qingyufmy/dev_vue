# 同库升级第十八批：真实账户回填冻结清单

已生成 [账户波次草案](dev-vue-account-wave-draft-20260906.json)，覆盖4条账户和274条归属历史，共278条来源记录。清单绑定现有加密备份快照、原结构摘要、165表原有列数据摘要、v2目标结构、归属审查、账户映射和当前执行代码/依赖锁文件哈希。清单不含原始登录号等业务载荷。

生成前重新验证真实归属审查，并在UTC一致性只读快照中读取全部原有列，与备份恢复证据逐表比较。生成后再次运行 --verify，完整清单一致。源库原271007行数据保持不变，未执行DDL/DML。

## 两轮复核

第一轮（内容与边界）：本清单只冻结账户及归属波次，业务目标仍是现有四张同库工作表；来源证据必须保存。它不激活运行、不切表、不删除旧表，不能被解释为全库升级已完成。

第二轮（漂移与未完成项）：目标实例、数据库、存储模式和结构必须与真实审查一致，原数据必须匹配备份；执行文件、依赖锁和SQL哈希进入清单摘要，代码或输入变化须重新冻结。工具清单排序不改变摘要，重复路径或路径穿越被拒绝。历史时间依据为显式NULL，admission.approved=false、executable=false，不能把草案直接传给真实回填执行器。

4项定向测试通过，覆盖拒绝源数据/结构/归属漂移、代码摘要变化、工具路径校验及不可执行草案。首次测试中的源数组与基准数组共享引用已在fixture中拆开，修改源时不会连带修改期望值；之后测试通过。真实清单两次读取结果一致。

## 未完成项

- historical_time_basis_required：两张表的历史墙钟转UTC仍需明确证据；完整时间依据内容应进入正式清单，不能只存哈希或套用终端UTC+3。
- historical_platform_currency_coverage：当前终端与绑定支持3个实体候选，但不等于全部历史平台和币种来源已核对。
- permission_projection_reconciliation：归属一致不等于订阅、风控、交易资格已经迁移。
- real_business_rehearsal_required：目前真实MySQL演练使用合成数据，尚未执行278条真实来源回填。

当前入口为 `node scripts/freeze-dev-vue-account-wave.mjs --read-only|--verify`；现有草案使用--verify，不覆盖。上述条件补齐后生成可执行版本，再做恢复副本业务回填及源库回填。自动部署升级总入口、其它业务域、正式切表和旧结构删除仍未完成。
