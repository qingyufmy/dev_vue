# 同库升级第五十一批：EMA34数据算法承接

只读查询当前dev_vue.auto_prompt_types的结构化指标声明：道诚策略id=1，指标id=ema34，kind=ema，enabled=true，source为M1/close/closed_only，参数period=34、minimum_bars=34、warmup_target_bars=60、evidence_window=5；另两条无指标声明。这与前期规则报告的EMA开关和shadow模式相符。M1行情计划只有30根，不能用该窗口直接满足34根计算或60根预热，也不能为了计算而扩大原展示窗口。

新增策略域calculateEma34Evidence和明确历史需求。使用旧算法的前34根SMA种子、2/35 EMA递推，保留最新/前值、关系、距离、斜率、最近5项统计、最新穿越及预热状态；这是技术指标的双精度近似运算，原OHLC十进制字符串不被修改，不用于资金账本计算。

有效输入下逐字段对照旧calculateIndicator：34/35/59/60/90根数值及完整analysis全部一致。另验证末尾未收盘K线排除、输入不变、历史不足、不完整预热、过期、未来已收盘K线、重复时间、内部未收盘K线、缺收盘状态及无效价格。9项测试、服务端类型检查和构建通过。

不可用指标明确value=null，算法版本为ema34-evidence/v1，不沿用旧baseEvidence对null经Number得到0的表现。新输入时间只接受明确UTC毫秒，不解析服务器墙钟；要求调用者提供引用时刻和缺口状态。未来闭合/未知收盘拒绝是证据有效性要求，不产生买卖判断或策略专属执行门。

本批仅计算核心，尚未接入生产指标读取和策略配置编译：下一步应为指标单独取得足够历史，保证相同账户/品种/周期/引用时刻，把结果与来源冻结，再接入模型数据。原30根M1展示窗口保持独立。缠论、完整政策运行、角色映射及业务回填继续待完成；无数据库写入、无终端操作，全量自动升级和删除清理尚未完成。
