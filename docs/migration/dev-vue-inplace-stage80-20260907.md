# 阶段 80：订单全字段来源校验

`v4-payment-order-source.mjs` 纳入 orders 的全部 23 个字段，逐项检查类型、NULL、文本长度、精确金额、用户父关系及墙钟日期，并保留完整源值/hash。订单金额、amount_confirmed、推荐抵扣和 crypto_amount 是独立字段，不以 truthy fallback 替换 0，不将链上期望金额当作到账事实。导入余额变动为 0，不触发支付/权益副作用。

新增真实只读工具 `review-dev-vue-payment-order-source.mjs`，适配完整 47 步结构，显式投影读取全部字段并核对原 165 表/271007 行。两次结果一致，12 行/23 字段全部可精确读取，来源 hash 为 `4c34f0462b1792e08f9b02ed782c3cc4e05588fddbb68b0c8de652658f7d8532`。报告不导出原订单标识、地址或完整业务行。

12 行均含尚未确认历史时区的 DATETIME，因此全订单 UTC 转换仍标记 `historical_time_basis_required`。原值保留；新目标合同需要明确未知历史时间的处置，不能用 MT5 展示默认 UTC+3、当前 MySQL 时区或迁移登记时间冒充订单发生时间。本次没有为其它语义作自动推断。

4 项源值测试通过，覆盖 23 字段完整性、零值/NULL、父关系、原周期、未知状态及负金额。真实读取验证如上，不代表订单目标表已创建或全部业务语义转换完成。下一步设计订单主体及支付关联的目标合同；旧历史、自动部署升级和删除门仍待完成。
