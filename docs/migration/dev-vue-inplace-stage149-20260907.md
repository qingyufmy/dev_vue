# 阶段 149：配置凭据来源状态预检

已追加 `scripts/review-dev-vue-settings-credential-state.mjs`，复用当前 58 步结构协调器、数据库 UUID 和旧八字段摘要校验。只读一致性事务内由 SQL 分类；应用不接收凭据正文，不读取密钥，不调用旧版自动加密迁移。

| 来源 | 实际状态 | 后续转换条件 |
| --- | --- | --- |
| sms/access_key_id、sms/access_key_secret | 非空，具有版本及 iv/ct/tag 字符串字段 | 验证密钥恢复、合法编码及认证解密后才能承接 |
| smtp/pass | 非空，具有相同封装形状 | 同上 |
| qiniu/access_key、qiniu/secret_key | 非空，不符合封装形状 | 区分原始文本与损坏封装，明确保护转换；不可直接当作已加密凭据 |

分类还覆盖 NULL 和空字符串：NULL 保留为待定语义，空字符串保留“未配置”，不得生成默认凭据或误报缺失密钥。本次五项均不是空值，因此不能通过空值豁免解除阻断。

此报告细化阶段 146 的统一凭据占位状态，不修改其历史报告。envelope_shape 只证明字段形状，未验证 Base64、长度、密钥可用性、真实性或供应商可用性。unprotected_or_invalid 不等于已确认明文。五项历史时间仍需独立依据。

## 验证与范围

语法检查通过；真实 dev_vue 的 --write 与独立 --verify 一致，58 步结构完整，五项来源八字段摘要与冻结清单一致。报告不包含正文或密钥。未执行 DDL/DML、回填、旧消费者切换或服务重启。

第一轮定向复核：只追加预检，保持历史迁移与 proof 文件不变；分类不改变来源字节，也不声称完成迁移。

第二轮定向复核：JSON_VALID 外层分支保护损坏 JSON；所有字段使用参数化范围，身份、结构、覆盖率与源摘要不符均失败；异常输出固定错误码。独立重复核验通过。下一步是凭据保护转换及恢复验证，之后连同时间依据接入真实回填；全域规范化和自动升级尚未完成。
