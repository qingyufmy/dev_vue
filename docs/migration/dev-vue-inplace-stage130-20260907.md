# 阶段130：配置八字段转换与受限写入

新增prepareSettingsRows与createSettingsWriter。转换保留原system_config八字段及完整来源hash，生成system_settings全部15列；value_text不trim、不JSON重编码、不补默认值，label/sort_order/NULL原样保留。创建/更新时间各有原值、偏移与证据引用，不能复用未经核实的默认时区。

每行逐键语义证据绑定原行hash、快照及完整待决要求。凭据必须附密钥恢复/认证解密依据，JSON数组附条目语义依据，地址附链地址依据；传入证据目录由后续批次准入核实真实性，本转换器不自行读取文件或解密。合成测试中的证据不能用于真实回填。

writer冻结准备结果，写前锁定源八字段并精确比较；目标15列全量回读。完全一致的重复记录不写入，冲突拒绝覆盖；verifyOnly不能创建记录，INSERT响应异常不自动重放。事务提交、来源归档、映射、回执及提交未知恢复留给批次框架接入。secret保持secret，不能修改准备条目后提升为public。

14项相关测试通过：原字段保留、独立时间/NULL、语义证据缺失、未知/用户配置排除、凭据要求、每个目标字段冲突、source漂移、写后损坏及连接异常。证据为纯转换/Mock连接，当前库没有本轮DML。

下一步接入57步批次准入、同事务来源归档和独立对账，再做真实恢复副本演练。当前历史时间与真实语义依据尚未全部取得，配置表仍未回填；system_config及其全部75行保留，全库规范化未完成。
