# 阶段 146：当前配置回填就绪度与历史时间核对

新增只读review-dev-vue-settings-readiness.mjs，使用58步协调器、原始结构校验和逐配置八字段摘要核对。59个候选源行摘要均与原库存一致；两次一致性快照生成/复核报告完全相同。SQL直接遮蔽五个凭据值，凭据内容不传入审查脚本或报告；非凭据值仅内存检查，报告不输出正文。

59项配置的created_at及updated_at全部非NULL，54项非凭据通过既有基础类型检查，5项凭据仍待密钥恢复/解密证明。当前没有能仅按source_null路径直接回填的配置，也没有业务语义已完整验收的批次。就绪报告为dev-vue-settings-readiness-review-20260907.json。

## 时间依据新证据

参考后端server/db.js当前设置MySQL连接timezone与会话time_zone为+08:00，但git历史显示会话SET语句由29599418在2026-07-08 13:49:50+08:00引入。源配置包含2026-06-12起的数据，不能用这段当前代码反推所有旧行。

按该提交作者墙钟作纯文本比较，有40行创建时间、16行更新时间早于该时刻。这只是排查分段，提交作者时间不是部署时间，更不是来源行写入证明；较晚行也不能据此自动批准+08转换。

旧后台还将system_config.updated_at用于分类最近更新和更新说明展示，丢弃时间不满足完整功能保留；不能把这些时间直接设NULL再宣称回填完成，也不能将终端UTC+3用作业务配置历史时间。核对记录见dev-vue-settings-time-source-review-20260907.json。

## 验证与后续

新脚本语法、实际只读生成与独立复核通过，无数据库写入。首次SQL构造误把值遮蔽表达式放入摘要函数，MySQL拒绝查询；修正为保留原八字段摘要、只遮蔽传回列后两次通过，没有产生源数据变更。

下一步继续寻找历史数据库会话/部署证据，并完成各键业务语义审查；证据不足的行保持待迁移。当前58步结构与页面能力不能替代真实配置回填。其他域规范化、全版本自动升级和旧结构清理也未完成。
