# 阶段182：学习域真实来源与迁移语义审查

本地工具 `scripts/audit-learning-import-local.mjs` 使用现有env连接dev_vue，在REPEATABLE READ只读事务内读取来源、关联用户和目标身份；没有DDL、DML或应用服务启动。输出写到仓库外的私有文件。

## 已核实的当前数据

| 检查 | 结果 |
| --- | --- |
| 来源课程 | 12条 |
| 来源学习进度 | 5条 |
| 用户/课程关联缺失 | 0 |
| 访问权限 | 12条均为logged_in |
| 发布状态 | 12条均为published |
| 媒体引用 | 12个bilibili_id，无其他非空媒体类型 |
| 观看时长超过报告总时长 | 2条，保留原值，不截断 |
| 非空历史时间 | 课程24字段、进度5字段，共29字段 |
| 当前学习目标四表 | 全部为空 |

当前完整来源摘要：

- courses：`adb1e720574773b724ff87b184ce3317042d9671b6b2e0da77f7b8857ef1f27a`
- progress：`63931036b6a068f0fd0ffdeb7be6e523405e67bc05c29b80792e3ea898d4b75d`

## 迁移语义

只读参考旧仓库的db.js、admin/content-system.js、routes/user.js、membership.js、routes/courses.js及public/src/main.js。私有语义记录绑定这些文件的SHA-256，未修改参考仓库。

- logged_in保持登录限制，published保持发布状态，不扩大访问权限。
- 课程、观看时长、报告总时长以冻结SQL数值文本执行秒到毫秒的精确转换；NULL、完成和答题标记按原值保留。
- bilibili_id按明确媒体类型原样保留，不归一化标识、不据此宣称视频当前可播放；本轮没有访问外部视频平台。
- 旧投影计数和has_stream_video保存在完整来源存证中，资源域迁移后再重建，不能用空目标统计覆盖原值。

## 待确认的历史时间

现有旧代码显式设置MySQL会话为UTC+8，课程更新及进度更新用NOW()，新建记录由数据库默认时间写入。这证明当前参考代码的写入约定，不能独立证明所有历史记录当时的部署配置。

已询问用户是否确认这些历史无时区时间按北京时间解释。答复前，历史时区保持未确认，不登记UTC+8为已验证事实，不将MT5默认UTC+3用于这些应用时间，不向当前库执行学习业务回填。

## 证据与下一步

私有目录 `D:/dev_codex/_private-recovery/mysql-20260907-01` 保存 learning-source-review-01.json及learning-value-review-01.json。前者包含完整来源、用户关联集合和逐行转换审查，后者记录来源代码摘要及保留语义。语法检查、实际--help及真实数据库只读读取通过。

时间依据确认后，生成绑定当前来源摘要的真实清单，预检并核验阶段181的演练证明，再提升当前库回填入口。现阶段没有回填、切换消费者或删除旧结构，全面数据库规范化仍未完成。


2026-09-07 后续：用户要求继续核对旧代码，已完成 Git 与实际库配对时间检查，详见[阶段184](dev-vue-inplace-stage184-20260907.md)。不再以重复询问用户时区作为下一步；按历史来源证据推进，当前候选尚未提升为可写清单。
