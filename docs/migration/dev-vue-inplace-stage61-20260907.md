# dev_vue 同库升级阶段 61：宏观来源与目标字段登记

新增只读入口 `node scripts/review-dev-vue-macro-source.mjs --write|--verify`。当前 dev_vue 两次真实读取结果一致，报告为 `dev-vue-macro-source-review-20260907.json`。

- 校验完整 40 步物理结构；原 165 表、271007 行原始字段对账一致。
- 登记 11 张宏观/经济日历目标表的全部 189 个字段，包含实际类型、NULL 和默认值。
- 冻结旧库目录没有与这 11 张表同名的专用表；75 个 system_config 分类/键名未匹配 macro/economic/calendar/fred/宏观/财经/经济日历。扫描不读取配置值，落盘只记录键集合 hash 与匹配项定位 hash。
- 目标表均未迁入历史行，也未执行新域初始化。

本报告仅覆盖专用表名与命名配置筛查。它不证明旧推理快照、提示词或 JSON 中没有嵌入宏观内容，不能据此删除旧历史。189 个字段的物理登记也不等于逐字段转换合同已通过。

明确剩余工作：嵌入旧快照内容的来源处置、新域提供商/模型/特征集初始化合同、宏观数据创建和采集运行链路。没有专用旧表时不能凭空构造观测、日历事件或历史模型版本；新域是否可用应由初始化及运行验收证明。

本批只读，无 DDL/DML、服务或模型启动。完整规范化、业务数据迁移、部署自动升级与旧结构清理仍未完成。
