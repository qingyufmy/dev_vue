# 旧版升级链检查（2026-09-15）

## 结论与范围

本轮只读检查源码及开发库 192.168.1.254/dev_vue，并修正两处迁移测试。没有执行 DDL/DML，没有连接虚拟机站点或公网实例，没有改动任何已执行 SQL 文件。现有证据不足以支持直接升级旧版。

必须澄清：执行迁移 SQL 文件与通过迁移执行器登记完成是两回事。本次 031 通过读取文件执行建表，未经过统一迁移记录流程；不能把它描述为升级登记也已完成。

## 发现

1. 新库迁移链完整读取 bootstrap + 001—031，共 32 个条目、174 条语句。migrate-v4-schema.mjs 配合 v4-schema-migration-runner.mjs 有目标确认、独立连接、数据库锁、文件校验和、语句检查点与显式恢复规则。该入口禁止源库等于目标库，拒绝无管理记录的非空数据库及不兼容历史表，不能直接用于当前旧库。
2. 开发库 schema_migrations 是旧结构（id varchar(255)、applied_at datetime），与新库执行器要求的 checksum/status/checkpoint 结构不兼容。已有原地升级记录放在 database_upgrade_steps_v4，不能覆盖旧历史表或把新库执行器的保护关掉来升级。
3. 已读到 market_source_selections、model_configuration_receipts_v4、user_model_assignments_v4 三张表。对应原地升级记录仅找到 inplace_081_01_market_source_selections；旧 schema_migrations 中没有 20260914/20260915 条目。030、031 缺少这两套记录中的登记。存在表不能直接视为已完成迁移，必须核对字段、约束、索引、表属性及文件摘要后，以新的核对回执确认，不能伪造原执行时间。
4. upgrade-dev-vue-schema.mjs 绑定 dev_vue、server/.env 和特定备份 serverUuid/演练证明，不能直接拿去对任意虚拟机/公网库执行。028 有独立原地来源注册；029 有 apply-market-source-schema-local.mjs 专用脚本。最近变更尚未证明已由同一个可携带的旧库升级入口完整覆盖。
5. scripts/deploy/README.md 明确远端宝塔 deploy.sh 不在本仓库；目前无法证明实际远端发布脚本如何组织备份、迁移及重启。应在目标环境升级前单独核对，不能用本地检查替代。

## 本轮修复与验证

- v4-schema-migration-runner.test.js 更新 028—031 顺序和总语句数断言，保留原有前缀断言。
- bridge-installation-upgrade-source.test.mjs 按固定迁移 ID 查找 028，不再假定它永远是最后一个文件。
- 64 项 Vitest 迁移/恢复/纠正/升级审查测试，加 1 项 Node 来源测试全部通过。
- 开发库只读检查证明的是当前结构与记录情况；尚无空库真实建库、旧库恢复演练、真实中断恢复或远端发布证据。

## 补齐步骤

1. 复用 database_upgrade_steps_v4 的原地升级机制，整理最新结构来源与清晰的统一入口。输入目标连接参数及确认目标，默认只读计划，不另建第三套台账。
2. 为最近增量增加明确前置条件：不存在时执行原迁移来源；存在且有匹配记录时跳过；存在但无记录时输出待核对，不盲目重放 CREATE，也不直接补写 completed。
3. 为已存在的 030、031 准备结构核对与登记方案。原 SQL 文件保持不变，登记记录注明核对时间、来源摘要与实际结构证据。任何真实补登记必须另行授权。
4. 在经过确认的旧版备份恢复库中，完成升级、重复运行、故障中断与恢复、关键业务数据对账。校验代码与迁移包一致后再准备虚拟机及公网升级。

## 两轮复审

第一轮（覆盖与简单性）：保留已有新库/原地升级职责，复用原地台账，避免为了新功能增设第三套执行系统。不能把历史专用脚本数量多误判为统一升级已完成。

第二轮（兼容与恢复）：不修改旧 schema_migrations 或已执行 SQL，不根据表名自动登记成功，不伪造已执行历史。先准备来源清单及只读计划，再在独立恢复库获得结构、重复执行和恢复证据。剩余风险为旧生产结构未知、现有历史记录不齐、远端部署入口未核实。

## 最近增量只读入口（第二阶段）

入口：`node scripts/inspect-recent-upgrades.mjs --env-file=server/.env`。连接参数从显式指定文件读取，可以检查其它已授权的恢复库；不接受 apply 参数，不执行写语句、不创建台账。返回 0 表示这三项都已有匹配记录和结构，2 表示存在待执行/待核对/冲突项，1 表示连接、来源或台账检查失败。退出 0 也不代表整个旧库升级就绪。

来源清单锁定 029—031 原始文件 SHA-256，复用 081 的原有摘要算法。为 030/031 预留 082/083 步骤名，仅在只读清单中声明，未登记数据库。检查字段顺序、类型、可空、字符排序、默认值、额外属性，索引列顺序、唯一性、前缀、类型、可见性、方向，以及外键目标/更新删除动作、引擎、表排序规则、触发器和检查约束。此报告是一次只读观察，不持有升级锁，不能作为未来写入时跳过重新检查的凭据。

开发库检查结果：029 结构与既有 081 记录匹配；030、031 检查范围内结构与原 SQL 匹配，但没有对应升级记录，均返回 reconciliation_required。未重放 CREATE，未补写 completed。

第一轮复审：只做最近增量清单和诊断，复用现有台账，不引入新的历史系统或自动修复。第二轮复审：文件摘要变化、缺表但有完成记录、结构差异、未完成记录分别阻断；相同表名不等于已完成。后续实际登记必须在授权后持有现有升级锁，重新核对并记录新的核对时间，不能声称恢复了原始执行日志。

## 030/031 核对登记流程（第三阶段）

新增 `scripts/reconcile-model-upgrade-history.mjs`，仅允许登记 082/083 两个既有表来源。必须显式提供 env-file、confirm-target（host:port/database）、server-uuid、独占新建 receipt 文件及 apply。不会创建业务表、不更改迁移 SQL、不修改已有台账记录。校验和沿用只读入口与 081 的算法。

执行先核对数据库身份，取得现有 aurum:inplace:<database> 锁，在事务内读取三表取得元数据锁并重新检查结构和台账，确认 081 完成。只有 reconciliation_required 的目标可以插入；既有完成记录跳过，其他状态阻断。两条登记同一事务，先持久化 prepared 核对证据，再写台账和提交，再保存 committed 证据。台账时间是本次登记时间，证据明确 originalExecutionTimeKnown=false。提交返回不确定或提交后证据写失败时要求只读核对，禁止把未知提交当作普通失败。

第一轮复审：复用现有升级台账，只登记本次缺失两项，不增加新的业务表或修改已执行迁移。第二轮复审：目标/实例/锁/前序步骤/结构冲突/部分失败均阻断，已有记录不覆写；独占证据文件防止覆盖；不宣称整个旧库已升级。实际写库仍需独立授权，恢复库演练尚未执行。

### 第三阶段实际验收

用户明确授权后，核对登记脚本在 192.168.1.254:3306/dev_vue 成功新增 082、083 两条记录。已保存 model-upgrade-registration-20260915.jsonl，包含 prepared/committed 两阶段回执。再次只读检查退出 0，029—031 全部 completed，结构摘要无变化。20 项相关离线测试通过；未执行业务 DDL 或模型/交易数据写入。当前结论仅覆盖最近三项，不代表旧版本全部升级或恢复演练完成。

后续优先整合从旧库实际创建缺失表的统一升级入口，不能直接重跑只接受旧步骤集合的历史专用脚本；然后在明确指定的旧库恢复环境中演练。核对登记脚本不能替代缺表迁移执行器。
