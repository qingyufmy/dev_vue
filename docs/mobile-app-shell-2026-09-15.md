# 移动端 App 体验第一阶段

范围：小于 768px 的应用导航与首页。底部固定五个入口，更多使用底部面板；顶栏时间、账户入口保持可见，状态可展开。首页账户指标双列。桌面不变。

复审一：复用已有路由与组件，保留全部功能入口，不新增业务状态或接口。运行控制始终单实例。

复审二：底部导航位于主滚动区域外并留安全区，面板支持关闭和键盘访问；页面切换关闭更多，隐藏导航不触发操作。验证移动与桌面布局，保持交易发送状态不变。

后续：各业务页面逐页处理表单、键盘、安全区和返回恢复；本阶段不宣称所有移动页面已经完成。
