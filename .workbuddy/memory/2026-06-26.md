# 2026-06-26 工作日志

## 管理员数据看板优化
- 倒计时融合到刷新按钮内部
- 自动刷新改为轻量更新(updateAdminDashboard)，不重建DOM/图表
- /ai 页面添加 presence 心跳(60秒)，更新 last_seen_at 在线计数

## 桥接软件 SSL 修复 (v1.9.11 → v2.0.0)
- SSL BAD_LENGTH 错误: 数据推送/心跳失败后立即断开重连
- 断路器机制: 连续5次快速断开(10秒内)停止重连
- 新增 TimeoutError 捕获(观摩账号超时)

## 桥接软件方案B: websockets 异步库迁移 (v2.0.0)
- websocket-client → websockets(asyncio) 异步架构
- SSL context: TLS 1.2+, 现代 cipher 套件
- 数据去重: hash 对比，数据无变化时跳过发送
- MT5 API 在 executor 中执行，不阻塞事件循环
- AURUM_Bridge.spec 加入 websockets hiddenimports

## Redis 缓存层
- 新增 server/redis.js: Redis 客户端模块，安全降级(Redis 不可用时走 MySQL)
- 桥接数据写入 Redis(TTL 3s): bridge:data:{userId}
- account/positions/quote 读取优先查 Redis 缓存
- signals/admin_dashboard/user_list 查询加 Redis 缓存(TTL 10s)
- 历史数据 Redis 长期缓存(5分钟 TTL)，持仓 ticket 变化时主动失效
- Redis: 192.168.1.254:6379, 配置在 server/.env
- npm install ioredis

## Bug 修复
- ws:// 时不传 ssl 参数(仅 wss:// 使用 ssl_ctx)
- stop() 中 self._loop 未赋值: 改用 asyncio.new_event_loop()
- asyncio.get_event_loop() → asyncio.get_running_loop()
- 持仓字段统一为 price_open/price_current/time/digits

## 观摩/私人模式分离
- 核心规则: hasOwnBridge → 用自己数据, 无桥接 → 用 admin 数据
- 修复: account/positions/quote/signals/signal_detail/signals_latest_id/audit_logs/signal_tickets/close_signal_tickets
- 统一使用 `hasOwnBridge = bridges.has(userId) && bridges.get(userId).ws?.readyState === 1` 判断
- 去除旧的"无数据才回退admin"逻辑，改为直接判断桥接状态

## 杠杆(leverage)数据修复
- 问题: 前端杠杆显示"--"，无真实数据
- 根因: 桥接实时推送(_collect_mt5_data)的 account 中缺少 leverage 字段，Redis 缓存中无此数据
- 修复: 
  1. 桥接 aurum_bridge_gui.py: _collect_mt5_data() 中加入 `"leverage": acc.leverage`
  2. 前端 app.js: handleBridgeData() 中实时更新杠杆显示
- 版本号更新: app.js?v=20260626v
