# 2026-06-25

## 修复：自动推理桥接回退 bug
- **问题**：`getBridgeTradeMode()` 有 admin 桥接回退逻辑，导致所有数据库用户（即使没连接自己的桥接）也通过 admin 的 MT5 运行自动推理
- **修复**：新增 `getOwnBridgeTradeMode()` 函数（`bridge-ws.js`），只检查用户自己的桥接，不回退到 admin
- **修改文件**：
  - `server/bridge-ws.js`：新增 `getOwnBridgeTradeMode()` export function
  - `server/routes/ai.js`：`runAutoCycle`、`runSmartCloseCycle`、`startSmartCloseScheduler`、`startAutoScheduler` 全部改用 `getOwnBridgeTradeMode`
- **提交**：`main` 分支 `5c7037e`，仅包含以上2个文件修改，无数据看板代码

## 全面审计 + 修复 /ai 系统 11 个 bug
- 审计报告：5高危 + 4中危 + 2低危
- 修复内容 (`f4eeb54`):
  1. 删除对 `<span>` 设置 `.checked` 的无效代码 (Bug #1)
  2. 桥接断开/disconnect 时更新 auto badge + loadStatus (Bug #2, #11)
  3. `handleAutoToggle` 先等 `loadStatus` 再读 `state.autoConfig` 构建 badge (Bug #3)
  4. `auto_status` 返回实际调度器运行状态 (`isAutoSchedulerRunning`) 而非镜像 `enabled` (Bug #4)
  5. `handleAutoToggle` 添加桥接连接验证 (Bug #5)
  6. `startAutoScheduler` 消除竞态条件：在 await 前设置占位 state (Bug #6)
  7. 心跳处理器添加休市检查 `marketClosed`，不再覆盖休市 badge (Bug #7)
  8. Plus 用户补充 `clickable-badge` class (Bug #8)
  9. `handleBridgeData` 市场状态变化时刷新 auto badge (Bug #9)
  10. `handleAutoToggle` 添加防抖锁 `_autoToggleLock` (Bug #10)
- 修改文件：`public/ai/app.js`, `public/ai/index.html`, `server/bridge-ws.js`, `server/routes/ai.js`

## xlsx 本地化 + 按需加载
- 下载 xlsx@0.18.5 (881KB) 到 `public/ai/js/xlsx.full.min.js`
- HTML 中移除 CDN 引用，改为仅管理员导出时动态注入 `<script>` 标签

## dev 分支重置 + 文案调整
- 将 dev 分支强制重置为 main（放弃之前的数据看板代码）
- 推理页面文案调整：
  - 推理正文 → 分析正文，行情判断 → 行情分析
  - 推理依据 → 分析依据，历史推理 → 历史信号
  - 展开完整推理 → 展开完整分析，收起推理正文 → 收起分析正文
- **提交**：`dev` 分支 `c69a066`

## 管理员数据看板
- **功能**：管理员专属数据看板，通过侧边栏"看板"入口访问
- **数据内容**：
  - 系统概览：总用户/今日新增/实时在线/桥接数/总收入/付费订单
  - 信号分析：总信号/今日/本周/已执行/平均置信度 + 类型分布饼图 + 品种柱状图 + 30天趋势折线图
  - 交易审计：总操作/成功/失败/成功率 + 操作类型分布 + 14天操作趋势
  - 用户排行：信号数TOP10 + 最近在线TOP10
  - 在线桥接：当前所有连接的桥接列表
- **后端**：`bridge-ws.js` 新增 `admin_dashboard` WebSocket handler，12个并行SQL查询
- **前端**：Chart.js@4.4.7 本地化，admin-only 控制可见性
- **提交**：`dev` 分支 `c803266`

## 看板加载失败排查 (19:19)
- **问题**：管理员看板页面报 `Unknown column 'profit' in 'field list'`
- **排查**：逐一测试 admin_dashboard 12条SQL全部通过，服务器端代码无 `profit` 列引用
- **根因**：旧服务器进程未加载最新 admin_dashboard handler 代码
- **修复**：重启服务器后看板正常加载
