# 2026-06-24 工作日志

## 全面代码审计第二轮修复（20项安全与稳定性问题）

### 致命级（7项修复）
- **SSRF**: bilibili-proxy 严格校验 hostname（new URL + endsWith）
- **命令注入**: bat 安装脚本改用 Base64 编码传 config
- **JWT 回退**: ai.js 移除 `|| 'wall-street-skill-secret'`，统一从 config.js 导入
- **配置暴露**: system-config-public 添加白名单（PUBLIC_CATEGORIES Set）
- **XSS**: feedback.js 邮件 escapeHtml 完整转义（&、<、>、"、'）
- **DOM 竞态**: apiProvider 事件绑定移到 bindEvents()（DOMContentLoaded 后）
- **Python 线程安全**: aurum_updater 后台线程改用 root.after() 调度 GUI

### 高危级（10项修复）
- SQL 空 IN 子句：admin.js 空 userIds 提前返回
- Bridge 竞态：会员检查期间缓冲消息，检查完重放
- Map 内存泄漏：_broadcastThrottle 改为模块级+定时清理
- 重复路由：user.js PATCH /notifications 第二个改为 GET /progress
- 用户删除：补充清理 12+ 张关联表
- 视频上传：execSync → 异步 execFile（ffprobe/ffmpeg）
- 支付禁用：POST /payment 返回 503（待接真实网关）
- Python 平仓：检查 order_send 返回值，返回 partial 状态
- Python 会员：check_plan 异常时 expired=True（fail-safe）
- Python 命令解析：用 .get() 防 KeyError 断连

### 中危级（5项修复）
- 密码重置 verifyToken 使用后标记 used=1
- admin.js bcrypt.hashSync → await bcrypt.hash()
- Python 分页：int() 转换 + 边界检查
- Python websocket ImportError 时 mt5.shutdown()
- Python updater PID 参数 ValueError 处理

### 验证
- 服务重启成功，curl 验证：SSRF 返回 400、smtp 接口返回 403、payment POST 返回 503、toolbox 接口正常
- 提交: c686f41，推送 dev 分支
