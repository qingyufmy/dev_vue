# 2026-06-24 工作日志

## 续：删除复盘 + 信号执行重构 + 安全规则
- 删除了全部复盘(Trade Review)逻辑（~270行），不再自动4小时复盘审计
- 信号执行改为精准风控参数获取 `getExecuteRiskConfig`，不再全链路穿透 `getActiveConfig`

## 手动推理脱敏 + 自动执行
- 17:51: 新增 `getAnalyzeApiKey` 函数，手动推理只走模型共享一条回退（管理员 model_sharing_enabled=1 时共享 API Key + 模型参数），不做 system_config 和 global_auto_config 穿透
- handleAnalyze 中 `getActiveConfig` 替换为 `getAnalyzeApiKey`
- 手动推理信号生成后，若用户 enable_auto_trade=1 且 signal_type != hold，自动通过 mt5Bridge('open') 执行

## model_sharing 配置展示修复 + JS/CSS 版本号
- 17:59: ai_config handler 修复：无配置用户 + 管理员 model_sharing=1 时，返回 `_model_shared=true` 标记，前端显示"使用管理员共享模型"
- 所有项目 JS/CSS 文件统一加 `?v=20260624` 版本号，防止浏览器缓存旧文件
- dev commit: 048a4ed
- dev: f00e6f7 → main: cc3fe6a
- 自动信号标记 `config_id=0`，用于区分来源
- dev → main 合并
- 🔴 新增安全红线：自动推理 system_prompt 为一级敏感数据，永远不能让任何用户查看

## 自动推理重启后不运行修复
- 根因: bridge close handler 调用 upsertAutoConfig 将 auto_scheduler.enabled 清零
- 服务器重启时所有 close handler 异步触发 → DB 全部 enabled=0 → initAutoSchedulers 找不到任何人
- 桥接重连后 _initBridge 虽尝试恢复，但 UI 已显示 ON(来自 user_bridge_settings) 而 DB 为 OFF
- 修复: close handler 仅停止内存定时器，不再修改 DB 中 auto_scheduler.enabled

## v1.9.9 打包上传
- 合并 dev→main，打包 AURUM_Bridge.exe 上传七牛云
- EXE: 65,335,672 bytes, MD5: d20287cc7de17710bc20a051a921316a
- 版本号 1.9.9，含 MT5 报价重试/信号绑定/审计修复/API Key 兜底

## MT5 报价错误 (Requote/Price Off) 修复
- 根因: `_get_filling_mode` 优先 FOK（零滑点），黄金波动大时必挂；`_process_command` 无重试
- 修复: IOC 填单模式优先；新增 `_order_send_with_retry` 对 10003/10004/10006/10024 重试3次，延迟150ms刷新 tick

## 登录成功后页面不刷新修复
- 18:36: persistAuthSession 中 renderView() 只在 progress.syncFromServer() 异步回调里执行，同步失败导致静默跳过
- 修复: closeModal() 后立即同步调用 renderView()，移除冗余 state.currentView === 'home' 单独判断
- main.js 版本号 → ?v=20260624b
- dev: 427ee91, main: add2dcc

## 信号-订单绑定 & 复盘触发异常修复
- 根因1: executeOrder 成功后只保存 is_executed/execution_result，未提取 result.order 写入 ai_signals.trade_ticket → 信号页面无法跳转持仓
- 根因2: trade review 无前置条件检查，即使窗口内无任何交易也调用 AI → 出现"三条成功"误导审计
- 根因3: 部分 MT5 券商在 requote 时自动成交（result.order>0），旧代码无此检查导致重试开重复仓位
- 修复: signalOrderPayload 已含 signal_id；executeOrder 成功后提取 ticket 写入 trade_ticket；trade review 三空(无信号/无成交/无持仓)跳过；first-run 延迟 30s→300s；桥接 `_order_send_with_retry` 检查 result.order>0 不再重试避免重复开仓
- open/close(单仓) 操作改用重试包装器

## 全面代码审计第二轮修复（20项安全与稳定性问题）

### 致命级（7项修复）
- **SSRF**: bilibili-proxy 严格校验 hostname（new URL + endsWith）
- **命令注入**: bat 安装脚本改用 Base64 编码传 config
- **JWT 回退**: ai.js 移除 `|| 'wall-street-skill-secret'`，统一从 config.js 导入
- **配置暴露**: system-config-public 添加白名单（PUBLIC_CATEGORIES Set）
- **XSS**: feedback.js 邮件 escapeHtml 完整转义（&、<、>、"、'）
- **DOM 竞态**: apiProvider 事件绑定移到 bindEvents()（DOMContentLoaded 后）
- **Python 线程安全**: aurum_updater 后台线程改用 root.after() 调度 GUI

### 高危级（10项修复）
- SQL 空 IN 子句：admin.js 空 userIds 提前返回
- Bridge 竞态：会员检查期间缓冲消息，检查完重放
- Map 内存泄漏：_broadcastThrottle 改为模块级+定时清理
- 重复路由：user.js PATCH /notifications 第二个改为 GET /progress
- 用户删除：补充清理 12+ 张关联表
- 视频上传：execSync → 异步 execFile（ffprobe/ffmpeg）
- 支付禁用：POST /payment 返回 503（待接真实网关）
- Python 平仓：检查 order_send 返回值，返回 partial 状态
- Python 会员：check_plan 异常时 expired=True（fail-safe）
- Python 命令解析：用 .get() 防 KeyError 断连

### 中危级（5项修复）
- 密码重置 verifyToken 使用后标记 used=1
- admin.js bcrypt.hashSync → await bcrypt.hash()
- Python 分页：int() 转换 + 边界检查
- Python websocket ImportError 时 mt5.shutdown()
- Python updater PID 参数 ValueError 处理

### 验证
- 服务重启成功，curl 验证：SSRF 返回 400、smtp 接口返回 403、payment POST 返回 503、toolbox 接口正常
- 提交: c686f41，推送 dev 分支

## 第三轮：功能逻辑审计与修复（10项：3致命 + 7高危）

### 致命级
- **F1**: WS连接失败bootstrap永久挂起 → onClose/onError 中 _fireReady() resolve
- **F2/F3**: 所有开仓/平仓DEAL订单添加price字段（tick.ask/bid）

### 高危级
- **api.js**: 抽取 _safeJson() 统一JSON解析保护（post/put/del/patch/postForm）
- **confidence显示**: 自适应0-1和0-100两种标度
- **authMiddleware**: 统一会员过期检查并自动降级（原来只在/auth/me）
- **用户删除**: withTransaction 事务保护17个表操作
- **save_config**: withTransaction 事务保护先停用后写入
- **Python开仓方向**: 缺少type时显式拒绝而非默认BUY
- **Python modify**: 实现TRADE_ACTION_SLTP修改持仓止损止盈

### 基础设施
- db.js 新增 withTransaction() 事务辅助函数

提交: 9650e38 → 21b43c9 → 5c90e11 → d5a6a8a → 259a73c → 5e28c23 → d9677e3 → c866b10

## v1.9.8 版本发布

- 版本号: aurum_bridge_gui.py APP_VERSION → "v1.9.8"
- 后端接口: /api/ai/bridge/version → version: '1.9.8', build_date: '2026-06-24'
- 打包: PyInstaller 打包 AURUM_Bridge.exe (65,197,229 bytes, MD5: 8fb41bceea2f58ed3f2f06811d9366d8)
- 上传: 七牛云 aurum bucket → https://qiniu.acadfx.com/AURUM_Bridge/AURUM_Bridge.exe (hash: loUfchm5JMeyH_vw0AwXHA83WsE4)
- Git: 提交 8b491b6 → push dev → merge main (68ac333) → tag v1.9.8

## CORS 修复 — 生产环境 Origin 头白名单
- 服务器 Nginx SSL 终结后转发 Origin 为 HTTP，白名单缺少 http://www.cnfxtrade.com
- 添加诊断日志 (CORS拒绝时输出被拒Origin)
- 补全 HTTP 变体: http://www.cnfxtrade.com, http://cnfxtrade.com
- 提交: 7426df8 (诊断) → af4964c (补全) → merge main (1543cdb)

## v1.9.8 重新打包 — MetaTrader5 修复
- 根因: spec 中 `collect_all('MetaTrader5')` 把 `_core.pyd` 当普通二进制而非 Python 扩展模块
- 修复: 改用 `hiddenimports=['MetaTrader5']` 让 PyInstaller 正确识别 .pyd
- 同步: aurum_bridge_gui.py 导入前探测 MT5 安装目录+详细异常输出
- EXE: 65.3MB, MD5=df7eccc3b10a1a539a16e0edce1c4b39
- 已上传七牛云 aurum/AURUM_Bridge/AURUM_Bridge.exe
- 后端 /bridge/version 已更新 MD5/文件大小
- 提交: 8891e65 → merge main (9946d24)

## main.js — 彻底移除 YouTube Player 全部代码
- 删除: initYouTubePlayer() 函数 (28行)、destroyPlayer/startWatchTimer 中 ytPlayer 引用
- 清理: getCardBackground(YouTube封面回退)、renderEpisodeCard(hasCover/card-duration)、hasEpisodeVideo、getFilteredEpisodes
- 清理: 2处视频加载分支、renderEpisodeDetailTemplate(ytPlayer guard + ytPlayer div + youtubeId三元)
- 清理: 管理后台表单 (courseYoutubeId输入/提取/提交)、quiz按钮判断(!ep.youtubeId)
- 保留: watchTimer + accumulatedTime (Bilibili/local仍需要)、episode数据模型youtubeId映射(无害)
- 提交: 9e40830 → merge main (85ea278)

## main.js — 删除 YouTube Player 变量声明
- 删除: ytPlayer / watchTimer / accumulatedTime 变量声明
- ⚠️ 文件中仍有 6 处引用这些变量 (初始化、获取时长、付费控制)，需完整清理

## 品牌重命名 & DB 开关状态持久化（dev 分支）

### 品牌重命名
- **首页 Logo**: "华尔街没有技术" → "量见"，新增副标"道诚出品"（logo-subtitle CSS + 响应式隐藏）
- **全站「街哥」→「量见」**: index.html, main.js(20+处), site.webmanifest, auth.js, feedback.js, db.js, config.js 全部替换
- **AI交易系统品牌**: "AURUM"/"AURUM AI MT5 Control Panel" → "AI交易实验室"
  - index.html: title, brand-name, subtitle(清空), pro overlay
  - app.js: toast 下载提示更新
  - aurum_bridge_gui.py: 窗口标题、footer、交易comment

### DB 开关状态持久化
- **新增表**: `user_bridge_settings` (user_id PK, trade_send_enabled DEFAULT 0, auto_reasoning_enabled DEFAULT 0)
- **_initBridge()**: 异步读取 DB 设置 tradeEnabled，恢复自动推理调度器
- **toggle_trade**: 同步写入 DB (ON DUPLICATE KEY UPDATE)
- **toggle_auto**: 同步写入 DB
- **桥接断开**: 自动重置两个开关为 0
- **handleBridge**: 改为 async 确保 _initBridge 完成后再重放缓存消息
- **前端 handleHeartbeat**: 响应 trade_enabled/auto_reasoning_enabled 字段即时更新 badge

提交: 19be5e0，仅推送 dev 分支（未合并 main）

## 桥接开关状态逻辑修复（dev 分支）

### Bug 1 (严重) — admin tradeEnabled 强制为 true
- 根因: `dbTradeEnabled || true`，当 DB 值为 0 (false) 时，`false || true = true`
- 影响: 管理员手动关闭交易发送后，桥接重连仍会被打开
- 修复: 引入 `hasDbRow` 标记，admin 逻辑改为 `hasDbRow ? dbTradeEnabled : true`
  - 无 DB 记录 → 使用 admin 默认 true
  - DB 明确存 0 → 尊重 DB 值 false

### Bug 2 (严重) — 旧桥接 close 竞态条件
- 根因: `existing.ws.close()` 触发旧 close handler → `bridges.delete(userId)` + DB 重置
- 影响: 新桥接替换旧桥接后，旧 close 事件会删掉新桥接的数据并重置 DB 为 0
- 修复: close handler 中加入 guard 检查 `bridges.get(userId).ws !== ws`，不匹配则直接 return

### Bug 3 (优化) — browser 心跳缺少开关状态
- 根因: 普通心跳只返回 mt5_connected/mt5_alive，缺少 trade_enabled/auto_reasoning_enabled
- 修复: 心跳中补充两个字段（仅用户自有桥接时有意义，fallback 时为 undefined）
- 同步: initBridge 中 memory 对象添加 autoReasoningEnabled 字段，toggle_auto 同步更新

提交: 487baba，仅推送 dev 分支（未合并 main）

## 桥接 close handler DB 硬重置修复（dev 分支）

- 根因: close handler 每次断连将 trade_send_enabled + auto_reasoning_enabled 强制清零
- 用户手动开交易→关桥接→开桥接后走 _initBridge 读 DB 拿到 0→状态丢失
- 修复: 删除 close 中的 DB 重置(316-319行)，状态完全由 toggle_trade/toggle_auto 写入 + _initBridge 读取
- 保留: scheduler stop + auto_state 通知(断连时不跑调度器，重连时 DB 恢复)
- 保留: Bug#2 guard(旧close不删新bridge)
- 提交: 34a96b2，仅推送 dev 分支

## 心跳覆盖自动推理badge修复（dev 分支）

- 根因: browser 每 1 秒发 hb → 服务器回复含 `auto_reasoning_enabled` 布尔值
  → `handleHeartbeat()` 直接用裸文本 `"自动推理开启"` 覆盖 badge
  → 品种/间隔信息 1 秒内丢失
- 修复1: handleHeartbeat 优先使用 `state.autoConfig` 重建完整标签
  - 开启 + 有 config → `"自动推理运行中 · XAUUSD · 5分钟"`
  - 关闭 → `"自动推理关闭"`
  - 开启 + 无 config → 保持当前 badge 不变（等 loadStatus 填充）
- 修复2: `auto_state(bridge_connected)` 触发 `loadStatus()` 刷新完整状态
- 仅改 1 文件 `public/ai/app.js` (+16 / -3)
- 提交: 08d4b2b，仅推送 dev 分支

## 排查历史图表柱子点击 main vs dev 筛选字段不一致

- 问题: 服务器 main → 点柱子筛选**开仓时间**，本地 dev → 筛选**平仓时间**（正确）
- 排查: `f443aba` (2026-06-23) 已将 onClick 从 `filterEntryFrom/To` 改为 `filterCloseFrom/To`
- 结论: **main 分支 GitHub 上代码是正确的（平仓时间）**，问题在服务器端：
  - 可能没 pull 最新 main (`git pull origin main`)，或 pull 了没重启 (`pm2 restart all`)
  - 也可能浏览器缓存了旧版 app.js (Ctrl+Shift+R 强制刷新)
- 建议: 服务器 `git pull && pm2 restart all`，浏览器强制刷新

## v1.9.8 桥接 EXE 重新打包上传（品牌更新）

- dev → main 合并: 4个品牌/开关相关的 dev commit 合并到 main (19be5e0, 487baba, 34a96b2, 08d4b2b)
- PyInstaller 打包: AURUM_Bridge.exe (65,335,672 bytes, MD5: d20287cc7de17710bc20a051a921316a)
- 七牛云上传: https://qiniu.acadfx.com/AURUM_Bridge.exe (key: AURUM_Bridge.exe)
- ai.js 更新: file_size → 65335672, md5 → d20287cc..., changelog 更新

## MT5 多层级自动检测 + 手动路径选择器
- 文件: `public/ai/aurum_bridge_gui.py` (+244/-15)
- 4级降级探测:
  1. 注册表 HKCU — Software\MetaQuotes\Terminal\{INSTANCE}\InstallPath
  2. 进程检测 — wmic terminal64.exe/terminal.exe
  3. 注册表 HKLM (64-bit view) — 系统级安装
  4. 文件系统 glob — "MetaTrader*" / "*MT5*" / "*MetaTrader*5*" / "*MetaQuotes*"
- UI: QComboBox 下拉（显示来源标签）+ "浏览..." 按钮手动指定
- 手动路径持久化到 %APPDATA%\AURUM_Bridge\config.json → mt5_path
- APP_NAME → "AI交易实验室"
- dev commits: 7af9eff, 5662e1a（未打包、未合并 main）
- 从源码启动验证: PID 38452, 窗口标题 "AI交易实验室" ✅
- dev commit: 82231ad + 0fad27b (EXE 修正), main 已同步 (03c3574)
- 七牛云目录上传: AURUM_Bridge/ 目录覆盖上传
- 删除 AI 界面空 brand-subtitle 元素及 CSS（17b4cc7 → dev）
- 上传 AURUM_Bridge.exe (65,335,672 bytes) 到七牛云 qiniu.acadfx.com/AURUM_Bridge/AURUM_Bridge.exe，Hash: liXhlkRY1c4JlDPk62j3QraB7PiW
- 七牛云 AK/SK 已存储到 DB system_config 和项目 MEMORY
- 修复交易复盘 no_ai_key_for_trade_review bug：getActiveConfig 未兜底 global_auto_config，导致无自有 key 用户复盘全部跳过。新增 global_auto_config 作为第4级 fallback

## MT5 路径选择器移至设置页

- BridgePage: 删除 MT5 路径选择器 UI 和 5 个方法（_refresh_mt5_paths/_on_mt5_path_changed/_browse_mt5_path/_set_mt5_manual/_get_selected_mt5_path）
- BridgePage._toggle_bridge: 改为从 config 读取 mt5_path（不再依赖自身 UI）
- SettingsPage: 在账户信息卡片和版本信息之间插入 MT5 路径选择器卡片
- SettingsPage: 添加 5 个 MT5 方法 + __init__ 初始化 _mt5_installations/_manual_mt5_path/_mt5_loaded
- load_settings: 首次进入时 QTimer.singleShot 触发 _refresh_mt5_paths
- 按钮简化为"浏览"
- dev commit: 6ef3601（未合并 main，未打包）

## 合并 dev → main (2026-06-24 14:03)
- dev commit aa94533 merge 到 main (04ebf8a)
- 提交内容: bridge close handler 不重置 DB 自动推理标志、审计日志如实反映、信号 trade_ticket 绑定
- 已 push origin main + origin dev

## 自动推理重启恢复 — 三重保障 (14:08)
- **根因分析**: 两个存储表可能不同步 — `toggle_auto` 写双表，但 `save_auto`(legacy) 只写 `auto_scheduler` 不写 `user_bridge_settings`
- **修复1** `save_auto` handler: 同步写入 `user_bridge_settings.auto_reasoning_enabled`
- **修复2** `_initBridge`: 双表源检查 — 同时查 `user_bridge_settings.auto_reasoning_enabled` 和 `auto_scheduler.enabled`，任一为 true 就恢复调度器。`auto_scheduler` 有记录而 `user_bridge_settings` 没有时自动回写同步
- **修复3** `initAutoSchedulers`: 逐迭代 try/catch(一个失败不影响其他) + 详细日志 + `user_bridge_settings` 回退兜底(当 auto_scheduler 为空时从 settings 恢复)
- **修复4** `startAutoScheduler`/`runAutoCycle`: 静默退出点添加 console.log 追踪
- 本地验证: initAutoSchedulers 找到 6 个 enabled 调度器全部启动成功
- dev→main 已合并 (c1429a9)

## 桥接 EXE 修复 v1.9.9 (14:18)
### Bug 1: MT5 导入失败 (Multiarray failed to import)
- 根因: `os.add_dll_directory()` 只对 ctypes/LoadLibraryEx 生效
  - .pyd (Python C 扩展) 走 Windows 标准 DLL 搜索顺序, 依赖 PATH 环境变量
- 修复: 在 `add_dll_directory` 之外, 同时将 MT5 安装目录加入 `os.environ['PATH']`
- 错误日志改进: 输出完整 traceback 最后5行 + 安装提示

### Bug 2: APP_VERSION 仍为 v1.9.8
- 修复: 源码 `APP_VERSION = "v1.9.9"`

### 没桥接不调度 — 已确认
- 调度器 tick 循环检查 `getBridgeTradeMode(userId) !== 4` (无桥接→-1)
- `runAutoCycle` 也有同样检查
- 结论: 无桥接时调度器每5秒检查一次, 不消耗 AI API 调用, 逻辑正确无需修改

### MT5 路径选择器
- 代码存在于 SettingsPage (1577-1616行), 已合并到 main
- 当前七牛云 v1.9.9 EXE 打包时该功能未合并, 需重新打包
- dev commit: cdc0712 → main c1429a9（未打包）

## 记住密码不生效修复 (14:23)
### Bug: 勾选"记住密码"后每次仍要输密码
- 根因: `_do_login` L1305 总是 `cfg.pop("saved_password", None)`, 注释写"token已足够"
  - token过期时, auto_login失败, 用户必须手动输入密码, 记住密码形同虚设
- 修复:
  1. `_do_login` L1302-1306: 记住密码勾选 → 保存 `saved_password`; 取消 → 删除
  2. `LoginPage.load_config` L1266-1267: remember为true且有saved_password时预填密码
- dev commit: 5b4297f（后续合入main并打包）

## 合并 + 打包 v1.9.9 上传七牛 (14:26)
- dev→main 合并 (d8f26a8, 含所有修复)
- tag: v1.9.9
- PyInstaller 打包: AURUM_Bridge.exe 63MB (65343239 bytes)
- MD5: d4a102842df91e2726dc480e5db3d29d
- 上传七牛: https://qiniu.acadfx.com/AURUM_Bridge_v1.9.9.exe
- 服务端 /bridge/version 端点已更新
- dev commit: 24b796f → main d8f26a8
- 14:36: 通用兜底路径 AURUM_Bridge/AURUM_Bridge.exe 也覆盖为 v1.9.9 (之前仍是v1.9.8)
- 前端 app.js 兜底URL无需改, 通用路径自动跟最新版本

## numpy._core.multiarray 导入失败修复 (14:46)
- 客户软件报错: ImportError: numpy._core.multiarray failed to import
- 根因: MT5目录被 prepend 到 PATH, Windows DLL搜索优先找到MT5目录里的 msvcp140.dll/openblas,
  而非 PyInstaller 打包的 numpy 2.5.0 所需版本, 导致 numpy C扩展加载失败
- 修复 (aurum_bridge_gui.py BridgeWorker.run):
  1. 将 `import MetaTrader5` 移到 PATH 修改之前
  2. MT5目录改为 append 到 PATH 末尾（低优先级），不再 prepend
- dev commit: a437445 (已合并main, v1.9.10已打包)

## _order_send_with_retry req未定义导致开仓全部失败 (14:54)
- 服务器审计日志: auto_trade_disabled exec_failed: cannot access local variable 'req'
- 根因: `_order_send_with_retry` 方法中第374行 `tick = self.mt5.symbol_info_tick(req.get("symbol"))` 
  引用了尚未定义的 `req` 变量 — `req` 要到第377行 `build_req_fn(tick)` 才被创建
- 结果: 无论买卖信号，所有开仓命令直接抛 UnboundLocalError，无一成功
- 修复: `_order_send_with_retry` 改为接收 `symbol` 参数，调用方 (open/close) 传入
- dev commit: 1375d9f → 已合并main (v1.9.10)

## v1.9.10 发布 (14:58)
- 版本: v1.9.10, tag: v1.9.10
- EXE 64MB (65,341,802 bytes), MD5: 41dfe8db4e96ab1e740f6a08ccc6b083
- 下载: https://qiniu.acadfx.com/AURUM_Bridge_v1.9.10.exe
- 通用: https://qiniu.acadfx.com/AURUM_Bridge/AURUM_Bridge.exe (已覆盖)
- API /bridge/version: version=1.9.10, changelog/download_url/md5/file_size 全部同步
- dev commit: e62793b → main b5c95a7
- 15:04: 本地项目重启完成，端口3000正常

## numpy._core.multiarray 导入冲突最终修复 (15:08)
- 某客户 MT5 目录 C:\Program Files\MetaTrader 5 包含 openblas.dll 与 PyInstaller 打包的 numpy 2.5+ 冲突
- 修复: 彻底删除 `os.add_dll_directory()` 和 PATH 修改，PyInstaller 已自带所有依赖，mt5.initialize() 通过 IPC 连接终端无需额外 DLL
- dev commit: 764c79e (未合并main, 未打包)

## 桥接软件全面审查修复 (15:11)
修复项:
1. 🔴 MT5 资源泄露: 两条 4003 拒绝路径 (WS连接失败/WebSocket关闭码) 缺少 mt5.shutdown() → 已补
2. 🔴 MT5 路径不持久化: _on_mt5_path_changed 仅设标签未存 config → 已补 update_config({"mt5_path": path})
3. 🟡 _resolve_symbol None→"None"字符串绕过空值检查 → 加显式 is None 判断
4. 🟡 5处裸 except: → load/save_config→Exception; date解析→ValueError/KeyError/TypeError; symbol resolve→Exception
5. 🟡 批量平仓/全平: 直接 order_send 无重试 → 新增 _order_send_simple_retry 包装器，批量 close/close_all 均使用
6. ✅ 确认无自动启动MT5代码，已提示用户手动启动
- dev commit: e46390b (未合并main, 未打包)

## v1.9.10 全面审查修复版最终发布 (15:21)
- dev→main 合并 (53de94a), tag v1.9.10 更新 (e833f81)
- PyInstaller 打包: AURUM_Bridge.exe 62.3MB (65,342,042 bytes)
- MD5: a01a577f1d35001f2ae4c22bdede6039
- 上传七牛云: https://qiniu.acadfx.com/AURUM_Bridge_v1.9.10.exe (版本化) + https://qiniu.acadfx.com/AURUM_Bridge.exe (通用)
- API /bridge/version: file_size=65342042, md5=a01a577f1d35001f2ae4c22bdede6039, changelog 更新含全部修复项
- 本地服务已重启，版本接口返回正确 (2026-06-24 15:21)

## 代码审查标准制定 (15:54)
- 基于项目全量代码探索（608次提交，74+项历史问题）制定 CODE_REVIEW.md
- 四层审查等级：阻断🔴/重要🟡/建议🔵/信息⚪
- 通用清单：安全(SQL注入/XSS/鉴权)+正确性(空异常/变量未定义/None传播)+数据库(幂等/N+1)+跨层一致性(桥接标志/版本同步)
- 语言专项：Node.js 10项 + Python 8项检查
- 历史问题防回归速查表（9项已知陷阱）
- 审查流程：单人/发版/多人三种模式的完整定义
- 基础设施路线图：4周补齐 ESLint/Ruff/GitHub Actions/Pre-commit
- 附录：PR模板 + 裸异常白名单

## 开发者技术提升指南 (16:03)
- 基于项目真实代码创建 DEVELOPER_GUIDE.md
- 反模式速查：空异常吞噬(30+处) / 变量定义顺序 / None→"None" / 资源不释放 / 状态不一致 / 路由鉴权重定义 / 定时器异常静默 / 魔法值硬编码 — 每项附❌真实代码+✅修复示例
- Node.js+Python两种技术栈的正确模式对照表
- 6周分阶段提升路线：止血(ESLint/Ruff)→固本(常量提取/N+1排查)→进阶(模块拆分/状态机/结构化日志)
- 六条核心编码准则(基于项目血泪教训提炼)
- AI协作提效5个场景 + 每周自检清单

## 历史页面导出功能 (16:15)
- 新增 ws command `export_history`: 聚合 MT5 历史订单 + ai_signals 推理 + close_signal_tickets 平仓分析，支持所有筛选条件
- 前端: 历史页 card-header 添加导出按钮（`.admin-only` class，`applyRoleUI()` 控制可见性）
- 使用 SheetJS CDN (xlsx@0.18.5) 生成双 Sheet Excel:
  - Sheet1 交易记录(28列): 订单信息 + 信号推理(分析/理由/置信度/止盈止损) + 平仓信号分析
  - Sheet2 统计汇总: 总笔数/胜率/盈亏/方向分布/筛选条件
- 鉴权: 前端按钮仅管理员可见 + 后端 handler 检查 role==='admin'
- 安装: npm xlsx 包（后端暂未用，前端 CDN 加载）
- dev commit: 86ea23b
- 16:19: 修复导出历史 — 按用户要求移除平仓分析，只保留订单号绑定的推理结果（删除 close_signal_tickets 查询、6列前端列、38行代码），导出从28列精简到22列。dev commit: 899c1f6
- 16:22: 修复导出推理列为空 — 根源：原方案只做 trade_ticket IN 精确匹配，auto_trade 未开启时 trade_ticket=NULL 全空。改为日期范围全量查询 + 两级匹配（优先 ticket 精确，回退 symbol+close_time 分钟匹配）+ normalizeSymbol 去 .s/.m/.c 后缀。dev commit: 8f97e21
- 16:32: 修复导出推理全部匹配错误（hold信号） — 根源：时间回退匹配过于粗暴，symbol+close_time 分钟精度匹配到不相关信号。改为完全复用 signal_tickets handler 的精确 ticket 绑定逻辑：is_executed=1 → trade_ticket/execution_result JSON → ticket map，未命中就留空不猜。dev commit: d0c1a1f
- 17:07: 🔴 修复严重数据隔离漏洞 — getActiveConfig 在用户无 API Key 时回退到 global_auto_config，连带泄露了 system_prompt。所有普通用户在「手动推理配置」界面可见管理员的自动推理策略提示词。修复：移除 system_prompt 的全局回退（第225行）。dev commit: 0fdac0a
- 17:18: 🔴 修复完整隔离 — 手动推理配置(ai_config)彻底不回退任何数据。getActiveConfig 新增 skipFallbacks 参数，ai_config 命令跳过 model_sharing/system_config/global_auto_config 三层回退。同时移除 auto_symbols/interval 的全局回退。dev commit: 472d1b8

## 删除复盘逻辑 + 信号执行风控参数精准获取 (17:27)
- **删除复盘(Trade Review)全部代码**: TRADE_REVIEW_JSON_CONTRACT、parseIsoTime、closedOrderInWindow、compactSignalForReview、buildTradeReviewContext、reviewTrades、runTradeReviewCycle、startTradeReviewScheduler、stopTradeReviewScheduler、/review/status 和 /review/run API 端点、startAutoScheduler/stopAutoScheduler 中的回调、相关导出 — 共删除约 270 行
- **信号执行不再调用 getActiveConfig 做全链路穿透**: 新增 getExecuteRiskConfig(userId, signal) 精准获取风控参数(enable_auto_trade/selected_take_profit/max_position_size)，不做任何穿透回退，不泄露 API Key
  - 自动信号(config_id=0) → 全局配置风控参数(或用户 auto_config_override 覆盖)
  - 手动信号 → 用户自己的手动推理配置风控参数
- **runAutoCycle 自动信号标记**: config_id 从 config?.id||null 改为硬编码 0，用于区分自动/手动信号来源
- bridge-ws.js execute handler 改为调用 getExecuteRiskConfig 替代 getActiveConfig
- dev commit: e5b2d70（未合并 main）

## 桥接软件换账号登录后页面不跳转修复 (18:39)
- **Bug**: 桥接软件登出后换账号登录，显示"✅ 登录成功"但页面不跳转
- **根因**: `_on_login_success` 中先操作 `self.bridge_page` 的控件（`_log`/`_set_status`等），若任一控件操作异常抛错，`self.stack.setCurrentIndex(1)` 永远不执行。PyQt 信号槽异常静默吃掉。
- **修复1**: 把 `self.stack.setCurrentIndex(1) + QApplication.processEvents()` 移到最前面，页面先切换，再用 try/except 包裹控件操作
- **修复2**: `LoginPage.load_config` 若 `saved_password` 不存在时 `input_password.clear()` 清空密码框，避免登出后旧密码残留
- dev commit: 11f6996（未合并 main）

## model_sharing 前端显示 + 观摩模式三级分层 (18:46)

### model_sharing 显示优化
- 管理员 model_sharing_enabled=1 时，其他用户手动推理配置界面：
  - API Key 输入框显示隐藏字符 `••••••••••••`，设为 disabled + 半透明
  - configStatus 和 sharedInfo 提示"当前使用管理员共享的API Key"
- saveConfig 处理: 共享模式下 apiKey 字段忽略隐藏字符，提交时为空
- HTML modelSharedInfo 文案改为"当前使用管理员共享的API Key"

### 观摩模式逻辑重构
- **Plus**: 观摩模式 → observeBanner("升级 Pro"), 不可下桥接(handleGatewayModeClick 拦截), 无模型页(model tab 隐藏)
- **Pro 无桥接**: 观摩模式 → observeBanner("下载并启动MT5桥接"链接), 可下桥接(handleGatewayModeClick 允许), 模型页只显示用户自己的数据(不显示管理员共享的 _model_shared)
- **Pro 有桥接/Admin**: 完全访问，隐藏 observeBanner

### 其他
- 回退 LoginPage.load_config 密码框清空修改（用户不需要）
- app.js 版本号: ?v=20260624b
- dev commit: 621c202（未合并 main）

## 删除 observeBanner 黑框 + model_sharing 调试 (19:20)
- 完全删除 observeBanner HTML/CSS/JS（用户要求去掉底部黑框）
- 后端 bridge-ws.js ai_config handler: 用户有配置但无 API Key 时也注入 _model_shared
- 前端 loadConfig: console.log 调试确认 isUsingShared=true, _model_shared=true
- 版本号: styles.css/app.js → v=20260624e
- dev commit: 945d22c（未合并 main）

## 共享模型 API Key 输入框解除锁定 (19:30)
- 管理员 model_sharing=1 后，其他用户 API Key 输入框不再 disabled
- 改为 password 类型 + placeholder "留空则使用管理员共享的API Key"
- configStatus 提示"当前使用管理员共享的API Key（可自行填写覆盖）"
- saveConfig 移除 isUsingShared 强制发空值逻辑，用户填了就用用户的
- 版本号: app.js → v=20260624g
- dev commit: ccc6c48（未合并 main）

## autoConfigOverride 对 Pro 开放 + sidebar 观摩提示 (19:26)
- autoConfigOverrideWrap 可见性：从仅 `currentConfigHasApiKey` 扩展到 `currentConfigHasApiKey || isUsingSharedModel || plan==='pro'`
- 新增 sidebar 底部观摩提示 (sidebarObserveHint)：金黄色边框+图标
- Plus: 提示"升级 Pro"链接；Pro无桥接: 提示"下载MT5桥接"链接；Pro有桥接/Admin: 隐藏
- 新增 helper: showSidebarObserveHint(html) / hideSidebarObserveHint()
- 版本号: styles.css/app.js → v=20260624f
- dev commit: b719384（未合并 main，⚠️ git add -A 误带 build/docs 文件）

## v1.9.11 正式发布 (19:40)
- 版本号: aurum_bridge_gui.py APP_VERSION → "v1.9.11"
- 打包: AURUM_Bridge.exe (69M, MD5: db57acd3...), aurum_updater.exe (12M)
- 上传七牛云: AURUM_Bridge_v1.9.11.exe + AURUM_Bridge/aurum_updater.exe + AURUM_Bridge/AURUM_Bridge.exe
- 服务端 /api/bridge/version: version→1.9.11, download_url→AURUM_Bridge_v1.9.11.exe
- Git: dev→5390b29, main→f4ca360, tag v1.9.11 已推送
