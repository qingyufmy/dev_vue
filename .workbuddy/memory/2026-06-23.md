# 2026-06-23

## 项目启动
- 启动 AURUM AI Trading System，Express 服务运行在 localhost:3000
- DB 连接: MySQL @ 192.168.1.254:3306, database=huaerjie

## 21:21 - 历史页面图表点击联动修正
- 图表柱点击 → 设置列表平仓日期筛选 → 刷新列表，图表不变
- 图表仅在图表专用日期筛选（chartDateFrom/chartDateTo）应用时才变化
- 服务已重启验证，已 push 到 dev 分支 (f443aba)

## 21:25 - history 统计重新计算
- `history` handler 不再直接透传 bridge 的 statistics，而是根据过滤后的订单重新计算
- `total_profit`: 仅计算筛选范围内的订单盈亏
- `net_result`: 本金(initCapital) + 入金(deposit) + 从开始到 close_to 日期的累计收益（全量订单累加，不受 direction/profit 筛选影响）
- 已 push 到 dev (da2ccda, 834cc49)
- .env 配置位于 server/.env，包含七牛云、MySQL、JWT 等配置
- GitHub 仓库: qingyufmy/wall-street-skill-local
- 19:45 aurum_bridge_gui.py 已从提交 9c6651e 恢复到 GitHub 主分支版本（1762行），两个 bug 修复
- 19:52 从源码启动 GUI 验证，桥接启动和地址保存均正常
- 20:40 修复前端历史页面：统计区还原为原始 5 项；胜率/盈亏比改按单笔交易计算；最大回撤以 initCapital 为基准；盈点列显示 API profit_points；历史列表按平仓时间(close_time)倒序

## 桥接软件 Bug 修复
- aurum_bridge_gui.py 本地版本 (1769 行) 与 GitHub 主分支 (1762 行) 存在代码结构差异
- 通过 git fetch + git show 从提交 9c6651e 恢复了正确版本
- 本地未提交的修改导致了两个 bug:
  1. 服务器地址修改保存不生效
  2. 登录成功后启动桥接提示"需要登录"
- 文件已恢复为 1762 行，与提交 9c6651e 一致
- 验证了 config 读写流程正常，登录→保存 token→读取 token→启动桥接逻辑链完整

## 前端历史页面修复
- 21:00 三个问题修复:
  1. bridge-ws.js: 排序改 close_time DESC（第1页=最新20条）; 新增 entry_from/entry_to 日期筛选
  2. app.js: 图表 total_trades 改用 statistics.trade_count
  3. 统计区保持原始5项（盈利/信用/入金/提款/结余）
- 21:07 aurum_bridge_gui.py: history 命令改为接受服务器传来的 date_from/date_to 参数，不再硬编码31天
- 21:07 bridge-ws.js: chart daily 扩充为 {profit, trade_count, wins, losses}，支持每日粒度胜率/盈亏比计算; 前端 _applyHistoryData 改用 daily 聚合数据计算图表统计
- 已重启桥接 GUI（PID 33052）使 Python 端改动生效
- 21:11 图表与列表完全分离：
  1. bridge-ws.js: history 只返回表格数据（去掉 chart）；history_chart_data 独立返回图表数据（默认30天，daily 含 trade_count/wins/losses，按单笔交易计算盈亏比=avg_win/avg_loss）
  2. app.js: loadHistory 只处理表格，loadHistoryChart 只处理图表（调用 history_chart_data），两个缓存独立
  3. 图表日期筛选（chartDateApply/chartDateReset）不影响表格；表格筛选（historyFilterApply/historyFilterReset）不影响图表
  4. 图表点击设置图表日期，不再联动表格过滤器
  5. 移除 _chartDateFrom/_chartDateTo 全局变量

## 21:30 - 全面清理
- 删除根目录 6 个一次性脚本 (_create_rules_table.cjs, _dbg.cjs, _debug.cjs, _drop_changelogs.cjs, _save_qiniu.cjs, _test_history.cjs)，部分含密钥
- 移除 bridge-ws.js 中 2 条调试 console.log
- 移除 server 重复/死路由:
  - posts.js: POST pin/feature/lock (保留 PATCH 版本)
  - user.js: PUT /notifications, POST+GET /user-notices
  - admin.js: PUT commissions/:id (保留 PATCH), PATCH rules (保留 PUT), PUT quiz (保留 POST)
  - courses.js: GET /bilibili-duration/:bvid (保留 bilibili-info 替代)
- 已 push 到 dev (406ec31)

## 21:48 - 全面代码审计完成
- 审计范围: Server (23 问题) + 前端 HTML/CSS/JS (17) + App.js (13+) + 桥接 Python (21) = 74+ 问题
- 🔴致命 15 个: 验证码绕过、CORS 全开、JWT 硬编码 5 处、XSS 3 处、Token 明文传输、密码明文存储、SSL 降级
- 🟠高危 13 个: N+1 查询、同步阻塞 I/O、_bridgeLocks 竞态、限速缺失、崩溃保护缺失、广播无限流
- 审计报告输出: .workbuddy/outputs/code-audit-2026-06-23.md

## 23:25 — 代码修复会话：20/74 问题已修复
🔴致命 (11/12): C3 JWT统一 · C1 验证码绕过 · C4/C5 XSS · C10 SSL · C9 密码 · C2 CORS · C6 CSP · C7 CSRF · C12 Chart.js · (C8 Token URL 暂缓)
🟠高危 (6/13): H4 rate-limit · H5 崩溃保护 · H6 DB重连 · H9 广播限流 · H1 N+1优化 · H7 bcrypt异步
🟡中危 (3/22): M5 referral_credit · P3 WS重连退避 · M17 空值防御
