# 管理后台（admin）覆盖规则

- 默认浅色，强调扫描效率、批量操作、审计和权限可见性，不复制交易终端视觉。
- 桌面使用固定侧栏和顶栏；表格表头可固定，筛选条件与结果摘要始终相邻。
- 关键写操作显示作用范围、影响数量和幂等结果；危险操作必须二次确认。
- 状态用 Badge、Alert、Progress、Table 与 Sheet 表达；禁止为每个字段套一张卡片。
- 小屏优先转为摘要列表加详情 Sheet，不能靠横向无限滚动维持桌面表格。
