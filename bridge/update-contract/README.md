# Bridge 更新签名合同夹具

此目录保存 .NET 与 Rust 更新器共同使用的公开验签夹具，用于锁定以下兼容合同：

- NIST P-256 / ECDSA，SHA-256，IEEE P1363 固定宽度签名；
- `AURUM-RELEASE-V2` 清单规范化文本；
- `AURUM-PACKAGE-V1` 模块规范化文本；
- 本机 HTTP 仅用于自动化测试，正式发布包仍必须使用 HTTPS。

目录中只包含一次性测试密钥的公钥和已签名清单，不包含私钥，也不能用于发布真实更新。
