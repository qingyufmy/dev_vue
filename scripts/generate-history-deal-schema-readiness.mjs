import assert from 'node:assert/strict'
import { readFile, writeFile } from 'node:fs/promises'
import { loadHistoryDealProvenanceUpgrade } from './lib/history-deal-provenance-upgrade.mjs'
const root = new URL('../', import.meta.url)
const plan = await loadHistoryDealProvenanceUpgrade(root)
const [step] = plan.added
const value = { step: { id: step.id, checksum: step.checksum }, table: { table: step.table, schemaSha256: step.afterHash } }
const rendered = '// Generated by scripts/generate-history-deal-schema-readiness.mjs; do not edit.\n'
  + 'export const historyDealSchema = ' + JSON.stringify(value, null, 2) + ' as const\n'
const destination = new URL('server/src/modules/trade-history/infrastructure/history-deal-schema.ts', root)
if (process.argv[2] === '--check') assert.equal(await readFile(destination, 'utf8'), rendered)
else { assert.equal(process.argv.length, 2); await writeFile(destination, rendered) }
console.log('History deal provenance requirements match the appended 052 target step')
